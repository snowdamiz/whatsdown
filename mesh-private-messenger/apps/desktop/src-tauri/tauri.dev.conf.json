{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "io.morseapp.desktop.dev",
  "build": {
    "runner": {
      "cmd": "cargo",
      "args": [
        "--config",
        "target.'cfg(target_os = \"macos\")'.runner = ['node', '../scripts/run-signed.mjs']"
      ]
    }
  }
}
