[package]
name = "messenger-protocol"
version = "0.1.0"
description = "Canonical Morse messenger protocol codecs"
license = "MIT"

[dependencies]
mesh-binary = { path = "../../../mesh-lang/packages/mesh-binary" }
