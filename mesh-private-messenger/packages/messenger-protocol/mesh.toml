[package]
name = "messenger-protocol"
version = "0.1.0"
description = "Canonical Whatsdown messenger protocol codecs"
license = "MIT"

[dependencies]
mesh-binary = { path = "../../../mesh-lang/packages/mesh-binary" }
