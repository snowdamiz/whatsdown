{
  "name": "morse-cloudflare",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test *.test.mjs",
    "deploy": "node prepare-build.mjs && wrangler deploy --config wrangler.build.json",
    "dev": "node prepare-build.mjs && wrangler dev --config wrangler.build.json"
  },
  "dependencies": {
    "@cloudflare/containers": "0.3.7"
  },
  "devDependencies": {
    "miniflare": "5.20260918.0-alpha",
    "wrangler": "4.135.0"
  }
}
