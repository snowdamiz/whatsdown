# BLOCKER — auto-mode recovery failed

Unit `complete-milestone` for `M061` failed to produce this artifact after idle recovery exhausted all retries.

**Reason**: Artifact verification failed after 3 retries for complete-milestone "M061".

This placeholder was written by auto-mode so the pipeline can advance.
Review and replace this file before relying on downstream artifacts.