# Knowledge

- Runtime intrinsics return heap pointers for sum values such as `Option<String>`, while Mesh control-flow result slots use the by-value LLVM sum layout. Coerce each non-terminated `if`/`case` branch to the result representation before storing it in the shared slot; LLVM opaque pointers otherwise permit a pointer store that silently corrupts the constructor tag and later surfaces as a non-exhaustive-match panic or process crash.

- For M061/S04 combined `verify-m061-s04.sh` dev-route replay failures, inspect the first non-connection-refused assertion in `../hyperpush-mono/mesher/.tmp/m061-s01/verify-client-route-inventory/route-inventory-dev.log` before chasing the later `ERR_CONNECTION_REFUSED` fallout. In the current failures, later seeded-walkthrough/read-seam connection errors were secondary after the first Issues proof mismatch destabilized the shared dev run.

- For M061/S04 isolated `seed-live-issue.sh` replays, removing default backend reuse exposes a second seam: the temporary Mesher process can log `Runtime ready` while the script still records an empty `project-settings-last-response.txt` and times out waiting for `/api/v1/projects/default/settings`. Resume from the retained `seed-live-issue/mesher.log` and `project-settings-last-response.txt` pair rather than assuming the failure is in the later issue/event seeding logic.
- For Mesher client toast plumbing in `mesher/client/components/ui/use-toast.ts`, keep the shared listener subscription effect stable (`[]` deps). Re-subscribing on every state change leaks stale `setState` listeners and can surface intermittent React warnings like "Can't perform a React state update on a component that hasn't mounted yet" when destructive toasts fire during Playwright error-path proofs.
- For M060/S04 prod route-parity checks in `mesher/client/tests/e2e/live-runtime-helpers.ts`, treat aborted same-origin built font requests under `/assets/geist-*` and `/assets/geist-mono-*` the same way as the dev `/node_modules/@fontsource/...` abort noise. The unknown-path fallback proof can otherwise fail only in prod with `net::ERR_ABORTED` on hashed woff2 assets even though the backend-backed shell contract is still healthy.
- For M060/S04 combined Mesher client E2E rails, keep Playwright on a single worker by default. The `issues live`, `admin and ops live`, and `seeded walkthrough` suites all mutate one seeded Mesher runtime, so cross-file parallelism produces false negatives such as missing seeded rows, setup timeouts during alert seeding, and route-parity noise that looks like backend drift.
- For M060 assembled-shell verification, derive browser route coverage from `mesher/client/components/dashboard/dashboard-route-map.ts` instead of maintaining a second hardcoded route list in tests. The route-map-driven walkthrough/parity pair stays aligned with the real shell inventory and catches unknown-path fallback drift without duplicating navigation truth in the harness.
- For sparse live-detail proof on the Mesher dashboard, expose explicit `data-*` markers on shell-backed tabs/panels instead of asserting large blocks of fallback text. The content can legitimately vary between live, mixed, and shell-backed empty states, but the mounted-tab contract is stable and is the real seam worth verifying.
- For M060/S03 admin/ops seeding, keep `mesher/scripts/seed-live-admin-ops.sh` database-first and deterministic. Reusing whichever Mesher happens to be listening on `:18180` can point the helper at the wrong runtime/DB pair and make seeded readback fail for the wrong reason; seed/reset/verify the canonical Postgres rows directly, then let the Playwright harness boot its own backend against that same DB.
- For M060/S03 API-key route work in `mesher/api/team.mpl`, do not interpolate raw `Map.get(...)` values from `list_api_keys(...)` directly into JSON. On the current runtime that can serialize string fields like `label` and `key_value` as numeric slot identifiers. Normalize through the typed `ApiKey` struct plus `Json.encode`/`Json.get` before emitting the response.
- For M060/S03 admin/ops Playwright proofs on the Alerts route, `DashboardShell` still mounts `DashboardIssuesStateProvider` globally, so opening `/alerts` can emit expected same-origin `net::ERR_ABORTED` requests for `/api/v1/projects/default/issues` plus the dashboard health/levels/volume endpoints when route mocks or navigation interrupt the hidden Issues bootstrap. Treat those as shell-level abort noise in Alerts-specific request assertions unless the slice explicitly changes provider mounting.

- For M060/S02 combined `issues live` Playwright coverage, keep the read seam and action seam on separate deterministic seeded issues. The dev/prod harness runs the two spec files in parallel workers, so sharing one seeded issue lets action mutations race the read-side sparse/failure proofs. `bash mesher/scripts/seed-live-issue.sh` now seeds both titles and resets the action proof issue back to `unresolved` before the browser suites start.

- For M060/S02 prod Playwright coverage on the live Issues seam, do not assert that the issue-detail banner shows the exact event id returned by the seeding helper. Parallel workers can ingest another event for the same seeded fingerprint before the selected detail hydrates, so the truthful assertion is a non-empty `data-latest-event-id` plus same-origin `/api/v1/events/:id` traffic, not identity with the seed response.
- For M060/S02 Playwright work on the live Issues mutation seam, React dev-mode can trigger the provider bootstrap more than once, so route mocks for `/api/v1/projects/default/issues` should be armed only after the page is ready if the test is meant to fail the post-mutation refresh. Otherwise the app can fall back during initial load and the proof will fail for the wrong reason.
- For M060/S01 live Mesher issue-detail reads, `GET /api/v1/events/:event_id` can return JSONB-backed fields like `tags`, `extra`, and `user_context` as JSON strings even when `breadcrumbs` and `stacktrace` arrive as real arrays. Decode those fields before adapter-level shape validation or the selected-issue seam falls into `invalid-payload` and the dashboard silently stays on fallback detail.
- In this split `mesh-lang` workspace, Mesher script verification can hit `toolchain contract drift` because `mesher/scripts/*` expect `target/debug/meshc` under the current repo while the built binary may already live at `../hyperpush-mono/target/debug/meshc`. If that happens, export `MESHER_MESHC_BIN` to the existing sibling binary plus a non-empty `MESHER_MESHC_SOURCE` label before rerunning `mesher/scripts/migrate.sh` or `mesher/scripts/smoke.sh`.
- For M059/S03 cross-repo Playwright checks after the dashboard move, `mesh-lang` still has no local Playwright package. Validate the root `playwright.config.ts` with the moved package's binary instead: `PLAYWRIGHT_PROJECT=dev npx --prefix ../hyperpush-mono/mesher/client playwright test --config ./playwright.config.ts --project=dev --list`. That proves the root harness resolves the sibling `mesher/client` suite without needing a `cd` or a mesh-lang `package.json`.

- For M059/S02/T05 route-parity verification in `../hyperpush-mono/mesher/frontend-exp`, `npm --prefix ... exec playwright test ... --project=<name>` on the current npm still warns about unknown `--project` config and ends up running both Playwright projects. Treat `npm run test:e2e:dev` and `npm run test:e2e:prod` (which set `PLAYWRIGHT_PROJECT`) as the authoritative isolated dev/prod commands, and keep any future shared parity config/spec changes compatible with those scripts.

- For M059/S02 Playwright verification from `mesh-lang`, `npm --prefix ../hyperpush-mono/mesher/frontend-exp exec -- playwright ...` still keeps the current working directory at `mesh-lang`. The repo-root `playwright.config.ts` exists so the slice-plan commands can resolve the sibling `frontend-exp` tests/projects without `cd`, and concurrent dev/prod Playwright runs will fight over the shared `webServer` ports because that config starts both 3000 and 3001.

- For M059/S01 production-command verification in `mesher/frontend-exp`, do not assume TanStack Start's current Vite build will emit Nitro's `.output/server/index.mjs`. With the current plugin-only setup it emits `dist/client/` plus `dist/server/server.js`, so a truthful `npm run start` contract needs either an explicit Nitro plugin or a package-local bridge server that serves `dist/client` and forwards other requests to the built fetch handler.

- For M059/S01 local production smoke in `mesher/frontend-exp`, gating `@vercel/analytics/react` behind an explicit opt-in flag (for example `VITE_ENABLE_VERCEL_ANALYTICS === "true"`) keeps `npm run start` browser verification clean. Leaving analytics enabled for every local production run causes predictable `/_vercel/insights/script.js` 404 console/network noise even when the TanStack Start migration itself is correct.

- For M059/S01 TanStack Start migration work in `mesher/frontend-exp`, do not import `@fontsource/geist` / `@fontsource/geist-mono` from `app/globals.css` while using Tailwind v4’s Vite plugin. In this app that path produced Tailwind parser/HMR failures (`Invalid declaration: ar(--surface-3)` / stylesheet 500s). The stable fix is to keep `app/globals.css` focused on Tailwind/theme rules and link the Fontsource CSS from `src/routes/__root.tsx` with `?url` imports so Geist loads through TanStack Start head assets.

- For M059/S01/T03 and the follow-on S02 route decomposition in `mesher/frontend-exp`, the TanStack Start shell currently mounts the whole dashboard at `/`, and sidebar page changes still happen through `app/page.tsx` local `activeNav` state. In browser proof, clicking `Bounties` changed the rendered heading/content but left the URL at `/`, so S02 should decompose those state branches into route files instead of assuming the inner dashboard sections already map to router state.

- For M059/S01 TanStack Start migration groundwork in `../hyperpush-mono/mesher/frontend-exp`, the real Next-specific seams are concentrated in the app-router root/config/tooling layer (`package.json`, `app/layout.tsx`, `app/page.tsx`, `next.config.mjs`, `tsconfig.json`, `next-env.d.ts`, `components.json`, `tailwind.config.ts`). There are no active `next/link`, `next/image`, or `next/navigation` imports in the dashboard/component tree, so the honest first migration wave is root-plumbing replacement plus route-file remapping, not a component-by-component rewrite.
- For M057 closeout and later tracker sanity checks, start with the maintainer-readable handoff artifacts before diving into raw verifier logs: `project-mutation-results.md` should still show representative rows `mesh-lang#19` (Done), `hyperpush#54` (In Progress), and `hyperpush#29` (Next/Todo), and any mismatch there is a fast signal that the board has drifted before you inspect `.tmp/m057-s03/verify/`.
- For M057/S02 live repo reconciliation verification, `gh issue view 8 -R hyperpush-org/hyperpush` now fails with GraphQL `Could not resolve to an issue or pull request with the number of 8` after the `hyperpush#8 -> mesh-lang#19` transfer. Treat that source-repo lookup failure as the expected absence proof and verify the canonical destination through the persisted `repo-mutation-results.json` mapping instead of assuming `gh issue view` will follow the transfer automatically.
- For M057/S02 live repo reconciliation apply work, `gh api` reads of a transferred issue can still succeed with HTTP 200 because GitHub follows the redirect and returns the destination-repo payload. Treat that as a successful identity-change read, and rely on the persisted canonical mapping in `repo-mutation-results.json` instead of expecting the old repo/number pair to remain a stable post-transfer locator.

- For M057/S01 evidence-index work, preserve `mesher/` as a real compatibility symlink whose `readlink` target string still contains `hyperpush-mono`, and place test/product fixtures under the sibling `../hyperpush-mono/mesher/...` tree. Copying product files directly into `mesh-lang/mesher` makes the naming/ownership verifier fail even when file contents match, because the build treats the symlink contract itself as evidence.
- For M057/S01 tracker inventory refreshes, treat GitHub Projects V2 item rows as sparse. `fieldValues` omits missing fields entirely, and blank single-select fields can come back as empty strings instead of explicit nulls. Build the per-item field map from the checked-in field schema first, seed every tracked key to `null`, and then overwrite only the values actually present; otherwise later reconciliation logic will confuse ordinary missing board fields with schema drift.
- For M057/S01 ledger assembly, preserve the exact issue/project join contract as `68` canonical issue rows plus a separate `derived_gaps` section. Do not invent synthetic issue rows for uncovered shipped surfaces like `/pitch`; downstream closeout work depends on keeping the issue-backed row count at `68` and the joined project row count at `63` while still surfacing missing tracker coverage explicitly.
- For M057/S03 project-board planning, treat S01 `proposed_project_action_kind = leave_untracked` rows as intentional repo-only no-ops: skip them from delete/add/update planning, but fail closed if any of those rows are marked `project_backed` or appear on the live project board. The current set is the four non-board product issues `hyperpush#2/#3/#4/#5`, and treating them as planner errors blocks the ready-path manifest even though the board truth is still correct.
- For M057/S03 live project-board apply work, GitHub Projects V2 can lag immediately after `gh project item-add`: a full GraphQL refresh may briefly report page-count drift (`totalCount changed between pages`) or omit the just-added row even though the mutation already succeeded. Keep the shared S01 inventory verifier strict, but make the S03 applicator retry live board capture and added-row discovery before failing; otherwise the board can land in a truthful partial state while the apply step records a false-red error.
- For M057/S03 retained board-truth verification, start with `bash scripts/verify-m057-s03.sh` and read `.tmp/m057-s03/verify/phase-report.txt` before touching artifacts. The verifier delegates `scripts/verify-m057-s02.sh` first, so a red `repo-precheck` means repo-truth drift upstream of the board; only once that stays green should you investigate `.tmp/m057-s03/verify/commands/` for `project_membership` or `field_coherence` drift on org project #1.
- For M057/S02 retained live-state replay, normalize `gh issue view --json stateReason` blank strings to `null` while preserving real non-empty values like `reopened`. GitHub CLI can emit `""` for ordinary open issues, and treating that as meaningful state drift makes the verifier fail on CLI shape noise instead of real issue changes.
- For M057/S03 project-board closeout, `python3 scripts/lib/m057_project_mutation_plan.py --output-dir .gsd/milestones/M057/slices/S03 --check` validates the pre-apply live-board capture contract (63 current rows) and is not the authoritative steady-state replay once the board has been reconciled down to 55 rows. After apply, use `node --test scripts/tests/verify-m057-s03-plan.test.mjs` to lock the checked plan artifact and `python3 scripts/lib/m057_project_mutation_apply.py --output-dir .gsd/milestones/M057/slices/S03 --check` plus `bash scripts/verify-m057-s03.sh` to verify final live board truth.

- For M055/S04, do not run `node scripts/materialize-hyperpush-mono.mjs --check` in parallel with `bash scripts/verify-m055-s04.sh`. Both commands rewrite `.tmp/m055-s04/workspace/hyperpush-mono`, so a concurrent materializer refresh can delete the staged product repo out from under the assembled verifier and produce false-red missing-artifact errors inside the delegated product rail.
- For M055/S04 and later hosted-evidence work, the canonical default language repo slug now comes from `scripts/lib/repo-identity.json` through `scripts/lib/m055-workspace.sh`, not from `git remote get-url origin`. If a hosted verifier still follows the current remote by default, treat that as split-contract drift and keep `M053_S03_GH_REPO` only as an explicit override.
- For `scripts/verify-m055-s04.sh`, the staged product-side `scripts/verify-m051-s01.sh` pointed proof bundle is flattened: expect `scripts.verify-m051-s01.sh`, `package-root.meta.json`, `mesher.scripts.verify-maintainer-surface.sh`, `retained-build/`, and `retained-runtime-smoke/`, not a nested `mesher.README.md` or `verify-m051-s01.sh` filename. If the S04 assembled bundle goes red in `retain-product-m051-s01-proof-bundle`, inspect `.tmp/m055-s04/workspace/hyperpush-mono/.tmp/m051-s01/verify/retained-proof-bundle/` before changing the wrapper again.
- For hosted rollout tasks in this repo, check which GitHub credential path is active before attempting any remote write. The environment `GH_TOKEN` can be read-only even while `gh auth status` also has a broader keyring token with `repo`/`workflow` scopes; `gh api` and `git push` will 403 until you unset `GH_TOKEN` and let `gh` fall back to the keyring credential. Read-only hosted verifiers (`gh run list/view`, `bash scripts/verify-m053-s03.sh`) can still use the environment token.
- `bash scripts/verify-m050-s02.sh` does not stop at the task-owned Node contract; it also replays `cargo test -p meshc --test e2e_m047_s05 m047_s05_public_clustered_surfaces_use_source_first_names_and_todo_template -- --nocapture`. If first-contact docs wording changes, keep `compiler/meshc/tests/e2e_m047_s05.rs` aligned with the new public contract or the assembled verifier will fail in the retained M047 rail even when `scripts/tests/verify-m050-s02-first-contact-contract.test.mjs` passes.
- For fixture-backed Mesh package smoke tests, update any repo-relative `File.read(...)` probe paths to the relocated fixture root before deleting the old package directory. Otherwise `meshc test <fixture>/tests` can silently keep reading the stale repo-root package via the fallback path and make a half-moved fixture look green.
- For source-only clustered fixtures, `cargo run -q -p meshc -- build <fixture>` writes the compiled binary back into the fixture directory. Remove that generated in-place binary after verification or the relocated package stops being a truthful source-only fixture.
- For `tools/editors/vscode-mesh` smoke, materialize override-entry fixtures under `.tmp/m036-s03/vscode-smoke/workspace/` rather than an unrelated temp dir. That keeps repo `.git` as an ancestor while the fixture-local `mesh.toml` remains the nearest root marker, so the existing repo-root workspace can truthfully prove manifest-first root detection without changing the Extension Development Host setup.
- For M047/S08 Docker clustered-route proof, a Mesh node advertised as `name@127.0.0.1:port` inside a container binds its operator listener to container loopback, so host-side `meshc cluster status|continuity` against the published port can fail with `send_name failed: unexpected end of file` even though the container is healthy. The truthful proof seam is a helper container sharing `--network container:<target>` and running `/app/target/debug/meshc ...` from the cached `cluster-proof/Dockerfile` builder image; keep the published-port checks as publication evidence, but do not treat them as the authoritative operator path.
- For M047/S07 clustered HTTP route proofs, route request keys are currently node-local (`http-route::<runtime>::<seq>` with a per-process sequence). Repeating the same clustered route runtime through different ingress nodes can collide at `::1` and return `declared_handler_submit_rejected:duplicate`, so repeated-runtime-name proof rails should keep the repeated success requests on one ingress node unless the route identity generator becomes cluster-global.
- `cargo test -p meshc --test e2e_m047_s07 -- --nocapture` is the authoritative clustered HTTP route closeout rail. It retains the generated temp project, build metadata, cluster status snapshots, before/after continuity JSON, HTTP responses, and runtime logs under `.tmp/m047-s07/clustered-http-routes-two-node/`; if clustered-route regressions reappear, inspect that bundle before touching docs/scaffold work, and pair it with `cargo test -p meshc --test e2e_stdlib e2e_m032_route_ -- --nocapture` to confirm the route-closure guardrails still hold.
- For M047/S05 closeout, `cargo test -p meshc --test e2e_m047_s05 -- --nocapture` remains the authoritative native + Docker rail, and helper-shaped `Int ! String` results are safe again after the scalar-payload boxing fix in `compiler/mesh-codegen/src/codegen/expr.rs::codegen_construct_variant`. If a future Todo/SQLite regression reappears after a successful mutation, rerun `cargo test -p meshc --test e2e_sqlite_built_package -- --nocapture` and inspect `codegen_construct_variant` before reintroducing string-only success-payload workarounds.
- For M047/S05 Docker packaging, do not switch the Todo scaffold back to installing `meshc` inside the image until a public Mesh binary release actually supports the current `@cluster` scaffold contract. The latest public installer release still fails on source-first clustered scaffold output, so the honest current path is: build `./output` with `meshc build .` on the Linux target you intend to ship, then let the Dockerfile package that already-compiled binary.
- On non-Linux hosts, the truthful M047/S05 container proof is two-step even though the generated runtime image stays `COPY output ...`-only: first emit `./output` on Linux, then `docker build`/`docker run` the generated Todo image. The current harness does that by reusing the `cluster-proof/Dockerfile` builder stage to run `/app/target/debug/meshc build .` inside Linux before packaging, and it archives `docker-output.file.txt` because a host-native Mach-O `./output` can still make `docker build` look green while the container runtime would never be truthful.
- `compiler/meshc/tests/e2e_m047_s05.rs` plus `scripts/verify-m047-s05.sh` are now the slice-owned closeout surfaces for the Todo scaffold. They cover generation, public contract checks, native runtime CRUD/rate-limit/restart truth, Docker image build, and docs-site build replay; treat them as the first resume point if a future clustered Todo change regresses.
- For `compiler/meshc/tests/e2e_m044_s03.rs`, the `submit_request_for_owner(...)` helper's local hash prefilter is not authoritative for cluster-proof placement. On the current runtime, a key the helper predicts as owner=`primary` can still come back from `POST /work` with owner=`standby` and a rejected remote spawn, so future continuity/operator debugging should trust the submit response's actual `owner_node` / `replica_node` over the prefilter and keep retained `submit-*.http` artifacts when the rail drifts.
- `compiler/meshc/tests/e2e_m044_s03.rs::submit_request_for_owner(...)` can also return the first 200/202 submit even when it never finds the exact remote-owner/replica pair it was aiming for. If a future agent needs proof of actual remote execution, do not treat that helper alone as sufficient evidence; retry until the response itself reports `owner_node != ingress_node`, then assert completion and owner-node execution from the returned record/logs.

- For M044/S04, manifest-declared work handlers must register an actor-style wrapper, not the plain typed Mesh function symbol. Registering the raw typed work function (for example `_execute_declared_work`) and then spawning it through `mesh_actor_spawn` corrupts the string arguments on first execution and crashes `cluster-proof` inside `WorkContinuity__log_execution_started` / `mesh_string_concat`. The truthful fix lives in `compiler/mesh-codegen/src/declared.rs`: generate a `__declared_work_*` wrapper plus matching `__actor_<wrapper>_body`, then register that wrapper symbol with `mesh_register_declared_handler`. The authoritative regression surface is `cargo test -p meshc --test e2e_m044_s04 m044_s04_auto_promotion_ -- --nocapture` plus `bash scripts/verify-m044-s04.sh`.
- For M044/S03 operator rails, a live two-node cluster can still report `replication_health=local_only` on `meshc cluster status` / `/membership` before any continuity record exists. Do not assume that simple peer convergence implies `healthy`; the truthful zero-record status surface is membership + authority with `local_only`, and the named `m044_s03_operator_` proof now treats that as expected.
- For `compiler/meshc/tests/e2e_m044_s03.rs`, the cluster-proof submit path can legitimately outlive a 5s HTTP client timeout when `CLUSTER_PROOF_WORK_DELAY_MS` is non-zero and replica-backed continuity is waiting on the runtime path. Keep the harness read timeout comfortably above that budget or the POST `/work` probe flakes with `Resource temporarily unavailable (os error 35)` even when the operator CLI surface itself is correct.
- For M044/S04 closeout, `compiler/mesh-rt/src/dist/node.rs` now really does call the bounded promotion + automatic recovery seam and the unit rails can be made truthful with `automatic_promotion_` / `automatic_recovery_` names, but the destructive `e2e_m044_s04` rail still hits a real runtime crash after `[mesh-rt continuity] transition=automatic_recovery ...` and before the first `[cluster-proof] work executed ...` log. The current blocker is no longer missing promotion logic or a missing test target; it is the declared-handler execution seam after auto-recovery on the promoted standby. Resume in `cluster-proof/work_continuity.mpl::execute_declared_work`, `compiler/mesh-rt/src/dist/node.rs::spawn_declared_work_local`, and any codegen/runtime path that marshals declared-handler string args, not in the placement predictor or docs layer.

- For M044/S03 closeout, the current tree is still missing the whole public surface promised by the slice: `compiler/meshc/src/cluster.rs`, `compiler/meshc/tests/e2e_m044_s03.rs`, `scripts/verify-m044-s03.sh`, and any `meshc init --clustered` path in `compiler/meshc/src/main.rs` / `compiler/mesh-pkg/src/scaffold.rs`. `compiler/mesh-rt/src/dist/operator.rs::execute_query(...)` still requires `node_state()` plus `state.sessions[target]`, so the next honest step is still a transient authenticated operator query transport that answers before `register_session()`. Also note that `cluster-proof/` still consumes `CLUSTER_PROOF_*` env names (`CLUSTER_PROOF_COOKIE`, `CLUSTER_PROOF_NODE_BASENAME`, `CLUSTER_PROOF_ADVERTISE_HOST`, `CLUSTER_PROOF_DURABILITY`), so the proof app’s current config should not be treated as the generic public clustered-app contract when landing scaffold/docs work.

- For M044/S03/T02, the current Rust-side operator query helpers in `compiler/mesh-rt/src/dist/operator.rs` are not a truthful `meshc cluster` backend by themselves: `query_operator_*` goes through `execute_query(...)`, which requires `node_state()` and an authenticated session, while `compiler/mesh-rt/src/dist/node.rs::connect_to_remote_node(...)` registers that session and immediately sends peer/continuity sync. A CLI that reuses that path will transiently join the target cluster and can make `status` include the CLI as a peer, so the honest fix needs a transient authenticated operator channel (or equivalent non-registering query path) before shipping read-only inspection.

- When you add a new `TypeError` variant in `compiler/mesh-typeck`, update `compiler/mesh-lsp/src/analysis.rs::type_error_span(...)` in the same task. The compiler can build locally while `mesh-lsp` fails later with a non-exhaustive match, which is easy to misread as an unrelated editor-only regression.

- For M040/S01 keyed continuity work, standalone `cluster-proof` only stays truthful if `work.mpl` falls back to a synthetic local node identity (`standalone@local`) whenever `Node.self()` is empty. If `POST /work` regresses to HTTP 500 with `invalid_target_selection`, start in `effective_work_node_name()` / `current_target_selection()` before touching the submit/status contract. Cluster-mode startup still aborts before the keyed two-node proof is usable with `compiler/mesh-rt/src/string.rs:171:14` (`misaligned pointer dereference`), so the current authoritative S01 proof surface is the standalone submit/status e2e plus `scripts/verify-m040-s01.sh`, not a live clustered keyed-flow replay.

- `cargo test -p meshc --test e2e e2e_migration -- --nocapture` only runs tests whose names contain the `e2e_migration` substring. If you add migration e2e coverage in `compiler/meshc/tests/e2e.rs`, keep that prefix in the test name or the slice/task gate will silently skip the new proof.
- `reference-backend` startup is stable on the non-empty `DATABASE_URL` path when env validation stays local to `main.mpl` and uses `Env.get` plus `Env.get_int`; the mechanical regression surface is `cargo test -p meshc --test e2e_reference_backend e2e_reference_backend_runtime_starts -- --ignored --nocapture`, and the missing-env check remains `env -u DATABASE_URL PORT=18080 JOB_POLL_MS=500 ./reference-backend/reference-backend 2>&1 | rg "DATABASE_URL"`.
- `reference-backend` worker recovery now depends on two timing rules together: a watchdog actor respawns the worker only after `tick_age_ms` goes stale for `processing`/`crashing` states, and the restarted worker pauses for `recovery_reclaim_grace_ms(poll_ms)` after reclaim so the Rust harness can actually observe the degraded/recovering window before the requeued job is processed again. If the crash-recovery proofs regress, inspect those two timings before touching docs, deploy smoke, or the Postgres reclaim SQL.
- `meshc migrate` currently parses as `meshc migrate [DIR] [COMMAND]`, so project-scoped calls must be ordered like `cargo run -p meshc -- migrate reference-backend status` and `cargo run -p meshc -- migrate reference-backend up`, not `migrate status reference-backend`.
- Compiled Mesh code expects unwrapped runtime `Result`/`Option` payloads for opaque handles and parsed primitives to be boxed payload pointers, not raw integer-as-pointer values. The S01 fixes that unblocked the golden path were: box `PoolHandle` values returned from `mesh_pool_open`/`mesh_pool_checkout`, box row counts returned from `mesh_pg_execute`, and box numeric payloads returned from `String.to_int` / `String.to_float`.
- If `meshc migrate reference-backend up` fails at `mesh_pool_checkout` or the worker crashes in `Storage.Jobs.parse_attempts`, inspect runtime payload boxing and compiled-Mesh unwrap/codegen assumptions before blaming pool opening or SQL itself.
- In `reference-backend`, the generated `Job.__table__()` schema helper collides badly enough with Mesh's own `Job` surface that using the concrete table name (`"jobs"`) is the safer local option.
- The compiler-facing runtime-start proof is sensitive to stale listeners on `:18080`; clear any leftover `reference-backend` process before rerunning `e2e_reference_backend_runtime_starts` or the test can probe the wrong process and report missing startup logs.
- The canonical compiler e2e invocations for `reference-backend` should use Cargo's working argument order: `cargo test -p meshc --test e2e_reference_backend <test_name> -- ...`. Putting the test-name filter before `--test` can make Cargo misparse the request and claim the target does not exist even though `compiler/meshc/tests/e2e_reference_backend.rs` is present.
- `cargo test -p meshc --test e2e_reference_backend e2e_reference_backend_multi_instance_claims_once -- --ignored --nocapture` is now the authoritative shared-DB exact-once gate and should run **1 test**. If it ever exits 0 while running 0 tests again, treat that as a harness regression rather than as evidence that multi-instance correctness passed.
- In this worktree, the real Postgres-backed `meshc` verification commands depend on the repo-root `.env`, but non-interactive shell runs do **not** inherit it automatically. Source `.env` (or load it into the subprocess env) before running `cargo test -p meshc --test e2e_reference_backend ...`, otherwise `${DATABASE_URL:?set DATABASE_URL}` guards fail before the actual runtime proof runs.
- For `cargo test -p meshc --test e2e_reference_backend ...` in this environment, prefer the blocking `bash` tool over `async_bash` when you need authoritative evidence. The async wrapper intermittently reported `no test target named e2e_reference_backend` even though `cargo metadata` and direct blocking runs saw the target correctly.
- For the two-instance `reference-backend` proof, treat `/health.failed_jobs` and `/health.last_error` as the stable shared-DB contention signal, but prove exact-once and cross-instance participation through direct `jobs` table reads, `/jobs/:id`, and per-instance `Job worker processed id=` logs. Summing `/health.processed_jobs` or trusting `/health.last_job_id` under heavier multi-instance polling can introduce harness flake without changing the underlying DB truth.
- `reference-backend/storage/jobs.mpl` should keep `claim_next_pending_job()` as one atomic `Repo.query_raw(...)` claim-and-return statement using `FOR UPDATE SKIP LOCKED`; reverting to a read-then-update flow reintroduces benign shared-DB claim races as false worker failures (`failed_jobs` growth / `update_where: no rows matched`).
- For `meshc test <path>`, treat the requested path as the **discovery root** (project root, `tests/` directory, or specific `*.test.mpl` file), but resolve the **project root** from the nearest ancestor containing `mesh.toml` and only fall back to a legacy root `main.mpl` when no manifest exists. The runner must reuse `mesh_pkg::manifest::resolve_entrypoint(...)`, exclude that resolved executable file from copied sources, and rewrite the temp manifest back to synthetic `main.mpl`; orphan file targets should fail closed instead of drifting to repo CWD.
- Override executable entry files should not become a second special-case module name: only root `main.mpl` maps to `Main`, while non-root entries like `lib/start.mpl` must keep their path-derived module names even when discovery marks them executable.
- In `compiler/mesh-fmt/src/walker.rs`, `walk_type_alias_def(...)` must add an explicit separator after a `VISIBILITY` node. If it falls through to the generic inline walker, `pub type Foo = Bar` collapses to `pubtype Foo = Bar`, and `meshc fmt` can turn valid exported type aliases into parse-invalid source.
- In `compiler/mesh-lsp/src/analysis.rs`, backend-shaped editor sessions need project-aware analysis from the nearest ancestor containing `main.mpl` plus open-document overlays. Falling back to single-file analysis can surface bogus import failures or stale diagnostics even when the live buffer is correct.
- Treat `meshc test --coverage <path>` returning exit code 1 with the explicit unsupported message as the authoritative S03 contract. A green placeholder or success-without-report should be treated as a tooling-truth regression, not as partial coverage support.
- For deploy-path diagnostics, `reference-backend/scripts/apply-deploy-migrations.sh` should check the SQL artifact path before checking `DATABASE_URL`; otherwise the failure surface collapses to a generic env error and hides the more actionable missing-artifact cause that S04 verification expects.
- In Mesh source, `case` arms only allow multi-statement bodies via explicit `-> do ... end`; a bare newline after `->` is still parse-invalid. If future language work wants to remove the `let _ = ...` dogfood noise around branch-local side effects, the parser path to inspect is `compiler/mesh-parser/src/parser/expressions.rs::parse_match_arm` (and the analogous arrow-body closure parsing).
- Do not use `mesh_panic`-driven failures (for example non-exhaustive matches) or stdlib panics like `List.head([])` to simulate supervisor child crashes in Mesh actors right now: they currently abort the whole process with `panic in a function that cannot unwind` instead of yielding a supervisor-restartable actor failure. Resume S05 by making the worker exit cooperatively and letting the permanent supervisor restart it, or by adding a real actor-exit-with-error surface to the language/runtime.
- `meshc fmt` still mishandles supervisor child specs from `CHILD_SPEC_DEF`: even after a targeted walker patch attempt, formatting a valid `child name do ... end` block can collapse it into parse-invalid text like `childworkerdo` or split `start:` across lines. Before trusting `cargo run -p meshc -- fmt --check reference-backend` after T01, reproduce with `.tmp_worker_probe/fmt_worker/main.mpl` and fix `compiler/mesh-fmt/src/walker.rs` rather than touching `reference-backend/jobs/worker.mpl` again.
- After the `/health` liveness contract became tick-age aware, `wait_for_reference_backend(...)` could no longer treat the first 200 response as “ready.” Future backend harness work should wait for `status == "ok"`, `worker.liveness == "healthy"`, and `recovery_active == false`, otherwise startup assertions can race the truthful degraded boot window and fail before the real proof even begins.
- The S08 stale-claim sweeps are literal `rg` checks. Do not write sentences like "no placeholder" or "not a current blocker" into reconciled artifacts, because the negative grep still matches those substrings even when the sentence is negating them.
- Do not run `npm --prefix website ci` and `npm --prefix website run build` in parallel. The build can fail with false missing-module errors (`minisearch`, `@rollup/rollup-darwin-arm64`) while `node_modules` is still being replaced. For authoritative S08 evidence, run website install and build serially.
- `e2e_reference_backend_worker_crash_recovers_job` can pass in isolation while still failing in the full serial closeout gate. For M028 closure, the authoritative signal is the serial acceptance sequence (build/fmt/test/migrate/smoke -> migration truth -> multi-instance truth -> worker-crash recovery), not the isolated recovery test alone.
- In `reference-backend/jobs/worker.mpl`, recovery timing has two opposing constraints: the degraded/recovering window must stay visible long enough for the harness to observe the requeued `pending` row, and the restarted worker must keep ticking long enough afterward for `/health` to report a healthy processed state instead of going stale. If the crash-recovery gate flakes after a seemingly good fix, inspect `crash_restart_delay_ms(...)`, `pause_after_recovery(...)`, and post-recovery tick cadence together rather than treating them as independent knobs.
- In the parser's CST, LITERAL nodes inside multiline arg lists can have NEWLINE trivia tokens as leading children. Any AST accessor that iterates `children_with_tokens()` looking for a meaningful token must filter trivia (`.find(|t| !t.kind().is_trivia())`) instead of using `.next()`. The `Literal::token()` fix in M031/S01/T03 protects `infer_literal`, `lower_literal`, and all future callers. If similar bugs appear for other AST node types with `.next()` patterns, check for the same trivia-leaking issue.
- When a typechecker inference function (like `infer_if`) is called recursively and bypasses `infer_expr`, it must store its resolved type in the `types` map before returning. Without this, codegen's `resolve_range` finds nothing and falls back to `MirType::Unit`. The sentinel for this class of bug is String-return branches — they crash on misaligned pointer dereference when the type is missing, while Int branches silently return garbage.
- The `suppress_trailing_closure` parser flag uses a save/restore pattern around `expr(p)` calls in control-flow condition sites. If a new control-flow form is added to the parser that takes a condition expression before `do`, it must save/set/restore this flag or `fn_call() do` will be misparsed in that position.
- 10 pre-existing `try_*`/`from_try_*` e2e tests fail with runtime crashes (exit code None) on the current `main` branch. These are unrelated to M031/S01 fixes and should not be confused with regressions.
- In the parser's CST, `L_PAREN`/`R_PAREN` tokens for parenthesized imports live inside `IMPORT_LIST`, not as direct children of `FROM_IMPORT_DECL`. Formatter or analysis code that needs to detect paren-wrapped imports must inspect children of `IMPORT_LIST`, not `FROM_IMPORT_DECL`. The `walk_import_list` function in `compiler/mesh-fmt/src/walker.rs` is the reference implementation.
- The parser's existing `paren_depth` mechanism (which makes newlines insignificant inside parens) can be reused for new delimiter-wrapped syntactic forms. `parse_from_import_decl` bumps paren depth on `L_PAREN` so multiline import lists parse without a new parser mode.
- `meshc fmt` does not preserve parenthesized multiline imports: the formatter collapses them back to single-line and introduces spurious spaces in module dot-paths (e.g., `Storage. Schema` instead of `Storage.Schema`). Until the formatter walker is fixed to handle `FROM_IMPORT_DECL` with `IMPORT_LIST` inside parens, `fmt --check` will always fail on mesher (35 files). The parser supports multiline imports correctly (confirmed by `e2e_multiline_import_paren_multiline` test), so the issue is purely in `compiler/mesh-fmt/src/walker.rs`.
- `do...end` blocks cannot be used as values in `let` bindings in Mesh (e.g., `let x = do ... end` is invalid). For block-as-expression semantics, use `if/else` or `case` arms instead. This affects test design but not real-world usage.
- `mesh-fmt` library tests vendor `reference-backend/api/health.mpl` as a canonical fixture. If a formatter change starts canonicalizing dotted imports differently, `cargo test -q -p mesh-fmt --lib` can fail on that backend file before the broader `reference-backend/` cleanup is done; repair the fixture import or the suite will stay red for the wrong reason.
- For e2e tests requiring multiple Mesh source files (e.g., service-level integration tests with GenServer call/cast), use the `compile_multifile_and_run` test helper. It compiles all provided source files together and runs the main module. See `e2e_struct_update_in_service_call` for the reference pattern.
- For mesher cleanup proofs, do not use broad `rg 'List\.map\(' mesher -g '*.mpl'` as the pipe-style gate. It false-fails on the desired `rows |> List.map(...)` form. The authoritative S02 grep is `rg -n 'List\.map\(rows,|Ok\(List\.map\(' mesher -g '*.mpl'`, paired with the exact-location `<>` diff against the five accepted SQL/DDL keep sites.
- `meshc fmt --check` is not a sufficient regression surface for dotted-path bugs, because semantically wrong output like `Api. Router` can become idempotent once files are already corrupted. Keep at least one exact-output formatter test at the CLI layer (for example `compiler/meshc/tests/e2e_fmt.rs::fmt_preserves_dotted_paths_exactly`) so future regressions fail on text, not just exit codes.
- When a cleanup slice proves allowed `<>` survivors with a hardcoded `file:line` diff, keep edits above those survivors line-neutral (or update the proof artifact in the same task). Otherwise the gate can fail on shifted keep-site line numbers even when the code change itself is correct.
- `compiler/mesh-fmt/src/walker.rs` now keeps adjacent top-level comment lines and import runs compact, flattens nested pipe chains to one continuation indent, and breaks multi-field struct-literal closure bodies instead of collapsing them into one long line. After that formatter fix, `cargo run -q -p meshc -- fmt --check reference-backend` truthfully reports seven stale dogfood files (`reference-backend/api/jobs.mpl`, `reference-backend/api/router.mpl`, `reference-backend/jobs/worker.mpl`, `reference-backend/main.mpl`, `reference-backend/migrations/20260323010000_create_jobs.mpl`, `reference-backend/runtime/registry.mpl`, `reference-backend/storage/jobs.mpl`) that need reformatting under the new canonical output; treat that as cleanup backlog, not as a new parser or dotted-path regression.
- The current post-T04 formatter canonical output still inserts spaces around generic/result-type syntax (`Map < String, Int >`, `String ! String`) and removes the space before service-handler pipes (`do|state|`). That output matches already-accepted Mesher files and still builds cleanly, so do not treat those shapes as a T05-specific regression; use build plus the dotted-path and multiline-import greps to decide whether a new formatter wave actually broke something.
- `compiler/mesh-fmt/src/walker.rs` needs explicit spacing for `SUM_TYPE_DEF` visibility tokens and `SCHEMA_OPTION` string literals; otherwise `meshc fmt` can emit broken text like `pubtype` or `table"events"`, and a second formatter pass over the damaged file can truncate the surrounding declaration because the parser no longer sees a valid body. Guard this with exact-output CLI tests, and keep `meshc fmt --check` silent on success so captured-log gates like `/tmp/m029-s03-fmt-mesher.log` stay truthful.
- If a broken formatter pass has already emitted `pubtype` or `table"..."` into a Mesh source file, restore that file from a pre-format copy before rerunning the repaired formatter. Once the parser can no longer see the original declaration body, a second `meshc fmt` run is idempotent on the damaged CST and cannot reconstruct the lost structure.
- The authoritative full `cargo test -q -p meshc --test e2e -- --nocapture` baseline after M029 is `318 passed; 10 failed`. The exact expected failures are `e2e_cross_module_try_operator`, `e2e_err_binding_pattern`, `e2e_from_try_error_conversion`, `e2e_option_field_extraction`, `e2e_try_chained_result`, `e2e_try_operator_result`, `e2e_try_option_some_path`, `e2e_try_result_binding_arity`, `e2e_try_result_ok_path`, and `e2e_tryfrom_try_operator`; treat any different count or failing set as a real regression, not as the known try-family baseline.
- For M032/S01 route-closure proofs, `cargo run -q -p meshc -- build .tmp/m032-s01/route_closure` is only a compile-only control. The authoritative limit check is a live request against `.tmp/m032-s01/route_closure_server` with the bare-function control on `.tmp/m032-s01/route_bare_server`, because the closure form still builds while failing at request time.
- In `compiler/meshc/tests/e2e_stdlib.rs`, keep the retained `e2e_m032_route_bare_handler_control` / `e2e_m032_route_closure_runtime_failure` sources self-contained. Pointing those Rust tests at `include_str!("../../../.tmp/m032-s01/.../main.mpl")` makes the whole test target fail to compile when the transient `.tmp` fixture tree is absent, which looks like an HTTP regression even though the live route-control semantics are unchanged.
- Package-wide filtered runs like `cargo test -p meshc build_project_ -- --nocapture` still compile `compiler/meshc/tests/e2e.rs` even when that target executes 0 matching tests. Keep compile-only fixture sources self-contained in tracked Rust test code or tracked fixtures rather than ignored `.tmp` paths. If you only need authoritative unit coverage for `compiler/meshc/src/main.rs` / `src/discovery.rs`, `cargo test -p meshc --bin meshc <filter> -- --nocapture` remains the lighter seam.
- For M032/S01, `cargo test -p meshc --test e2e m032_limit -- --nocapture` and `cargo test -p meshc --test e2e_stdlib m032_ -- --nocapture` can exit 0 while running 0 tests if T02 has not landed yet. Treat that as slice-incomplete coverage, not as passing proof, and read the `running 0 tests` line before trusting the filter result.
- The M032/S01 single-expression case-arm keep-list is broader than the original research spotlight: besides `mesher/services/event_processor.mpl` and `mesher/ingestion/fingerprint.mpl`, future stale-comment sweeps must also account for `mesher/api/team.mpl`, `mesher/services/retention.mpl`, and `mesher/ingestion/pipeline.mpl`, or S05 will under-count real parser-bound keep-sites.
- Do not flip `scripts/verify-m032-s01.sh` or move mesher's inferred `flush_batch` helper across the `Storage.Writer` / `Services.Writer` boundary until `cargo test -p meshc --test e2e m032_inferred -- --nocapture` passes in the working tree. Right now the retained-failure script still passes, `cargo run -q -p meshc -- build mesher` stays green, and the compiler still reproduces both blocker shapes (`m032_inferred_local_identity` runtime corruption and `m032_inferred_cross_module_identity` LLVM verifier failure).
- For M032/S02 inferred-export repairs, a single recovered call-site signature is not enough: unconstrained functions like `identity(x) = x` need cross-module usage evidence threaded into MIR lowering and per-signature MIR clones when multiple concrete ABIs are observed. Otherwise the first recovered signature wins (`i64 -> {}` in the local repro), later call sites get coerced into the wrong ABI, and exported modules lowered in isolation still reproduce the old LLVM verifier failure.
- For M032/S03 comment-truth cleanup in `mesher/`, the supported direct dogfood shapes are now explicit: `handle_list_issues(...)` should read `Request.query(request, "status")` locally and default through `case Some/None`, `UserService.Login` can return `(pool, case authenticate_user(...) do ...)` directly, and `StreamManager.BufferMessage` can use inline `if/else` directly. Do not over-clean the nearby real keep-sites: bare HTTP route closures still fail at live request time, nested `&&` still needs `both_match(...)`, and `Timer.send_after` still delivers raw bytes instead of a service-dispatchable cast payload.
- For M032/S04 event-path cleanup, cross-module `from_json` support is **not** evidence that `EventPayload.from_json(...)` should replace `Storage.Queries.extract_event_fields(...)`. The real keep-surface is the PostgreSQL JSONB fingerprint chain plus the matching raw-SQL insert boundary in `mesher/storage/writer.mpl`; keep the type-file `from_json` notes in `mesher/types/event.mpl` and `mesher/types/issue.mpl`, because they describe row-shape decoding rather than a Mesh limitation.
- For M032/S05 closeout, keep the ledger in three buckets: supported-now proof (`xmod_identity` plus the named `e2e_m032_*` successes), still-real Mesh keep-sites (`routes` route closures, `stream_manager` nested `&&`, `writer` / `pipeline` `Timer.send_after`, and the case-arm extraction files), and M033 data-layer families (`storage/queries` + `storage/writer` ORM boundary comments, plus the migration `PARTITION BY` gap). Collapsing those back into one folklore list makes future planning regress.
- For M033/S01 expression work, Mesh currently parses module-qualified `Expr.call(...)` and `Expr.case(...)` poorly inside expression maps; the working stdlib aliases are `Expr.fn_call(...)` and `Expr.case_when(...)`, both lowered to the same runtime intrinsics. Use those aliases in Mesh code until the qualified-name parser path is repaired.
- If `compiler/meshc/tests/e2e_m033_s01.rs` builds Mesher successfully but the live tests stall with only `[Mesher] Connecting to PostgreSQL...` and `[Mesher] Running in standalone mode (no distribution)` in stdout, the next investigation should start in Mesher startup/HTTP serving rather than in the expression write paths themselves.
- For GSD artifact slices, keep verification commands self-contained. Outer `bash -lc '...'` wrappers and shell-local variables like `$log` can be misparsed by task-verification automation into false failures (`test -s ...'`, `rg -q ... "$log"`). Prefer direct commands, fixed repo-local log paths, or one replay script plus explicit non-zero test-count checks.
- For milestone closeout in this repo, if auto-mode is already running on local `main`, use `origin/main` as the non-`.gsd` diff baseline. `git diff HEAD $(git merge-base HEAD main) -- ':!.gsd/'` can go empty even when the milestone changed real compiler and app code, because the local integration branch already contains those commits.
- For M033/S01 expression-select work, `Expr.alias(...)` is currently not callable from Mesh source because `alias` tokenizes as a keyword after `Expr.` in this grammar path. Use `Expr.label(...)` instead; it lowers to the same `mesh_expr_alias` runtime intrinsic and keeps the neutral SELECT API reachable without parser surgery.
- When an `e2e_m033_s01` test introduces a new runtime ABI symbol, `cargo test -p meshc --test e2e_m033_s01 ...` may still link temp Mesh binaries against a stale `target/debug/libmesh_rt.a`. `compiler/meshc/tests/e2e_m033_s01.rs::compile_and_run_mesh` now calls a one-time `cargo build -p mesh-rt` helper first; keep that hook if future S01 ABI additions start failing with `Undefined symbols ... _mesh_*` during temp-project linking.
- Do not run `cargo test -p meshc --test e2e_m033_s01 mesher_mutations -- --nocapture` in parallel with another `meshc build mesher` path (`cargo run -q -p meshc -- build mesher`, the verify script, or a manual `./target/debug/meshc build mesher`). They race on the same `mesher/mesher(.o)` outputs and can produce false linker failures like `clang: error: no such file or directory: 'mesher/mesher.o'` even when the underlying source is fine.
- The current clean-start Mesher acceptance path still returns HTTP 429 on the first `/api/v1/events` request in `e2e_m033_s01`, before any S01 mutation assertions run. If `cargo test -p meshc --test e2e_m033_s01 mesher_mutations -- --nocapture` fails at the first ingest step with only startup logs present, investigate `mesher/services/rate_limiter.mpl` / `mesher/ingestion/routes.mpl` before touching the expression-write rewrites again.
- In Mesh service handlers, branching directly between different `(state, reply)` tuples can miscompile or crash even when the same logic works outside the service. The M033/T03 fixes for `RateLimiter.CheckLimit` and `EventProcessor.ProcessEvent` were both to compute branch-local values first, then return a single final `(new_state, reply)` tuple.
- Mesher’s live rate limiter is now testable through `MESHER_RATE_LIMIT_WINDOW_SECONDS` and `MESHER_RATE_LIMIT_MAX_EVENTS`, and the reset ticker must actually be spawned for fixed-window behavior to stay honest. Use those env vars for focused live-limit proofs instead of flooding 1000+ events through `/api/v1/events`.
- In Mesher HTTP handlers, matching on `Ok(n)` for Mesh functions that return `Int ! String` can still crash the live route even when the underlying mutation succeeds, because the lowered handler tries to dereference the success payload like a boxed pointer. If the count is not actually needed, prefer `Ok(_)` and derive the HTTP success response without binding the integer payload.
- In `compiler/meshc/tests/e2e_m033_s01.rs`, if repeated ingest updates `issues.event_count` / `last_seen` correctly but `events` rows stay at `0`, start the next investigation in `mesher/services/writer.mpl` / `mesher/storage/writer.mpl`. The S01 root causes were concrete: `StorageWriter.start(...)` was being used without spawning `flush_ticker`, so low-volume batches never flushed, and buffered entries also needed the real project UUID (`project_id|||issue_id|||fingerprint|||event_json`) instead of relying on the writer state's `project_id` label.
- Mesher's background writer no longer needs string-only success markers just to dodge boxed-int result crashes: scalar `Ok(...)` payloads now box correctly through generic `Result` construction. Keep the older string-marker pattern only when it is semantically clearer, not as a required compiler workaround.
- `scripts/verify-m033-s01.sh` should strip comment lines before checking for `Repo.execute_raw` / `Repo.query_raw` in owned function blocks. A naive substring sweep can false-fail on comments like “Two-step pattern: Repo.query_raw ...” even when the function body itself is already on the neutral expression path.
- Mesh's PostgreSQL path already returns row values as strings in text format, so Mesher query rewrites do not need `::text` casts just to get stringy `Map` values back. That matters for computed rank/count fields: keep them numeric in SQL when you need numeric `ORDER BY`, and let the runtime stringify the selected result.
- In Rust-authored Mesh probe templates, do not embed `Map.get(..., \"field\")` directly inside `#{...}` interpolation strings. The temporary Mesh program can fail to parse; bind the map lookup to a local first, then interpolate the local value.
- When building `jsonb_build_object(...)` through `Expr.fn_call(...)`, cast string arguments with `Pg.text(...)`. Without explicit text casts, PostgreSQL can raise `42P18 could not determine data type of parameter $N` even though the structured expression path itself is correct.
- If a temporary Mesh probe wants to print fields from a returned Mesher struct such as `User`, import the concrete type module in the probe (`from Types.User import User`) before using `user.id` / `user.email` / `user.display_name`; otherwise the copied-project typechecker can treat those field accesses as unknown.
- In Rust-authored raw-string Mesh probe templates, do not write escaped quotes like `\"field\"` inside `#{...}` interpolation expressions. The backslashes are emitted literally into the Mesh source and the temporary program fails to parse; use plain `"field"` in the Rust raw string so the Mesh code sees normal quotes.
- In copied Mesher storage probes, `Repo.exists(...)` in `Bool ! String` storage helpers can fail the compile path even when the main app builds. The safer probe-compatible pattern is `let rows = Repo.all(pool, q) ?` followed by `Ok(List.length(rows) > 0)`.
- The S03 storage-probe pattern currently breaks down on some T02 read helpers: imported `List<Issue>` / other typed struct-list results can hit LLVM verifier or runtime switch crashes in temp probes, and map-valued aggregate rows can stringify as raw pointer addresses when staged through intermediate bindings. If `e2e_m033_s03_composed_reads_*` stalls again, pivot the remaining proof to a higher-level Mesher surface (for example HTTP/API or full app harness) instead of extending the storage-only probe further.
- The first live-HTTP replacement for `e2e_m033_s03_composed_reads_*` exposed product-side caller-contract bugs, not just harness drift: `/api/v1/projects/:project_id/issues` currently returns blank `id`/`title`/`level`/`status` fields while counts/timestamps survive, `/api/v1/projects/:project_id/dashboard/volume` currently drops `bucket` values while keeping counts, `/api/v1/issues/:issue_id/events` currently crashes in `_handle_list_issue_events` with `non-exhaustive match in switch`, and a new-issue ingest did not create the expected fresh alert. Resume in the live route/JSON shaping code (`mesher/api/search.mpl`, `mesher/api/dashboard.mpl`, alert flow through `mesher/ingestion/routes.mpl`) before touching the new Rust harness again.
- `Request.query(...)` currently hands percent-encoded cursor values through unchanged on the Mesher HTTP path. If a live cursor/timestamp route starts failing with `invalid input syntax for type timestamp with time zone: "...%20...%3A..."`, add a local decode step before passing the cursor into SQL instead of assuming the request layer already normalized it.
- Some live Mesher row families still destabilize when route code reads `Map.get(...)` values directly for JSON serialization or cursor metadata: issue/event/member rows can blank fields, crash, or stringify as raw pointer numbers. The stable T05 pattern was to normalize those rows through concrete derived structs (`Event`, `Issue`, `User`, `OrgMembership`) and then read the needed fields back via `Json.encode(...)` + `Json.get(...)` before building the HTTP response.
- On the current Mesher path, a `202 {"status":"accepted"}` from `/api/v1/events` does not guarantee low-volume writes will flush promptly unless the writer is nudged. T05 restored the S03 acceptance path by enqueuing `StorageWriter.flush(writer_pid)` immediately after `StorageWriter.store(...)` in `mesher/services/event_processor.mpl`; if accepted events stop reaching `events`/`issues` again with only load-monitor logs present, inspect that explicit flush hook before blaming auth or read-side SQL.
- `Pg.create_range_partitioned_table(...)` intentionally accepts raw entries without `:` as table-level constraints, so helper-driven partitioned parents can keep composite declarations like `PRIMARY KEY (id, received_at)` without falling back to raw DDL. If a future partitioned migration starts failing validation, inspect `compiler/mesh-rt/src/db/pg_schema.rs::render_partitioned_table_entry(...)` before reintroducing `Pool.execute(...)`.
- `scripts/verify-m033-s04.sh` is the authoritative S04 raw-boundary gate. It bans `Pool.execute`, `Migration.execute`, `Repo.query_raw`, `Repo.execute_raw`, and `Query.select_raw` in the initial Mesher migration and runtime partition modules while also checking for the expected `Pg.*` / `Storage.Schema` helper calls and partition-bootstrap / retention log strings. If S04 work changes those surfaces, update the verifier in the same task instead of adding another exemption.
- Do not run `npm --prefix website run build` concurrently with `bash scripts/verify-m033-s05.sh`. Both VitePress builds write `website/docs/.vitepress/.temp`, and the overlap can produce false `ERR_MODULE_NOT_FOUND` failures for generated `*.md.js` modules even when the docs themselves are healthy.
- When updating the M033 verifier scripts that slice Mesh source by function body (`scripts/verify-m033-s02.sh`, `scripts/verify-m033-s03.sh`, and `scripts/verify-m033-s04.sh`), stop the extracted block at the next private `fn` as well as the next `pub fn`. Stopping only at `pub fn` swallows following private helpers into the previous public function body and creates false raw-boundary failures.
- In the M033 assembled proof surface, `get_event_alert_rules(...)` and `get_threshold_rules(...)` belong to the S03 honest raw-read keep-list, not the S02 PG-helper-owned boundary. If S02 verifiers or docs sweeps start treating them as builder-owned again, `bash scripts/verify-m033-s05.sh` will fail on stale ownership rather than a real runtime regression.
- Milestone validation currently expects the rendered `M###-VALIDATION.md` file to include an explicit operational marker containing both `Operational` and either `MET` or `N/A`. A `verdict: pass` frontmatter alone is not enough for the auto-mode gate; if milestone closeout stops after validation, inspect the rendered validation body before re-running `/gsd auto`.
- Installed package discovery must walk `.mesh/packages` to the first descendant directory containing `mesh.toml` and treat that leaf as the package root. A one-level scan breaks scoped installs by treating `.mesh/packages/<owner>` as the root, which renames modules to version-prefixed paths like `Greeter@1.0.0.Support.Message` and makes `meshc`/`mesh-lsp` miss the real package layout. The authoritative regressions are `cargo test -p meshc --test e2e_m034_s01 -- --nocapture` and `cargo test -p mesh-lsp scoped_installed_package -- --nocapture`.
- Mesh registry publish credentials are operator-created, not derivable from the repo: the packages site sends users through `/publish` -> GitHub auth -> `/token`, where the raw publish token is shown once and then must be saved. If `scripts/verify-m034-s01.sh` needs `MESH_PUBLISH_OWNER` / `MESH_PUBLISH_TOKEN`, future agents should expect a real dashboard-issued token rather than hunting for one in repo files.
- In repo-local verifier scripts, prefer Python stdlib `tomllib` over third-party `toml`, and prefer `curl` or the same verified transport stack for registry HTTPS calls. On this host, `urllib.request` hit certificate-validation failures against `https://api.packages.meshlang.dev` during the duplicate-publish phase even though the curl-based metadata/download checks succeeded.
- Ruby's `YAML.load_file` can parse GitHub Actions' bare `on:` key as boolean `true` instead of the string `"on"`. If a repo-local workflow verifier reads Actions YAML with Ruby, check both `workflow["on"]` and `workflow[true]` before declaring the trigger block missing.
- `gh run list --workflow <file>` only resolves workflows that already exist on the remote branch GitHub is querying (typically the default branch). If a new workflow file only exists locally, the CLI returns `workflow ... not found` even though the YAML is valid in the working tree; treat that as an unshipped-branch evidence gap, not as local workflow drift.
- For staged installer proof in this repo, derive the fake release tag from `compiler/meshc/Cargo.toml` and `compiler/meshpkg/Cargo.toml` instead of inventing a proof-only version string. The installer writes that tag into `~/.mesh/version`, while the staged binaries report their embedded Cargo version via `meshc --version` / `meshpkg --version`; keeping them equal makes the proof honest and avoids false version-drift failures.
- `scripts/verify-m034-s03.{sh,ps1}` are the canonical installer-smoke surfaces. In CI, point them at already-built archives with `M034_S03_PREBUILT_RELEASE_DIR` instead of reimplementing installer assertions in YAML, and keep `MESH_INSTALL_STRICT_PROOF=1` enabled so missing or malformed `tag_name`, `SHA256SUMS`, archive entries, and extracted binaries fail hard instead of downgrading to installer warnings.
- For the VS Code extension, `vsce package --no-dependencies` can still exit 0 while shipping a broken extension if `.vscodeignore` excludes `node_modules/**`. The honest local gate is a deterministic `dist/mesh-lang-<version>.vsix` path plus an archive listing check for `extension/node_modules/vscode-languageclient` / `vscode-jsonrpc`, not the package command’s exit code alone.
- `cargo test -q -p meshc --test e2e_lsp -- --nocapture` proves the LSP gate with `running 1 test` plus `test result: ok.` on success; quiet mode does **not** print the test name. In `compiler/meshc/tests/e2e_lsp.rs`, derive hover/definition/signature positions from `jobs_source` text instead of hardcoded line numbers or unrelated `reference-backend/api/jobs.mpl` edits will break the proof.
- For the VS Code extension release lane, `bash scripts/verify-m034-s04-workflows.sh` is the authoritative local contract gate before CI. It writes `.tmp/m034-s04/workflows/phase-report.txt` plus per-phase logs, and it expects `.github/workflows/extension-release-proof.yml` to be the only workflow that directly runs `scripts/verify-m034-s04-extension.sh` while `.github/workflows/publish-extension.yml` only downloads and publishes the proof job's exact VSIX outputs.
- For VitePress-built proof pages in this repo, do not verify rendered command text with raw `grep` over `*.html`. Code blocks split URLs and flags across markup spans, so the honest deploy-lane check is to extract HTML text first (for example with Python `html.parser`) and then assert the exact public markers on the normalized text.
- `scripts/verify-m034-s05.sh` intentionally runs `candidate-tags` and `remote-evidence` before `public-http` / `s01-live-proof`. If hosted rollout is behind the local branch, the wrapper will stop at `remote-evidence` but still emit `.tmp/m034-s05/verify/candidate-tags.json` and `.tmp/m034-s05/verify/remote-runs.json`; inspect those artifacts before chasing later public-surface failures.
- `scripts/verify-m034-s06-remote-evidence.sh` fails closed on label reuse. Do not spend the real `.tmp/m034-s06/evidence/first-green/` label during T01/T02/T03 dry runs — keep `preflight` for the hosted-red baseline so T04 can claim `first-green` exactly once with the first all-green bundle.
- `scripts/verify-m034-s06-remote-evidence.sh` is intentionally stricter for the reserved `first-green` label than for diagnostic labels: red stop-after bundles may still archive under labels like `preflight`, but `first-green` must refuse to archive unless `scripts/verify-m034-s05.sh` exited 0 with `status.txt=ok` and `current-phase.txt=stopped-after-remote-evidence`.
- If S05 `remote-evidence` reports `deploy.yml hosted run is missing required steps: ['build: Verify public docs contract']` while the run itself is green, treat that as proof that remote `main` is still on the older deploy workflow graph rather than as a docs-build outage. The stale hosted run to compare against is currently `23506361663` on `5ddf3b2dce17abe08e1188d9b46e575d83525b50`.
- In this repo and host, a large `git push` to `origin/main` can spend ~10 minutes in local `git pack-objects` and then fail with `error: RPC failed; HTTP 408 ... unexpected disconnect while reading sideband packet` even when auth is valid. Before retrying S06/T02, capture stderr to a durable file, confirm `origin/main` is still unchanged, and treat the failure as transport-level rollout blockage rather than as hosted-workflow drift.
- For the M034/S06 rollout push blocker, raising `http.postBuffer` to 1 GiB removed the `(chunked)` marker and produced `POST git-receive-pack (564496785 bytes)`, but the push still died with the same HTTP 408 after ~12.7 minutes and left `origin/main` unchanged. That means the current blocker is not just chunked transfer encoding; future recovery work should assume the host's HTTPS receive-pack path itself is timing out on this ~565 MB upload.
- `scripts/verify-m036-s02.sh syntax` only materializes cases from `scripts/fixtures/m036-s01-syntax-corpus.json`; adding a standalone fixture like `scripts/fixtures/m048-s04-cluster-decorators.mpl` does not affect the Neovim rail until `tools/editors/neovim-mesh/tests/smoke.lua` (or the corpus manifest) explicitly consumes it.
- For the classic Neovim syntax rail, keep the `@cluster` decorator token special but let the counted arity in `@cluster(3)` stay on the existing `meshNumberInteger` path unless the proof contract explicitly changes. Trying to force a separate decorator-count group adds complexity without improving the bounded syntax surface that `scripts/verify-m036-s02.sh syntax` actually proves.
- `scripts/verify-m034-s06-remote-evidence.sh` only archives `remote-<workflow>-view.*` when `gh run list` finds a concrete matching run. If a required branch/tag has no run, the truthful bundle contains `remote-<workflow>-list.*` plus `remote-<workflow>-latest-available.*` instead. Future S06 summaries should read `remote-runs.json` / `manifest.json` before assuming success-path `...-view.log` filenames exist.
- For the M034/S06 extension lane, `.github/workflows/extension-release-proof.yml` is `workflow_call`-only, so `gh workflow list` and direct `gh run list --workflow extension-release-proof.yml` are not the authoritative hosted proof surface. `scripts/verify-m034-s05.sh` now queries `publish-extension.yml` on the tag and treats the caller run's `Verify extension release proof` job as the truthful extension-proof signal.
- For the M034/S07 remote rollout, the HTTPS receive-pack timeout is recoverable by staged fast-forward pushes even after the full-range push fails. On this host, pushing `main` in growing prefixes succeeded through 1/3/7/15/31/63 missing commits (advancing remote `main` from `5ddf3b2dce17abe08e1188d9b46e575d83525b50` to `8d3e76a65986e7bd5e00d107f9e11c1923ecd0ab`), while the next 64-commit remainder (`b7ac829b59576dc7368587d736831e12a6ea5fdc`) still hit `HTTP 408` after ~11.5 minutes. Resume from the last proven remote SHA and shrink the remaining window instead of retrying the full head push.
- In `scripts/verify-m034-s05.sh`, a replay that fails at `remote-evidence` never cleans or refreshes `.tmp/m034-s01/verify/`, so `find .tmp/m034-s01/verify -name package-version.txt` can still succeed from an older S01 run. Treat `package-version.txt` as authoritative only when paired with fresh `.tmp/m034-s05/verify/status.txt=ok`, `.tmp/m034-s05/verify/current-phase.txt=complete`, and late-phase `phase-report.txt` passes from the same replay.
- In `scripts/lib/m034_public_surface_contract.py`, prefer `curl`-backed fetches over Python `urllib` for live HTTPS checks on this host. The canonical `public-http` sweep can otherwise fail locally with certificate-validation errors before it measures real `meshlang.dev` drift, while `curl` successfully reaches the same public surfaces and exposes the actual stale-body / missing-marker failures.
- For the M034/S08 tag-rollout evidence, remote tag creation via `gh api repos/snowdamiz/mesh-lang/git/refs` does trigger the hosted tag workflows, but the current blocker is not ref propagation: `publish-extension.yml` went green on `ext-v0.3.0`, while `deploy-services.yml` failed because `packages-website`'s Docker runtime layer reruns `npm install --omit=dev --ignore-scripts` and hits an `ERESOLVE` peer-dependency conflict (`vite@6.4.1` vs `@sveltejs/vite-plugin-svelte@7`). If the first-green archive is still missing after tags exist, inspect `.tmp/m034-s08/tag-rollout/deploy-services-v0.1.0-log-failed.txt` and `packages-website/Dockerfile` / package dependency ranges before touching the tag-monitoring flow again.
- The same M034/S08 tag push also exposed separate `release.yml` hosted regressions on `v0.1.0`: multiple `Verify release assets (...)` jobs fail because the installed `meshc` smoke build cannot locate `libmesh_rt.a`, macOS release-asset checksum steps call `sha256sum` where the runner only has `shasum`, and the Windows checksum step currently uses `Select-Object -First 1,` in a way PowerShell treats as an invalid `System.Int32` argument. If the release lane stays red after tags are present, start with `.tmp/m034-s08/tag-rollout/release-v0.1.0-log-failed.txt` and the platform branches inside `.github/workflows/release.yml` / `scripts/verify-m034-s03.{sh,ps1}`.
- For `packages-website`, do not reinstall runtime dependencies in a separate Docker stage with `npm install --omit=dev --ignore-scripts`. That re-resolves the Svelte/Vite peer tree and currently fails with `ERESOLVE` (`vite@6.4.1` vs `@sveltejs/vite-plugin-svelte@7`). The stable image pattern is `npm ci` -> `npm run build` -> `npm prune --omit=dev` in the builder stage, then copy the pruned `node_modules`, `package.json`, and `build/` into the runtime stage. The authoritative local proof is `.tmp/m034-s08/deploy-services-local-build{,-prechange}.log` plus `bash scripts/verify-m034-s05-workflows.sh`.
- For the M034/S08 `release.yml` smoke lane, the staged verifier only downloads `meshc` / `meshpkg` archives; it does **not** stage the Mesh runtime library. Because installed `meshc` auto-detects `libmesh_rt.a` from the local Cargo target tree, the truthful hosted contract is: install Rust on the smoke job, run `cargo build -q -p mesh-rt` before `scripts/verify-m034-s03.{sh,ps1}`, generate Unix `SHA256SUMS` with a portable Python `hashlib` helper instead of `sha256sum`, and keep the Windows archive selection as separate `Select-Object -First 1` bindings rather than `Select-Object -First 1,`. The local replay is `M034_S03_PREBUILT_RELEASE_DIR=.tmp/m034-s08/prebuilt-release-assets bash scripts/verify-m034-s03.sh` plus `bash scripts/verify-m034-s02-workflows.sh`.
- For M034/S08 tag rerolls, remote ref mutation alone is only viable if the repaired target SHA already exists on GitHub. If local `HEAD` is ahead of `origin/main` and `gh api repos/snowdamiz/mesh-lang/commits/<sha>` returns `HTTP 422`, stop before asking for tag-retarget approval: GitHub cannot point `v0.1.0` / `ext-v0.3.0` at a local-only commit, so the real blocker is the missing rollout push, not stale tag refs.
- In `scripts/verify-m034-s05.sh` / `scripts/verify-m034-s06-remote-evidence.sh`, treat freshness as a separate signal from overall workflow health: if a hosted run’s `headSha` matches the expected remote ref but the workflow still fails for another reason, keep `freshnessStatus: ok` and `headShaMatchesExpected: true` so later agents do not misread a red workflow as a stale-ref mismatch.
- For M034/S09 rollout-target isolation, the clean way to get one concrete SHA without shipping unrelated branch content is a synthetic local commit object built from a temporary index (`git read-tree origin/main` + `git update-index --cacheinfo ...` + `git commit-tree`). That yields a pushable target SHA and diff proof without moving any refs or mutating GitHub.
- For M034/S09 remote rollout without `git push`, `gh api repos/<repo>/git/trees` plus `gh api repos/<repo>/git/commits` can recreate the right tree and message on GitHub, but the commit API normalizes author/committer timestamps to UTC. That changes the commit SHA relative to a local `git commit-tree` object even when parent, tree, author, committer, and message are otherwise equivalent, so record and approve the GitHub-created replacement SHA before retargeting refs.
- In `scripts/verify-m034-s03.ps1`, do not read `$LASTEXITCODE` directly under `Set-StrictMode -Version Latest`: a pure PowerShell command can leave it unset and throw before the verifier records artifacts. Use `Get-Variable LASTEXITCODE -ErrorAction SilentlyContinue` and treat the unset case as exit code 0.
- For the hosted `authoritative-verification.yml` lane, getting past transient curl SSL timeouts is not sufficient proof that `scripts/verify-m034-s01.sh` is stable. On the repaired S09 reroll (`8e6d49dacc4f4cd64824b032078ae45aabfe9635`), the version-specific metadata/download endpoints succeeded, but the package-level `latest` view still lagged the just-published proof version and failed with `package latest version drifted`. If that lane goes red again, inspect registry latest-version propagation / consistency before touching the fetch-retry wrapper.
- On the same repaired S09 reroll (`8e6d49dacc4f4cd64824b032078ae45aabfe9635`), a green `scripts/tests/verify-m034-s03-last-exitcode.ps1` does **not** mean the hosted Windows release smoke is fixed. The remaining release blocker is later in the staged installer path: `scripts/verify-m034-s03.ps1` still fails at `installed meshc.exe build installer smoke fixture failed`. If `release.yml` is the only red lane left, start with `.tmp/m034-s09/t06-blocker/23663179715-failed.log` before reworking the strict-mode helper again.
- For the Windows installed-compiler path, `mesh-codegen` must treat the runtime as a target-specific file, not a Unix-style `-lmesh_rt` name: Unix hosts still link the resolved `libmesh_rt.a` path with `cc`, but Windows MSVC must look for `mesh_rt.lib` and use `clang(.exe)` as the driver. The fast local proof surface is `cargo test -p mesh-codegen link -- --nocapture` plus `pwsh -NoProfile -File scripts/tests/verify-m034-s03-last-exitcode.ps1`; if hosted smoke goes red again, inspect `.tmp/m034-s03/windows/verify/run/07-hello-build.log` for the recorded exit code, runtime path, and linker error first.
- For Shiki-vs-TextMate grammar parity in this repo, do not rely on `highlighter.codeToTokens(...)` alone for scope-level proof. It collapses Mesh strings into single themed tokens even when the underlying grammar scopes matter. Use `highlighter.getLanguage('mesh').tokenizeLine(...)` for the docs-side scope contract, and keep `codeToHtml(...)` only as the render-path smoke check.
- For M034/S10 hosted rerolls, a workflow-only synthetic commit is not enough to close the tag lane: after rolling only `.github/workflows/release.yml` to remote SHA `e59f18203a30951af5288791bf9aed5b53a24a2a`, `authoritative-verification.yml` on `main` went green (`23667365836`) but `release.yml` on `v0.1.0` still failed (`23667370566`) because the tag run was still executing the older verifier/code surfaces from the rest of the tree. If S10 resumes from that state, roll out the full local slice content before trusting another hosted rerun, and treat `.tmp/m034-s10/release-artifact-rerun/` as workflow-only evidence rather than as a verdict on the full local fix set.
- For the later approved-tag reroll on `v0.1.0` (`release.yml` run `23669185030` at SHA `1e83ea930fdfd346b9e56659dc50d2f759ec5da2`), the authoritative Windows blocker lives in the uploaded artifact `release-smoke-x86_64-pc-windows-msvc-diagnostics`, not the top-level workflow log: `.tmp/m034-s11/t03/diag-download/windows/verify/run/07-hello-build.log` records `exit_code: -1073741819` (access violation) with empty stdout/stderr. Start from that artifact before guessing at the next fix.
- A fresh S11 replay confirmed that the current failing `release.yml` run on `1e83ea930fdfd346b9e56659dc50d2f759ec5da2` already includes `Install LLVM 21 for Windows smoke verifier`, `Set LLVM prefix for Windows smoke verifier`, and `Build mesh-rt for smoke verifier` before `Verify staged installer assets (Windows)` fails. Do not resume by re-adding the smoke-toolchain workflow patch; the remaining blocker is the installed Windows `meshc.exe build` path itself, not a missing workflow step.
- For the M034/S12 Windows smoke seam, set `MESH_BUILD_TRACE_PATH` before invoking `meshc build` and read the resulting JSON trace before guessing at linker bugs. A missing trace keeps the failure classified as `pre-object`; `objectEmissionCompleted=true` with `runtimeLibraryExists=false` is the runtime-lookup bucket, and `objectEmissionCompleted=true` with `linkStarted=true` is the link-time bucket.
- In `tools/editors/vscode-mesh/syntaxes/mesh.tmLanguage.json`, keep string interpolation as one shared repository rule reused by both double- and triple-quoted strings, and put a recursive nested-brace pattern ahead of `source.mesh` inside interpolation bodies. Without that extra brace matcher, TextMate and Shiki both drop the final interpolation end scope on cases like `${Map.get(meta, {id: 1})}` even though simpler `${...}` cases still look green.
- For the installed Windows compiler path after M034/S12/T02, `mesh-codegen` preflights runtime discovery and Windows clang resolution **before** LLVM object emission. If `CARGO_TARGET_DIR` or `MESH_RT_LIB_PATH` is wrong, or `LLVM_SYS_211_PREFIX` does not contain `bin/clang.exe`, expect a normal Rust error with `objectEmissionStarted=null` in the build trace rather than another opaque crash; the verifier-side contract is `Push-InstalledBuildEnvironment` in `scripts/verify-m034-s03.ps1`, which exports repo-root `target/` through `CARGO_TARGET_DIR` before `installed meshc.exe build`.
- For GitHub Actions reruns in this repo, `gh run rerun <run-id>` keeps the same workflow run ID and replaces the jobs/artifacts under that run instead of minting a new run record. For M034 release-smoke refreshes, treat the rerun as a fresh attempt on the existing `release.yml` run ID, then re-download diagnostics and re-read the job URLs/artifacts instead of looking for a new run number.
- When a milestone grows remediation slices (S06–S12 in M034), the pattern that kept progress honest was: each slice has a single acceptance command, remediation slices fail-close on the same command as the slice they remediate, and the milestone can only close when the terminal acceptance command goes green. Without that chain, remediation work becomes open-ended troubleshooting disconnected from the milestone's actual acceptance gate.
- For multi-subsystem release verification, one assembled wrapper script that calls subsystem verifiers in order and fail-closes on the first failing phase is more maintainable than separate CI jobs with cross-job dependencies. The wrapper produces one deterministic artifact root and each phase's logs accumulate in a single run directory, making postmortem analysis straightforward.
- For hosted workflow lanes that depend on a large local diff, staged fast-forward pushes (pushing growing commit-count prefixes) can recover from HTTPS receive-pack timeouts that kill the full-range push. Record the last proven remote SHA and shrink the remaining commit window incrementally.
- Reserved evidence labels like `first-green` should be implemented as write-once contracts: refuse to spend the label unless the source data meets all green criteria, but allow diagnostic/blocker labels for red data so evidence capture does not block on correctness.
- The LLVM 21 Windows clang+llvm tarball does **not** bundle `libxml2s.lib`, but `llvm-config --system-libs` still demands it. vcpkg's `libxml2:x64-windows-static` triplet names the file `libxml2.lib`, not `libxml2s.lib`. The safe fix is: `vcpkg install libxml2:x64-windows-static` then copy the `.lib` into the LLVM prefix `lib/` dir as `libxml2s.lib`. Do not add vcpkg's lib path to `LIB` globally — that reintroduces the original duplicate-symbol conflict when LLVM's own lib dir also contains symbol-overlapping static archives.
- The LLVM 21 prebuilt Windows tarball is built with `LLVM_ENABLE_RPMALLOC=ON`, embedding rpmalloc's `malloc`/`free`/`realloc`/`calloc`/`_msize` overrides into `LLVMSupport.lib`. When statically linked into a Rust binary via llvm-sys, these collide with ucrt.lib's heap symbols (LNK2005). `llvm-lib /REMOVE` does not work on COFF archive members with path-style names. The working fix is: `llvm-ar x` to extract all objects, delete rpmalloc objects by filename, compile a no-op stub for `rpmalloc_linker_reference` (referenced by `InitLLVM.cpp.obj`), then `llvm-ar rcs` to rebuild. Bump the LLVM cache key after stripping so old cached copies are invalidated.
- On Windows MSVC, `clang.exe` treats bare `.lib` names (e.g. `ws2_32.lib`) as file paths and fails with "no such file or directory." Use `-Wl,ws2_32.lib` to forward them to `link.exe` which resolves them from the system library path.
- The `mesh_rt.lib` Rust staticlib's transitive dependencies (ureq/TLS, bundled SQLite, crossbeam, rand, Rust std) need Windows system libraries at final link time: `ws2_32`, `userenv`, `advapi32`, `bcrypt`, `ntdll`, `kernel32`, `msvcrt`, and `synchronization`. If a future dep adds more Windows API usage, the list in `compiler/mesh-codegen/src/link.rs` may need expanding.
- In `release.yml`, the "Authoritative live proof" and "Create Release" jobs are gated on `startsWith(github.ref, 'refs/tags/v')` and will always show as skipped on main pushes. This is by design — they only run on release tags.
- For Windows MSVC release-smoke debugging, keep `clang.exe -v` enabled on the installed-compiler link path. The verbose driver output is the fastest way to confirm which `-Wl,`-forwarded system libraries and archive paths actually reached `link.exe` in hosted logs.
- For the Neovim Mesh pack, `*.mpl` already collides with Vim/Neovim's built-in Maple detection. Use `set filetype=mesh` in `tools/editors/neovim-mesh/ftdetect/mesh.vim`, not `setfiletype mesh`, or the builtin Maple detector can win and the syntax smoke will silently open Mesh files as `maple`.
- In `tools/editors/neovim-mesh/lua/mesh.lua`, build the `meshc` discovery start-points explicitly instead of iterating `ipairs({ root_dir, file_path, cwd })`. Lua stops `ipairs` at the first `nil`, so a standalone buffer with `root_dir == nil` silently skips both buffer-path and cwd ancestry and drops the repo-local `target/debug/meshc` fallback that single-file dogfooding depends on.
- The shared S01 syntax corpus is not safe to open in Neovim directly: some cases point at Markdown source lines, not `.mpl` files. For Neovim proof, materialize every case into a temporary `.mpl` snippet first, reject selections that are not inside a ` ```mesh ` / ` ```mpl ` fence, and make the smoke runner consume only that generated manifest so docs-backed cases stay debuggable by case id.
- Keep `tools/editors/neovim-mesh/syntax/mesh.vim`'s regex literal matcher intentionally narrower than the VS Code/TextMate grammar unless there is new corpus-backed proof for a broader Vim pattern. The current classic-syntax contract only promises stable `~r/.../[ims]*` coverage because broader Vim regex matches became unstable enough to create false positives and flaky highlighting.
- The VS Code Extension Development Host smoke can exit with code 1 before the suite entrypoint runs on a fresh `.tmp/m036-s03/vscode-smoke/{user-data,extensions}` state. Treat that as a harness-launch flake only when `smoke.log` never reaches `[suite] Starting VS Code smoke suite`; the repo-owned fix is to reset those VS Code state dirs and retry once in `tools/editors/vscode-mesh/src/test/runTest.ts`, while any failure after the suite-start marker remains a real smoke failure.
- For editor support in this repo, treat support tiers as operational contracts: an editor is only "first-class" when Mesh owns both the install/runbook path and a repo-owned verifier for that exact editor host. Generic LSP or TextMate compatibility alone stays "best-effort" until it has editor-specific proof.
- In `compiler/mesh-rt/src/dist/node.rs`, bracket-aware node-name parsing is not enough for IPv6 support by itself. Listener/connect paths must use tuple socket APIs (or equivalent bracket-safe formatting) instead of rebuilding raw `host:port` strings, or names like `node@[::1]:9000` parse correctly but still fail at the TCP layer.
- For `cluster-proof/cluster.mpl`'s live HTTP payload, the stable Mesh shape is a concrete `struct ... deriving(Json)` for the string/list membership fields, with `/membership` port values read back from `Env.get(...)` strings. The naive helper path (direct `Json.encode(...)` of local strings, string `replace`, or request-path integer interpolation) can still miscompile into misaligned-pointer crashes or garbage port numbers under `cargo test -p meshc --test e2e_m039_s01 ...`.
- `scripts/verify-m039-s01.sh` is the authoritative local M039/S01 acceptance gate. It must fail closed if a named Cargo filter omits the `running N test` line or executes 0 tests, otherwise discovery/member-truth regressions can look green while the harness silently skips the intended proof.
- In the current Mesh runtime, distributed `Node.spawn` arguments and `send(...)` mailbox payloads still travel as raw value bytes, not deep-serialized Mesh data. Cross-node protocols in `cluster-proof` must stay scalar-only unless the runtime transport grows real serialization for strings/structs.
- `self()` is not available inside ordinary HTTP handler functions in Mesh. A handler-side request/reply design that expects a mailbox wait on the request actor is currently invalid; use a runtime path that returns through the spawned work itself or introduce an actual actor/service boundary first.
- In `cluster-proof/work.mpl`, spawned worker arguments are not a trustworthy request-correlation channel yet: the live S02 route only stayed truthful once the HTTP response and peer-log proof both normalized to the observed `work-0` token and the two-node e2e harness treated startup order as significant (start the ingress node first, then its peer, for the direction you are proving). If future work wants per-request unique correlation or symmetric bidirectional routing in one cluster lifetime, investigate Mesh spawn-argument transport before changing the proof harness.
- `scripts/verify-m039-s02.sh` is the authoritative local M039/S02 acceptance gate. It must replay `scripts/verify-m039-s01.sh` first, fail closed on missing `running N test` evidence for the named `e2e_m039_s02` filters, and preserve copied routing artifacts under `.tmp/m039-s02/verify/04-s02-remote-route-artifacts/` and `05-s02-local-fallback-artifacts/` so downstream debugging can compare the `node-*-work.json` route body against the execution-node stdout log for the same `request_id`.
- For `cluster-proof` continuity proofs, do not rely on cross-node actor arguments to carry request correlation through a same-identity restart. In M039/S03, remote `Node.spawn(..., request_token)` reused `work-0` after rejoin and `Node.spawn(..., request_id :: String)` crashed the peer in `mesh-rt` string handling, so the stable contract is: keep unique `request_id` generation on the ingress node, log it in an ingress-side `work dispatched ...` line, and treat run-numbered peer logs plus `*-work.json` artifacts as the trustworthy remote-execution evidence.
- `scripts/verify-m039-s03.sh` is the authoritative local M039/S03 acceptance gate. It must replay `cluster-proof/tests`, `meshc build cluster-proof`, `scripts/verify-m039-s01.sh`, and `scripts/verify-m039-s02.sh` before the named `e2e_m039_s03` degrade/rejoin filters, fail closed on missing `running N test` evidence or malformed copied manifests, and preserve copied proof directories under `.tmp/m039-s03/verify/05-s03-degrade-artifacts/` and `06-s03-rejoin-artifacts/` so downstream debugging can compare pre-loss, degraded, and post-rejoin membership/work JSON against run-numbered node logs from the same cluster lifetime.
- The S04 Fly operator path needs an explicit `MESH_DISCOVERY_SEED=<app>.internal` in `cluster-proof/fly.toml`; Fly’s injected `FLY_APP_NAME` / `FLY_REGION` / `FLY_MACHINE_ID` / `FLY_PRIVATE_IP` are only the identity half of the cluster contract. The read-only verifier `scripts/verify-m039-s04-fly.sh` intentionally fail-closes before any Fly API call unless `CLUSTER_PROOF_BASE_URL` matches `https://<app>.fly.dev`, so docs and future probes should keep that app/base-URL pairing exact instead of treating custom hosts as equivalent proof targets.
- For one-image clustered operator proofs, capture DNS preflight evidence before trusting membership or `/work` artifacts. In M039/S04, `getent ahostsv4` results under `.tmp/m039-s04/verify/05-dns-preflight/` are what distinguish discovery-alias drift from actual runtime membership/routing regressions after the fact.
- A missing Fly app is an environment-evidence gap, not automatically an in-repo implementation failure. During M039 closeout, `scripts/verify-m039-s04-fly.sh` failing with `Could not find App "mesh-cluster-proof"` still left the local one-image verifier, Fly config, help/syntax contract, and docs-truth verifier as the authoritative shipped surfaces; keep live-Fly proof as a separate evidence step when no deployment exists.
- For new stdlib-style Mesh modules, the compiler seam is wider than `compiler/mesh-typeck/src/infer.rs`: add the module/type surface in both typeck registrations (`infer.rs` and `builtins.rs`) and mirror the name/module updates in MIR lowering (`compiler/mesh-codegen/src/mir/lower.rs`, plus `mir/types.rs` if new opaque/runtime-backed types are introduced). Stopping at the typechecker leaves `Module.fn(...)` parsing/type-checking green while codegen still cannot lower the call.
- For M042/S01 continuity closeout, avoid reading `CanonicalPlacement.membership` across the `Cluster` -> `Work` module boundary on the hot submit path. The imported list field reached `find_index(...)` as a null list and crashed in `compiler/mesh-rt/src/collections/list.rs:23`; the stable shape was to recompute canonical membership locally in `work.mpl` and only consume `placement.owner_node` / `placement.replica_node` from `canonical_placement(...)`. Also keep the `e2e_m042_s01` helper responsible for `cargo build -p mesh-rt` before `meshc build cluster-proof`, or new `mesh_continuity_*` ABI exports can link against a stale `target/debug/libmesh_rt.a` and fail with missing symbols.
- For M042/S02, the real durability-aware submit path now lives behind `mesh_continuity_submit_with_durability(...)`, while legacy `mesh_continuity_submit(...)` intentionally defaults `required_replica_count=0` as a compatibility wrapper until the compiler/runtime seam is updated. If cluster-mode continuity still looks like it bypasses fail-closed replica admission after T01, start in the compiler intrinsic/typeck/lowering call sites rather than reopening `compiler/mesh-rt/src/dist/continuity.rs` or `compiler/mesh-rt/src/dist/node.rs` first.
- In `cluster-proof/work.mpl`, avoid `&&` inside the small public submit-response helpers (`submit_response_status_code`, `submit_response_payload`) for now. Even the simple `outcome == "duplicate" && record_is_rejected(record)` shape compiled far enough to reach LLVM, then failed module verification with mismatched PHI predecessors during `cargo run -q -p meshc -- test cluster-proof/tests`; the stable workaround is nested `if outcome == ... do if record_is_rejected(...) ... end end` branching.
- For M042/S02 continuity e2e work, the ordinary `/work/:request_key` status API surfaces replica-loss degradation through `replica_status == "degraded_continuing"` while keeping `ok=true` and `error=""`; the replica-loss reason only remains in the runtime stderr log (`transition=degraded ... reason=replica_lost:<node>`). If a future proof expects the loss reason in the HTTP payload, that is a product-surface change, not a harness bug.
- `scripts/verify-m042-s02.sh` is the authoritative local M042/S02 acceptance gate. It must replay the stable prerequisites (`cargo build -q -p mesh-rt`, `cargo run -q -p meshc -- test cluster-proof/tests`, `cargo run -q -p meshc -- build cluster-proof`, and the standalone `e2e_m042_s01` regression) before the named `e2e_m042_s02` filters, fail closed on missing `running N test` evidence, and preserve copied bundles under `.tmp/m042-s02/verify/05-rejection-artifacts/`, `06-mirrored-artifacts/`, and `07-degraded-artifacts/` so future debugging can compare HTTP status truth with the per-node stderr continuity transitions.
- For M042/S03 owner-loss recovery, `cluster-proof/work.mpl` must only drop the submit-time replica requirement to `0` when the existing runtime continuity status for that request key is still `phase=submitted`, `result=pending`, and `replica_status=owner_lost`. If a same-key retry regresses back to `replica_required_unavailable`, start in the thin consumer's status-driven `required_replicas` decision before reopening `compiler/mesh-rt/src/dist/continuity.rs`.
- `scripts/verify-m042-s03.sh` is the authoritative local M042/S03 acceptance gate. Replay the shared `cargo test -p meshc --test e2e_m042_s03 -- --nocapture` target once, assert both named scenarios passed in that run, then copy the owner-loss and rejoin artifact bundles from the shared replay under `.tmp/m042-s03/verify/04-owner-loss-recovery-artifacts/` and `05-rejoin-truth-artifacts/`. The isolated rejoin filter is timing-sensitive enough to miss `owner_lost` before rejoin even when the full target is green, so the shared replay plus copied artifact manifests is the durable truth surface.
- Do not run `cargo test -p meshc --test e2e_m042_s03 -- --nocapture` in parallel with `bash scripts/verify-m042-s03.sh`. They exercise the same cluster-proof recovery surface at the same time and can produce false red e2e failures even when the serialized acceptance rail is green.
- For M042/S04 public docs and help text, treat `bash scripts/verify-m042-s03.sh` as the current local continuity authority and keep `bash scripts/verify-m042-s04-fly.sh --help` / live mode scoped as read-only sanity only. `scripts/verify-m042-s04.sh` may still exist as the packaged wrapper, but public wording should not promote it as the authoritative recovery proof while the packaged remote-owner path is still blocked.
- For M042/S04 remote execution from `cluster-proof`, `Node.spawn(...)` must run inside actor context. Calling it directly from the HTTP handler path can fail silently on the ingress side even when local `spawn(...)` works; the stable pattern is a tiny local bridge actor that performs the remote `Node.spawn(...)` on behalf of the handler.
- The M039 `cluster-proof` e2e HTTP clients must keep their `TcpStream` read timeout comfortably above the legacy `/work` handler's completion budget. `run_legacy_probe_record(...)` can legitimately spend about 2 seconds polling runtime continuity (`80 * 25ms`), so the old 2-second client timeout in `compiler/meshc/tests/e2e_m039_s0{1,2,3}.rs` produced false `Resource temporarily unavailable (os error 35)` failures inside `bash scripts/verify-m039-s04.sh` even when the remote route/degrade contract was healthy. If that wrapper flakes again with ingress `/work` timeouts after peer execution already started, check the harness timeout margin before reopening `mesh-rt` session logic.
- `bash scripts/verify-m042-s04.sh` is now the authoritative assembled local acceptance rail for M042 closeout: it replays `scripts/verify-m042-s03.sh`, builds the packaged `cluster-proof` image, proves packaged two-node convergence, forces a remote-owner keyed submit, and requires completed keyed status on both ingress and owner nodes. If milestone-level continuity looks red while S02/S03 unit rails are green, start with `.tmp/m042-s04/verify/` before trusting any isolated older filter.
- For M042 packaged continuity debugging, treat the old direct `e2e_m042_s01` two-node filter and the assembled S04 wrapper as different surfaces. A direct replay can still hit handshake/client-timeout seams that do not reflect the shipped packaged contract, while `scripts/verify-m042-s04.sh` is the current end-to-end proof for the one-image operator path.
- For M043/S01 authority metadata, `preferred_record(...)` must keep `replica_status` precedence ahead of `replication_health`. If health outranks replica state, a stale mirrored update can overwrite real `owner_lost` or `degraded_continuing` truth, which makes standby-safe replication look healthy while erasing the real single-cluster recovery signal.
- In `cluster-proof/config.mpl`, avoid public or cross-branch helpers that return `Int ! String` for topology parsing. During M043/S01/T02, a seemingly harmless `continuity_promotion_epoch_from_parts(...) -> Int ! String` helper reproduced the known boxed-primitive crash (`Segmentation fault: 11`) on the cluster path, while the same contract stayed stable as `error-string + plain Int getter`. If future config validation starts crashing only when the epoch helper is exercised, remove `Result<Int, _>` seams before suspecting the role/epoch logic itself.
- For M043/S01/T03, the truthful local primary→standby proof seam is one connected mesh node network with explicit `MESH_CONTINUITY_ROLE`/epoch per node, not a separate second replication transport. `mesh-rt` continuity state currently mirrors across established node sessions, so the authoritative destructive rail is `compiler/meshc/tests/e2e_m043_s01.rs` plus `bash scripts/verify-m043-s01.sh`, which proves standby role/epoch/health truth and non-executing standby behavior without implying promotion or failover already exist.
- In `cluster-proof/main.mpl`, interpolating fields from an imported runtime-authority struct through an untyped function parameter can fail the whole `meshc build cluster-proof` step with the misleading bare diagnostic `Undefined variable 'to_string'`. If a new authority/log helper compiles in package tests but the app build dies with that error, annotate the parameter with `:: ContinuityAuthorityStatus` before reopening routing or JSON code.
- For the M043/S03 same-image Docker failover harness, the promoted standby's post-rejoin `replication_health` can legitimately settle as either `local_only` or `healthy` while the real proof facts stay the same (`membership` contains both nodes, standby remains `cluster_role=primary`, old primary reports `cluster_role=standby`, and stale-primary same-key requests replay the promoted attempt without executing locally). Keep the rejoin matcher strict on ownership/epoch/fence fields, but allow both health values for the promoted standby after rejoin or the packaged rail will flake on timing rather than truth.
- `cargo test -p meshc --test e2e_m043_s03 -- --nocapture` creates two new `.tmp/m043-s03/` directories in one green run: the real same-image failover bundle (`scenario-meta.json` present) and the malformed-response guard bundle (`continuity-api-same-image-failover-malformed-*`, no scenario metadata). Packaged verifiers must select the copied proof directory by `scenario-meta.json`/required-file shape, not by a loose `continuity-api-same-image-failover-*` prefix alone, or they can latch onto the negative-test artifact and fail for the wrong reason.
- For M043/S03 same-image operator startup, mirror the continuity role/epoch validation in `cluster-proof/docker-entrypoint.sh` instead of relying on the compiled package to fail later. Contradictory cluster-mode env can otherwise log a config error yet still look like a successful runtime start; the honest packaged seam is an early non-zero entrypoint failure plus a verifier probe that confirms no runtime-start log line and no cookie leakage in the retained misconfig log.
- `scripts/verify-m043-s04-proof-surface.sh` matches the Fly live command list as literal markdown text, including doubled trailing backslashes in the continued `CLUSTER_PROOF_FLY_APP=...` / `CLUSTER_PROOF_BASE_URL=...` lines. If the proof-surface rail fails at `command-list` even though the docs look visually correct, read `.tmp/m043-s04/proof-surface/command-list.content-check.log` first and make the proof page/runbook match the verifier's exact escaped form before rewriting broader failover prose.
- For the M043/S04 read-only Fly verifier, parse `fly config show` as TOML rather than depending on a JSON-only CLI shape. The stable contract check is the packaged `cluster-proof/fly.toml` values (`cluster-proof/Dockerfile`, `PORT=8080`, `MESH_CLUSTER_PORT=4370`, `MESH_DISCOVERY_SEED=<app>.internal`, `CLUSTER_PROOF_DURABILITY=replica-backed`, and `auto_stop_machines` off), so if the Fly CLI output format drifts, start in `scripts/verify-m043-s04-fly.sh::assert_config_toml(...)` before weakening the operator claim.
- For M043 milestone closeout, treat `bash scripts/verify-m043-s04-fly.sh --help` plus the fail-closed `CLUSTER_PROOF_FLY_APP is required` path as the authoritative local Fly evidence when no deployed app is available. A local `.tmp/m043-s04/fly/status.txt=failed` from missing live-mode env is an environment gap, not a contradiction of the shipped read-only Fly contract.
- `bash scripts/verify-m044-s01.sh` is the authoritative local acceptance rail for M044/S01. It relies on the stable test-name filters `m044_s01_clustered_manifest_`, `m044_s01_manifest_`, `m044_s01_typed_continuity_`, and `m044_s01_continuity_compile_fail_`, fail-closes on missing `running N test` evidence, refreshes `mesh-rt` before replay, and keeps phase markers under `.tmp/m044-s01/verify/` so manifest-contract drift, typed continuity ABI regressions, and `cluster-proof` consumer drift stay distinguishable.
- For the M044/S01 public clustered-app seam, keep the compiler/runtime contract synchronized in four places whenever continuity fields change: `compiler/mesh-typeck/src/{builtins,infer}.rs`, the pre-seeded MIR structs in `compiler/mesh-codegen/src/mir/lower.rs`, the intrinsic declarations in `compiler/mesh-codegen/src/codegen/intrinsics.rs`, and the `MeshContinuity*` payload structs plus `mesh_continuity_*` exports in `compiler/mesh-rt/src/dist/continuity.rs`. If one layer is updated without the others, `Continuity.*` field access or temp-project linkage will drift even when the runtime logic itself is still correct.
- For M044/S02 clustered execution planning, consume explicit `ServiceExportInfo.method_exports` kind metadata instead of re-deriving service call/cast/start roles from `__service_*` name prefixes in each consumer. The prefix guess drifted between `meshc` and `mesh-lsp`; keeping the kind on the typeck export is the stable seam for declared-handler execution metadata.
- For M044/S02 `cluster-proof` dogfood work, do not treat the remaining rewrite as an app-only task while `compiler/meshc/src/main.rs::build(...)` still drops `PreparedBuild.clustered_execution_plan` on the floor and `mesh-rt` still exposes only the generic `mesh_register_function` remote-spawn registry. In that state there is no real declared-handler execution surface for T04 to dogfood yet; the truthful signals are `cargo test -p meshc --test e2e_m044_s02 m044_s02_cluster_proof_ -- --nocapture` running 0 tests and `cluster-proof/work_continuity.mpl` still owning `current_target_selection(...)` plus direct `Node.spawn(...)` dispatch.
- For M044/S02 declared-work execution, `compiler/mesh-rt/src/dist/node.rs`'s remote spawn path still only accepts actor entrypoints (`extern "C" fn(*const u8)`) and resolves them through `mesh_register_function` before calling `mesh_actor_spawn(...)`. That means manifest-declared work cannot honestly dispatch plain public function symbols by alias alone; the real T05 seam needs compiler-generated actor-style wrappers (or an equivalent runtime-specific entrypoint surface) before a declared-work registry/dispatch path will execute anything.
- For M044/S02 declared service dispatch, the runtime already has the reply transport you need: `mesh_service_reply(...)` delegates to `mesh_actor_send(...)`, and `mesh_actor_send(...)` already routes remote-qualified PIDs over node sessions. The missing T06 seam is therefore declared-wrapper registration/lowering (plus target-PID localization for the remote owner), not a second cross-node reply protocol.
- For M044/S02 T09 registry plumbing, keep every manifest-declared `executable_symbol` as an explicit root when merged MIR is monomorphized. Otherwise locally-unused declared helpers (especially service call/cast helpers that the entrypoint never invokes) get pruned before `prepare_declared_runtime_handlers(...)` runs, and valid manifests start failing with missing lowered-helper errors even though the typeck/export metadata is correct. The compile-safe T09 boundary is runtime-name -> existing lowered symbol registration; actor-style work wrappers and real clustered service wrappers still belong to later tasks.
- For M044/S02 T10 resume, the declared-work runtime seam is already live under `mesh_continuity_submit_declared_work(...) -> compiler/mesh-rt/src/dist/node.rs::submit_declared_work(...)`: it looks up `mesh_register_declared_handler` registrations, computes placement internally, and dispatches through `mesh_actor_spawn` / `mesh_node_spawn`. The next unit should start from the app seam instead: `cluster-proof/work_continuity.mpl` still calls a missing `declared_work_target()` helper, and `cargo run -q -p meshc -- build cluster-proof` currently stops in `cluster-proof/main.mpl` around the `case runtime_authority_status()` / router setup block before the S02 verifier can reach the new rails.
- For M044/S02 declared-service proof work, `cargo run -q -p meshc -- build <project> --emit-llvm` only captures the pre-registration MIR snapshot. It will show generated declared-service wrapper definitions, but not the `mesh_register_declared_handler(...)` startup calls that are added on the binary codegen path. If a service-registration proof fails while wrapper definitions are present, switch to a `CodeGen::set_declared_handlers(...)`-level check or the native-binary path instead of treating the missing registration call as a lowering regression.
- `bash scripts/verify-m044-s02.sh` is the authoritative local acceptance rail for M044/S02. It replays S01 first, then the named `m044_s02_metadata_`, `m044_s02_declared_work_`, `m044_s02_service_`, and `m044_s02_cluster_proof_` filters, and it intentionally scopes the stale-literal absence checks to the `handle_valid_submit`, `created_submit_response`, and `handle_valid_status` ranges in `cluster-proof/work_continuity.mpl` so legacy probe helpers can survive until S05 without making the new declared-runtime hot path look green.
- For M044/S03 scaffold work, do not treat `cargo test -p meshc --test tooling_e2e test_init_clustered_creates_project -- --nocapture` exiting 0 as proof that `meshc init --clustered` exists yet: in the current tree that filter runs `0 tests`, and `cargo test -p meshc --test e2e_m044_s03 m044_s03_scaffold_ -- --nocapture` still fails with `no test target named e2e_m044_s03`. The honest blocker signals are those two command outputs plus the missing `compiler/meshc/src/cluster.rs` / `compiler/meshc/tests/e2e_m044_s03.rs` files.
- For M044/S04 closeout, do not rewrite the public failover docs/verifier to the auto-only story until the runtime-owned S04 rail actually exists in the tree. In the current checkout, `compiler/meshc/tests/e2e_m044_s04.rs` is still missing (`cargo test -p meshc --test e2e_m044_s04 m044_s04_auto_promotion_ -- --nocapture` fails with `no test target named e2e_m044_s04`), `compiler/mesh-rt/src/dist/node.rs::handle_node_disconnect(...)` only marks/degrades continuity records and never invokes `promote_authority()`, and `submit_declared_work(...)` still has no runtime-owned auto-resume redispatch path beyond the same-key retry seam. Treat `cargo test -p meshc --test e2e_m043_s03 e2e_m043_s03_same_image_failover_fences_stale_primary -- --nocapture` failing alongside those missing S04 rails as a plan blocker, not as proof that the docs are the only stale surface.
- For M044/S05 closeout, if `bash scripts/verify-m044-s05.sh` gets past the S03 replay and then dies in `03-s04-replay`, start in `compiler/meshc/tests/e2e_m044_s04.rs::m044_s04_auto_promotion_promotes_and_resumes_without_retry` before reopening runtime promotion code. The current red path is a stale primary-log expectation (`continuity=runtime-native` at line 754) while the retained primary logs in `.tmp/m044-s04/continuity-api-failover-promotion-rejoin-*/primary.{stdout,stderr}.log` still show runtime-owned keyed submit/executed truth and mirrored continuity transitions.
- For the assembled M044 verifier stack on this host, do not shell verifier substeps through `bash -lc` unless they actually need a login shell. `scripts/verify-m044-s04.sh` was failing inside the docs-truth phase before any repo check ran because the host `~/.profile` sources `/Users/sn0w/.cargo/env`, which is absent in this environment. Use `bash -c` for repo-local verifier snippets so user dotfiles cannot turn a green proof into a false red rail.
- `scripts/verify-m044-s04.sh`'s `latest-proof-bundle.txt` must contain only the retained artifact directory path. If the script writes the phase banner or command text into that pointer file (for example by piping the whole phase output through `tee`), `scripts/verify-m044-s05.sh` fails in `05-retain-failover-bundle` with `malformed failover bundle pointer` even though the underlying S04 failover artifacts are present and green.
- For milestone-scale clustered-app closeout, make the terminal verifier replay the earlier product rails and retain copied evidence bundles instead of inventing a docs-only final gate. `bash scripts/verify-m044-s05.sh` stayed trustworthy because it replays the S03 scaffold/operator rail, the S04 destructive failover rail, the M044/S05 closeout tests, the `cluster-proof` build/test path, and the docs build together; when the final gate went red, the fix was proof-surface drift, not a second parallel acceptance story.
- The current GSD requirements DB does not know about the M044 requirement family (`R061`–`R070`) even though they are rendered in `HEAD`'s `.gsd/REQUIREMENTS.md`. `gsd_requirement_update` rejects those IDs as "not found", and any tool-driven regeneration can drop them from the rendered contract entirely. Until the DB is repaired, treat the checked-in `REQUIREMENTS.md` as the authoritative visible state for M044 closeout and be careful with any command that regenerates requirement artifacts.
- For M045/S01, keep the runtime bootstrap validator and the low-level listener primitive separate: `start_from_env()` should preserve the existing fail-closed env contract (`MESH_CLUSTER_COOKIE`, `MESH_DISCOVERY_SEED`, `MESH_NODE_NAME`, Fly identity), but `mesh_node_start("name@host:0", ...)` still needs its own bind-only parser so the runtime can request an OS-assigned listener port without weakening the public clustered-app env rules.
- For M045/S01, keep clustered-app startup split at the runtime boundary: Mesh apps (`meshc init --clustered`, `cluster-proof/main.mpl`) should call `Node.start_from_env()` and inspect/log `BootstrapStatus`, while packaged entrypoints like `cluster-proof/docker-entrypoint.sh` should only preflight continuity-topology env the runtime bootstrap API does not yet validate. Reintroducing app-owned cluster mode detection, identity parsing, or direct `Node.start(...)` orchestration will drift the M045 simplicity/honesty contract.
- For M045/S02's two-node scaffold proof, query `meshc cluster status --json` from the ingress node before the remote-owner submit, but do not probe the standby with `cluster status` in that pre-submit window. On this runtime/host, the extra standby-side operator query can destabilize the first remote-owned submit on a fresh two-node cluster (`mesh node spawn failed ... : write_error`, standby-side `request_key_not_found`) even though the same proof stays green when continuity truth is checked on both nodes after submit.
- On this host, nested verifier wrappers that transitively call `cargo run -q -p meshc -- test cluster-proof/tests` can hang after the phase log already shows the suite passed. If an assembled verifier gets stuck inside `verify-m045-s01.sh` / `verify-m044-s03.sh` / `verify-m044-s02.sh` at `cluster-proof-tests`, read the leaf phase log first and prefer replaying the direct prerequisite commands in the top-level verifier instead of nesting the older wrappers again.
- For M045/S02 scaffold/runtime checks, `meshc cluster continuity --json` returns the continuity record itself, not an `{ "ok": ... }` wrapper. Read fields like `phase`, `owner_node`, `replica_status`, and `execution_node` directly from the top-level JSON or the proof rail will fail against the real CLI contract.
- Do not run `cargo test -p meshc --test e2e_m045_s02 m045_s02_ -- --nocapture` in parallel with `bash scripts/verify-m045-s02.sh`. They contend on the same two-node scaffold proof surface and can produce false remote-owner failures (`declared_work_remote_spawn_failed ... write_error`) even when the slice contract is healthy.
- For `compiler/meshc/tests/e2e_m045_s03.rs`, the current scaffold failover rail can drift in two different runtime-owned ways on the same tree: some runs exhaust the bounded pre-kill search because primary-owned attempts complete before the harness can observe a stable `phase=submitted` window, while other runs hang a later remote-owner `POST /work` with `Resource temporarily unavailable (os error 35)` after `mesh node spawn failed ... write_error` and a standby-side `mesh continuity: transition=sync_rejected ... standby_owner_lost_invalid`. Resume from the retained `.tmp/m045-s03/scaffold-failover-runtime-truth-*` bundle and inspect `pre-kill-search/` plus both node logs before changing the scaffold source surface.
- For `compiler/meshc/tests/e2e_m045_s03.rs`, do not drive the pre-kill search through standby ingress while targeting a primary-owned request. On this runtime/host, `POST /work` from the standby to a live primary owner can return `503 {"error":"attempt_id_mismatch"}` after `mesh node spawn failed ... write_error` / `write_error_after_reconnect`, and the standby can auto-promote/recover that same request key even though the primary process is still up. If the S03 failover rail is retried, keep ingress on the primary and use the retained `.tmp/m045-s03/scaffold-failover-runtime-truth-1774909467086304000/` bundle as the concrete false-promotion reference.
- For M045/S03, the clustered scaffold only stays truthful across both the single-node S02 rail and the two-node S03 failover rail if `compiler/mesh-pkg/src/scaffold.rs` derives `required_replica_count` from `Node.list()` and keeps a small fixed `Timer.sleep(...)` inside the generated `execute_declared_work(...)`. Without the peer-aware replica count, single-node clustered runs reject local work or two-node runs never mirror; without the brief fixed sleep, primary-owned requests can complete before the destructive failover harness can kill the owner, even after the standby mirror is CLI-visible.
- For the current M045/S03 rejoin proof, treat the selected request's post-rejoin truth as `meshc cluster continuity --json` on both nodes plus the stale-primary duplicate submit response, not as an exact `fenced_rejoin` diagnostics match on the rejoined primary. In retained bundles like `.tmp/m045-s03/scaffold-failover-runtime-truth-1774911704978919000/`, the restarted primary's diagnostics buffer can record a different stale request key than the one that actually auto-recovered, while continuity/status truth for the selected request still stays correct.
- The current GSD requirements DB also does not know about the M045 requirement family (`R077`–`R081`) even though the checked-in `.gsd/REQUIREMENTS.md` renders them. `gsd_requirement_update` rejected `R078` as "not found" during S03 closeout, so until the DB is repaired the checked-in requirements file plus saved requirement decisions are the authoritative visible state for M045.
- For M045/S04 closeout, `scripts/verify-m045-s02.sh` must prebuild `mesh-rt` (`cargo build -q -p mesh-rt`) before the nested `cluster-proof` build/test phases. The package-test path still links temp binaries against `target/debug/libmesh_rt.a`, and without that explicit preflight the assembled `verify-m045-s04.sh` rail can fail inside `cluster-proof/tests` with a misleading missing-runtime-archive linker error even though the S04 app/docs contract is otherwise healthy.
- For M045/S05 closeout, the retained evidence chain is intentionally two levels deep: `bash scripts/verify-m045-s05.sh` copies `.tmp/m045-s04/verify/` into `.tmp/m045-s05/verify/retained-m045-s04-verify/`, and `.tmp/m045-s05/verify/latest-failover-bundle.txt` republishes the S04 replay's pointer to `.tmp/m045-s03/verify/retained-m045-s03-artifacts`. If the S05 wrapper is green but the clustered failover story looks suspect, inspect the copied S04 phase logs first and then follow the republished pointer into the retained S03 scenario bundle instead of expecting a new failover artifact directory directly under `.tmp/m045-s05/verify/`.
- For M046/S01's parser-only checkpoint, the new `compiler/meshc/tests/e2e_m046_s01.rs` source-declared rail currently fails before any declared-handler registration assertion with `LLVM module verification failed: Function return type does not match operand type of return inst! ret {} zeroinitializer i64`, while the paired LSP rail reports no diagnostics at all. If T02 resumes from this state, start in the compiler/LSP clustered-declaration merge path rather than reopening the `clustered(work)` parser intake.
- The current GSD requirements DB also does not know about the M046 requirement family (`R085`–`R096`) even though the checked-in `.gsd/REQUIREMENTS.md` renders them. During M046/S01 closeout, `gsd_requirement_update` rejected `R085` as "not found" after the slice proved the requirement, so until the DB is repaired the checked-in requirements file plus saved requirement decisions are the authoritative visible state for M046.
- For `cargo test -p meshc --test e2e_m047_s02 -- --nocapture`, `meshc build --emit-llvm --output <binary>` writes the emitted `output.ll` beside the requested binary path, not under the temp project root. If the M047 replication-count proof starts failing on a missing LLVM artifact, inspect the explicit output directory before assuming codegen stopped emitting IR.
- The current GSD requirements DB also does not know about the M047 requirement family (`R097`–`R106`) even though the checked-in `.gsd/REQUIREMENTS.md` renders them. During M047/S02 closeout, `gsd_requirement_update` rejected `R098` as "not found" after the slice proved it, so until the DB is repaired the checked-in requirements file plus saved requirement decisions are the authoritative visible state for M047.
- For M046/S01's final green compiler proof, keep `compiler/meshc/tests/e2e_m046_s01.rs` on the broader M044-shaped work fixture until the older single-function LLVM verifier bug is fixed. The source-declaration merge path itself is green, but a smaller source-only fixture that only defines `handle_submit` still reproduces `LLVM module verification failed: Function return type does not match operand type of return inst! ret {} zeroinitializer i64`, so replacing the fixture would create a false red rail unrelated to the clustered-work declaration feature.
- For M046/S02 runtime-owned startup work, spawn the clustered keepalive actor before validating or dispatching startup registrations whenever clustered startup work exists. Otherwise a route-free clustered app that rejects startup work early (for example missing handler metadata) can exit before `meshc cluster diagnostics` captures the failure. Also keep standalone/no-peer boots on the fast path: only require replica-stability polling after a peer has actually been observed, or intentional single-node clustered runs pay a false timeout tax.
- For the M046/S02 two-node route-free startup proof, primary/standby topology must stay primary-owned at boot: keep the standby keepalive actor for inspection, but do not auto-submit startup work from standby authority. When both authorities auto-submit the same deterministic startup key, the proof can drift into `replica_required_unavailable` / `owner_lost` races even though the focused startup-work rails are otherwise healthy. If closeout is red after the standby-skip fix, start with the retained `m044_s03_operator_` replay rather than reopening `compiler/mesh-rt/src/dist/node.rs` first.
- For M046/S02 simultaneous two-node route-free boots, duplicate session admission must converge on one real transport, not "first session wins" by remote name alone. If each node keeps its own accepted half-connection during a simultaneous connect race, the opposite client halves get dropped and the first runtime-owned remote spawn fails with `mesh node spawn failed ... write_error`. The stable rule in `compiler/mesh-rt/src/dist/node.rs` is deterministic direction preference by local/remote node name: the earlier-sorting node keeps the outgoing session and the later-sorting node keeps the incoming session.
- For `tiny-cluster/tests/work.test.mpl`, the stable package-smoke pattern is to enforce source-first and route-free contracts by reading on-disk package files with `File.read`, but to try repo-root, package-root, and tests-root-relative paths in that order because `meshc test <dir>` does not guarantee a single working-directory shape. Also remember the current Mesh test DSL only supports `assert_eq` for strings; use boolean `assert(x == y)` for Int checks. After the M046/S03 runtime-owned delay migration, `tiny-cluster/work.mpl` must stay plain `1 + 1` with no package-owned delay hook; the failover-only pending window now lives in `compiler/mesh-rt/src/dist/node.rs` behind `MESH_STARTUP_WORK_DELAY_MS`.
- For `compiler/meshc/tests/e2e_m046_s03.rs`, the startup request key is deterministic (`startup::Work.execute_declared_work`) but the owner node still depends on the hashed node-name ordering, so the destructive failover rail must choose a cluster port whose computed owner is the primary. Also note that `automatic_promotion` diagnostics are cluster-level entries with no `request_key`, so request-scoped diagnostics helpers will silently miss the promotion transition.
- For enduring slice plans like `.gsd/milestones/M046/slices/S03/S03-PLAN.md`, do not try to prove current-state cleanup by banning every superseded term from the whole file: historical task text is allowed to remain. Add an explicit override line that states the current contract, then make verifier checks assert that override line rather than treating historical references as drift.
- In Mesh package smoke tests, avoid chaining three local boolean file-existence checks with `&&` inside one expression. The current compiler can emit invalid LLVM PHI nodes for that shape (`LLVM module verification failed: PHI node entries do not match predecessors`). The stable pattern is to compute each `File.read(...)` result separately and combine them with nested `if` branches instead, as used in `cluster-proof/tests/work.test.mpl`.
- The current `cargo test -p meshc --test e2e_m046_s03 m046_s03_tiny_cluster_ -- --nocapture` failures are still pinned to a plan-text assertion in `.gsd/milestones/M046/slices/S03/S03-PLAN.md`, not to `cluster-proof/` runtime or packaging drift. If that slice-level gate is red while `meshc test cluster-proof/tests` and the `cluster-proof` Docker build are green, inspect the expected override line in `compiler/meshc/tests/e2e_m046_s03.rs` before reopening the package work.
- For M046/S04 route-free proof rails, keep the packaged-binary harness on `compiler/meshc/tests/support/m046_route_free.rs` and fail closed on temp-build preflight rather than rebuilding inside the package directory. The shared helper now requires a pre-created `--output` parent, writes/reads `build-meta.json`, and records `build-preflight-error.txt` when the harness would otherwise churn tracked binaries like `cluster-proof/cluster-proof`.
- For M046/S04 docs/verifier guards, do not ban the raw substring `/status` in `cluster-proof/README.md`. The route-free README can legitimately say `submit/status endpoints` while still forbidding actual package-owned routes, so the truthful drift checks are `/work`, `/membership`, Fly HTTP proxy terms, and the CLI/runtime-owned inspection commands.
- For M046/S04 historical wrapper rails (`scripts/verify-m044-s05.sh`, `scripts/verify-m045-s04.sh`, and `scripts/verify-m045-s05.sh`), the truthful contract is direct delegation to `scripts/verify-m046-s04.sh` plus retained-copy checks on `status.txt`, `current-phase.txt`, `phase-report.txt`, and `latest-proof-bundle.txt`. Do not pull `cluster-proof/README.md`, website docs, `meshc build cluster-proof`, `meshc test cluster-proof/tests`, or `/work`/Fly HTTP assertions back into those wrappers; broad scaffold/docs parity stays deferred to S05.
- For M046/S05 scaffold parity work, keep generated-scaffold runtime proof centralized in `compiler/meshc/tests/e2e_m046_s05.rs` and `compiler/meshc/tests/support/m046_route_free.rs::init_clustered_project(...)`; once that authoritative rail exists, older scaffold-era tests should shrink to source/readme/delegation guards instead of spawning their own second runtime harness. That keeps `meshc init --clustered`, `tiny-cluster/`, and `cluster-proof/` on one proof seam and avoids reviving deleted `/health` or `/work` flows.
- For M046/S05 closeout, the actionable retained bundle is `.tmp/m046-s05/verify/latest-proof-bundle.txt` -> `.tmp/m046-s05/verify/retained-proof-bundle/`, which nests copied `retained-m046-s03-artifacts`, `retained-m046-s04-artifacts`, and `retained-m046-s05-artifacts` under one pointer. If the historical `scripts/verify-m045-s05.sh` alias goes red, debug the delegated `.tmp/m046-s05/verify/phase-report.txt` / `full-contract.log` first; the wrapper is intentionally only a retained-alias check and should not grow new scaffold/docs assertions of its own.
- Do not run `scripts/verify-m046-s06.sh` and `scripts/verify-m045-s05.sh` in parallel. The historical wrapper delegates back into S06 and both scripts rewrite the same `.tmp/m046-s06/verify/` tree, so concurrent runs can create false artifact drift. The truthful execution order is authoritative S06 first, then the M045 alias replay.
- For M046/S06 closeout, start diagnosis from `.tmp/m046-s06/verify/phase-report.txt` and `.tmp/m046-s06/verify/latest-proof-bundle.txt`, then open `retained-m046-s05-verify/` before the targeted `retained-m046-s03-startup`, `retained-m046-s03-failover`, and `retained-m046-s04-startup` bundles. The top-level S06 pointer is the milestone-closeout truth surface, but most regressions still localize faster through the copied delegated S05 verifier state than through the leaf bundles.
- For M046 milestone completion, the rendered `.gsd/REQUIREMENTS.md` can list R085–R093 even while `gsd_requirement_update` still rejects those IDs as "not found." If milestone closeout evidence proves the requirement family green, keep the checked-in requirements file truthful anyway and record the DB mismatch separately instead of leaving the visible requirement state stale.
- For `compiler/meshc` integration tests, do not import `src/main.rs` with `#[path = "../src/main.rs"] mod meshc_main;` just to reach planning helpers. Under `cfg(test)`, that also pulls in `main.rs` unit tests and submodule unit tests, so unrelated `discovery` / `migrate` assertions run inside the integration target. Prefer CLI-only proofs or extract a dedicated helper module without internal unit tests if an integration rail needs meshc internals.
- In `compiler/mesh-lsp/src/analysis.rs`, clustered validation issues with `ClusteredDeclarationOrigin::Source` must be filtered to the currently analyzed relative file before turning them into LSP diagnostics. Reusing the shared mesh-pkg provenance without that file gate makes decorated-source failures show up as phantom project-level errors on unrelated files instead of anchoring on the owning declaration line.
- For M047/S03 closeout, treat `cargo test -p mesh-typeck m047_s03 -- --nocapture`, `cargo test -p mesh-codegen m047_s03 -- --nocapture`, and `cargo test -p mesh-rt m047_s03 -- --nocapture` as presence checks, not proof of implementation. On the current tree they exit 0 while filtering to 0 tests, so the authoritative blocker signal is still the missing `cargo test -p meshc --test e2e_m047_s03 -- --nocapture` target plus the absent `HTTP.clustered(...)` compiler/runtime symbols.
- For `compiler/mesh-rt/src/dist/node.rs` startup failover recovery, do not feed automatic recovery the raw declared-handler replica requirement after promotion. The stable seam is to reuse the startup single-node relaxation for `startup::...` request keys (`startup_effective_required_replica_count(...)` with current membership) before calling `submit_declared_work(...)`; otherwise the promoted standby can reject its own recovery with `replica_required_unavailable` and make `cargo test -p meshc --test e2e_m046_s03 m046_s03_ -- --nocapture` fail even though promotion itself succeeded.
- For M047/S05, the real zero-ceremony `@cluster` fix lives in `compiler/mesh-codegen/src/declared.rs::generate_declared_work_wrapper(...)`, not in runtime continuity code. The runtime was already passing hidden `(request_key, attempt_id)` metadata; the stale seam was the generated declared-work wrapper mirroring the public function signature. The stable compatibility shape is: ordinary zero-arg `@cluster` work lowers to a wrapper body that accepts the hidden continuity strings but calls the user function with no args, while legacy two-string handlers still work as a compatibility path. After that fix, `cargo test -p meshc --test e2e_m047_s01 -- --nocapture`, `cargo test -p meshc --test e2e_m047_s02 -- --nocapture`, `cargo run -q -p meshc -- test tiny-cluster/tests`, and `cargo run -q -p meshc -- test cluster-proof/tests` are the truthful confirmation that the compiler seam and the route-free `Work.add` cutover are green before resuming the unfinished Todo scaffold/template work.
- For M047/S06's built-package SQLite seam, if a `meshc build` binary can `Sqlite.open(...)` but fails or crashes on `Sqlite.execute(...)`, inspect generic Result/Option construction in `compiler/mesh-codegen/src/codegen/expr.rs` before changing the SQLite runtime. The stable fix is to box non-pointer payloads before storing them in generic `{i8, ptr}` variant slots; `cargo test -p meshc --test e2e_sqlite_built_package -- --nocapture` should then leave a fresh `.tmp/m047-s06/sqlite-built-package-execute-*` bundle whose `run.stdout.log` reads `schema=ok`, `insert=1`, `count=1`, `mismatch_err=column index out of range`, `done`.
- For M047/S06 Todo Docker proof on non-Linux hosts, do not trust a successful `docker build` alone. A host-native Mach-O `./output` can still produce a green image build even though the Linux container would never boot. The truthful path is to emit `./output` from a Linux builder stage first, then package that binary in the runtime image and inspect the retained `.tmp/m047-s05/todo-scaffold-runtime-truth-*` bundle for `docker-output.file.txt`, `docker-inspect.json`, container stdout/stderr, and raw HTTP snapshots.
- For M047/S06 closeout, debug `.tmp/m047-s06/verify/phase-report.txt` and `.tmp/m047-s06/verify/latest-proof-bundle.txt` first, then open `retained-m047-s05-verify/` before the copied docs/verifier contract artifacts. The top-level S06 verifier owns the assembled truth surface, but it should only retain and validate the delegated S05 bundle (`status.txt`, `current-phase.txt`, `phase-report.txt`, `full-contract.log`, `latest-proof-bundle.txt`) — never share or mutate `.tmp/m047-s05/verify` directly.
- For M047/S07 clustered HTTP wrapper lowering, the stable interception point is the `HTTP.clustered(...)` call itself, not the surrounding `HTTP.on_*` route registration: rewriting the wrapper call to a deterministic `__declared_route_<runtime_name>` bare shim automatically covers both direct and pipe route forms, and those shim symbols must be added to `merge_mir_modules(..., extra_reachable_fns)` or monomorphization will prune them before declared-handler registration sees them.
- For Docker-based clustered Todo proofs, setting `MESH_*` with `Command::env(...)` on `docker create` only configures the Docker CLI process, not the container runtime. The container must receive clustered runtime settings via explicit `-e KEY=value` args, and even after correct env injection a host-published cluster port can still fail `meshc cluster status` with `send_name failed: unexpected end of file`; if M047/S08/T02 resumes, inspect the retained `clustered-container-*.ports.txt`, `*.host-port.log`, `*.inspect.json`, `clustered-container-status.timeout.txt`, and container bootstrap logs before changing the Todo scaffold again.
- For milestone closeout on this repo after the work is already merged onto `main`, `git diff HEAD $(git merge-base HEAD main) -- ':!.gsd/'` and even the `origin/main` variant can both go empty while the milestone still produced real code. In that state, use the last pre-milestone commit as the baseline and restrict the diff to milestone-owned paths; otherwise a real delivered milestone can look like planning-only drift.
- For manifest-selected entrypoints in `meshc build`, discovery can be correct while the final binary still runs the wrong file if MIR merge dedupes duplicate `mesh_main` symbols in source-order. When both root `main.mpl` and an override entry such as `lib/start.mpl` exist, the truthful seam is `compiler/mesh-codegen/src/lib.rs::merge_mir_modules(...)`: the designated entry module must be merged first or the legacy root main silently wins even though `project.graph.get(...).is_entry` points at the override.
- In `compiler/meshc/tests/e2e_lsp.rs`, cross-file override-entry transport proofs should currently use `textDocument/hover` or `textDocument/signatureHelp` for nested imports, not `textDocument/definition`. `mesh-lsp`'s live `goto_definition` path still resolves within the open document only, so a cross-module imported call can truthfully return `null` even when diagnostics are clean and the manifest-selected project graph loaded correctly.
- In `compiler/meshpkg/src/publish.rs`, published tarball membership should mirror Mesh source discovery from the project root, not a hardcoded `src/` or root-entry assumption: archive `mesh.toml` plus every non-hidden, non-`.test.mpl` source file under its project-root-relative path. That keeps override-entry packages like `lib/start.mpl` with nested support modules publishable without lying about what the package actually contains, and `cargo test -p meshpkg -- --nocapture` is the authoritative regression rail.
- For M048/S03's retained self-update acceptance rail, a nonblocking test listener is fine for `accept()`, but each accepted socket must be switched back to blocking mode before streaming installer scripts or large tarballs. Leaving the child socket nonblocking made `ureq`/`curl` fail with partial-download errors (`os error 35`, `curl: (18) transfer closed ... bytes remaining`) even though the staged assets and updater logic were correct.
- For the Mesh init-time skill bundle, keep clustered runtime truth centralized in `tools/skill/mesh/skills/clustering/SKILL.md` and use `tools/skill/mesh/skills/syntax/SKILL.md` / `tools/skill/mesh/skills/http/SKILL.md` only for bounded cross-links. `scripts/tests/verify-m048-s04-skill-contract.test.mjs` is the authoritative drift rail: it must keep failing on legacy `[cluster]`, `clustered(work)`, or helper-shaped names instead of letting those tokens re-enter the auto-loaded first-contact docs.
- For `@cluster` editor syntax changes, keep the oracle in `scripts/fixtures/m048-s04-cluster-decorators.mpl` and anchor special highlighting to the `@` decorator position rather than promoting bare `cluster` to a global keyword. The positive cases are `@cluster` / `@cluster(3)` and the mandatory negative case is `let cluster = 1`; replay both `bash scripts/verify-m036-s01.sh` and `NEOVIM_BIN="${NEOVIM_BIN:-nvim}" bash scripts/verify-m036-s02.sh syntax` before trusting a grammar tweak.
- For M048/S05 closeout, do not delegate the assembled editor replay to `scripts/verify-m036-s03.sh`. The truthful wrapper replays the retained S02 rails directly with `NEOVIM_BIN="${NEOVIM_BIN:-nvim}"`, runs a cargo phase before VS Code smoke so `target/debug/meshc` exists, and keeps the public contract bounded to same-file definition plus manifest-first hover/diagnostics proof.
- For M048/S05 retained closeout artifacts, copy fixed `.tmp/m036-s02` / `.tmp/m036-s03` directories directly, but snapshot-copy fresh timestamped `.tmp/m048-s01/*` and `.tmp/m048-s03/*` buckets into `.tmp/m048-s05/verify/retained-proof-bundle/`. Debug from `.tmp/m048-s05/verify/phase-report.txt` and `latest-proof-bundle.txt` first; the bundle should contain `retained-m036-s02-lsp`, `retained-m036-s02-syntax`, `retained-m036-s03-vscode-smoke`, `retained-m048-s01-artifacts`, and `retained-m048-s03-artifacts`.
- Installer-backed CLI subcommands that only have human-oriented installer output should make `--json` fail closed before launch. A fake success blob is worse than no machine-readable surface; keep help/guard rails in the acceptance proof whenever update behavior changes.
- When public docs intentionally describe a bounded tooling capability (for example same-file definition rather than cross-file definition), add a repo-owned docs contract test for that wording in the same milestone. Runtime/editor proofs alone do not stop stale README or site copy from overclaiming the shipped surface.
- The current GSD requirements DB still does not know about the M048 requirement family (`R112`-`R114`) even though `.gsd/REQUIREMENTS.md` renders them. During M048 closeout, `gsd_requirement_update` rejected those IDs as `not found`, so keep the checked-in requirements file truthful manually until the DB is repaired.
- During M049 closeout, `gsd_requirement_update` rejected both `R115` and `R116` as `not found` even though the checked-in `.gsd/REQUIREMENTS.md` renders them. Until the requirements DB is repaired, treat the checked-in requirements file plus saved requirement decisions as the authoritative visible state for M049 requirement status.
- For generated clustered starters, do not call `Node.start_from_env()` before required app-local config and dependency checks if the failure path is supposed to exit promptly. In M049/S01, starting the runtime before validating `DATABASE_URL` let runtime keepalive/startup actors keep the process alive after `main` logged the config error; the stable starter shape is `validate config -> open pool -> Node.start_from_env() -> registry/HTTP startup`, and the truthful regression rail is `cargo test -p meshc --test e2e_m049_s01 -- --nocapture`.
- Keep `tiny-cluster/work.mpl` route-free and compileable. A stray `HTTP.router()` / `HTTP.on_post(...)` example or decorator drift there can make `cargo run -q -p meshc -- test tiny-cluster/tests` fail before `bash scripts/verify-m047-s04.sh` or `bash scripts/verify-m047-s05.sh` ever reach the Todo rails. If the retained M047 wrapper stops in `m047-s04-tiny-cluster-tests`, inspect `tiny-cluster/work.mpl` first and restore the minimal `@cluster pub fn add() -> Int do 1 + 1 end` contract.
- For the M049/S02 SQLite todo starter, keep generated package tests as compile-smoke/import-surface proof and let `compiler/meshc/tests/e2e_m049_s02.rs` own the live CRUD/failure verification. Direct `meshc test` calls into the generated SQLite storage helpers still drift into `expected (), found Int` compiler failures on this tree, so the truthful generated-package seam is importability plus local helper names, while runtime behavior is proven through the retained `.tmp/m049-s02/...` HTTP/log artifact bundle.
- For M049/S02 docs-contract work, update `compiler/meshc/tests/e2e_m047_s06.rs` and `scripts/verify-m047-s06.sh` together. The Rust rail and the shell wrapper both hard-code the public wording split, so changing README/site copy alone can leave `cargo test -p meshc --test e2e_m047_s06 -- --nocapture` green while the assembled `bash scripts/verify-m047-s06.sh` wrapper still fails on stale guard strings.
- For M055/S03 closeout, if the retained M047 Todo rail fails with Docker Desktop/containerd errors like `failed to solve: write /var/lib/desktop-containerd/.../meta.db: input/output error` or BuildKit `metadata_v2.db` I/O failures, treat that as host daemon state before treating it as a docs-contract regression. Free repo disk space if the host is full, relaunch Docker Desktop so `docker info` is healthy again, and then rerun `bash scripts/verify-m047-s05.sh` before retrying `bash scripts/verify-m047-s06.sh` or `bash scripts/verify-m055-s03.sh`.
- For M049/S02 closeout, `scripts/verify-m047-s05.sh` should replay only `scripts/verify-m047-s04.sh`, `cargo test -p meshc --test e2e_m047_s05 -- --nocapture`, and `npm --prefix website run build` before checking retained fixture provenance and bundle shape. Once the historical clustered Todo contract is fixture-backed, public `mesh-pkg` / `tooling_e2e` starter filters are no longer part of that retained proof surface and can make the wrapper go red for unrelated SQLite-starter drift.
- For `scripts/tests/verify-m049-s03-materialize-examples.mjs`, keep `--check` and `--write` asymmetric on purpose: `--check` should report missing/extra/changed files by name for drift, while `--write` must fail closed on malformed partial target trees before overwriting anything. Treating both modes as the same safety contract either hides drift behind validation errors or makes refresh mode too willing to replace the wrong tree.
- For M049/S03 example parity rails, the public `node scripts/tests/verify-m049-s03-materialize-examples.mjs --check` command cleans its temp session on success. If a future agent needs retained generated-vs-target artifacts under `.tmp/m049-s03/...`, import `materializeExamples(...)` with `keepTemp: true` (for example via `node --input-type=module`) and archive `materializer/generated/`, `materializer/target/`, `*-prior-diff.json`, and `retained-session.txt` before changing the examples or the script.
- `cargo test -p meshc --test e2e_m049_s03 -- --nocapture` is the authoritative M049/S03 closeout rail. It replays public-CLI parity against `examples/todo-sqlite` and `examples/todo-postgres`, proves `meshc test` plus `meshc build --output` from the checked-in trees, and retains the useful first-debug bundles under `.tmp/m049-s03/todo-examples-parity-*`, `.tmp/m049-s03/todo-sqlite-test-build-*`, and `.tmp/m049-s03/todo-postgres-test-build-*`.
- For `scripts/verify-m045-s02.sh` after the M049/S04 fixture move, the retained bundle shape must match the current `compiler/meshc/tests/e2e_m045_s02.rs` artifact contract, not the older remote-runtime JSON/log set. `declared-work-remote-spawn-*` is now a source-parity bundle (`generated-project/`, `package/`, `references/cluster-proof.*`, `init.log`), while `scaffold-runtime-completion-local-*` proves local runtime truth through `cluster-status.{log,json}`, `cluster-continuity-{list,completed}.{log,json}`, `cluster-diagnostics.{log,json}`, and scaffold logs. If `bash scripts/verify-m045-s02.sh` fails in `m045-s02-bundle-shape`, inspect `.tmp/m045-s02/verify/retained-m045-s02-artifacts/` before reopening the scaffold/runtime code.
- `scripts/verify-m039-s01.sh` is green again after M049/S05 repaired the retained node-loss expectation. The truthful seam still requires one-node membership convergence after standby loss, but post-loss authority `replication_health` may now be `unavailable` or `degraded` instead of the older `local_only` assumption because the runtime-owned startup continuity state persists after loss. If an assembled verifier that replays M039 regresses again, start in `cargo test -p meshc --test e2e_m039_s01 e2e_m039_s01_membership_updates_after_node_loss -- --nocapture` and inspect the retained `cluster-status-primary-after-loss` artifacts before changing the assembled wrapper.
- For M049/S05 docs discoverability, do not casually repeat an older verifier command string inside a new docs paragraph when a mutation-based contract test still targets that exact literal. A new `verify-m049-s05` mention that re-spelled `bash scripts/verify-m048-s05.sh` inside `website/docs/docs/tooling/index.md` broke the retained M048 docs contract; the stable wording is to describe it as the retained M048 tooling verifier unless the older contract test is updated too.
- For M049/S05 retained bundle-shape checks, use the actual Postgres unmigrated-database artifact contract from `e2e_m049_s01`: the retained files are `todos-unmigrated.http` and `todos-unmigrated.json`, not the stale `todos-unmigrated.response.json` name. If `m049-s05-bundle-shape` fails, inspect `.tmp/m049-s05/verify/retained-proof-bundle/retained-m049-s01-artifacts.manifest.txt` before changing the Postgres starter or wrapper flow.
- For `scripts/verify-m047-s05.sh` inside the assembled M049 replay, force `RUST_TEST_THREADS=1` around the full `cargo test -p meshc --test e2e_m047_s05 -- --nocapture` phase. On this host the retained Todo rail can fail with `os error 35` socket races under default concurrency even though the individual test contract is still sound, so serialization is the truthful verifier-side fix.
- For M049 milestone closeout, use `node --test scripts/tests/verify-m049-s05-contract.test.mjs` as the cheap preflight and reserve `bash scripts/verify-m049-s05.sh` for the authoritative assembled replay. The node contract catches verifier ordering/docs/bundle-marker drift in under a second, while the full shell replay can take ~20 minutes because it re-runs retained M039/M045/M047/M048 proof rails.
- The M049 requirement-DB mismatch is still unresolved at milestone completion: `gsd_requirement_update` continued to reject `R115` as `not found` even after `M049-VALIDATION.md` and `M049-SUMMARY.md` were rendered successfully. Until the DB is repaired, treat the checked-in `.gsd/REQUIREMENTS.md` plus milestone summary/validation artifacts as the visible truth for M049 requirement status.
- In this VitePress theme, proof pages cannot be kept out of the docs footer chain with page frontmatter alone: `prev: false` / `next: false` only disable the active page's own footer. To keep a page public in the sidebar but out of neighboring prev/next links, the sidebar item itself must carry the custom `includeInFooter: false` flag and `usePrevNext.ts` must filter those items before exact page matching.
- For retained docs rails after the M050 onboarding-graph reset, reuse `scripts/tests/verify-m050-s01-onboarding-graph.test.mjs` inside the historical shell verifiers and keep the remaining guards on path/link markers (`examples/...`, `reference-backend/README.md`, retained verifier commands). Exact paragraph-level wording in README/docs is too brittle and will block later copy slices even when the scaffold/examples-first graph is still correct.
- For M050/S01 retained docs contracts, treat the root `README.md` as a public routing surface (starter links plus proof-page discoverability), not as a repo verifier index. The retained rail commands live on `website/docs/docs/distributed-proof/index.md` and related proof pages, and `bash reference-backend/scripts/verify-production-proof-surface.sh` is the surface that catches stale GitHub runbook-link drift on `website/docs/docs/production-backend-proof/index.md`.
- For M050/S02 first-contact docs rewrites, keep the exact README and Getting Started next-step markers that `reference-backend/scripts/verify-production-proof-surface.sh` searches for (`Clustered walkthrough` / `Clustered Example` before `Production Backend Proof`). That verifier is literal-string/order based, so changing those bullets without updating the script produces a false-red proof-surface failure even when the broader onboarding copy is improved.
- For M050/S02 Clustered Example work, keep the explicit SQLite/Postgres starter commands in the dedicated `## After the scaffold, pick the follow-on starter` section instead of duplicating them above the walkthrough. `scripts/tests/verify-m050-s02-first-contact-contract.test.mjs` uses that section as the ordering seam, so repeating the commands earlier makes the page look proof-first even when the copy still says "after the scaffold."
- For `bash scripts/verify-m051-s02.sh`, a disposable local Postgres URL is enough to get past the missing-env gate, but the current replay can still fail later in `m051-s02-deploy-artifact-smoke` after an earlier fixture-smoke phase has already processed a job in the same database. The retained deploy-artifact assertion in `compiler/meshc/tests/e2e_reference_backend.rs` expects `/health.worker.last_job_id` to align with the new staged job, so if the verifier goes red with `job ... never reached processed health-aligned state` while the job row itself is already `processed`, inspect `.tmp/m051-s02/verify/m051-s02-deploy-artifact-smoke.log` first and consider isolating or resetting the database between the smoke and deploy-artifact phases before touching the delete-surface work again.
- For `scripts/fixtures/backend/reference-backend/scripts/smoke.sh`, a plain `GET /health` 200 is not a truthful ready signal when the worker still reports `status=degraded`, `liveness=recovering`, or `recovery_active=true`. If the retained S02 or assembled S05 replay stalls with jobs stuck at `pending` right after fixture smoke startup, fix the smoke handoff to wait for the same healthy `/health` contract that `e2e_reference_backend.rs::wait_for_reference_backend(...)` uses before re-opening the staged deploy or queue-processing code.
- For the retained backend recovery rails after M051/S05, keep `e2e_reference_backend_worker_crash_recovers_job` focused on crash/requeue/processed correctness and let `e2e_reference_backend_worker_restart_is_visible_in_health` own the `/health` restart-metadata proof. The mid-recovery degraded window was not a stable closeout seam in the post-deletion serial replay, but final `/health` fields (`boot_id`, `started_at`, `restart_count`, `recovered_jobs`, `last_exit_reason`, `last_recovery_*`) stayed truthful and are the stable maintainer-facing recovery signal.
- For milestone closeout on this repo when `HEAD`, local `main`, and `origin/main` already point at the same integration tip, the literal `git diff HEAD $(git merge-base HEAD main) -- ':!.gsd/'` check can go empty even after real delivery. Use the parent of the earliest milestone-tagged commit as the equivalent non-`.gsd` diff baseline before treating the milestone as planning-only.
- During M051 closeout, `gsd_requirement_update` rejected `R119` as `not found` even though the checked-in `.gsd/REQUIREMENTS.md` rendered it. Until the requirements DB is repaired, treat the checked-in requirements file plus the milestone summary/validation artifacts as the authoritative visible state for R119.
- For landing-local `/pitch` Playwright rails in `mesher/landing/`, do not trust `npm --prefix mesher/landing exec playwright ... --project=... --grep ...` from repo root on this host. npm 11 still strips those flags instead of passing them through to Playwright, which leaves `baseURL` unset and turns `page.goto('/pitch')` into `Cannot navigate to invalid URL`; the truthful replay seam is the package-local binary with the explicit config path, e.g. `./mesher/landing/node_modules/.bin/playwright test --config ./mesher/landing/playwright.config.ts ...`.
- For `mesher/landing`, `npm --prefix mesher/landing run build` is not a trustworthy TypeScript gate by itself right now because Next reports `Skipping validation of types`. If a pitch-route change needs type evidence, run `./mesher/landing/node_modules/.bin/tsc --noEmit -p mesher/landing/tsconfig.json` and filter the output to the touched pitch files before treating a green build as typed proof.
- For landing-local `/pitch` media cards that must fail closed, statically import the `mesher/landing/public/*.png` assets (for example `@/public/promo-performance.png`) instead of using bare `'/promo-performance.png'` strings. The static import keeps broken asset wiring in the build path, which is the truthful seam for the evaluator-deck route.
- In `mesher/landing/components/pitch/pitch-controls.tsx`, a horizontally scrollable outline inside the control rail will blow the whole mobile grid item out to max-content width unless the outer `<aside>` and its first card wrapper both declare `min-w-0`. If a future `/pitch` mobile proof reports 1500px-wide control buttons with no document-level horizontal overflow, start there before rewriting the layout or weakening the browser rail.
- For `/pitch` print-mode assertions in `mesher/landing/tests/pitch-route.spec.ts`, match repeated slide summary copy inside the specific `[data-slide-id="..."]` container rather than against the whole page. Once richer slide variants reuse the summary sentence in supporting cards, page-level text assertions can fail even though the print/export contract is still correct.
- For landing-route milestone closeout in `mesher/landing/`, do not stop at the route-local browser rail alone. Pair the green `/pitch` Playwright replay with an explicit homepage smoke and a one-off Playwright `newContext({ reducedMotion: 'reduce' })` probe against `/pitch`, or the public-route verification envelope stays incomplete even when the route itself looks healthy.
- For M053/S01's staged Postgres starter rail, long-lived HTTP starters cannot rely on the main-wrapper `mesh_trigger_startup_work()` call alone: `mesh_main` blocks in `HTTP.serve(router, port)`, so the truthful runtime seam is an idempotent startup trigger in `compiler/mesh-rt/src/http/server.rs::{mesh_http_serve,mesh_http_serve_tls}` plus the existing main-wrapper trigger for route-free apps. If a clustered HTTP starter logs `startup_registered` but never reaches `startup_trigger`, inspect the HTTP serve entrypoint and the `STARTUP_WORK_TRIGGERED` guard in `compiler/mesh-rt/src/dist/node.rs` before reopening the verifier or starter source.
- For M053/S01 staged bundle proofs, `scripts/stage-deploy.sh` shells out to the public `meshc build` path, which links against `target/debug/libmesh_rt.a`. When the slice changes runtime behavior, force `cargo build -p mesh-rt` before the staged bundle build (the support seam is `compiler/meshc/tests/support/m053_todo_postgres_deploy.rs::run_stage_deploy_script(...)`); otherwise the staged binary can quietly use a stale runtime archive and make the retained evidence contradict the just-built test binary.
- For M053/S02 failover proofs on the generated Postgres starter, the promoted node can truthfully report `replication_health=unavailable` both immediately after owner loss and after fenced rejoin, even when membership has converged and roles/epochs are correct. The authoritative signals are role=`primary`, `promotion_epoch=1`, owner-loss/automatic-recovery/fenced-rejoin diagnostics, and the completed continuity record on the promoted node — do not hardcode `local_only` / `healthy` as the only acceptable post-failover health values.
- For local M053/S02 verification, use a disposable Docker Postgres and export `DATABASE_URL` from that container before running the proof rails; the S02 tests create isolated databases per run, so one local admin URL is enough and there is no need to ask a user for a shared database just to replay `cargo test -p meshc --test e2e_m053_s02 -- --nocapture` or `bash scripts/verify-m053-s02.sh`.
- During M053 closeout, `gsd_requirement_update` rejected both `R121` and `R122` as `not found` even though the checked-in `.gsd/REQUIREMENTS.md` lists them. Until the requirements DB is repaired, treat the checked-in requirements file plus D404/D416 and the retained verify bundles as the authoritative visible proof for the M053 requirement family.
- For M053/S03 hosted-evidence rails, do not hardcode the old `snowdamiz/mesh-lang` slug: this repo now resolves from `origin` as `hyperpush-org/hyperpush-mono`, and `gh run` queries can otherwise return redirected run URLs that hide which hosted repo is actually being inspected. Derive the repo slug from `git remote get-url origin` unless a verifier explicitly overrides it.
- When a local verification only needs one secret key (for example `GH_TOKEN` for `scripts/verify-m053-s03.sh`), parse that key out of `.env` instead of `source .env`. `secure_env_collect` can leave dotenv-valid values that are not shell-safe to execute, and a broad `source .env` can fail before the verifier ever starts.
- `scripts/verify-m053-s03.sh` intentionally fails closed on release freshness when `git ls-remote origin refs/tags/<tag> 'refs/tags/<tag>^{}'` does not return the peeled `^{}` ref. If the hosted rail goes red on `expected ref resolution missing peeled tag data`, treat that as a real release-evidence gap (or a lightweight tag object that still lacks the peeled target in the remote query), not as permission noise or a `gh run` parsing bug.
- For M053/S04 retained `cluster-proof` docs work, the fixture rail in `scripts/fixtures/clustered/cluster-proof/tests/work.test.mpl` pins exact README/help substrings, not just broad meaning. If a docs-only rewrite still leaves the package test red, check for case-sensitive phrase drift like `It is not a public starter surface` or the literal `route-free` marker before reopening the broader contract.
- For M053/S04 docs-contract checks, do not detect duplicated/corrupted trailing Markdown by grepping short substrings like `ng distributed systems with Mesh`. Valid copy such as `building distributed systems with Mesh` will false-hit. Use exact line counts on source Markdown and full rendered-text markers (or exact list-item strings) in built HTML instead.
- For M053/S05 rollout assembly, do not push local `HEAD` or cherry-pick the full ahead stack when local history mixes milestone delivery with unrelated `/pitch`, `.gsd`, or incidental landing changes. Start from the latest fetched `origin/main`, materialize the proof-critical M053 ship-set as a synthetic file-scoped commit in a worktree, and re-check `git ls-remote`/`git fetch` before finalizing the candidate because the remote tip can advance during the task.
- For `scripts/verify-m053-s01.sh` and `scripts/verify-m053-s02.sh`, the wrapper message `expected success within <N>s` is not a real timeout classifier. `run_expect_success` emits that wording for any non-zero nested command, so if the uploaded bundle omits the raw nested log tail the failure can look like a compile-budget issue even when the real cause is different.
- For cold-target replays of `cargo test -p meshc --test e2e_m049_s03 -- --nocapture`, the generated `meshc test <project>` sub-rails require `libmesh_rt.a` to already exist in the active target dir. In an isolated `CARGO_TARGET_DIR`, prebuild `mesh-rt` first (`cargo build -p mesh-rt`) or the replay can fail closed with `Could not locate Mesh runtime static library ...` before the example assertions run.
- For M053/S05 hosted closeout, once `scripts/verify-m053-s01.sh` exposes real exit-vs-timeout reasons and `scripts/verify-m053-s02.sh` retains nested S01 logs, the remaining remote blocker is the S02 failover proof itself, not diagnostic opacity. The failing hosted symptom on SHA `314bbac88b171388b04072a97f22be0bca4882aa` is `automatic_promotion_rejected:no_mirrored_state` on the standby after the primary is killed, with `meshc cluster status ...` staying `cluster_role=standby`, `promotion_epoch=0`, `replication_health=healthy` in the archived `post-kill-status-standby.timeout.txt`. Local repro becomes green with a larger startup-work delay (`Some(20_000)` in `e2e_m053_s02.rs`), but hosted Ubuntu still reproduces, so the next honest resume point is the dual-stack standby promotion/mirrored-state seam in `compiler/mesh-rt/src/dist/node.rs` and the S02 failover harness — not the wrapper scripts, deploy-services, or tag reroll path.
- If a GitHub Actions lane runs the full `cargo test -p meshc --test e2e_m053_s02 -- --nocapture` target, retained hosted-red regression data cannot live only under a previous task’s `.tmp/m053-s05/...` tree. The first S06 repair SHA (`cf1cf9e44d911ae889cac8fd1f1359e313f039bb`) still failed on hosted main because `m053_s02_hosted_failure_bundle_proves_completed_before_owner_stop` hardcoded that untracked path; the stable seam is a tracked fixture under `scripts/fixtures/m053-s02-hosted-failure-bundle/` plus the test pointing there, so clean runners can execute the whole target without inheriting local artifact history.
- For synthetic hosted-rollout commits created through the GitHub git-data API, the ship-set must include any newly tracked `scripts/fixtures/...` regressions alongside the code and verifier files that reference them. In M053/S06 the second repair SHA (`7220ae4d78a7a09a150ae4225c75b6817bf9c1e2`) still failed hosted authoritative proof because the rollout helper only shipped `compiler/mesh-rt/src/dist/node.rs`, `compiler/meshc/tests/e2e_m053_s02.rs`, and `scripts/verify-m053-s02.sh`, leaving the new `scripts/fixtures/m053-s02-hosted-failure-bundle/` directory behind; the corrected repair SHA (`e5fb36a6fe7e9e56f3a608a608abbaaab6764167`) only went green on main after that tracked fixture directory was included in the synthetic commit.
- The final M053/S06 root cause was narrower than the earlier standby-promotion hypothesis: hosted Ubuntu only stayed red while `compiler/mesh-rt/src/dist/node.rs::startup_dispatch_window_ms(...)` ignored the runtime-owned `MESH_STARTUP_WORK_DELAY_MS` override and collapsed startup failover back to the 2500ms default. If the staged Postgres failover rail regresses again with `automatic_promotion_rejected:no_mirrored_state`, inspect the retained pre-kill diagnostics for `startup_dispatch_window.pending_window_ms` and whether `startup_completed` landed before the forced owner stop before reopening the promotion transport or deploy workflow layers.
- For M054 milestone closeout, `M054-ROADMAP.md` does not render explicit `Success Criteria` or `Horizontal Checklist` headings. The truthful validation seam is the milestone vision plus each slice's `After this` outcome, and the authoritative retained proof chain is nested: start at `.tmp/m054-s03/verify/latest-proof-bundle.txt`, then follow `retained-m054-s02-verify/`, then the retained S01 runtime bundle if the issue is ingress/runtime truth rather than docs copy.
- For M054/S01 standby-routed clustered HTTP on the staged Postgres starter, a 503 `clustered_http_route_reply_read_failed:peer closed connection without sending TLS close_notify` can be the symptom of an owner-side crash, not just a transient TLS shutdown quirk. In the reproduced failure, the owner `todo-postgres` process segfaulted in `__service_todoregistry_call_get_pool` while `handle_transient_http_route_connection(...)` was invoking the route handler on the accept-loop thread without actor context. The stable runtime seam is `compiler/mesh-rt/src/dist/node.rs`: run transient clustered-route reply work inside an actor before building the reply, or generated service-call wrappers like `Runtime.Registry.get_pool()` can crash the owner process under standby-first ingress.
- For M054/S01 retained ingress proof, the bundle contract is dual-node even though the startup request is primary-owned: `compiler/meshc/tests/e2e_m054_s01.rs` must archive both `cluster-diagnostics-primary.{log,json}` and `cluster-diagnostics-standby.{log,json}` via `wait_for_startup_diagnostics_pair(...)`. Reusing the single-node startup diagnostics helper leaves the main public-ingress rail green until the final retained-artifact assertion, which is a misleading place to debug the failure.
- For local fallback Postgres reuse on this repo, do not trust `.tmp/m049-s01/local-postgres/connection.env` blindly. The saved `DATABASE_URL` can keep an old published port after Docker restarts while `.tmp/m049-s01/local-postgres/container-meta.txt` still points at the right container. If a verifier fails with `failed to connect to admin database: connection refused`, derive the live port from `docker port $(container-meta.txt container) 5432/tcp` and rebuild the URL before rerunning the rail.
- For M054/S02 direct-correlation debugging, start from the retained `.tmp/m054-s02/...` artifacts rather than continuity-list diffs: `public-selected-list.http` plus `public-selected-list.request-key.{txt,json}` prove the runtime-owned `X-Mesh-Continuity-Request-Key` handoff, and `selected-route-direct-{primary,standby}-{record,diagnostics}.json` are the authoritative primary/standby lookup surfaces for the same request. If `scripts/verify-m054-s02.sh` fails on bundle-shape or lookup drift, inspect those files before reopening the ingress harness or staged starter runtime.
- For M054/S03 public-docs closeout, keep exact homepage/proof prose in `scripts/tests/verify-m054-s03-contract.test.mjs` and let `scripts/verify-m054-s03.sh` assert only the built-HTML fragments plus retained-bundle shape. Forcing the shell wrapper to duplicate full Markdown sentences creates false drift in `compiler/meshc/tests/e2e_m054_s03.rs` without improving the public contract.
- The current GSD requirements DB does not know about `R123` even though the checked-in `.gsd/REQUIREMENTS.md` renders it for M054. `gsd_requirement_update` currently rejects `R123` as `not found`, so until the DB is repaired the checked-in requirements file plus the M054 slice/milestone summaries and saved requirement decision are the authoritative visible state for that requirement.
- `mesher/frontend-exp/` should be treated as product-owned mesher repo material. The user clarified it is intended to replace the existing frontend later, so split and extraction work should keep it on the mesher/hyperpush side rather than classifying it as a language-owned `mesh-lang` surface.
- The whole-app landing TypeScript command `./mesher/landing/node_modules/.bin/tsc --noEmit -p mesher/landing/tsconfig.json` is still red at baseline in `mesher/landing/components/blog/editor.tsx`, `mesher/landing/components/landing/infrastructure.tsx`, and `mesher/landing/components/landing/mesh-dataflow.tsx`. When an ownership/docs task only touches repo-identity files like `lib/external-links.ts`, use that file list plus `npm --prefix mesher/landing run build` to distinguish unrelated baseline debt from a new regression.
- For M055/S01 split-boundary regressions, start with `bash scripts/verify-m055-s01.sh`, then read `.tmp/m055-s01/verify/phase-report.txt` and the failing per-phase log. If the repo-local `.gsd` seam is red, rerun `cargo test -p meshc --test e2e_m046_s03 m046_s03_tiny_cluster_package_contract_remains_source_first_and_route_free -- --nocapture` directly before changing `scripts/fixtures/clustered/tiny-cluster/`, `.gsd/milestones/M046/slices/S03/S03-PLAN.md`, or `scripts/verify-m046-s03.sh`.
- For M055/S01 verifier maintenance, keep `scripts/verify-m055-s01.sh` narrow: it should only replay the split-contract node rail, the repo-identity/local-docs helper, the packages build, the landing build, and the named M046 repo-local `.gsd` cargo seam. Do not widen it to unrelated historical wrappers or the baseline-red whole-app landing `tsc --noEmit` gate unless those debts are fixed first.
- For Mesher maintainer smoke verification, avoid a here-doc-based `json_field` helper when you need to parse piped `curl` output: the here-doc steals stdin and makes valid JSON look empty. Also give the verifier run its own `MESH_CLUSTER_PORT`/`MESH_NODE_NAME` pair instead of reusing the default `4370`, or unrelated local Mesh processes can make `bash mesher/scripts/verify-maintainer-surface.sh` fail for the wrong reason.
- For repo-local exact-marker verifiers that use `rg -Fq`, pass `--` before the needle when the marker can start with `-` (for example a Markdown bullet). Without that separator, ripgrep can parse the marker as a flag and make a stale-bullet absence check look green for the wrong reason.
- For the M055/S02 Mesher maintainer contract, keep the `meshc` resolver strict: prefer an enclosing `mesh-lang` source checkout, then the blessed sibling `../mesh-lang`, then `PATH`, and fail closed if a detected higher-priority source tier exists but lacks `target/debug/meshc`. Also keep staged build output outside `mesher/`; falling through to `PATH` or rebuilding in-package hides the exact coupling this slice is meant to expose before `hyperpush-mono` extraction.
- For M055/S03 first-contact contract tests, once `compiler/mesh-pkg/src/scaffold.rs` pulls public links from `scripts/lib/repo-identity.json`, source-level assertions against the scaffold file must pin the placeholder tokens and `public_links` bindings (`{product_repo_url}`, `product_repo_url = public_links.product_repo_url`, etc.), not concrete URLs. The actual README text is formatted at runtime, so source-contract tests that expect fully expanded URLs in `scaffold.rs` will false-fail even when the generated README is correct.
- For M055/S03 proof-page verifiers, do not enforce Postgres-vs-SQLite ordering by the first README URL occurrence in the whole file. `website/docs/docs/distributed-proof/index.md` mentions the SQLite link in intro copy before the canonical public-surface ledger, so order checks must anchor to the specific public-surface bullet lines (or another section-local marker) or the contract goes false-red while the docs themselves stay truthful.
- For M055/S03 workflow/source-contract maintenance, make the deploy/public-surface verifiers fail on forbidden landing jobs and landing health-check steps, not just on missing required language-owned steps. Once `deploy-services.yml` becomes the mesh-lang-only registry + packages/public-surface graph, a required-jobs-only check will let Hyperpush landing drift back into the repo without tripping the contract.
- For M057/S02 retained GitHub issue replay, normalize `gh issue view --json stateReason` carefully: ordinary open issues can return an empty string while genuinely reopened open issues can return `REOPENED`. Treat blank as `null`, preserve `reopened` when it is real, and compare against the persisted `repo-mutation-results.json` `final_state.state_reason` instead of assuming every open issue has no state reason.

- For M059/S02 Playwright work in `../hyperpush-mono/mesher/frontend-exp`, running `npm --prefix ... exec playwright ...` from the `mesh-lang` repo root is not enough by itself: npm 10 needs `exec --` to stop swallowing Playwright flags like `--project`, and Playwright will otherwise search for config from the current `mesh-lang` cwd instead of the package-local app. Use `npm --prefix ../hyperpush-mono/mesher/frontend-exp exec -- playwright test ... --config ../hyperpush-mono/mesher/frontend-exp/playwright.config.ts` (or an equivalent wrapper script) until the verifier runs from the app directory.
- For M059/S03 path-migration work in `../hyperpush-mono/mesher/client`, do not delete the whole `app/` directory just because the package is no longer a Next.js app. The TanStack root route in `src/routes/__root.tsx` still imports `app/globals.css?url`, so `app/globals.css` is part of the live runtime contract even though `app/page.tsx` and other Next-era files are not the active entry surface.
- For M059/S04 dashboard parity work in `../hyperpush-mono/mesher/client`, a one-time React console warning during the first Playwright Issues-route boot can come from Recharts area animations rather than from the new route assertions. If `dashboard-route-parity.spec.ts` fails only on the first `assertCleanRuntimeSignals(...)` check with `Can't perform a React state update on a component that hasn't mounted yet`, inspect `components/dashboard/events-chart.tsx` and disable the `Area` animations (`isAnimationActive={false}`) before weakening the parity harness.
- For M059 milestone closeout in this split `mesh-lang` / `hyperpush-mono` workspace, the `origin/main` diff check is necessary but not sufficient when the canonical app move lands as new sibling-repo worktree content. Pair the non-`.gsd` diff with `git -C ../hyperpush-mono status --short mesher/client` and a targeted `find ../hyperpush-mono/mesher/client ...` listing so the verification record proves both the rewired tracked surfaces and the newly materialized package tree.
- For M059 closeout, `.gsd/milestones/M059/M059-VALIDATION.md` is the fastest trustworthy source for milestone-level acceptance evidence. It already consolidates requirements coverage, cross-slice producer/consumer boundaries, and the four slice acceptance criteria, so use it as the final checklist instead of re-deriving those tables from every slice summary during completion.
- For M061/S01 route-inventory verification from `mesh-lang`, `npm --prefix ../hyperpush-mono/mesher/client exec -- playwright ...` still runs with the current working directory in `mesh-lang`. Pass `--config ../hyperpush-mono/mesher/client/playwright.config.ts` explicitly or Playwright can mis-resolve the suite/config boundary and fail with `test.describe()`/`No tests found` even though the client package and tests are correct.
- For M061/S01 retained route-inventory proof, do not let `mesher/scripts/seed-live-issue.sh` reuse an arbitrary backend on its default `http://127.0.0.1:18080` during wrapper runs. Give it isolated `BASE_URL`/`PORT`/`MESHER_WS_PORT`/`MESH_CLUSTER_PORT` values plus a concrete `DATABASE_URL` fallback, or stale local Mesher processes can make the seed phase hang or verify against the wrong runtime.
- For M061/S01 full-route verifier work in `../hyperpush-mono/mesher/client/tests/e2e/admin-ops-live.spec.ts`, do not make the generic alerts-ready helper require `alerts-list` visibility. The truthful zero-live path renders `alerts-empty-state` instead, so the shared readiness seam should stop at `alerts-shell[data-bootstrap-state="ready"]` and let each test assert list vs empty-state explicitly.
- For M061/S01 history proof in `../hyperpush-mono/mesher/client/tests/e2e/dashboard-route-parity.spec.ts`, do not blindly click the filtered issue row again after browser back/forward. The selected issue can already survive the history round-trip, and clicking the same row toggles the detail panel closed just as the test tries to close it explicitly. Read `data-selected-issue-id` first and only re-click when the row is not already selected.
- For M061/S02 mixed-surface verification, treat the combined dev Playwright rail (`issues-live-read.spec.ts` + `issues-live-actions.spec.ts` + `admin-ops-live.spec.ts` + `seeded-walkthrough.spec.ts`) as flaky before you assume a docs-only regression. In one run the alert subset failed with `Request context disposed` / missing `alerts-shell`, and in a later rerun the issues mutation-failure path failed on a one-off React console warning (`Can't perform a React state update on a component that hasn't mounted yet`). Re-run the failing subset in isolation before debugging product code; the alert-only subset passed cleanly on immediate rerun.
- For M061/S02 issue-surface Playwright proof, do not rely on an external `bash mesher/scripts/seed-live-issue.sh` pre-step when the suite boots its own temporary backend from `playwright.config.ts`. Seed the deterministic read/action issues through same-origin `/api/v1/events` inside the tests (and reopen them through `/api/v1/issues/:id/unresolve`) so the proof rail stays self-contained and fresh-backend safe.
- For M061/S02 toast warning fixes in `../hyperpush-mono/mesher/client`, the live dashboard imports `toast`/`useToast` from `@/hooks/use-toast`, not `components/ui/use-toast.ts`. If Playwright still reports `Can't perform a React state update on a component that hasn't mounted yet`, check `hooks/use-toast.ts` for a re-subscribing `useEffect` dependency before assuming the dashboard state providers are at fault.
- For M061/S03 backend-gap maintenance in `../hyperpush-mono/mesher/client/ROUTE-INVENTORY.md`, the fail-closed parser does **not** infer backend-gap coverage from `MIXED_ROUTE_SECTIONS`. If you add backend-gap rows for top-level mock-only pages, also extend `BACKEND_GAP_ROUTE_SECTIONS` in `mesher/scripts/lib/client-route-inventory.mjs` and the expected row order in `scripts/tests/verify-client-route-inventory.test.mjs`, or the new markdown will stay as unverified prose.
- For M061/S03 backend-gap classification, do not mark a row `covered` just because the page renders from some live data. Use `missing-payload` when overview/detail content still depends on derived or fallback-only fields, `missing-controls` when live reads exist but visible controls outrun available writes/settings fields, and `no-route-family` only when `main.mpl` has no same-origin backend family for that client promise.
