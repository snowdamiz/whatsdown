# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D007 | M023 | proof | First serious proof bar | Plan for a real on-chain creator→fund→claim→treasury loop, not a mostly mocked or simulated demo | This materially changes the milestone shape and is central to the product and hackathon story | No | human |
| D008 | M023 | arch | Treasury architecture | Architect the treasury around both buyback-and-burn and dividend-style routing, while sequencing live proof rather than trying to perfect both at once | The user wants both modes supported, but the first live milestone still needs a focused path | Yes — if execution evidence shows one mode should become canonical | human |
| D009 | M028 bootstrap | scope | Primary planning focus for the repo during milestone bootstrap | Anchor the GSD project contract around Mesh language maturity rather than the creator-fund product | The user explicitly chose the language as the primary contract for the new milestone, and M028 is meant to plan the language/platform direction rather than the current product effort. | Yes — if the repo returns to product-first planning later | human |
| D001 | M028 bootstrap | scope | Primary planning focus for the repo during milestone bootstrap | Anchor the GSD project contract around Mesh language maturity rather than the creator-fund product | The user explicitly chose the language as the primary contract for the new milestone, and M028 is meant to plan the language/platform direction rather than the current product effort. | Yes — if the repo returns to product-first planning later | human |
| D002 | M028 bootstrap | architecture | First serious production proof shape for Mesh | Use API + DB + migrations + background jobs as the canonical backend path | The user wants trust for a real production backend in any capacity, and this backend shape is the first deep proof target before expanding to broader backend forms. | Yes — if execution proves a narrower or broader proof path is necessary | collaborative |
| D003 | M028 bootstrap | prioritization | What Mesh should try to beat Elixir on first | Prioritize easier deployment, raw performance, and better DX before trying to match ecosystem breadth | The user defined the early comparison target as easier deployment, raw performance, and better DX, and explicitly called out untrustworthy concurrency and weak docs/examples as unacceptable. | Yes — once the trust baseline is established | collaborative |
| D004 | M028 bootstrap | pattern | How M028 should spend its first milestone energy | Use evidence-first hardening and honest proof before broad new syntax or feature expansion | The repo already exposes a wide backend/runtime/tooling surface; the highest-leverage next step is converting claims into trusted proof rather than increasing surface area without raising credibility. | Yes — if the canonical backend path exposes a blocking feature gap that must be added immediately | agent |
| D005 | M028/S01 planning | architecture | Where the canonical backend golden path should live | Create a new top-level `reference-backend/` Mesh package with env-driven `DATABASE_URL`/`PORT`/`JOB_POLL_MS`, a Postgres `jobs` schema, HTTP create/read endpoints, and a timer-driven worker instead of promoting `mesher/` directly | `mesher/` is already a broad product app with clustering, WS, alerting, and frontend concerns. A new narrow package gives S01-S06 one auditable proof target that matches the milestone contract and reuses Mesher only as a donor for patterns. | Yes — if execution proves the package location or runtime contract needs to change | agent |
| D006 | M028/S01/T03 | pattern | How `reference-backend` HTTP handlers should access the shared Postgres pool | Add a tiny package-local `RuntimeRegistry` service and have job handlers resolve the pool from it | This keeps the reference backend self-contained, avoids importing Mesher’s broader pipeline registry, and preserves the env-driven startup contract without reintroducing inline connection strings in handlers. | Yes — if Mesh later gains a cleaner package-local dependency injection pattern | agent |
| D010 | M028/S01 slice closure | runtime/codegen | How Mesh runtime sum/option payloads should carry opaque handles and primitive parse results when compiled Mesh code unwraps them | Box opaque handles and primitive parsed values before storing them in runtime `MeshResult`/`Option` payloads, then unwrap/dereference them consistently in consumers | S01 proved that compiled Mesh programs were crashing because `PoolHandle` and `String.to_int`/`String.to_float` payloads were being stored as raw integer-as-pointer values while compiled Mesh pattern matching and result unwrapping treated them as boxed payload pointers. Boxing the payloads in the runtime fixed migration execution, worker processing, and the reference-backend golden path without leaving an application-level workaround. | Yes | agent |
| D011 | M028/S01/T02 | runtime/config | Where the reference-backend startup env validation should happen on the non-empty DATABASE_URL path | Keep DATABASE_URL/PORT/JOB_POLL_MS validation local to reference-backend/main.mpl using Env.get and Env.get_int before pool startup, instead of routing parsed startup values through imported config helpers | S01 reproduced a crash on the real Postgres-backed startup path when the startup parsing/wiring crossed module boundaries in the wrong way. Validating the required env vars locally in main.mpl preserved the explicit missing-env failure mode, let the backend reach /health on a real DATABASE_URL, and gave the runtime-start regression test one stable code path to prove. | Yes | agent |
| D012 | M028/S02/T03 | verification | Which surfaces are authoritative for multi-instance exact-once proof on reference-backend | Use `/health.failed_jobs` and `/health.last_error` as the stable shared-DB contention signal, but prove exact-once and cross-instance participation through direct `jobs` table reads, cross-instance `/jobs/:id`, and per-instance `Job worker processed id=` logs. | Under heavier two-instance polling, `/health.processed_jobs` and `/health.last_job_id` can drift enough to cause false negatives even when DB truth is correct. S02 needed a trustworthy proof surface, so exact-once moved to the shared durable row plus HTTP and log cross-checks while `/health` stayed focused on benign-vs-real failure visibility. | Yes | agent |
| D013 | M028/S02/T03 | verification | Which surfaces are authoritative for multi-instance exact-once proof on reference-backend | Use `/health.failed_jobs` and `/health.last_error` as the stable shared-DB contention signal, but prove exact-once and cross-instance participation through direct `jobs` table reads, cross-instance `/jobs/:id`, and per-instance `Job worker processed id=` logs. | Under heavier two-instance polling, `/health.processed_jobs` and `/health.last_job_id` can drift enough to cause false negatives even when DB truth is correct. S02 needed a trustworthy proof surface, so exact-once moved to the shared durable row plus HTTP and log cross-checks while `/health` stayed focused on benign-vs-real failure visibility. | Yes | agent |
| D014 | M028/S02/T02 | runtime/storage | How reference-backend should claim pending jobs under shared Postgres contention | Keep the fix in application code by implementing claim-next as one `Repo.query_raw(...)` SQL statement with `FOR UPDATE SKIP LOCKED` and `RETURNING`, instead of changing `compiler/mesh-rt/src/db/repo.rs`. | S02 reproduced ordinary multi-instance claim races inflating worker failure counters. A single claim-and-return SQL statement removes the read/update race window on the real app path without widening runtime surface area, and the existing raw-query seam was already sufficient. | Yes | agent |
| D015 | M028/S03 planning | planning | How S03 should sequence and scope daily-driver tooling trust work | Execute S03 in this order: formatter/LSP-formatting hardening, then truthful `meshc test` project-dir support plus a real `reference-backend/tests/` path and an honest coverage contract, then JSON-RPC LSP integration proof, then docs/editor truth sync. Do not spend S03 on full line-level coverage instrumentation unless a small real artifact falls out cheaply. | The live trust breakers are the formatter panic on `reference-backend`, the false `meshc test` directory workflow, and the green `--coverage` stub. Fixing those first gives the LSP and docs work a verified backend contract instead of speculative behavior. Full coverage instrumentation is deeper than the rest of S03 and would risk delaying the higher-leverage trust repairs. | Yes | agent |
| D016 | M028/S03 | tooling/lsp | How `meshc lsp` should analyze backend-shaped files opened from a real project | Resolve the nearest ancestor containing `main.mpl` as the project root, analyze dependencies in project order, and overlay open-document contents when producing diagnostics/navigation/formatting results. | Single-file analysis was too shallow for `reference-backend/` and produced misleading import failures or stale results. Project-aware resolution plus live-buffer overlays makes editor behavior match the real compiler path and supports trustworthy diagnostics, hover, definition, formatting, and signature help on backend code. | Yes | agent |
| D017 | M028/S03 | tooling/test-runner | What `meshc test <path>` should mean on backend-shaped projects | Use the requested path as the test discovery root, but resolve project import-copy scope from the nearest ancestor containing `main.mpl`; keep `--coverage` as an explicit unsupported non-zero contract until real reporting exists. | S03 needed project-root, tests-directory, and specific-file invocation to work on `reference-backend/` without breaking local imports. Splitting discovery scope from project scope preserves the real backend workflow, and failing `--coverage` honestly avoids green placeholder behavior that would undermine tooling trust. | Yes | agent |
| D018 | M028/S04 planning | deployment | What boring native deployment path S04 should prove for `reference-backend` | Use an artifact-first deployment path: stage the compiled `reference-backend` binary plus a deploy-time SQL artifact and thin package-local scripts for staging, SQL apply, and probe-only smoke; keep `meshc migrate` as the developer/build-stage migration flow rather than a runtime-host dependency. | Research proved the compiled app binary already runs correctly outside the repo root, while `meshc migrate` remains a compile-at-apply workflow that requires the compiler toolchain, `libmesh_rt.a`, and source-tree context. The lowest-risk honest deployment story is therefore to ship a staged native binary and a boring Postgres migration artifact that can be applied without turning the runtime host into a Mesh compiler workstation. | Yes | agent |
| D019 | M028/S04 | deployment | How the boring runtime-host schema apply path should relate to the canonical Mesh migration workflow for `reference-backend` | Keep `reference-backend/migrations/20260323010000_create_jobs.mpl` canonical for compiler-driven dev/CI workflows, but ship a separate checked-in deploy SQL artifact (`reference-backend/deploy/reference-backend.up.sql`) that preserves `_mesh_migrations` and can be applied through `psql` on the runtime host. | S04 needed a boring native deployment path that does not require `meshc`, source checkout, or compiler/runtime build assets on the runtime side. Separating the deploy-time SQL artifact from the canonical Mesh migration keeps developer workflows honest while giving operators a thin artifact-first apply path. | Yes | agent |
| D020 | M028/S05 planning | M028/S05 planning | How S05 should sequence supervision and recovery hardening | Repair and strengthen the Mesh source-level supervisor proof first, then wire `reference-backend` onto that supervisor path, then add abandoned-job recovery plus health visibility, and keep the authoritative end-to-end proof in `compiler/meshc/tests/e2e_reference_backend.rs` with package-local documentation in `reference-backend/README.md`. | Research showed the Rust runtime supervisor core is materially stronger than the current Mesh source-level proof, and the compiler/runtime bridge may still be partially broken for real child startup. Building the backend recovery story before that bridge is trustworthy would risk a fake supervision claim. Sequencing the slice this way keeps one authoritative backend harness, closes the real crash-after-claim trust gap, and gives S06 concrete supervision/recovery proof to promote instead of inventing a separate docs-only story. | Yes | agent |
| D021 | M028/S06 planning | documentation | How S06 should publish the production backend proof surface | Gate public promotion on green `reference-backend` proof commands, keep `reference-backend/README.md` as the deepest runbook, add one canonical website proof page, and use targeted cross-links plus a verifier script instead of duplicating long command blocks across generic docs. | Research showed the current public docs are toy-first while the real backend proof path had drifted red. Restoring the backend truth gate first and then concentrating public guidance into one canonical narrative keeps R008/R009 honest, reduces maintenance drift, and gives future agents one mechanical doc-truth surface to verify. | Yes | agent |
| D022 | M028/S07 planning | recovery-proof | How S07 should close the unfinished recovery contract | Sequence S07 as: real supervisor-owned restart semantics plus stale/lease reclaim first, then health/restart visibility hardening, then deterministic whole-process restart proof in `compiler/meshc/tests/e2e_reference_backend.rs`; keep doc reconciliation out of S07 and update both the canonical migration and staged deploy SQL artifact if recovery SQL changes. | Research showed the live failures come from fake in-worker reboot logic and unsafe blanket reclaim, not from missing documentation. This order fixes the root cause first, preserves S04’s artifact-first deployment contract, and keeps all authoritative recovery proof in the single backend harness that later docs can safely cite. | Yes | agent |
| D023 | M028/S07 closure | runtime/recovery | How reference-backend should enforce worker restart for the S07 recovery proofs | Use an explicit watchdog actor that respawns `supervised_job_worker` when `/health` tick-age shows a stuck `processing`/`crashing` worker, instead of relying on the Mesh source-level `supervisor` block for this slice. | Live S07 repros showed the worker could enter `crashing` and stop ticking without any real child restart, even though the health contract and tests expected a restarted worker boundary. The watchdog approach stays inside the app proof surface, turns restart ownership into observable behavior immediately, and keeps crash/process-restart proofs green while preserving the existing stale-cutoff reclaim and health metadata contract. | Yes | agent |
| D024 | M028/S08 planning | planning | How S08 should sequence final proof-surface reconciliation | Execute S08 in three passes: first reconcile the public runbook/proof verifier, then rewrite stale S05/S06 closure artifacts from the green S07 command set, then rerun the full proof surface before sealing milestone validation and R008. | Research shows the remaining gap is truth-surface drift, not backend correctness. Fixing the public proof hierarchy first creates a durable guard against doc drift, updating the internal closure artifacts removes contradictory evidence, and delaying milestone/requirement closure until after full reruns keeps the slice evidence-first instead of papering over regressions. | Yes | agent |
| D025 | M028/S08 | documentation | How the reconciled production proof surface should detect public doc drift | Use exact string checks in `reference-backend/scripts/verify-production-proof-surface.sh` over the shared recovery-aware command list and `/health` recovery vocabulary across the README and proof page. | S08's remaining risk was truth-surface drift, not missing runtime coverage. Exact command and vocabulary checks turn drift into a mechanical verifier failure instead of a manual-reading problem and keep the promoted README/docs surfaces aligned with the green S07 proof contract. | Yes | agent |
| D026 | M028/S08 | validation | Which acceptance story should closure artifacts use once the recovery contract is green | Treat the S07 recovery-aware UAT command order as the canonical acceptance script for reconciled S05, S06, and S08 closure artifacts instead of preserving slice-local historical scripts. | Keeping multiple partially overlapping acceptance scripts would preserve contradictory evidence even after S07 closed the runtime gap. Reusing one canonical command order keeps public docs, internal closure artifacts, milestone validation, and requirement tracking on the same proof path. | Yes | agent |
| D027 | M031 bootstrap | arch | How to disambiguate trailing closures from control-flow block openers | Use parser context: suppress trailing-closure parsing when inside a control-flow condition position (if, while, case, for conditions). The `do` after `)` in those contexts is the block opener, not a trailing closure. | The trailing-closure heuristic (`do` after `)` = trailing closure) causes `if fn_call() do` to be misparsed. Context-based disambiguation preserves existing trailing-closure usage (`test("name") do ... end`, `describe("name") do ... end`) while fixing control flow. The alternative (requiring `do \ | args\ | agent |
| D028 | M031 bootstrap | convention | Whether bare expression statements (without `let _ =`) should be the idiomatic pattern for side effects in Mesh | Remove all `let _ =` bindings for side-effectful expressions in both reference-backend and mesher. Bare expression statements are confirmed to work at the codegen level (lower_block handles them correctly). | Investigation confirmed that `lower_block` in `compiler/mesh-codegen/src/mir/lower.rs` already handles bare expressions in blocks. Multiple test programs (bare `println()`, `ServiceCall.inc()`, `spawn()`) compiled and ran correctly without `let _ =`. The 157+72 instances in dogfood code are unnecessary ceremony. | No | collaborative |
| D029 | M031 bootstrap | convention | When to replace `<>` string concatenation with `#{}` interpolation in dogfood cleanup | Only replace `<>` with interpolation where the result is clearly more readable. Keep `<>` for raw SQL construction with complex expressions and raw JSONB embedding where interpolation would be awkward. | Not all `<>` usage is bad. Cases like `"too many events (max " <> String.from(n) <> ")"` are clearly better as `"too many events (max #{n})"`. But SQL construction like `"CREATE TABLE IF NOT EXISTS events_" <> date_str <> " PARTITION OF events..."` is borderline — interpolation works but the `<>` makes the SQL template structure more visible. Apply judgment per-case. | Yes | agent |
| D030 | M031/S01/T02 | typechecker | How to fix else-if chain type resolution in the typechecker | Add `types.insert(if_.syntax().text_range(), resolved_type)` before both return paths in `infer_if`, matching the storage pattern `infer_expr` uses for all other expression types. | The root cause was `infer_if` bypassing `infer_expr` during recursion, so the inner if-expression's resolved type was never stored in the `types` map. Codegen fell back to `MirType::Unit`, producing garbage values (Int) or misaligned pointer crashes (String). The fix is the minimal insertion that makes recursive inference consistent with the top-level inference path. | No | agent |
| D031 | M031/S02/T02 | formatter | Where to handle parenthesized import formatting in the formatter walker | Extract a dedicated `walk_import_list` function that handles the `IMPORT_LIST` node, rather than modifying `walk_from_import_decl`, because `L_PAREN`/`R_PAREN` tokens live inside `IMPORT_LIST` in the CST, not as direct children of `FROM_IMPORT_DECL`. | The plan assumed parens would be at the `FROM_IMPORT_DECL` level, but CST inspection showed they're children of `IMPORT_LIST`. Handling them at the correct tree level avoids fragile parent-child assumptions and keeps the walker dispatch consistent with CST structure. | Yes | agent |
| D032 | M031/S04/T02 | dogfood | Whether to convert mesher long import lines to parenthesized multiline form | Deferred — meshc fmt collapses parenthesized multiline imports back to single-line and introduces spurious spaces in module dot-paths (e.g. `Storage. Schema`), making `fmt --check` fail. Parser supports the syntax correctly; the issue is purely in the formatter walker. | The parser proven by e2e tests handles multiline imports correctly, but the formatter undoes them. Applying multiline imports only to have `fmt --check` permanently fail is worse than keeping long single-line imports until the formatter is fixed. | Yes | agent |
| D033 | M031/S05 | language | Whether `do...end` blocks can be used as values in `let` bindings | No — `let x = do ... end` is not valid Mesh syntax. Block-as-expression context is provided by if/else, case, and function bodies, not standalone `do...end`. | Discovered during S05 test authoring: the parser does not accept `do...end` as a value expression in let bindings. This is consistent with how Mesh structures block contexts through control flow and function bodies rather than arbitrary block expressions. | Yes | agent |
| D034 | M029 | convention | How to handle mesher JSON serializers with raw JSONB fields | Use `json {}` macro where all fields are simple strings/ints; use `#{}` interpolation where raw JSONB fields (condition_json, action_json, tags, exception, stacktrace, breadcrumbs, extra, user_context, condition_snapshot) prevent macro use | The json macro auto-quotes string values, which would double-quote already-JSON JSONB fields. Interpolation gives explicit control over quoting. This splits the work cleanly: simple serializers get the macro, complex ones get interpolation — both are better than 300-char `<>` chains. | Yes | collaborative |
| D035 | M029/S01 planning | milestone-planning | When to satisfy the reference-backend dotted-path repair requirement in M029 | Complete the `reference-backend/` import repair in S01 alongside the formatter fix, instead of deferring it to S03. | R027 is a direct S01 requirement in the slice brief, and `meshc fmt --check reference-backend` can pass on already-corrupted but idempotent source. S01 therefore has to leave the dogfood backend in a semantically correct round-trippable state, not just ship formatter unit fixes. | Yes | collaborative |
| D036 | M029/S01/T01 | formatter | How mesh-fmt should emit CST PATH nodes used by imports and qualified impl headers | Route `SyntaxKind::PATH` through a dedicated `walk_path(...)` formatter that preserves dot-separated token text instead of using the generic inline spacing walker. | Imports (`from Api.Router import ...`) and qualified impl headers (`impl Foo.Bar for Baz.Qux do`) both reuse the parser's `PATH` node. The generic inline walker inserts spaces before identifiers after `.` and corrupts semantically meaningful module/type paths into `Foo. Bar`. A dedicated PATH walker localizes the fix to the shared root cause without broad spacing changes elsewhere in the formatter. | Yes | agent |
| D037 | M029/S02 planning | verification | How to prove Mesher pipe cleanup and remaining `<>` sites in M029/S02 | Use `rg -n 'List\.map\(rows,\ | Ok\(List\.map\(' mesher -g '*.mpl'` as the authoritative pipe-style gate, and prove remaining `<>` usage with an exact-location diff against the five designated SQL/DDL keep sites. | The roadmap's broad `rg 'List\.map(' mesher -g '*.mpl'` check would false-fail on the desired pipe style because it matches both wrapping and piped `List.map(...)` calls. The exact-location `<>` diff prevents future work from silently introducing new concatenation sites while still preserving the accepted SQL/DDL assemblies in `schema.mpl` and `queries.mpl`. | agent |
| D038 | M029/S03 planning | planning | How to decompose M029/S03's Mesher formatter-closeout work | Split S03 into two manual multiline-import rewrite tasks, three directory-scoped formatter canonicalization waves, and one final proof task that writes `S03-UAT.md`, while keeping `reference-backend/` regression-only unless formatter behavior regresses. | Live S03 research reduced the true hand-edit surface to 10 imports across 8 files, but the remaining Mesher formatter-red set still spans 35 files. A single bulk formatter task would be too large and opaque for one executor window, while splitting by real source clusters keeps each task reviewable, preserves explicit dependencies from manual import edits into formatter waves, and leaves the final task responsible for the full dual-app proof surface plus a durable closeout artifact. | Yes | agent |
| D039 | M029/S03/T04 | formatter | Canonical source-file, pipe-chain, and struct-literal formatting behavior in mesh-fmt | Top-level formatter output now keeps adjacent comment lines and import runs compact, flattens pipe chains to a single continuation indent, and forces multiline struct-literal closure bodies when needed instead of collapsing them into one line. | Mesher cleanup exposed three formatter regressions at once: blank lines between every top-level comment/import, stair-stepped pipe indentation, and inline closure bodies with multi-field struct literals collapsing into unreadable single lines. Fixing all three in the formatter keeps canonical output readable while preserving the existing blank-line rule for real top-level item boundaries. | Yes | agent |
| D040 | M029/S03 closeout | tooling/formatter | How final formatter-closeout gates should treat success output and spacing-sensitive regressions | Keep `meshc fmt --check` silent on success and fix formatter/CLI regressions with exact-output tests instead of weakening captured-log acceptance gates. | S03 closeout exposed two real truth-surface failures: formatter spacing regressions (`pubtype`, `table"..."`) and non-empty output on successful `fmt --check` runs. The slice used captured-log emptiness as part of the acceptance contract, so relaxing that gate would hide regressions instead of fixing them. Silent success plus exact-output tests keeps formatter proof text-sensitive rather than exit-code-only. | Yes | agent |
| D041 | M032 bootstrap | scope | What bar M032 should use for language and tooling changes | Focus on proven blockers and stale-workaround retirement rather than broad language-design expansion | The current milestone is meant to make mesher's limitation surface truthful and retire the blockers that actually affect it now. A wide design sprint would dilute proof and risk replacing folklore with new speculative surface area. | Yes — if a broader change is the only honest way to retire a proven blocker | human |
| D042 | M032 bootstrap | scope | How to sequence the current mesher dogfood cleanup and data-layer work | Split the work into M032 for workaround-truth plus Mesh blocker retirement and M033 for ORM expressiveness plus schema extras | The current pressure actually has two different jobs: first remove stale folklore and fix still-real Mesh blockers, then improve the ORM and migration surfaces against a cleaner truthful mesher baseline. Combining them would blur root-cause fixing with API expansion and make proof weaker. | Yes — if M032 discovers substantially more ORM-only pressure than expected | human |
| D043 | M032 bootstrap | architecture | How the ORM and migration surfaces should balance portability with database-specific behavior | Keep a neutral core and add explicit vendor-specific extras instead of pretending all recurring mesher pressure is portable | The user wants portability without fake abstraction. Mesher's real pressure points are not generic SQL; they include JSONB, full-text, pgcrypto, and partition DDL. Making vendor-specific behavior explicit is more honest than hiding it behind raw fragments or a misleading neutral API. | Yes — if execution shows one layer belongs in the core after all | human |
| D044 | M032 bootstrap | sequencing | Which vendor-specific data-layer work lands first | Implement Postgres extras first and defer SQLite extras until the neutral extension shape is proven | Mesher is the real proof surface for this wave and it is currently applying Postgres-specific pressure. Shipping PG extras first keeps the work anchored to live demand while still preserving a clean path for SQLite later. | Yes — if a near-term SQLite-backed dogfood target appears | human |
| D045 | M032 bootstrap | scope | What bar M032 should use for language and tooling changes | Focus on proven blockers and stale-workaround retirement rather than broad language-design expansion | The current milestone is meant to make mesher's limitation surface truthful and retire the blockers that actually affect it now. A wide design sprint would dilute proof and risk replacing folklore with new speculative surface area. | Yes — if a broader change is the only honest way to retire a proven blocker | human |
| D046 | M032 bootstrap | dogfood | How much mesher itself is allowed to move during M032 and M033 | Use mesher as the proof target but keep its product behavior stable; improve Mesh and the data layer underneath it instead of redesigning the app | The user wants mesher to prove the repaired platform path, not become a moving product target. Stable behavior makes it easier to distinguish real platform gains from app-level rewrites. | Yes — if a narrow product-facing change is unavoidable to dogfood the repaired path honestly | human |
| D047 | M032/S01/T02 | verification | How M032/S01 records limitation truth for mesher folklore | Keep both permanent Rust e2e tests and a direct fixture replay script; the script must exercise the `.tmp/m032-s01` programs from repo root and use a live HTTP request for route closures instead of relying on compile-only builds. | The stale-vs-real audit spans both passing and failing behaviors, and one of the retained limits (`route_closure`) is compile-pass but runtime-fail. Encoding the proofs only in Rust tests would hide the exact audited commands, while compile-only replay would misclassify the HTTP closure limitation. Using both surfaces leaves later slices with durable named tests and a direct matrix replay that exposes the real failure symptoms. | Yes | agent |
| D048 | M032/S02 planning | dogfood | How S02 will satisfy the mesher dogfood requirement for the inferred-export fix | Use `mesher/storage/writer.mpl` as the real dogfood surface by exporting the inferred-parameter batch-write helper now living beside `insert_event`, and have `mesher/services/writer.mpl` import it instead of adding a synthetic repro-only helper. | R013 requires a real blocker to be fixed in Mesh and then used in mesher. Existing comment cleanup alone is too weak, but inventing a synthetic exported identity helper would fake product pressure. Extracting the batch-write helper keeps product behavior stable, exercises the repaired inferred-export path on a real module boundary, and naturally pairs with the required wording cleanup in `mesher/storage/writer.mpl`. | Yes | agent |
| D049 | M032/S02 | compiler/lowering | How to repair unconstrained inferred function exports without widening Mesh's generic machinery | Collect concrete function-usage signatures from local and cross-module call sites, pass that evidence into MIR lowering, and emit per-signature MIR clones only when an inferred function is used at multiple concrete ABIs. | The `identity(x) = x` blocker was not a general typechecker failure; it was a lowering-time ABI problem. Recovering only the first observed parameter type still left later call sites miscompiled (`i64 -> {}` for local polymorphic uses) and exported modules lowered in isolation never saw downstream evidence at all. A narrow usage-driven specialization path fixes the real blocker, keeps existing already-constrained inferred functions unchanged, and avoids turning S02 into a broad generic-function redesign. | Yes | agent |
| D050 | M032/S03 | dogfood | How M032/S03 should retire stale request/handler folklore in mesher | Use currently supported inline Mesh patterns directly in the real mesher handlers, while preserving only the verified route-closure, nested-&&, and timer keep-site comments. | The stale wrappers and limitation comments in mesher were making Mesh look weaker than current reality. Dogfooding Request.query, inline service-call case handling, and inline cast if/else in the audited modules retires folklore without widening scope or erasing the still-real limits that S01 already proved. | Yes | agent |
| D051 | M032/S04 | dogfood-boundary | How S04 should reconcile solved cross-module from_json support with Mesher's event ingestion/storage path | Retire stale module-boundary from_json comments, but keep Mesher's SQL-side extract/write path unchanged until a separate parity proof justifies a Mesh-side rewrite | Cross-module from_json is already supported and regression-covered, but Mesher's fingerprint fallback chain still depends on PostgreSQL JSONB operators plus CASE/jsonb_array_elements/string_agg, and the matching insert path already lives on the same raw-SQL side of the boundary. Replacing that path in S04 would be a product-path redesign without parity evidence, not an honest folklore cleanup. | Yes | agent |
| D052 | M033 planning | milestone-planning | How M033 should sequence ORM expressiveness and schema-extra work | Sequence M033 as neutral write-expression core first, then explicit PostgreSQL JSONB/search/crypto extras, then harder read-side raw-boundary retirement, then partition/schema lifecycle helpers with live catalog proof, and finish with public docs plus integrated Mesher acceptance. | The highest risk is getting the neutral-core boundary wrong. Proving computed writes first validates the reusable expression contract without overbuilding. PG-heavy families should land only after that contract is real, the harder read-side families should stop at honest coverage instead of chasing a fake universal AST, partition helpers need their own live Postgres proof surface, and docs must come last so they describe shipped behavior rather than anticipated API shape. | Yes | collaborative |
| D053 | M033/S01 planning | data-layer-api | How S01 should represent the neutral write-expression core | Add a dedicated neutral expression builder plus expression-aware Query/Repo entrypoints, and keep PG-specific helpers out of that core. | The current Query/Repo surface already uses opaque builder pointers. Extending that model with a dedicated neutral expression layer is the cleanest way to cover column refs, literals, NULL, function calls, CASE, COALESCE, and conflict-update expressions without smuggling PostgreSQL syntax through raw strings. This keeps R036 honest, preserves the later SQLite extension seam required by R040, and lets Mesher rewrite portable write paths now while leaving JSONB/crypto/search-specific helpers explicit for later slices. | Yes | agent |
| D054 | M033/S02 planning | data-layer-api | How S02 should expose PostgreSQL-only JSONB/search/pgcrypto behavior without reopening the neutral core | Add PG-only expression helpers under `Pg`, extend the existing expression/query/repo plumbing just enough for casts, expression-valued SELECT/WHERE, and expression-valued inserts, and keep `extract_event_fields` as an explicit raw keep-site unless the helper path stays obviously honest. | S01 already established a neutral expression core and reserved query plumbing (`select_params`) that explicit PG helpers can compose with. Keeping vendor-specific behavior under `Pg` advances R037 while preserving the R040 SQLite seam. Deferring `extract_event_fields` unless it stays clearly honest avoids overbuilding a fake universal read DSL and keeps the raw boundary truthful for R038/S03. | Yes | agent |
| D055 | M033/S01/T03 | runtime | How Mesher rate-limit thresholds are configured for live ingest | Keep the 60-second / 1000-event defaults, but read optional MESHER_RATE_LIMIT_WINDOW_SECONDS and MESHER_RATE_LIMIT_MAX_EVENTS env vars and spawn the window-reset ticker from the rate-limiter startup helper. | This preserves existing production behavior while making the live ingest limiter truthfully testable at small thresholds and restoring the intended fixed-window reset behavior. | Yes | agent |
| D056 | M033/S01 T01 | data-layer-api | What Mesh-callable aliasing surface S01 should use for expression-valued SELECT items until the parser keyword collision is repaired | Use `Expr.label(...)` as the public Mesh-callable alias entrypoint for now, while still lowering to the same runtime alias intrinsic as `mesh_expr_alias`. | `Expr.alias(...)` currently collides with keyword parsing after `Expr.` in Mesh source. S01 needed a working SELECT aliasing surface immediately to prove the neutral expression contract, and `Expr.label(...)` preserves that capability without widening the slice into parser surgery. | Yes | agent |
| D057 | M033/S01 T01 | data-layer-api | What Mesh-callable aliasing surface S01 should use for expression-valued SELECT items until the parser keyword collision is repaired | Use `Expr.label(...)` as the public Mesh-callable alias entrypoint for now, while still lowering to the same runtime alias intrinsic as `mesh_expr_alias`. | `Expr.alias(...)` currently collides with keyword parsing after `Expr.` in Mesh source. S01 needed a working SELECT aliasing surface immediately to prove the neutral expression contract, and `Expr.label(...)` preserves that capability without widening the slice into parser surgery. | Yes | agent |
| D058 | M033/S01 closeout | mesher-runtime | How the async StorageWriter path should carry identity and flush low-volume event batches on the live Mesher ingest path | Buffered writer entries now carry `project_id\|\|\|issue_id\|\|\|fingerprint\|\|\|event_json`, writers are started through `start_writer(...)` so the timer flush ticker is always spawned, and writer flush helpers return non-integer success markers when the count is not needed. | The live S01 proof showed that low-volume repeated ingest updated `issues` but did not persist `events` because the flush ticker was never started, the writer state's `project_id` label was not the UUID needed by the raw event insert, and `Int ! String` success payloads were still brittle enough to crash the background path. Encoding the real project UUID into the buffered payload and using the timer-backed start helper makes the persistence boundary truthful for downstream slices. | Yes | agent |
| D059 | M033/S02/T02 | data-layer-api | How S02 JSONB rewrites should express PG-native extract/build/existence operations that do not yet have dedicated Pg wrappers | Use Expr.fn_call(...) with PostgreSQL-native functions like jsonb_extract_path, jsonb_extract_path_text, jsonb_exists, and jsonb_build_object alongside the explicit Pg casts/operators, instead of expanding the dedicated Pg helper surface further inside S02. | The T01/T02 expression seam already supports structured, parameterized vendor-specific function calls without pretending those operations are portable. Reusing Expr.fn_call keeps Mesher on Query.where_expr / Query.select_expr / Repo.insert_expr for the real runtime path, shrinks raw whole-query usage, and avoids inventing a wider fake-neutral AST or rushing new one-off Pg wrappers before there is broader demand. | Yes | agent |
| D060 | M033/S02 | verification | How the S02 PG-helper acceptance suite should exercise real Mesher storage paths | Use temporary Mesh projects that copy Mesher storage/types modules for the S02 Postgres proof bundle instead of routing the slice verification through the S01 HTTP readiness harness. | This lets the proof bundle call the real auth/search/jsonb/alert storage helpers directly against live Postgres while avoiding unrelated HTTP startup/readiness noise, so failures localize to the new PG helper boundary instead of to Mesher server boot behavior. | Yes | agent |
| D061 | M033/S03 planning | data-layer-api | How S03 should retire the hard read-side raw families without reopening the neutral ORM boundary | Prefer Mesher-side decomposition plus the existing `Query` / `Expr` / `Pg` surfaces for S03 read rewrites, and keep only an explicit named leftover list instead of adding general derived-table or scalar-subquery support in this slice. | The current runtime already covers most projection, filter, join, grouping, and ordering work honestly, and the remaining hard read families can be retired or narrowed through multiple smaller builder-backed reads plus Mesh-side combination. Adding a broader derived-table or scalar-subquery surface here would overstate portability, recreate the fake-universal-AST risk already called out in M033 planning, and work against the R036/R040 requirement to keep the neutral baseline honest while leaving vendor-specific behavior explicit. | Yes | agent |
| D062 | M033 S03 T03 | testing | Composed-read verification surface for Mesher S03 | Use the live Mesher HTTP/API harness for composed-read proofs instead of extending the copied storage-only Mesh probe | The storage-only probe already reproduced compiler/runtime artifacts that are specific to staged typed-list and aggregate consumption rather than the actual Mesher caller contracts. Replacing it with a live Mesher-backed harness keeps verification aligned with the real search/dashboard/detail/alerts/team surfaces and exposes genuine contract regressions separately from probe-only failures. | Yes | agent |
| D063 | M033/S03/T05 | pattern | How live Mesher event-alert cooldown eligibility should be determined | Filter cooldown-eligible event rules inside `get_event_alert_rules(...)` and treat the selector as the fireable-rule boundary instead of doing a second per-rule cooldown read in the alert loop. | The live `/api/v1/events` alert path became unstable when cooldown was checked in a separate follow-up read. Folding cooldown into the selector kept the data contract on one explicit SQL boundary and restored deterministic alert firing in the S03 acceptance harness. | Yes | agent |
| D064 | M033/S03/T05 closeout | data-layer-api | How S03 should treat read helpers whose builder-backed live path still mis-shapes rows or crashes Mesher | Keep those read families as explicit named raw keep-sites in `mesher/storage/queries.mpl` and enforce that boundary with `scripts/verify-m033-s03.sh` instead of forcing a dishonest builder rewrite. | The live Postgres-backed S03 harness showed that some read families were still unstable on the real Mesher caller path when forced through the current builder/runtime combination. Preserving them as named keep-sites keeps the raw boundary honest, prevents fake portability claims, and gives S04/S05 a mechanically enforced ledger of what still remains outside the current honest ORM surface. | Yes | agent |
| D065 | M033/S04 | data-layer-api | What the neutral migration index surface should cover in M033/S04 | Keep `Migration.create_index(...)` strictly neutral: support exact `name:...`, `unique:true\|false`, trailing `where:...`, and per-column `:ASC` / `:DESC`, while rejecting PostgreSQL-specific method/opclass syntax and routing those cases through explicit `Pg.*` helpers. | Mesher needed stable index names, partial predicates, and ordered columns on the honest portable path, but pretending `USING`, opclasses, or partition DDL are neutral would blur the vendor boundary and recreate the fake-portability problem M033 is trying to remove. | Yes | agent |
| D066 | M033/S04 | architecture | Where Mesher should own runtime partition lifecycle after the S04 schema-helper work | Keep Mesher's runtime partition create/list/drop lifecycle in `Storage.Schema` as thin wrappers over explicit `Pg.create_daily_partitions_ahead(...)`, `Pg.list_daily_partitions_before(...)`, and `Pg.drop_partition(...)`, and remove those helpers from `Storage.Queries` instead of widening the generic query API. | Partition bootstrap, retention cleanup, and catalog-oriented drop behavior are explicitly PostgreSQL-shaped schema concerns. Centralizing them in `Storage.Schema` keeps the query layer focused on row access, preserves an honest boundary for future vendor-specific extras, and gives startup/retention code one authoritative helper surface. | Yes | agent |
| D067 | M033/S04 | verification | How S04 should enforce the migration/runtime partition helper boundary after the live Postgres proof lands | Treat `compiler/meshc/tests/e2e_m033_s04.rs` plus `scripts/verify-m033-s04.sh` as the authoritative S04 acceptance surface, and remove the old S03 exemption for partition/catalog helpers so raw-boundary regressions fail mechanically in the named migration/runtime files. | The slice promise is not just helper availability but a durable honest boundary. A live catalog harness plus a verifier that bans raw DDL/query tokens and checks expected helper/log surfaces turns future drift into named failures instead of folklore or silent regressions. | Yes | agent |
| D068 | M033 S05 planning | verification | Canonical S05 acceptance surface and documentation truth gate | Close S05 with a public docs rewrite plus `scripts/verify-m033-s05.sh`, a serial wrapper that runs the existing S02/S03/S04 verifiers, VitePress docs build, and an exact-string docs-truth sweep instead of starting with a new `e2e_m033_s05.rs` Rust harness. | S02-S04 already provide the live Postgres runtime and catalog proof surfaces that S05 needs to compose. The remaining gap is milestone-level replay and public contract honesty, not another runtime subsystem. A serial wrapper preserves the shared Postgres/port-5432 assumption, keeps failures localized to the first broken phase, and makes the remaining raw boundary plus neutral-vs-PG split mechanically enforceable in public docs without duplicating harness boilerplate. | Yes | agent |
| D069 | M033/S05 closeout | verification | Which slice owns `get_event_alert_rules(...)` and `get_threshold_rules(...)` in the M033 raw-boundary ledger | Treat both functions as deliberate S03 honest raw-read keep-sites, not S02 PG-helper-owned builder coverage. | The assembled S05 acceptance surface and public docs need one truthful owner for the remaining raw read families. These selectors now belong to the S03 named raw keep-list; keeping them there aligns the verifiers, the public contract, and the real Mesher read-side/runtime behavior instead of letting S02 claim builder coverage it no longer owns. | Yes | agent |
| D070 | M034 bootstrap | package-experience | Where the package-manager UX improvement wave should focus | Center package-manager experience improvements on the public packages website first, with CLI polish secondary, and do that after the underlying trust path is proven. | The user said the package-manager UI is simple and chose website-first when asked where the improvement effort should center. Sequencing it after M034 keeps polish from outrunning truth. | Yes — if later discussion or execution shows the biggest usability pain is actually CLI-first. | human |
| D071 | M034 bootstrap | scope | How to sequence the next Mesh maturity wave after M033 | Split the next maturity wave into four milestones sequenced as M034 delivery truth, M035 test-framework hardening, M036 editor parity and multi-editor support, and M037 package experience polish. | The user’s vision spans CI/CD hardening, package-manager end-to-end proof, test-framework readiness, editor support, and package UI improvement. Splitting these into four milestones preserves the full ambition without collapsing unlike work into one mushy milestone. | Yes — if execution evidence shows one milestone should merge or split further. | collaborative |
| D072 | M034 bootstrap | editor-support | How serious multi-editor support should be | Treat non-VSCode editor support as a first-class milestone goal rather than a best-effort secondary story. | The user explicitly rejected VS Code-only and best-effort secondary support, and wants a real path for Vim or another non-VSCode editor. | Yes — once the first serious secondary editor target is chosen during M036 discussion. | human |
| D073 | M034 bootstrap | package-experience | Where the package-manager UX improvement wave should focus | Center package-manager experience improvements on the public packages website first, with CLI polish secondary, and do that after the underlying trust path is proven. | The user said the package-manager UI is simple and chose website-first when asked where the improvement effort should center. Sequencing it after M034 keeps polish from outrunning truth. | Yes — if later discussion or execution shows the biggest usability pain is actually CLI-first. | human |
| D074 | M034/S01 planning | release-verification | What should be the canonical proof surface for the real registry publish/install path | Use a repo-local `scripts/verify-m034-s01.sh` verifier with one owner-scoped proof package name, unique per-run versions, `meshpkg --json`, direct registry HTTP checks, lockfile inspection, consumer build proof, duplicate publish rejection, and exact package-page visibility checks. | This creates one observable proof surface that later CI slices can call unchanged and avoids treating artifact builds, homepage counts, or undeclared assumptions as release confidence. | Yes | agent |
| D075 | M034/S01 planning | release-verification | What should be the canonical proof surface for the real registry publish/install path | Use a repo-local `scripts/verify-m034-s01.sh` verifier with one owner-scoped proof package name, unique per-run versions, `meshpkg --json`, direct registry HTTP checks, lockfile inspection, consumer build proof, duplicate publish rejection, and exact package-page visibility checks. | This creates one observable proof surface that later CI slices can call unchanged and avoids treating artifact builds, homepage counts, or undeclared assumptions as release confidence. | Yes | agent |
| D076 | M034/S01 planning | package-manager | How scoped installed registry packages should be represented and discovered locally | Keep the current owner/package@version on-disk layout under `.mesh/packages` and fix `meshc`/`mesh-lsp` discovery to recurse to leaf directories containing `mesh.toml`, rather than sanitizing scoped package names into a flat cache key. | This removes the real scoped-install build blocker without inventing a second name-encoding contract, preserves the registry/package name end-to-end, and keeps compiler and editor behavior aligned on the same package-root rule. | Yes | agent |
| D077 | M034/S01 | registry | Registry publish truth ordering for package versions | Upload or confirm the package blob before inserting the version row so registry metadata never becomes public truth ahead of storage truth. | M034/S01 proved that the real registry path should not expose a version row before the corresponding blob exists in object storage. Reordering publish to establish blob truth first removed the metadata-only false-green failure mode while keeping duplicate version handling intact. | Yes | agent |
| D078 | M034/S01 | package-manager | Named-install contract for registry packages | Treat `meshpkg install <name>` as a fetch-plus-lock operation that updates `mesh.lock` but does not edit `mesh.toml`, and expose that contract explicitly in CLI output, JSON, docs, and verifier checks. | M034/S01 needed the package-manager path to be mechanically provable. Making named install manifest-stable and surfacing `lockfile` plus `manifest_changed: false` removed misleading implications that the CLI mutates declared dependencies behind the user's back. | Yes | agent |
| D079 | M034/S01 | requirement | R007 | validated | M034/S01 now proves the real Mesh package workflow end to end: scoped installed packages build from their natural cache layout, and `bash scripts/verify-m034-s01.sh` successfully published `snowdamiz/mesh-registry-proof`, rechecked metadata/search/detail/download truth, verified tarball SHA and `mesh.lock`, confirmed named-install manifest stability, rejected duplicate publish with 409, and built/ran a consumer against the live registry package. | Yes | agent |
| D080 | M034/S02 planning | release-verification | How the authoritative Mesh package-manager proof should be wired into CI and release flows | Use one reusable GitHub Actions workflow that shells out to `bash scripts/verify-m034-s01.sh`, call it from a trusted-event verification lane (same-repo PRs, `main` pushes, `workflow_dispatch`, and a bounded schedule), and make `release.yml` gate tag releases on that same reusable proof while skipping fork-PR live-proof runs and avoiding `pull_request_target`. | This preserves the S01 verifier as the single proof surface, prevents YAML copies of publish/install logic from drifting, keeps publish secrets out of untrusted fork PR execution paths, and gives later release-hardening slices a reusable pattern built around real proof rather than packaging success. | Yes | agent |
| D081 | M034/S02/T03 | ci/security | How tag-release gating and permissions should be structured for the authoritative live-proof lane. | Keep `release.yml` workflow-wide permissions read-only, grant `contents: write` only to the `Create Release` job, and require a tag-only reusable authoritative-proof job before release publication. | This keeps ordinary build and proof jobs fail-closed on token scope while making tag publication depend on the same reusable live proof used elsewhere, so releases cannot bypass the real package-manager verification path. | Yes | agent |
| D082 | M034/S03 planning | installer/release-verification | What should count as canonical installer truth for M034/S03 release assets | Treat `website/docs/public/install.{sh,ps1}` as the canonical installer surface, keep `tools/install/*` byte-identical to those public scripts, prove them against staged release assets with repo-local verifiers, and include Windows `meshpkg` release/install coverage so installer truth means both shipped binaries. | The public docs-served scripts are the surface users actually execute, the repo copy already drifted once, and the current Windows path cannot satisfy the slice demo because it points at the wrong repo and only installs `meshc`. Canonicalizing on the public copies, proving them against staged release assets, and covering Windows `meshpkg` makes the release workflow verify the exact user-facing installer contract before publication. | Yes | agent |
| D083 | M034 S03 T01 | release | Canonical installer source and staged release proof hooks | Treat website/docs/public/install.{sh,ps1} as the only canonical installer sources, keep tools/install/* byte-identical copies, and use test-only MESH_INSTALL_RELEASE_API_URL, MESH_INSTALL_RELEASE_BASE_URL, and MESH_INSTALL_STRICT_PROOF hooks for staged proof against local release assets. | The docs-served installers are the real user contract, so proof must exercise those exact files instead of drift-prone repo-only copies. Environment-based override hooks let verification point the unmodified installer logic at staged assets and strict checksum/metadata failures without changing default GitHub behavior for normal users. | Yes | agent |
| D084 | M034/S03/T02 | release-verification | How non-tag release assets should be versioned for installer smoke | Derive staged release archive versions from the repo Cargo version and fail tag builds when the tag and Cargo version diverge. | The installer proof checks both the staged release tag written into ~/.mesh/version and the embedded meshc/meshpkg binary versions. Naming non-tag artifacts as dev makes the smoke path falsely fail even when the binaries are correct, so the workflow now keeps archive/version metadata aligned with the compiled binaries on all refs and rejects tag drift explicitly. | Yes | agent |
| D085 | M034/S04 planning | extension-release | Canonical proof surface and workflow shape for the VS Code extension release lane | Use a repo-local `scripts/verify-m034-s04-extension.sh` verifier plus a reusable extension-proof workflow; the tag-triggered publish workflow must publish the exact verified VSIX and fail closed if either marketplace publish fails, while reusing the existing `meshc` `e2e_lsp` proof instead of claiming new editor parity. | The current lane can package or publish the wrong extension because packaging, version/tag/docs drift, and workflow semantics are not checked mechanically. Centralizing these checks in one script keeps local execution, CI, and later S05 assembly on the same proof surface; a reusable workflow follows the S02 pattern; fail-closed dual-market publishing prevents partial success from looking complete; and reusing `e2e_lsp` keeps S04 honest about what it proves. | Yes | agent |
| D086 | M034/S04/T01 | vscode-extension packaging | How local VSIX packaging and install scripts resolve the publishable artifact | Centralize VSIX path resolution in tools/editors/vscode-mesh/scripts/vsix-path.mjs, make package/install-local use it, and clean dist/ to one versioned VSIX before packaging | The old contract hardcoded mesh-lang-0.3.0.vsix at the extension root and packaged with --no-dependencies, which let stale artifacts and missing runtime deps slip through. One helper makes the artifact path deterministic for humans, scripts, and later workflow gates while exposing a single reusable packaging surface. | Yes | agent |
| D087 | M034 S04 T03 | architecture | How the VS Code extension publish lane proves and hands off the verified VSIX | Make `.github/workflows/extension-release-proof.yml` the only workflow that directly runs `scripts/verify-m034-s04-extension.sh`; have it emit `verified_vsix_path` and `verified_vsix_artifact_name`, upload the exact verified VSIX as `extension-release-vsix`, and require `.github/workflows/publish-extension.yml` to download and publish only that artifact to both registries. | This keeps release truth in one repo-local verifier, prevents the publish lane from drifting back to globbing, repackaging, or partial-success publication, and preserves reusable diagnostics plus a local contract-verifier proof surface for future edits. | Yes | agent |
| D088 | M034/S04/T02 | release-verification | How the extension prepublish verifier should hand off the exact VSIX artifact to downstream workflows | Record both intended and verified VSIX paths as repo-root-relative files under `.tmp/m034-s04/verify/` and have downstream workflow steps consume that recorded path instead of recomputing or globbing. | M034/S04 needs one deterministic artifact handoff surface across local verification, reusable workflow proof, and publish jobs. Repo-root-relative path files keep the verified VSIX consumable from the repository root, avoid path drift between jobs, and prevent the publish lane from silently selecting a different file. | Yes | agent |
| D089 | M034/S04/T02 | verification | How the shared `e2e_lsp` prerequisite should locate backend symbols for the extension release gate | Make `compiler/meshc/tests/e2e_lsp.rs` derive hover, definition, and signature-help positions from `jobs_source` text at runtime instead of fixed line numbers. | The extension release verifier reuses `cargo test -q -p meshc --test e2e_lsp -- --nocapture` as an upstream proof. Hardcoded source positions turned unrelated edits in `reference-backend/api/jobs.mpl` into false release-gate regressions. Deriving positions from source text keeps the prerequisite stable while still proving the same real JSON-RPC behaviors. | Yes | agent |
| D090 | M034/S05 planning | release-verification | Canonical acceptance surface and candidate identity for the M034 public release assembly proof | Use `scripts/verify-m034-s05.sh` as the single S05 acceptance entrypoint, implemented as a serial wrapper over the existing S01-S04 proof scripts plus new deploy-workflow, exact deployed-content, and hosted-run checks. Treat the public release candidate as paired tags `v<Cargo version>` for binaries and `ext-v<extension version>` for the VS Code extension rather than inventing one unified version. | S05 is an assembly slice, not a new subsystem. Reusing the existing slice-owned verifiers keeps each proof surface honest, localizes failures to the first broken phase, and avoids duplicating live registry or extension assertions in yet another harness. The repo already enforces Cargo-aligned binary tags while the extension is independently versioned, so S05 must compose those two release identities explicitly instead of claiming a false single-tag story. | Yes | agent |
| D091 | M034/S05/T03 | verification | How the canonical S05 wrapper proves public HTTP release truth before live publish/install | Run a dedicated public-http phase before S01 that checks exact installer body diffs, normalized docs text markers, packages-site body markers, registry scoped-search JSON markers, and content-type headers while persisting per-URL body/header artifacts under .tmp/m034-s05/verify/. | This keeps the assembled release proof fail-closed on stale public deployment, avoids homepage-only or slug-only checks, and prevents the expensive S01 live publish/install proof from running when the public release contract is already red. During T03 it caught the current stale meshlang.dev install.sh asset exactly where the public surface drifted. | Yes | agent |
| D092 | M034/S05/T04 | verification | How the S05 verifier should surface hosted rollout gaps without losing downstream evidence | Run candidate-tag derivation and remote-evidence before public-http and S01 live proof, and persist .tmp/m034-s05/verify/candidate-tags.json plus remote-runs.json on every assembled run. | Hosted workflow rollout drift is external to the local repo. Placing the remote-evidence phase earlier keeps the verifier fail-closed on real GitHub gaps while still producing auditable candidate-tag and hosted-run artifacts for slice closeout and roadmap reassessment. | Yes | agent |
| D093 | M034/S06 planning | verification | How S06 captures hosted rollout proof without losing the first green evidence bundle. | Add a remote-evidence-only/archive operator path around `scripts/verify-m034-s05.sh` and preserve hosted rollout bundles under deterministic labels (`preflight`, `main`, `v0.1.0`, `first-green`), executing rollout in the order default branch -> binary tag -> extension tag before rerunning the full S05 assembly. | `scripts/verify-m034-s05.sh` currently deletes `.tmp/m034-s05/verify/` on every run and continues into later public phases after `remote-evidence`. S06 needs repeated hosted polling plus immediate preservation of the first all-green remote bundle, so the slice should archive labeled snapshots of the hosted-evidence state and delay the expensive full S05 replay until after remote proof is safely captured. | Yes | agent |
| D094 | M034/S06/T04 | release-process | How to handle the binary candidate tag while remote main is still missing the rollout commit | Do not create or push `v0.1.0` until remote `main` contains the rollout workflow graph; archive the blocked `v0.1.0` evidence bundle instead. | Remote `main` is still on `5ddf3b2dce17abe08e1188d9b46e575d83525b50`, `authoritative-verification.yml` is still missing on the remote default branch, the `v0.1.0` tag ref is absent, and there are no `release.yml` or `deploy-services.yml` push runs on `v0.1.0`. Tagging any other remote state would fabricate hosted rollout proof and mislead T05/S05 consumption. | Yes | agent |
| D095 | M034/S06/T05 | verification | How the S05 remote-evidence verifier should observe reusable extension proof runs | Derive `extension-release-proof.yml` evidence from the `publish-extension.yml` caller run on the same tag and require the proof job to be present/successful, instead of querying the reusable workflow as a standalone push workflow. | `tools/editors/vscode-mesh/package.json` still yields `ext-v0.3.0`, `.github/workflows/extension-release-proof.yml` is still `workflow_call`-only, and `gh workflow list` / `gh run list --workflow extension-release-proof.yml` do not expose it as an active top-level push workflow. Using the caller run keeps remote-evidence truthful without inventing a non-existent standalone workflow surface. | Yes | agent |
| D096 | M034/S07 planning | release-verification | How S07 should eliminate the green-but-stale split between hosted deploy checks and the final public replay | Keep `scripts/verify-m034-s05.sh` as the canonical S07 acceptance entrypoint, but factor the public installer/docs/packages markers and bounded freshness-wait semantics into one shared contract consumed by the S05 verifier and the hosted deploy workflows. | S06 and the S07 research showed that the repo can have green deploy runs while `meshlang.dev` still serves stale installer/docs content because the hosted workflows validate a weaker contract than `public-http`. Sharing the exact markers and freshness budget keeps CI/CD proving the same public surface the final assembly replay requires, while bounded wait semantics account for GitHub Pages propagation without hiding permanent drift. | Yes | agent |
| D097 | M034/S07 | release-process | How to recover blocked remote rollout when large HTTPS pushes time out | Advance the remote branch in bounded fast-forward prefixes and preserve reserved evidence labels until the truthful final rollout state exists | The host's HTTPS receive-pack path times out on the full rollout upload with HTTP 408, but staged fast-forward prefixes succeeded through 63 missing commits and moved remote main to 8d3e76a65986e7bd5e00d107f9e11c1923ecd0ab without rewriting history. Preserving the reserved main/first-green labels avoids fabricating hosted proof before the remote graph and candidate tags are genuinely current. | Yes | agent |
| D098 | M034/S08/T03 | deployment | How the packages-website Docker image produces runtime dependencies for Fly deploys | Build with `npm ci`, run `npm run build`, then `npm prune --omit=dev` in the builder stage and copy the pruned `node_modules` into the runtime stage instead of running `npm install --omit=dev --ignore-scripts` in runtime | The hosted `deploy-services.yml` failure was caused by the runtime-stage install re-resolving the Svelte/Vite peer tree and hitting an `ERESOLVE` conflict. Pruning the already-built lockfile-resolved tree keeps the image truthful, avoids a second dependency resolution path, and preserves the existing workflow contract. | Yes | agent |
| D099 | M034/S08/T04 | ci | How the release asset smoke job should satisfy meshc's runtime-library dependency and generate staged checksums across platforms | In `release.yml`, the `verify-release-assets` job installs Rust, builds `mesh-rt` locally before running `scripts/verify-m034-s03.{sh,ps1}`, uses Python `hashlib` for Unix `SHA256SUMS`, and keeps Windows archive selection in valid per-variable PowerShell bindings instead of publishing or downloading an extra runtime-library release artifact. | The staged smoke only consumes meshc/meshpkg archives, but installed `meshc` auto-detects `libmesh_rt.a` from the local Cargo target tree. Building `mesh-rt` on the verifier job satisfies the real runtime dependency without changing the public release bundle. Using Python for Unix hashing removes the macOS `sha256sum` assumption, and the explicit Windows bindings avoid the broken `Select-Object -First 1,` syntax that previously made the hosted tag lane red. | Yes | agent |
| D100 | M034/S09 planning | release-verification | How S09 should treat hosted-workflow freshness in the assembled release verifier | Require `scripts/verify-m034-s05.sh` remote-evidence to compare each hosted run's `headSha` against the current required ref SHA for `main`, `v0.1.0`, and `ext-v0.3.0`, and fail closed on mismatch instead of trusting branch/tag-name matches alone. | S09's remaining trust gap is not missing local proof surfaces; it is stale hosted runs still satisfying branch/tag checks after the repo-side fixes landed. Enforcing ref-SHA freshness in remote-evidence turns rollout freshness from operator folklore into a mechanical contract and directly supports R045's requirement to prove shipped release/deploy surfaces rather than infer them from artifact presence. | Yes | agent |
| D101 | M034/S09/T03 | release-rollout | Rollout SHA used for hosted S09 ref retarget | Use the GitHub-created commit c443270a8fe17419e9ca99b4755b90f3cb7af3a0 as the approved rollout target instead of the local synthetic commit 05ab52f6353c0b44824f66edff0f41da1d625b9b. | T03 had to avoid git push, so the rollout used the GitHub Git data API. That API reproduced the same parent, tree, and message as the local synthetic commit but normalized the author/committer timestamp to UTC, which changed the commit hash. The user explicitly approved retargeting main, v0.1.0, and ext-v0.3.0 to the GitHub-created equivalent SHA. | Yes | collaborative |
| D102 | M034/S09/T04 | release-workflows | How hosted extension rerolls should handle already-published `ext-v*` versions | Enable `skipDuplicate: true` on both `publish-extension.yml` publish steps and enforce that contract in `scripts/verify-m034-s04-workflows.sh`, while keeping the exact proof-artifact handoff checks unchanged. | The hosted reroll blocker was a duplicate Open VSX publish on the already-verified VSIX. Using the action's documented duplicate-safe mode makes reruns idempotent without masking missing-artifact or wrong-VSIX failures, because the workflow still verifies the proof output path before either publish step runs. | Yes | agent |
| D103 | M034/S09/T05 | release-rollout | How S09 repaired rerolls should be composed after an earlier rollout already moved the remote refs | Fast-forward from the live remote rollout SHA and build a minimal repaired target containing only `.github/workflows/publish-extension.yml`, `scripts/verify-m034-s01.sh`, and `scripts/verify-m034-s03.ps1`, instead of recomputing from the older pre-T03 `origin/main` state. | After T03 had already moved `main`, `v0.1.0`, and `ext-v0.3.0` to `c443270a8fe17419e9ca99b4755b90f3cb7af3a0`, the remaining blockers were the duplicate-safe extension reroll and two hosted proof failures. Recomputing from the older pre-rollout branch state would have reopened already-retired lanes and obscured which changes actually repaired the red hosted workflows. Fast-forwarding from the live remote state isolated the reroll to the real blockers and produced the repaired target `8e6d49dacc4f4cd64824b032078ae45aabfe9635`. | Yes | agent |
| D104 | M034 S10 T01 | architecture | Registry package latest-version source of truth | Recompute packages.latest_version from committed versions rows under a per-package row lock after each publish, and make metadata/search fail closed when the latest-version join is missing. | Last-writer-wins updates on packages.latest_version allowed overlapping publishes to move package-level latest backward even while the newer version row already existed. Re-deriving latest from committed version data removes the race, preserves description updates, and keeps named-install, package metadata, and search/list surfaces aligned on one truthful latest pointer. | Yes | agent |
| D105 | M034/S10/T02 | compiler | How mesh-codegen should link the Mesh runtime on Windows MSVC versus Unix hosts | Use target-aware linker selection and pass the resolved runtime static library path directly to the linker; keep `cc` + `libmesh_rt.a` on Unix and use `clang.exe`/`clang` with `mesh_rt.lib` on Windows MSVC. | Cargo emits different static-library names across target families, so the old Unix-only `cc -lmesh_rt` flow could not find or describe the Windows MSVC runtime correctly. Passing the resolved runtime file path avoids platform-specific `-l` naming assumptions, keeps Unix behavior intact, and makes Windows failures name the expected artifact and linker driver explicitly. | Yes | agent |
| D106 | M034/S10/T03 | verification | Hosted blocker refresh strategy on an already-correct rollout SHA | Prefer `gh run rerun --failed` on the existing workflow run when the lane already has a completed run on the correct `headSha`; only dispatch a new run if no correct-head run exists. | S10/T03 needed fresh hosted evidence without widening mutation surface. Reusing the existing rollout-SHA runs preserved a one-to-one link between blocker evidence and the already-approved rollout target, avoided creating parallel runs on different refs, and kept the refreshed authoritative/release verdict attributable to the same hosted state. | Yes | agent |
| D107 | M034/S11 | verification | How M034/S11 closeout treats missing first-green and hosted Windows release-smoke failures | Fail closed at remote-evidence, require the first-green archive before any full S05 replay, and treat the uploaded Windows diagnostics artifact as the authoritative blocker surface | The latest approved-tag reroll left five hosted lanes green on their expected refs while release.yml stayed red on v0.1.0. Top-level workflow summaries did not expose the actionable cause, but the uploaded release-smoke-x86_64-pc-windows-msvc-diagnostics artifact recorded exit_code -1073741819 during the installed meshc.exe smoke build in 07-hello-build.log. Stopping before the full replay avoids stale closeout evidence and keeps the remaining blocker tied to the only trustworthy diagnostic surface. | Yes | agent |
| D108 | M034/S12 planning | verification | How S12 should retire the remaining Windows release-smoke blocker and close M034 | Use a four-task diagnosis-first sequence: build a truthful local installed-Windows regression and diagnostic seam, repair the installed compiler/verifier handshake locally, rerun hosted `release.yml` on the approved `v0.1.0` ref only after that local proof is green and user-approved, then capture `first-green` exactly once and run the final full S05 replay. | S11 already proved the blocker is the hosted Windows installed-build path, not repo-local workflow drift. Putting the hosted rerun behind a local installed-build regression keeps R007 tied to evidence, avoids spending `first-green` on stale assumptions, and preserves a deterministic failure surface if the hosted lane is still red. | Yes | agent |
| D109 | M034/S12/T01 | observability | How the Windows staged smoke verifier classifies installed meshc build failures | Use an env-driven compiler trace file (`MESH_BUILD_TRACE_PATH`) plus verifier-side summary classification instead of inferring failure phases from process exit codes alone. | The hosted Windows smoke failure currently collapses into an access-violation exit code with empty stdout/stderr. A trace file written by the real compiler pipeline preserves object-emission, runtime-discovery, and linker-invocation progress without replacing the installed build path with a weaker synthetic helper. The PowerShell verifier can then classify failures truthfully as pre-object, runtime-lookup, or link-time from persisted evidence. | Yes | agent |
| D110 | M034/S12/T02 | compiler | How the installed compiler should handle runtime and Windows toolchain prerequisites before linking | Preflight link planning before object emission and allow an explicit `MESH_RT_LIB_PATH` override, while the Windows staged verifier exports `CARGO_TARGET_DIR` to the repo target tree. | The hosted Windows smoke blocker was opaque because prerequisite discovery happened too late and depended on installed-binary auto-discovery. Moving runtime/linker resolution ahead of LLVM object emission turns missing runtime or bad `LLVM_SYS_211_PREFIX` inputs into deterministic Rust errors, and exporting `CARGO_TARGET_DIR` keeps the verifier honest about where `mesh-rt` was built. | Yes | agent |
| D111 | M034/S12/T04 | observability | Reserved-label semantics for scripts/verify-m034-s06-remote-evidence.sh | Allow red stop-after remote-evidence bundles to archive under diagnostic labels, but require the reserved `first-green` label to refuse archiving unless the stop-after verifier exited 0 with `status.txt=ok` and `current-phase.txt=stopped-after-remote-evidence`. | Closeout still needs red hosted bundles for diagnosis, but the once-only `first-green` archive is milestone truth and cannot be spent on a failing hosted release lane. Separating diagnostic labels from the reserved label preserves debuggability without corrupting the final evidence contract. | Yes | agent |
| D112 | M038 | build | How to fix the Windows MSVC meshc.exe access violation caused by duplicate symbol resolution | Remove `/FORCE:MULTIPLE` and eliminate the vcpkg libxml2 install, since the LLVM 21 prebuilt tarball is self-contained. If LLVM still needs external libxml2, provide it from the tarball's own lib directory rather than an independently-built vcpkg copy. | The `/FORCE:MULTIPLE` flag lets the MSVC linker arbitrarily pick among duplicate symbol definitions, which causes LLVM functions to resolve to wrong copies and crash at runtime. The duplicate symbols most likely come from both LLVM's bundled XML2 and the separately-installed vcpkg libxml2. Eliminating the duplicate source is the clean fix; using a more targeted workaround flag is the fallback. | Yes | agent |
| D113 | M038/S01 | ci/release | How to handle LLVM 21 rpmalloc/ucrt heap symbol collision on Windows MSVC | Strip rpmalloc objects from LLVMSupport.lib via llvm-ar extract/rebuild and provide a no-op rpmalloc_linker_reference stub, rather than using /FORCE:MULTIPLE | /FORCE:MULTIPLE silently lets the linker pick between conflicting malloc implementations, which could cause subtle heap corruption. Removing the rpmalloc objects entirely lets ucrt.lib's heap be the only allocator, which is correct for embedding LLVM in a non-LLVM-tool binary. | Yes | agent |
| D114 | M038/S01 | codegen/link | How the installed meshc.exe should link Mesh programs on Windows MSVC | Pass Windows system libraries (ws2_32, userenv, advapi32, bcrypt, ntdll, kernel32, msvcrt, synchronization) via -Wl, prefix to clang.exe for MSVC targets | mesh_rt.lib is a Rust staticlib whose transitive deps need Windows system libraries at final link time. The -Wl, prefix forwards them to link.exe which resolves them from the system library path, while bare .lib names are misinterpreted as file paths by the clang driver. | Yes | agent |
| D115 | M036 planning | delivery | Neovim delivery shape for first-class non-VSCode support | M036 will deliver Neovim as a repo-owned runtime/support pack under `tools/editors/`, backed by install docs and smoke/regression checks, without requiring broader ecosystem distribution work in this milestone. | This is the smallest honest path that satisfies first-class support for a non-VSCode editor while keeping the milestone focused on usable proof instead of package-manager or ecosystem sprawl. | Yes | collaborative |
| D116 | M036/S01 planning | tooling | Shared editor/docs syntax proof location for M036 S01 | Use a repo-owned corpus manifest plus a Node parity test under `website/scripts/tests/` that loads the single shared TextMate grammar into both TextMate and Shiki, wrapped by `scripts/verify-m036-s01.sh`. | This is the smallest honest proof path for the already-shipping VS Code/docs surface: it exercises real Mesh files, pinpoints regressions by engine and corpus case, and avoids duplicating editor-specific grammar copies. | Yes | agent |
| D117 | M036/S01/T02 | editor-grammar | How the shared Mesh TextMate grammar should model string interpolation across VS Code and docs | Define one shared interpolation repository rule for both `#{...}` and `${...}` and include a recursive nested-brace pattern before `source.mesh` so double- and triple-quoted strings share identical interpolation scopes. | The parity verifier exercises the exact same grammar file through TextMate and Shiki. Reusing one interpolation rule keeps both surfaces on a single proof contract, and the recursive brace matcher prevents nested map/object literals inside interpolation expressions from consuming the outer closing brace and dropping the end scope. | Yes | agent |
| D118 | M036/S02 planning | tooling | Native Neovim implementation shape for M036 S02 | Implement first-class Neovim support as a repo-owned native runtime pack under `tools/editors/neovim-mesh/`, using classic Vim syntax plus Neovim 0.11 `lsp/mesh.lua` and pack bootstrap, with headless proof that reuses the S01 syntax corpus and existing `meshc lsp` transport truth. | This is the smallest honest delivery that gives Mesh an installable Neovim path the repo owns end-to-end. It avoids the scope multiplier of Tree-sitter and external plugin-manager requirements, preserves the audited S01 interpolation contract, matches the compiler's current `main.mpl` root-truth, and keeps repo-local dogfooding at least as good as the existing VS Code path. | Yes | agent |
| D119 | M036/S02/T02 | editor-support | How the Neovim pack should resolve meshc for rooted projects versus standalone single-file buffers | Search in this order: explicit override, ancestor target/debug and target/release paths from the resolved workspace root, current buffer path, and current cwd, then well-known install paths, then PATH; keep LSP root selection honest with main.mpl then .git and allow standalone buffers to attach with nil root_dir. | Rooting the Mesh LSP at the nearest main.mpl keeps Neovim aligned with the server's project-analysis truth, but standalone buffers outside any Mesh project still need a truthful dogfooding path to the repo-local target/debug/meshc when Neovim is launched from the repository. Including cwd ancestry in discovery preserves that repo-local workflow without lying about the workspace root or overclaiming cross-file support. | Yes | agent |
| D120 | M036/S02/T03 | verification | How the Neovim support pack should consume the shared interpolation corpus | Materialize every S01 syntax corpus case into per-case temporary `.mpl` files and make the final Neovim proof read only that generated manifest through one consolidated `tools/editors/neovim-mesh/tests/smoke.lua` runner. | The shared corpus mixes real `.mpl` sources with docs-backed markdown line ranges. Opening markdown paths directly in Neovim would produce false filetype/syntax results and make failures opaque. Per-case materialization keeps case ids and expected forms stable, localizes malformed-doc failures to the corpus phase, and lets one headless smoke runner prove both syntax and LSP through the repo-owned `pack/*/start/mesh-nvim` install path. | Yes | agent |
| D121 | M036/S03 planning | editor-support | Meaning of first-class versus best-effort editor support in public Mesh docs | Public editor support tiers for M036 will be operational: first-class means Mesh ships a repo-owned install path/docs plus a repo-owned verifier in this repository, while best-effort means generic LSP/TextMate reuse may work but Mesh does not ship editor-specific smoke proof for that editor in M036. | This keeps support promises tied to auditable proof instead of rhetoric, lets S03 publish a truthful contract backed by the existing VS Code packaging/shared-grammar proofs plus the Neovim pack and new VS Code smoke, and prevents accidental overclaiming for editors like Emacs, Helix, Zed, or Sublime. | Yes | agent |
| D122 | M036 S03 T02 | editor-support | How the VS Code extension should treat explicit mesh.lsp.path overrides during smoke verification | Treat any non-default mesh.lsp.path as authoritative, surface startup failure against that path, and expose the resolved path/source through extension activation exports for the host smoke. | The S03 proof must not go green through PATH or workspace-root fallback when the published workflow says the extension is pinned to an explicit repo-local meshc. Fail-closed override handling plus exported resolution metadata makes wrong-path drift attributable in the real Extension Development Host smoke. | Yes | agent |
| D123 | M036 S03 T03 | observability | How the S03 repo-root verifier determines downstream proof phases really completed | Treat the replayed upstream artifact markers as part of the acceptance contract instead of trusting command exit code alone | The VSIX proof, VS Code smoke, and Neovim replay each already emit concrete artifact directories or pass markers. Requiring those files and markers after the command exits keeps the wrapper fail-closed on partial success and gives future agents a concrete place to inspect failures by phase. | Yes | agent |
| D124 | M039 bootstrap | operability | What operator surface should define the distributed proof path | Deliver the distributed proof through one Docker image with a small env-driven operator contract for local and Fly runs. | The user wants to be able to run a Docker image and have clustering happen automatically from there. One image plus a small env surface keeps the runtime claim tied to a usable operator path rather than bespoke deployment steps. | Yes | collaborative |
| D125 | M039 bootstrap | architecture | What discovery model should anchor the distributed runtime proof path | Introduce a general discovery seam and use DNS-based discovery as the first canonical provider; treat Fly only as one proof environment. | The user wants general architecture rather than Fly-specific clustering. DNS is broad enough to work locally and on Fly without turning a control-plane API into the architecture, while still leaving room for later discovery adapters. | Yes | collaborative |
| D126 | M039 bootstrap | proof | What counts as honest balancing proof for the distributed milestone chain | Require runtime-native internal balancing and reject external orchestrators, proxies, or durable stores as the real balancing path. | The user explicitly rejected front-door-only spread and external coordination as the real mechanism. The proof app must show that Mesh itself moves work across nodes. | Yes | human |
| D127 | M039 bootstrap | operability | What operator surface should define the distributed proof path | Deliver the distributed proof through one Docker image with a small env-driven operator contract for local and Fly runs. | The user wants to be able to run a Docker image and have clustering happen automatically from there. One image plus a small env surface keeps the runtime claim tied to a usable operator path rather than bespoke deployment steps. | Yes | collaborative |
| D128 | M039/S01 planning | architecture | How discovery candidates and node identity should relate in the M039 cluster proof path | Use DNS discovery to return candidate socket addresses for initial connect attempts, but keep each node’s advertised `name@host:port` as the stable membership identity. For the local S01 proof, use dual-stack `localhost` discovery with distinct advertised IPv4/IPv6 hosts; on Fly, advertise each machine’s private IP while seeding discovery from app-level internal DNS. | Peer gossip and reconnect already reuse advertised node names as future dial targets. If discovery promoted a shared seed hostname or synthetic name into stable identity, membership truth and later rejoin behavior would be dishonest. Candidate-address connect plus stable per-node advertised identity preserves a general DNS-first architecture, keeps Fly compatible with private-IP advertisement, and gives S01 a truthful local replay without forcing SRV records or container-only proof. | Yes | collaborative |
| D129 | M039/S01/T01 | architecture | How runtime DNS discovery should relate dial targets to canonical node identity | DNS discovery in mesh-rt resolves seed addresses into temporary `discovery@host:port` dial targets, but remote membership is only accepted from the handshake-advertised node name after node-name validation succeeds. | The discovery seed is a bootstrap address set, not a stable identity source. Reusing `mesh_node_connect` keeps transport logic centralized, while validating the handshake name prevents malformed or shared bootstrap names from polluting session truth. | Yes | agent |
| D130 | M039/S01 | verification | What is the authoritative local acceptance surface for M039/S01 | Use `scripts/verify-m039-s01.sh` as the canonical local replay wrapper, and fail closed when a named Cargo filter does not report a non-zero `running N test` count. | The slice closes only if discovery convergence and membership shrinkage proofs actually run. Enforcing non-zero test counts plus preserving per-node stdout/stderr logs and phase logs prevents false-green replays when a filter drifts or a runtime proof stalls before emitting useful evidence. | Yes | agent |
| D131 | M039/S01 | architecture | How the M039/S01 proof app should derive cluster membership | Use a new `cluster-proof/` app and derive membership from `Node.self()` plus `Node.list()`, explicitly including self, instead of discovery candidates or registry guesses. | S01 needed a truthful proof surface for automatic discovery. Discovery candidates are only bootstrap inputs; the live membership contract had to come from runtime session state so the endpoint reflects real peers and still reports a correct self-only cluster after node loss. | Yes | agent |
| D132 | M039/S02 planning | architecture | What routing mechanism should the M039/S02 proof app use to prove internal balancing honestly | Use a read-only `cluster-proof` work endpoint that routes each request through direct `Node.spawn` to a deterministic non-self peer when one exists, falls back locally when none exists, and keeps `/membership` as the separate truthful diagnostics surface. | The milestone needs app-level evidence that Mesh moved work internally across the cluster, not a wider service-lifecycle design. Direct per-request `Node.spawn` maps cleanly onto the runtime claim already advertised in docs, keeps the slice narrow, avoids dragging global worker registration or rejoin semantics forward from S03, and makes the local proof crisp because the harness can hit a specific node port and still assert that another node executed the work. | Yes | agent |
| D133 | M039/S02 | verification | What is the authoritative local acceptance surface for M039/S02 | Use `scripts/verify-m039-s02.sh` as the canonical local replay wrapper, require the full S01 verifier as a prerequisite, fail closed on zero-test named filters, and preserve copied per-node routing artifacts under `.tmp/m039-s02/verify/`. | S02 should not look green if discovery and membership already drifted. Replaying S01 first keeps the routing proof grounded on a healthy cluster contract, while copied `node-*-work.json` and stdout/stderr bundles make ingress-vs-execution evidence inspectable after the run instead of burying it in transient temp directories. | Yes | agent |
| D134 | M039/S02 | architecture | How the M039/S02 proof app should prove internal work routing within current Mesh runtime limits | Use a direct spawn-and-return `/work` path that reports ingress/target/execution immediately and treat the execution-node log as the second proof signal instead of waiting for a coordinator/result-registry reply inside the HTTP path. | T01/T02 reproduced two real constraints: ordinary HTTP handlers are not actor contexts, and distributed spawn/send payloads still move as raw scalar bytes. The smaller direct spawn-and-return path keeps the slice honest, preserves `/membership` as the separate truth surface, and still proves that Mesh moved work to another node because the HTTP body and the peer log agree on target/execution identity. | Yes | agent |
| D135 | M039/S02 | requirement | R047 | validated | `bash scripts/verify-m039-s02.sh` passed after replaying S01 first, and the preserved `.tmp/m039-s02/verify/` artifacts show both halves of the proof contract: `/work` returns distinct `ingress_node`, `target_node`, and `execution_node` with `routed_remotely=true` on the two-node path and truthful self-only fallback on the one-node path, while the execution-node stdout log records the matching `request_id` execution line. | Yes | agent |
| D136 | M039/S03 planning | verification | What should count as the authoritative local proof shape for M039/S03 continuity | Treat S03 as a harness-first continuity slice: stabilize `cluster-proof` request correlation and restart-safe artifacts, add named `e2e_m039_s03` degrade/rejoin tests, and make `scripts/verify-m039-s03.sh` replay S01/S02 before fail-closing on phase-specific evidence. | S01 and S02 already prove truthful membership and routing surfaces. The main S03 risk is dishonest or clobbered evidence during node loss and rejoin, not missing product architecture. A harness-first plan keeps scope narrow, forces truthful pre-loss/degraded/post-rejoin evidence, and only justifies runtime changes if the live proof exposes a real reconnect defect. | Yes | agent |
| D137 | M039/S03/T01 | observability | How cluster-proof S03 correlates repeated /work requests across peer loss and same-identity rejoin | Keep the monotonic request_id generator on the ingress node, log correlation in an ingress-side `work dispatched ...` line, and treat run-numbered peer logs plus per-phase `*-work.json` artifacts as the trustworthy remote-execution evidence. | During M039/S03 proofing, cross-node actor arguments were not a stable correlation channel through rejoin: `Node.spawn(..., request_token)` reused `work-0` on the restarted peer, and `Node.spawn(..., request_id :: String)` crashed the peer in mesh-rt string handling. Ingress-side dispatch logging preserved truthful request correlation without widening the HTTP contract or relying on broken transport behavior. | Yes | agent |
| D138 | M039/S03 | requirement | R048 | validated | `bash scripts/verify-m039-s03.sh` now passes after replaying cluster-proof tests/build plus the S01 and S02 verifiers, and the preserved `.tmp/m039-s03/verify/` evidence shows the full continuity contract: pre-loss membership includes both nodes, degraded membership shrinks truthfully to self-only, degraded `/work` falls back locally with `work-1`, post-rejoin membership on both nodes includes the peer again, and post-rejoin `/work` routes remotely again with `work-2` without manual repair. | Yes | agent |
| D139 | M039/S03 | requirement | R048 | validated | `bash scripts/verify-m039-s03.sh` passed after replaying cluster-proof tests/build plus the S01 and S02 verifiers, and the preserved `.tmp/m039-s03/verify/` evidence shows the full continuity contract: pre-loss membership includes both nodes, degraded membership shrinks truthfully to self-only, degraded `/work` falls back locally with `work-1`, post-rejoin membership on both nodes includes the peer again, and post-rejoin `/work` routes remotely again with `work-2` without manual repair. | Yes | agent |
| D140 | M039/S04 planning | proof | How the S04 operator and documentation proof surfaces should be structured | Keep three distinct proof surfaces: `scripts/verify-m039-s04.sh` as the authoritative local Docker gate, `scripts/verify-m039-s04-fly.sh` as a read-only verifier against an existing Fly app, and `scripts/verify-m039-s04-proof-surface.sh` plus a dedicated `/docs/distributed-proof/` page as the docs-truth surface. | Local packaging/runtime drift, live Fly drift, and public-docs drift are different failure classes. Splitting them keeps verification honest, respects the no-unapproved-Fly-mutation rule, and lets the generic distributed guide stay focused on primitives while operator claims point at concrete runbooks and commands. | Yes | agent |
| D141 | M039/S04 planning | operability | Where the S04 local container identity fallback should live for the one-image cluster proof path | Derive `CLUSTER_PROOF_NODE_BASENAME` and `CLUSTER_PROOF_ADVERTISE_HOST` from the container `HOSTNAME` in the image entrypoint only when explicit local identity and Fly identity are both absent, rather than teaching `cluster-proof` or `mesh-rt` to trust hostnames directly. | S04 needs a quiet local Docker operator path without widening the Mesh runtime contract or making non-container hostnames implicitly authoritative. Keeping the fallback in the image wrapper preserves the existing app/Fly config seam, keeps precedence clear, and avoids turning a container convenience into general runtime behavior. | Yes | agent |
| D142 | M039/S04/T03 | observability | How the S04 Fly operator path establishes discovery and validates the target app identity. | Pin `MESH_DISCOVERY_SEED` in `cluster-proof/fly.toml` to `mesh-cluster-proof.internal` and make the read-only Fly verifier require `CLUSTER_PROOF_BASE_URL` to match `https://mesh-cluster-proof.fly.dev`. | `cluster-proof` cluster mode requires a discovery seed in addition to the Fly-injected identity env, and the repo already hardcodes `app = "mesh-cluster-proof"` in `fly.toml`. Pinning the `.internal` seed there makes the deploy contract real without adding a mutating verifier step, while strict base-URL/app matching keeps the read-only proof fail-closed on operator drift instead of probing the wrong service. | Yes | agent |
| D143 | M039/S04/T04 | documentation | Where distributed operator-proof claims live in the public docs | Publish distributed operator claims on a dedicated `Distributed Proof` page backed by `cluster-proof/README.md` and `scripts/verify-m039-s04-proof-surface.sh`, while keeping `Distributed Actors` as the primitive/tutorial guide. | The generic distributed guide teaches `Node.start`/`Node.connect` primitives, but those examples are not the operator-proof surface. A dedicated proof page plus a fail-closed verifier keeps README/docs/sidebar links pointing at the exact repo-root build, local verifier, Fly verifier, and runbook instead of implying tutorial examples prove production/operator readiness. | Yes | agent |
| D144 | M040 planning | verification | How M040 will divide authoritative failover proof between local Docker and Fly | Treat local Docker as the authoritative destructive failover proof surface, while keeping Fly on the same keyed contract and operator rail with truthful steady-state/read-only verification unless a later roadmap adjustment explicitly adds mutating Fly lifecycle proof. | The existing Fly verifier is intentionally read-only, whereas local Docker already owns destructive lifecycle evidence. Preserving that split keeps the milestone honest about real coverage while avoiding an implicit expansion of live-environment blast radius during the first continuity wave. | Yes | collaborative |
| D145 | M040/S01 planning | api-contract | How the first keyed continuity slice will shape the public `cluster-proof` request/status surface | Keep the existing `/work` rail as the namespace: add keyed submit plus keyed status semantics that separate stable `request_key` identity from per-attempt identity, preserve truthful owner/replica fields as real assignments or explicit placeholders, and fail-close conflicting reuse of the same request key. | S02 and S03 need a stable public contract before replica durability and operator proof surfaces can expand. Reusing the existing rail avoids a throwaway endpoint split, while separating request identity from attempt identity prevents duplicate-completion ambiguity and gives later failover work a durable status model to extend honestly. | Yes | agent |
| D146 | M040/S01 | implementation | How cluster-proof keyed submit maintains a buildable state transition path | Keep the keyed submit runtime split into explicit decision and next-state helpers instead of relying on the earlier failing tuple-extraction shape. | T02 recovered cluster-proof buildability by isolating submit_decision and submit_next_state, and the final S01 proof bundle still builds, serves keyed work, and passes e2e without reintroducing the opaque tuple path that previously left the runtime red. | Yes | agent |
| D147 | M040/S02 planning | architecture | How S02 will realize replica-backed continuity without widening Mesh into a generic distributed-state subsystem | Keep S02 continuity app-owned inside `cluster-proof/work.mpl` as mirrored owner/replica request records discovered through the existing global registry, and handle owner-loss recovery by rolling the active `attempt_id` forward from surviving replicated state rather than introducing checkpoint replay or a generic cluster-wide state platform. | This is the narrowest honest path that satisfies R049/R050 while respecting R057-R060: it preserves the existing `/work` rail, keeps internal node truth explicit, allows fail-closed admission when no replica survives, and makes same-key convergence after owner loss a restart-by-key story instead of fake process migration. | Yes | agent |
| D148 | Milestone reorganization after M039/M040 review | distributed-runtime | Where distribution, replication, and continuity logic should live in the Mesh stack | Implement distribution, replication, and continuity as runtime-native capabilities in mesh-rt/Rust, with a simple Mesh-level API, instead of primarily expressing that logic inside Mesh application code like cluster-proof/work.mpl. | The current app-level continuity plan is stressing unstable distributed seams and making core language claims depend on one proof app's Mesh implementation details. The user explicitly wants these capabilities to be first-class parts of the language. Moving the core machinery into mesh-rt keeps the hard distributed logic behind a stable runtime boundary while preserving cluster-proof as the proof surface and example consumer of the API. | Yes | human |
| D149 | Milestone reorganization follow-up after parking M040/M041 | distributed-runtime | How the current Mesh-authored distribution/continuity work should relate to the new runtime-native plan | Treat the existing Mesh-authored keyed continuity behavior as the semantic seed to absorb into first-class language/runtime features, not as throwaway prototype logic. | The user clarified that what has already been created in Mesh should still become part of the language itself. The runtime-native reorganization should therefore preserve and formalize the working submit/status/retry semantics rather than replacing them with a different hidden-only design. | Yes | human |
| D150 | M042 planning | architecture | What role cluster-proof should have during the M042 transition | Keep cluster-proof as the HTTP proof consumer and preserve the validated M039 operator rail, while moving the actual continuity algorithm into mesh-rt and deliberately avoiding a big-bang rewrite of legacy proof surfaces. | M039 is the validated baseline and M042 explicitly supersedes the follow-on app-level direction rather than rewriting history. Preserving the one-image Docker/Fly rail and legacy proof surfaces reduces rollout risk, keeps regressions visible, and prevents the proof app from remaining the real owner of runtime-distribution claims. | Yes | agent |
| D151 | M042 planning | roadmap | How to order M042 slices | Sequence the milestone as healthy-path runtime API first, then fail-closed replica admission, then owner-loss recovery, then proof-app/operator/docs reconciliation. | The highest risk is the ownership boundary itself, so S01 must prove the runtime can own keyed continuity semantics through the real entrypoint. Durability honesty is separable from recovery and needs its own proof surface before owner-loss promotion is trusted. Cleanup and public truth should come last so docs and operator rails describe a real capability rather than a planned one. | Yes | agent |
| D152 | M042 planning | architecture | What role cluster-proof should have during the M042 transition | Keep cluster-proof as the HTTP proof consumer and preserve the validated M039 operator rail, while moving the actual continuity algorithm into mesh-rt and deliberately avoiding a big-bang rewrite of legacy proof surfaces. | M039 is the validated baseline and M042 explicitly supersedes the follow-on app-level direction rather than rewriting history. Preserving the one-image Docker/Fly rail and legacy proof surfaces reduces rollout risk, keeps regressions visible, and prevents the proof app from remaining the real owner of runtime-distribution claims. | Yes | agent |
| D153 | M042/S01 planning | architecture | How the runtime-native continuity capability should be exposed to Mesh code in M042/S01 | Introduce a dedicated Mesh-facing `Continuity` module backed by new `mesh_continuity_*` runtime intrinsics, and move `cluster-proof` to that API instead of extending `Node` or `Global` with keyed-work semantics. | Keyed continuity is a workload-level contract, not a generic node or global-registry primitive. A dedicated `Continuity` API keeps the boundary explicit, lets `cluster-proof` become a thin proof consumer, and isolates future fail-closed durability and owner-loss recovery work from the lower-level clustering/discovery surfaces. | Yes | agent |
| D154 | M042/S01/T01 | architecture | How healthy-path keyed continuity state replicates across cluster nodes | Use a runtime-owned continuity registry in mesh-rt with explicit request records, full-record upsert replication over node sessions, and connect-time continuity snapshots. | This keeps request-key dedupe/conflict and completion ownership inside the runtime, preserves explicit owner/replica fields for later recovery work, and reuses the existing distribution substrate without requiring Mesh app code to maintain a parallel registry protocol. | Yes | agent |
| D155 | M042/S01 closeout recovery | architecture | How cluster-proof should consume canonical placement membership on the M042/S01 continuity path | Recompute canonical membership locally in `cluster-proof/work.mpl` and use `Cluster.canonical_placement(...)` only for owner/replica string assignments. | The healthy-path submit rewrite hit a null list dereference when `work.mpl` read the imported `CanonicalPlacement.membership` field across the module boundary, while the owner/replica string fields remained stable. Recomputing canonical membership locally preserves the routing contract without keeping the submit path on a fragile cross-module list-field seam. | Yes | agent |
| D156 | M042/S02 planning | architecture | How replica-backed durability requirements should cross the Mesh/runtime continuity seam in M042/S02 | Extend the existing `Continuity.submit(...)` API with an explicit replica requirement and make runtime-owned replica prepare/ack-or-reject determine admission truth; remove app-authored `acknowledge_replica(...)` from the live `cluster-proof` submit path. | Only `cluster-proof` consumes the continuity API today, so extending the existing submit call is a smaller and clearer seam than adding a second entrypoint. It keeps the boundary data-oriented, lets the runtime own rejection vs mirrored admission truth, and avoids preserving an optimistic proof-app acknowledgement path that would keep `mirrored` state synthetic. | Yes | agent |
| D157 | M042/S02/T01 | architecture | How to stage the durability-aware continuity submit API across S02 runtime work and later compiler plumbing | Add a new runtime entrypoint `mesh_continuity_submit_with_durability` and keep `mesh_continuity_submit` as a compatibility wrapper that defaults `required_replica_count` to `0` until the compiler/runtime seam is updated. | This lets T01 ship the real runtime-owned replica prepare/ack-or-reject path and disconnect downgrade behavior inside `mesh-rt` without breaking existing compiler-generated call sites mid-slice. T02 can then switch the compiler and Mesh-facing Continuity API to the new durability-aware entrypoint explicitly. | Yes | agent |
| D158 | M042/S02 closeout | verification | What is the authoritative local acceptance surface for M042/S02 replica-backed admission and degradation truth | Use `bash scripts/verify-m042-s02.sh` as the canonical local replay wrapper, require the stable prerequisites (`mesh-rt` continuity tests, `cluster-proof` tests/build, and the standalone M042/S01 regression), and fail closed on the named `e2e_m042_s02` filters plus archived artifact checks instead of reusing the full S01 verifier. | S02 needs its own truthful proof rail because the unrelated remote-owner completion crash still makes the full S01 two-node verifier an invalid acceptance gate. The dedicated wrapper keeps the slice scoped to stable local-owner durability behavior, proves rejected/mirrored/degraded status through preserved `.tmp/m042-s02/verify/` artifacts, and prevents skipped tests or missing evidence from looking green. | Yes | agent |
| D159 | M042/S03 planning | architecture | How owner-loss recovery should cross the runtime/Mesh continuity seam in M042/S03 | Keep the Mesh-facing `Continuity.submit/status/mark_completed` API unchanged for S03 and implement owner-loss recovery as ordinary same-key retry that rolls a new `attempt_id`, with `attempt_id` acting as the cluster-wide fencing token for local completions and replicated merge precedence. | The current continuity API is already sufficient if recovery retry is modeled as a normal submit that returns a new attempt. Treating `attempt_id` as the global fencing token solves both stale local completion and stale replicated terminal-upsert problems without pushing owner-repair logic back into `cluster-proof` or widening the Mesh ABI mid-milestone. | Yes | agent |
| D160 | M042/S03/T02 | architecture | How owner-loss recovery becomes visible and eligible in runtime-native continuity | Mark pending mirrored continuity records with explicit `replica_status=owner_lost` during `handle_node_disconnect(...)`, and let ordinary `Continuity.submit(...)` roll a new attempt only from that runtime-written state instead of adding a Mesh-facing repair API. | This keeps node-loss detection in `node.rs`, keeps recovery on the existing submit/status contract, gives operators an explicit status/log signal before retry, and lets stale rejoin sync stay fenced by the newer attempt token plus status precedence. | Yes | agent |
| D161 | M042/S03/T03 | runtime | How cluster-proof should admit same-key recovery submits after runtime-marked owner loss | Let `cluster-proof/work.mpl` downgrade the submit-time `required_replica_count` to `0` only when the existing runtime continuity status for that request key is `phase=submitted`, `result=pending`, and `replica_status=owner_lost`, and log the actual replica count used on the submit path. | The runtime already owns owner-loss truth and recovery rollover semantics. Without consuming that status, the app-level `replica-backed` policy rejects the rolled attempt immediately even though the runtime marked the request recoverable. Using the existing status surface keeps the fix thin and avoids inventing a separate repair API. | Yes | agent |
| D162 |  | requirement | R050 | validated | M042/S02+S03 now prove the two-node replicated continuity bar end to end: runtime-native submit mirrors or rejects truthfully, surviving replicas expose degraded/owner_lost state after node loss, and owner-loss retry/rejoin preserve the newer attempt as authoritative (`bash scripts/verify-m042-s02.sh`, `bash scripts/verify-m042-s03.sh`). | Yes | agent |
| D163 |  | requirement | R050 | active | Supersedes the earlier M042/S03 validation claim. The final verification evidence is not stable yet because `bash scripts/verify-m042-s03.sh` can fail in its S02 prerequisite replay with false `owner_lost` on a healthy mirrored two-node path. Keep R050 active until the runtime session-cleanup / duplicate-connection regression is fixed and the full S02+S03 replay passes cleanly. | Yes | agent |
| D164 | M042/S03 | requirement | R050 | validated | `cargo test -p mesh-rt continuity -- --nocapture`, `cargo run -q -p meshc -- test cluster-proof/tests`, `cargo test -p meshc --test e2e_m042_s03 -- --nocapture`, `bash scripts/verify-m042-s02.sh`, and `bash scripts/verify-m042-s03.sh` now pass together. The retained `.tmp/m042-s03/verify/` bundle shows owner-loss status truth, same-key retry rollover to a newer attempt_id, stale-completion fencing, and same-identity rejoin preserving the newer attempt as authoritative. | Yes | agent |
| D165 | M042/S04 planning | architecture | How S04 should reconcile the operator proof rail and public docs with the runtime-owned continuity boundary | Keep the validated M039 scripts as historical baseline, add M042-specific local and read-only Fly wrapper verifiers on top of them, and document the runtime-owned continuity contract in the distributed proof page plus distributed guide instead of rewriting M039 in place. | S04 is a closeout slice, not a new continuity-semantics slice. Preserving M039 as the baseline avoids rewriting validated history, while M042 wrappers let the packaged one-image rail prove the newer keyed submit/status contract without pretending the read-only Fly lane can prove destructive recovery. Putting `Continuity` in the distributed guide keeps the public API story with the rest of the runtime-distribution primitives instead of burying it in a generic stdlib page. | Yes | agent |
| D166 | M042/S04 recovery | architecture | M042/S04 remote cluster-proof execution seam | Use typed deep-copy remote spawn arguments in mesh-rt/codegen and launch remote work from a local bridge actor instead of calling Node.spawn directly from HTTP handlers | The old path treated remote string arguments like local heap pointers and also depended on actor context that HTTP handlers do not have. The stable repair was to carry per-argument type tags across the remote spawn boundary, deep-copy string values on the runtime side, and make the handler spawn a local actor that performs Node.spawn inside actor context. | Yes | agent |
| D167 | M042/S04 | documentation | Which command set and authority the distributed proof docs should present after M042/S04 | Treat `bash scripts/verify-m042-s03.sh` as the destructive local authority, keep `bash scripts/verify-m042-s04.sh` as the packaged local wrapper, and keep `bash scripts/verify-m042-s04-fly.sh` explicitly read-only in docs/help/verifiers. | S04 finished with both the historical M039 wrapper and the packaged M042 wrapper green, but the public claim still needs a single clear authority boundary. Promoting the S03 verifier as the local destructive continuity authority while keeping the Fly lane read-only matches the actual proof surfaces, preserves the one-image operator rail, and avoids implying that the Fly helper proves owner-loss/recovery behavior. | Yes | agent |
| D168 | M043 planning | planning | M043 slice order | Order slices as runtime replication/role truth first, promotion and fencing second, packaged operator rail third, and public proof/docs truth last. | The highest-risk seam is authority truth under failover, so the roadmap proves that capability through the real continuity path before spending work on packaging or documentation. Packaging and docs are downstream contracts that should be driven by the shipped runtime behavior, not invented ahead of it. | Yes | agent |
| D169 | M043 planning | architecture | First-wave M043 failover authority model | Use explicit operator-triggered standby promotion with runtime-enforced role/epoch fencing; do not infer promotion from simple peer disappearance. | The runtime has no quorum-bearing control plane or external authority, so automatic promotion on disconnect would overclaim safety and permit split-brain or stale-primary return. Operator-triggered promotion keeps the first disaster-continuity contract honest while still delivering full primary-loss survivability when standby replicas already exist. | Yes | collaborative |
| D170 | M043 planning | architecture | Ownership boundary for cross-cluster disaster continuity | Keep replication, authority state, merge precedence, and stale-primary fencing in mesh-rt; keep cluster-proof as a thin consumer/operator surface rather than a DR control plane. | M042 established the runtime-native direction, and pushing disaster logic back into Mesh app code would both regress that architecture and run into known transport/codegen limits. The public proof app should demonstrate the runtime capability, not secretly implement it. | No | collaborative |
| D171 | M043/S01 planning | planning | M043/S01 failover-boundary and status-surface scope | Keep S01 scoped to runtime-owned live primary→standby replication plus operator-visible role, promotion-epoch, and replication-health truth on existing membership/status surfaces; defer explicit promotion action and stale-primary fencing behavior to S02. | The highest-risk seam in S01 is making mirrored standby truth observable without overclaiming failover. Threading authority metadata through the runtime record and existing cluster-proof surfaces now gives S02 the fencing/promotion substrate it needs, while keeping this slice honest about what is and is not proven. | Yes | agent |
| D172 | M043/S01/T01 | architecture | How standby-side mirrored continuity should behave before explicit promotion exists | Project replicated records into the local cluster role/epoch on merge, and when a standby loses its upstream source, degrade replication_health instead of marking replica_status=owner_lost or enabling recovery rollover. | S01 needs truthful mirrored standby state without overclaiming failover. Reusing the single-cluster owner-loss path on standby would imply implicit promotion and make stale/disconnected mirrored truth look locally recoverable. Keeping standby copies as standby-role records with degraded replication health preserves observability while deferring real promotion and fencing to S02. | Yes | agent |
| D173 | M043/S01/T03 | continuity-proof | How M043/S01 proves primary→standby replication truth locally | Use one connected mesh node network with explicit `MESH_CONTINUITY_ROLE`/epoch on each node, and prove operator-visible primary/standby mirrored truth through membership and keyed-status surfaces instead of inventing a second replication transport. | The runtime currently replicates continuity state across established node sessions and exposes role/epoch/health as authority metadata. The destructive S01 rail therefore needs to prove mirrored standby truth, healthy pre-promotion status, and non-executing standby behavior on that real transport seam rather than pretending there is already a separate failover channel. | Yes | agent |
| D174 | M043/S01/T02 | architecture | How cluster-proof declares primary/standby topology for the first cross-cluster continuity slice | Require explicit MESH_CONTINUITY_ROLE and MESH_CONTINUITY_PROMOTION_EPOCH in cluster mode, validated in cluster-proof/config.mpl instead of inheriting the runtime's permissive primary default | S01 needed operator-visible role truth to be fail-closed. Letting cluster-proof silently inherit the runtime's primary default would make a misconfigured standby look authoritative and weaken the later promotion/fencing contract. Explicit config validation keeps the proof app a thin consumer while making topology drift obvious. | Yes | agent |
| D175 | M043/S02 planning | architecture | S02 Mesh-facing failover control and live authority status seam | Expose standby promotion and authority readback through narrow runtime-owned Continuity APIs, with `cluster-proof` consuming them via a single explicit promotion route instead of deriving live role/epoch/health from startup env or implementing Mesh-side DR orchestration. | S02 needs an explicit operator-triggered promotion boundary and truthful post-failover status without restarting the standby or rebuilding authority state in the proof app. Keeping promotion and live authority status in the runtime/compiler seam preserves D169 and D170, gives cluster-proof one thin operator surface, and makes stale-primary fencing observable through the same runtime-owned contract that the verifier exercises. | Yes | agent |
| D176 | M043/S02/T01 | architecture | Where mutable continuity authority lives during standby promotion and stale-primary fencing | Store continuity authority inside the runtime ContinuityRegistry state instead of a separate process-static OnceLock | Promotion must mutate authority without restarting the process or discarding the in-memory mirrored continuity registry. Co-locating authority with the registry lets promotion, merge fencing, and test-local authority changes happen on the same runtime-owned state while preserving mirrored request truth. | Yes | agent |
| D177 | M043/S02/T02 | architecture | Mesh-visible continuity failover API shape for standby promotion work | Expose `Continuity.promote()` and `Continuity.authority_status()` as narrow `Result<String, String>` built-ins backed by `mesh-rt`, with JSON payloads carrying `cluster_role`, `promotion_epoch`, and aggregate `replication_health`. | The existing Continuity Mesh API already returns JSON through `Result<String, String>`, so keeping the new promotion and authority-status calls on the same contract avoids a broader compiler/runtime type surface while still giving `cluster-proof` live runtime-owned authority truth. Aggregating authority health from current continuity records (`unavailable` > `degraded` > `healthy` > `local_only`) makes the operator-facing status reflect real failover risk instead of stale startup env. | Yes | agent |
| D178 | M043/S02/T03 | observability | How cluster-proof should surface promotion and runtime-authority failures after failover | Promotion and missing-status error paths now re-read runtime authority truth and fail closed with explicit authority-status errors instead of falling back to startup env-derived role or epoch fields. | Startup continuity env is only topology input and becomes stale after promotion. Re-reading Continuity.authority_status() keeps membership, promotion, and keyed error payloads truthful after failover, while explicit 409/503/504 responses prevent the proof app from inventing authority state when the runtime is degraded or returns malformed JSON. | Yes | agent |
| D179 | M043/S02 | requirement | R051 | validated | M043/S02 now proves the first disaster-continuity contract end to end: mirrored standby state survives primary loss, explicit standby promotion rolls a new same-key attempt through the promoted authority, the work completes on the promoted standby, and the restarted old primary rejoins fenced/deposed at the newer epoch. The authoritative evidence is `bash scripts/verify-m043-s02.sh` plus the retained `.tmp/m043-s02/verify/07-failover-artifacts/` bundle. | Yes | agent |
| D180 | M043/S03 planning | continuity-proof | How M043/S03 proves the same-image operator rail | Pair a dedicated Docker-backed `compiler/meshc/tests/e2e_m043_s03.rs` harness with a fail-closed `scripts/verify-m043-s03.sh` wrapper that replays `scripts/verify-m043-s02.sh` and validates copied Docker artifacts from the same-image run. | S03 is not inventing new runtime authority; it is packaging the shipped failover contract into a repeatable one-image operator rail. A named e2e file satisfies the non-trivial test-file requirement and gives execution a focused regression surface, while the shell wrapper keeps the established M043 proof pattern: replay prior rails first, then fail closed on retained JSON/log evidence instead of trusting exit codes alone. | Yes | agent |
| D181 | M043/S03 | operator-config | Where the same-image continuity role/epoch contract should fail for M043/S03 | Mirror the continuity role/epoch validation in `cluster-proof/docker-entrypoint.sh` and exit non-zero before the compiled package starts when cluster-mode continuity env is blank or contradictory. | The packaged same-image rail needs a fail-closed operator boundary. Letting contradictory role/epoch env reach the compiled binary could log a config error yet still look like a successful package start, which weakens the proof surface and makes misconfiguration ambiguous. Early entrypoint validation preserves valid primary/standby/stale-primary flows while giving the verifier one precise startup failure seam with no cookie leakage. | Yes | agent |
| D182 | M043/S04 planning | continuity-proof | How M043/S04 should reconcile the public proof surface with the shipped failover contract | Add dedicated `scripts/verify-m043-s04-proof-surface.sh` and `scripts/verify-m043-s04-fly.sh` rails, and make every public docs/runbook surface point to them while keeping `bash scripts/verify-m043-s03.sh` as the destructive local authority. | The shipped M043 runtime/operator contract adds explicit promotion, authority-role/epoch/health truth, same-image failover authority, and fenced stale-primary rejoin. Reusing the M042 public verifier names would keep keyed-continuity-era wording looking canonical and blur which rail actually proves the current failover story. Named M043 verifier rails let the docs, help text, and read-only Fly checks all speak the same bounded contract without overclaiming destructive Fly proof. | Yes | agent |
| D183 | M044 bootstrap | architecture | What boundary defines clustered execution in the first-class app model | Only service/message/work handlers declared clustered in `mesh.toml` receive continuity and failover semantics; undeclared code stays ordinary local Mesh code. | The user explored route-only versus replicating all server work and chose the honest middle line: clustered where declared, ordinary everywhere else. This avoids overclaiming that every function or all server work is replicated while still making clustering broader than HTTP routes. | Yes — if execution proves the declaration boundary is still too route-shaped or too leaky. | collaborative |
| D184 | M044 bootstrap | api | What the primary operator surface should be for clustered apps | Clustered apps expose built-in runtime operator surfaces first, CLI second, and HTTP only as an optional layer on top of the same truth. | The user wants the clustered feature to feel language/platform-owned rather than route-owned. Making runtime APIs primary and CLI the standard consumer keeps operator truth out of app-specific admin routes and prevents HTTP from becoming the real abstraction. | Yes — if execution shows a missing non-HTTP surface blocks ordinary operator workflows. | collaborative |
| D185 | M044 bootstrap | architecture | What failover control mode M044 should support | M044 ships automatic promotion only; there is no manual promotion or operator override path, and ambiguous cases must fail closed. | The user explicitly rejected manual promotion and confirmed that ambiguity must fail closed. This keeps automatic failover bounded, honest, and fenced instead of turning the milestone into a split-brain-prone override system. | Yes — only if a later milestone intentionally expands the operator contract. | collaborative |
| D186 | M044 bootstrap | configuration | How ordinary Mesh apps activate and use the clustered execution model | Apps opt into clustered mode in `mesh.toml` / app metadata, declare which handlers are clustered there, and provide only business logic for those handlers. | The user wants ordinary Mesh app authors to get clustering behind the scenes without writing explicit placement, promotion, continuity, or admin plumbing. Putting activation and declaration in app metadata makes the feature language-owned instead of proof-app-owned. | Yes — if planning finds a minimal amount of Mesh-code declaration is required to keep handler intent explicit. | collaborative |
| D187 | M044 bootstrap | pattern | How the proof app should relate to the new clustered-app model | `cluster-proof` must be fully rewritten onto the public clustered-app standard, with the old explicit clustering path removed from its code rather than preserved as an advanced public route. | The user wants the milestone to prove the feature is actually in the language/platform. Keeping the old proof-app path in `cluster-proof` would leave the public story split between the new model and the old folklore implementation. | Yes — only if a later milestone intentionally exposes a separate low-level expert surface distinct from the clustered-app model. | collaborative |
| D188 | M044/S01 planning | configuration | What declaration contract S01 should implement for clustered handlers | Use optional clustered-app metadata in `mesh.toml` that explicitly declares clustered service call handlers, service cast handlers, and public work functions, while keeping manifestless builds valid and validating those declarations after compiler export collection. | This matches the M044 requirement boundary of clustered where declared, reuses the compiler's existing service/function export knowledge, keeps ordinary ad hoc `meshc build` flows working, and gives S02 a concrete manifest-backed contract to execute instead of vague handler strings. | Yes | agent |
| D189 | M044/S01/T01 | configuration | Manifest syntax for clustered declaration targets | Use `[cluster] enabled = true` with a non-empty `declarations` array whose entries are `{ kind, target }`, where `work` targets use `<ModulePath>.<function>` and service handler targets use `<ModulePath>.<Service>.<method>`; reject ambiguous overloaded public work names and non-call/non-cast service helpers fail-closed. | S01 needed one explicit manifest contract that stays optional for non-manifest builds, resolves cross-module handlers without relying on import context, and keeps compiler and LSP validation on the same narrow boundary. Fully-qualified targets avoid collisions, and fail-closing overloaded work names or service start helpers prevents internal mangled names and broader service surfaces from leaking into the public contract. | Yes | agent |
| D190 | M044/S01/T04 | public-api | Mesh-facing continuity payload field shapes | Return typed Continuity structs with String/Int/Bool fields instead of introducing new builtin sum types for phase/result/role/status/outcome in M044/S01/T04. | The existing public contract in cluster-proof and the compiler e2es already expects direct field access over stringly enum-like values. Converting the outer API from Result<String, String> to typed structs removes JSON decoding without also expanding the slice into builtin enum registration, lowering, and pattern-matching changes. This keeps the runtime ABI, typeck registry, and downstream app rewrite aligned on one narrower seam. | Yes | agent |
| D191 | M044/S01/T05 | public-api | How cluster-proof should consume the M044 continuity surface | Consume builtin ContinuityAuthorityStatus/ContinuityRecord/ContinuitySubmitDecision directly throughout cluster-proof and keep only HTTP request/response payload wrappers as local Json-derived structs. | This removes the deprecated runtime JSON shim path, keeps the public continuity contract typed end-to-end inside Mesh code, and limits app-owned JSON work to the actual HTTP boundary where cluster-proof still needs shaped responses. | Yes | agent |
| D192 | M044/S01 | requirement | R062 | validated | M044/S01 replaced the public `Continuity.*` stringly JSON seam with typed Mesh structs across typeck, MIR, codegen, and runtime, rewrote `cluster-proof` onto those typed values, and proved the contract with the named `m044_s01_typed_continuity_` / `m044_s01_continuity_compile_fail_` rails plus the shim-absence check in `bash scripts/verify-m044-s01.sh`. | Yes | agent |
| D193 | M044/S01 | requirement | R061 | validated | M044/S01 proved the clustered-app activation contract through optional `[cluster]` metadata in `mesh.toml`, shared compiler/LSP validation, `cluster-proof/mesh.toml`, and the green `bash scripts/verify-m044-s01.sh` acceptance rail. | Yes | agent |
| D194 | M044/S02 planning | architecture | How manifest-declared handlers become runtime-executable in S02 | Compile manifest declarations into runtime execution metadata plus generated clustered entry wrappers; HTTP route handlers stay local adapters, and only declared work/service targets enter the runtime-owned clustered path. | The current remote dispatch path only knows registered top-level function symbols, while real service handlers live behind compiler-internal `__service_*` bodies and `cluster-proof` currently over-declares HTTP ingress functions. Generated clustered entry wrappers let S02 move placement, continuity, fencing, and failover ownership behind the runtime without widening the clustered boundary past the explicit S01 declaration contract. | Yes | agent |
| D195 | M044/S02/T01 | compiler | How declared clustered handlers carry executable identity past manifest validation | Return shared ClusteredExecutionMetadata from manifest validation, add explicit service method export kinds in typeck, and keep the resulting execution plan alongside prepared MIR in meshc. | This keeps manifest parsing/validation as the single source of truth for the declared clustered boundary, gives later runtime/codegen work stable executable-symbol metadata without re-parsing mesh.toml, and removes prefix-based call/cast inference drift between meshc and mesh-lsp. | Yes | agent |
| D196 | M044/S02/T11 | compiler | How declared service handlers should be exposed to the runtime registry | Generate distinct `__declared_service_*` wrapper symbols that delegate to the existing `__service_*_call/cast_*` helpers, and register those wrapper symbols instead of the raw service helpers. | The runtime registry needs a stable declared-handler surface that is separate from compiler-internal service helper names. Thin wrapper symbols keep the existing mesh_service_call/mesh_actor_send reply transport intact while making declared service targets distinguishable in planning and proof rails. | Yes | agent |
| D197 | M044/S02 | requirement | R063 | validated | S02 now proves the declaration boundary end to end: bash scripts/verify-m044-s02.sh passes, the named declared-work/service/cluster-proof rails run real tests, cluster-proof declares WorkContinuity.execute_declared_work instead of HTTP ingress handlers, and undeclared-local behavior stays out of the declared runtime registry and manifestless builds. | Yes | agent |
| D198 | M044/S02/T09 | compiler | How manifest-declared executable symbols survive MIR pruning before runtime registration | Treat every manifest-declared executable_symbol as an explicit merged-MIR monomorphization root before prepare_declared_runtime_handlers runs. | Declared clustered helpers are often locally unused by the Mesh entrypoint, so default MIR pruning can drop valid manifest-approved work/service helpers before runtime registration preparation. Keeping those symbols as explicit roots preserves the declared-handler contract without widening undeclared local behavior. | Yes | agent |
| D199 | M044/S03 planning | scaffolding | What baseline public template the clustered scaffold should generate | Make `meshc init --clustered` scaffold the narrowest honest clustered boundary: one declared public work handler plus a generic `MESH_*` bootstrap contract, with no `CLUSTER_PROOF_*` names or copied proof-app helper modules. | R066 needs a real clustered app path, and R052 requires replacing the proof-app env dialect with a standard clustered-app contract. A declared work handler is the smallest valid manifest-backed boundary from S01/S02, and a generic `MESH_*` bootstrap surface keeps the scaffold public instead of proof-app-shaped. | Yes | agent |
| D200 | M044/S03 planning | scaffolding | What baseline public template the clustered scaffold should generate | Make `meshc init --clustered` scaffold the narrowest honest clustered boundary: one declared public work handler plus a generic `MESH_*` bootstrap contract, with no `CLUSTER_PROOF_*` names or copied proof-app helper modules. | R066 needs a real clustered app path, and R052 requires replacing the proof-app env dialect with a standard clustered-app contract. A declared work handler is the smallest valid manifest-backed boundary from S01/S02, and a generic `MESH_*` bootstrap surface keeps the scaffold public instead of proof-app-shaped. | Yes | agent |
| D201 | M044/S03 blocker replan after T02 | architecture | How the public `meshc cluster` surface inspects a live node without corrupting membership truth | Implement live operator inspection through a transient authenticated query channel that never registers a cluster session, exchanges peer lists, or syncs continuity state; `meshc cluster` must depend on that path instead of the ordinary node-join transport. | T02 proved that the current `mesh-rt` query helpers connect through the real peer session path, which would make the CLI appear as a cluster member and falsify the status it reports. A separate authenticated transient query path preserves runtime-owned truth while keeping the operator surface read-only and bounded. | Yes | agent |
| D202 | M044/S03 | requirement | R066 | validated | M044/S03 added `meshc init --clustered`, the generated manifest-declared clustered app, the generic `MESH_*` bootstrap contract, the green `test_init_clustered_creates_project` and `m044_s03_scaffold_` rails, and the assembled `bash scripts/verify-m044-s03.sh` scaffold replay proving ordinary apps can bootstrap the public clustered model without copying `cluster-proof` internals. | Yes | agent |
| D203 | M044/S04 | architecture | How S04 satisfies the auto-only failover contract | Retire the public `Continuity.promote()` Mesh API and the `/promote` proof-app route in S04, leaving authority mutation as a runtime-internal automatic failover mechanism while operator surfaces stay read-only. | R067 and D185 require bounded automatic promotion with no manual override path. Keeping a public Mesh or HTTP promote surface would preserve a contradictory control seam, make the proof/docs story dishonest, and weaken stale-primary fencing guarantees by suggesting operators can mutate authority out of band. | Yes | collaborative |
| D204 | M044/S04/T03 | compiler-api | How stale Mesh continuity promotion calls are rejected after failover became auto-only | Remove the Mesh-visible Continuity.promote builtin/lowering/export path and reject Continuity.promote() in type checking with an explicit automatic-failover diagnostic, while keeping the Rust continuity promote_authority helper internal-only. | This preserves the runtime’s internal authority-transition seam for bounded automatic promotion while ensuring stale Mesh application code fails before lowering or linking with a message that points authors to the supported read-only authority_status surface. | Yes | collaborative |
| D205 | M044/S04 | requirement | R068 | validated | Validated by the green destructive failover rail: `cargo test -p mesh-rt automatic_recovery_ -- --nocapture`, `cargo test -p meshc --test e2e_m044_s04 m044_s04_auto_resume_ -- --nocapture`, and `bash scripts/verify-m044-s04.sh`, which prove declared clustered work survives primary loss through safe automatic promotion, automatic recovery on the promoted standby, and stale-primary fencing on rejoin. | Yes | agent |
| D206 | M044/S05 planning | configuration | How S05 handles cluster-proof bootstrap compatibility during the final rewrite | Switch `cluster-proof` directly to the public clustered-app `MESH_*` bootstrap contract and keep only `CLUSTER_PROOF_WORK_DELAY_MS` as a proof-only knob; do not preserve compatibility aliases for cookie, node identity, or durability env names. | R069 requires a full dogfood rewrite rather than a compatibility wrapper. Keeping the old `CLUSTER_PROOF_*` bootstrap env names alive for the real cluster contract would leave the product boundary split across the scaffold story, the proof app, and the docs/verifiers, which is exactly the milestone drift S05 is supposed to close. | Yes | collaborative |
| D207 |  | requirement | R070 | validated | Validated by the green docs/proof closeout rail: `cargo test -p meshc --test e2e_m044_s05 -- --nocapture`, `bash scripts/verify-m044-s05.sh`, and `npm --prefix website run build`, plus the exact-source/docs truth checks that require scaffold-first `meshc init --clustered` + `meshc cluster` guidance and reject legacy `CLUSTER_PROOF_*` bootstrap/env folklore or the old `/work` probe story. | Yes | agent |
| D208 | M045 bootstrap | docs | Which clustered-app surface should be the primary example and docs entrypoint | Use `meshc init --clustered` and one local scaffold-first example as the primary clustered-app teaching surface; keep `cluster-proof` only as a deeper secondary proof rail. | The user explicitly wants a clean simple example of clustering, distribution, and failover working from the language side. Leading with the scaffold-first example keeps the product story small and language-owned instead of making users reverse-engineer the proof app first. | Yes — if later evidence shows the scaffold cannot honestly carry the full clustered story. | human |
| D209 | M045 bootstrap | architecture | Where clustered behavior ownership belongs in the primary example | All cluster state, routing choice, authority/failover, and status truth for the primary example must come from the language/runtime, not from example-owned helpers. | The user explicitly said everything related to clustering, fail over, distribution, load balancing, cluster state, routing choice, authority/failover, and status truth should be part of the language and auto-magic. That makes example-owned distributed logic an anti-goal for this milestone. | No | human |
| D210 | M045 bootstrap | proof | Whether the primary example may stop at happy-path clustering or must also carry failover proof | The same tiny local example must prove the full clustered story end to end: cluster formation, runtime-chosen remote execution, and failover. | The user rejected a split where the simple example only showed clustering and a separate deeper rail proved failover. The public example must show everything working from the language side on one small example. | Yes — only if execution proves the current runtime surface cannot honestly support the full story on one tiny example yet. | human |
| D211 | M045/S01 | architecture | Which runtime/public API should own clustered app bootstrap in M045/S01 | Introduce a typed public bootstrap boundary as `Node.start_from_env()` that owns standalone-vs-cluster detection, `MESH_*` / Fly identity validation, and the runtime `Node.start(...)` call while keeping the existing `MESH_*` env contract stable; migrate the scaffold first, then `cluster-proof`, and preserve the low-level `Node.start(name, cookie)` primitive for explicit callers and low-level docs. | Current research and code inspection show discovery, operator inspection, and continuity authority already live in the runtime, while both the scaffold and `cluster-proof` still duplicate mode detection, identity resolution, and direct `Node.start(...)` orchestration. A typed runtime-owned bootstrap helper removes example-owned startup logic without forcing the low-level distribution primitive out of the language, and it gives the scaffold and `cluster-proof` one shared contract to prove. | Yes | agent |
| D212 | M045/S01/T01 | runtime-bootstrap | How the new runtime-owned bootstrap helper should relate to the existing public env contract and low-level node start semantics | Mirror the current cluster-proof env validation/error contract in Rust, while keeping low-level mesh_node_start bind-only :0 support behind a separate parser instead of relaxing bootstrap env validation | Downstream scaffold and cluster-proof work needs the runtime helper to preserve the public M044 fail-closed messages for cookie, discovery-seed, MESH_NODE_NAME, and Fly identity handling, but the existing low-level listener rail still expects mesh_node_start("name@host:0", ...) to request an OS-assigned port. Splitting the bind parser from the bootstrap validator keeps the public startup contract stable without regressing the low-level primitive or its listener test. | Yes | agent |
| D213 | M045/S01/T03 | architecture | How clustered Mesh apps consume bootstrap state | Expose `Node.start_from_env()` as a public Mesh builtin returning typed `Result<BootstrapStatus, String>`, and make scaffolded clustered apps log/inspect that status instead of reading bootstrap env or calling `Node.start(...)` directly. | This keeps cluster mode detection, identity resolution, and fail-closed startup inside the runtime while preserving a small public Mesh surface. The scaffold README can still document the stable `MESH_*` contract, but generated `main.mpl` no longer hand-rolls bootstrap logic or duplicate runtime validation. | Yes | agent |
| D214 | M045/S01/T04 | architecture | How cluster-proof should split startup responsibility between Mesh code and its container entrypoint | `cluster-proof/main.mpl` now delegates clustered bootstrap to `Node.start_from_env()`, while `cluster-proof/docker-entrypoint.sh` only preflights continuity topology and defers bootstrap validation/errors to the runtime helper | This removes proof-app-owned mode detection, identity resolution, and direct `Node.start(...)` orchestration from Mesh code while preserving fail-closed packaged startup for the continuity env the runtime bootstrap API does not yet validate. | Yes | agent |
| D215 | M045/S02 planning | architecture | Who owns completion and status truth for successful declared work in the scaffold-first clustered example | Make declared-work completion runtime/codegen-owned for the tiny clustered example, and do not add a scaffold-specific `Continuity.mark_completed(...)` shim or app-owned status/completion helper. | S02 must satisfy R079 and stay aligned with D209's language/runtime ownership rule. A scaffold-specific completion shim would make the public example teach app-owned status truth again and create cleanup debt for S04. Moving completion into the runtime/codegen seam keeps the example surface as `Node.start_from_env()` + submit route + pure work function while still allowing an honest end-to-end remote-execution proof. | Yes | agent |
| D216 | M045 S02 T01 | runtime | How internal declared-handler wrapper symbols participate in remote spawn registration | Register only manifest-approved declared wrapper executable symbols for remote spawn, while continuing to exclude other compiler-internal `__*` functions. | Declared work/service wrappers need to be discoverable by the remote DIST_SPAWN path, but broadening all internal helpers would widen the public execution surface and hide registration drift. The runtime still fail-closes on undeclared names, and missing wrapper registration remains explicit in owner-node diagnostics. | Yes | agent |
| D217 | M045/S02 | verification | M045/S02 tiny clustered example acceptance surface | Use ingress-side `meshc cluster status --json` plus dual-node `meshc cluster continuity --json` as the authoritative runtime-owned proof surface, and replay direct prerequisite commands in `scripts/verify-m045-s02.sh` instead of nesting older verifier wrappers. | The tiny scaffold example must prove remote-owner execution and completed continuity through the same public runtime inspection surfaces users will rely on, not through app-owned status helpers. On this host, standby-side pre-submit operator probing can destabilize the first remote-owned submit, and the older nested verifier stack can hang after `cluster-proof/tests` already logged green, so the truthful stable acceptance rail is a flattened replay of the direct prerequisites plus the retained S02 artifact bundle. | Yes | agent |
| D218 | M045/S03 planning | proof | How M045/S03 should prove tiny-example failover truth without regressing the docs-grade scaffold surface | Use a scaffold-first destructive failover proof driven by runtime CLI truth (`meshc cluster status\|continuity\|diagnostics`) and only add the smallest runtime-owned pending-window stabilization seam if harness-only observation is not deterministic; do not add scaffold-specific failover, promotion, or status routes. | The runtime-owned auto-promotion/recovery path is already productized and green, while the missing work is proving that same story on the tiny scaffolded example. Keeping the proof on the scaffold and verifying placement/failover through public runtime surfaces advances R078 directly and supports R077/R079/R080; if timing becomes the blocker, the honest fallback is a lower runtime/codegen seam rather than new app-owned distributed logic. | Yes | agent |
| D219 | M045/S03 | requirement | R078 | validated | M045/S03 closed the missing failover half of the tiny scaffold-first example. The authoritative proof is `bash scripts/verify-m045-s03.sh`, which replays clustered init, S02 runtime completion, M044/S04 failover prerequisites, and the new scaffold failover e2e with retained runtime-owned artifacts. | Yes | agent |
| D220 | M045/S03 | architecture | M045 scaffold failover proof window | The clustered scaffold now requests one replica only when `Node.list()` reports a connected peer, and the generated declared-work handler sleeps briefly before returning so the primary-owned mirrored pending state is observable during local failover proof. | Always requesting a replica broke the single-node clustered rail, while always requesting zero replicas made failover impossible. Peer-aware replica count preserves the tiny scaffold contract across both local-only and two-node runs, and the fixed brief handler delay avoids adding an example-specific env knob while keeping the destructive failover window truthful. | Yes | agent |
| D221 | M045/S04 planning | proof | Which verifier/docs surface should be the current clustered cleanup closeout after S04 | Introduce `compiler/meshc/tests/e2e_m045_s04.rs` plus `scripts/verify-m045-s04.sh` as the current source/docs/verifier contract, and update public docs to point at M045 rails instead of the older `verify-m044-s05.sh` story. | The repo still teaches the old M044 closeout rail even though M045 is now the primary scaffold-first clustered story. A dedicated M045 S04 contract test and assembled verifier let S04 retire stale public references without rewriting the final S05 docs stack in the same task. | Yes | agent |
| D222 | M045/S04 planning | architecture | Where the remaining cluster-proof declared-work seam should live after S04 cleanup | Move the manifest-declared handler to `Work.execute_declared_work` and leave `work_continuity.mpl` responsible only for keyed submit/status HTTP translation over runtime `Continuity`. | S04’s main live example-owned seam is that `work_continuity` still owns the declared-work target and manual completion path. Moving the target into `Work` aligns cluster-proof with the smaller scaffold-first example, removes wrapper-era glue, and keeps runtime-owned completion/failover truth as the only completion path. | Yes | agent |
| D223 | M045/S04/T02 | architecture | Where cluster-proof owns its declared-work target and completion behavior | `cluster-proof/work.mpl` owns `declared_work_target()` and `execute_declared_work(...)`, while `cluster-proof/work_continuity.mpl` stays a submit/status translator and does not call `Continuity.mark_completed(...)`. | This keeps the proof app aligned with the scaffold-first runtime contract: the runtime closes continuity records when declared work returns, and the HTTP wrapper no longer hides missing-handler or completion drift behind manual fallback logic. | Yes | agent |
| D224 | M045/S04/T03 | verification | Current ownership of the public distributed closeout rail | Use M045 S04 (`scripts/verify-m045-s04.sh` plus `e2e_m045_s04`) as the current public clustered closeout contract, and keep M044 S05 as a historical transition checker that validates the docs moved forward instead of claiming present-tense ownership. | The public docs/readmes need to point at the live scaffold-first M045 cleanup rail, while the older M044 closeout checks should remain truthful without failing merely because the primary story moved to newer rails. | Yes | agent |
| D225 | M045/S05 planning | docs | What the primary docs entrypoint and current closeout rail should be for the clustered story in M045/S05 | Add a first-class Getting Started page at `/docs/getting-started/clustered-example/` for the scaffold-first clustered tutorial, and make `scripts/verify-m045-s05.sh` the present-tense closeout rail while `scripts/verify-m045-s04.sh` remains the reused subrail inside it. | This satisfies R080 and R081 by teaching `meshc init --clustered` as the first path instead of routing readers straight to proof pages, while preserving R077, R078, and R079 by keeping the tiny example language-first and reusing the existing S02/S03/S04 product rails rather than inventing a docs-only success story. | Yes | agent |
| D226 | M045/S05 | requirement | R080 | validated | S05 made `meshc init --clustered` the primary public clustered entrypoint and proved that surface end to end through `cargo test -p meshc --test e2e_m045_s05 m045_s05_ -- --nocapture`, `npm --prefix website run build`, the route-marker sweep over the Getting Started docs/sidebar, and the green assembled replay `bash scripts/verify-m045-s05.sh`. | Yes | agent |
| D227 | M045/S05 | requirement | R081 | validated | S05 reordered the public teaching surface so clustered readers land on `/docs/getting-started/clustered-example/` first while deeper proof rails stay secondary, and the contract is now fail-closed by `cargo test -p meshc --test e2e_m045_s05 m045_s05_ -- --nocapture`, the public-doc marker sweep across README/docs surfaces, `npm --prefix website run build`, and the assembled wrapper `bash scripts/verify-m045-s05.sh`. | Yes | agent |
| D228 | M046 | proof | What workload the canonical clustered proof apps should run | Use deliberately trivial arithmetic work (`1 + 1` or equivalent) so any remaining orchestration or failure-handling complexity is clearly Mesh-owned. | The user wants the proof apps to be so small that the language/runtime, not the app, is visibly doing the distributed work. A trivial workload makes that ownership boundary unmistakable. | No | human |
| D229 | M046/S01 | language/compiler | How S01 should express and merge source-level clustered-work declarations | Use a narrow contextual `clustered(work)` prefix on `fn\|def`, feed source-declared work into the same clustered execution planner used by manifest declarations, and fail closed on same-target manifest/source duplicates. | This is the smallest honest change that satisfies R085 and R090 without widening Mesh into a generic annotation system, adding `@` lexer work, or creating a second runtime boundary. Explicit duplicate failure keeps the merge behavior deterministic and prevents silent double registration during migration between declaration forms. | Yes | agent |
| D230 | M046/S01/T01 | parser | How source-level `clustered(work)` should be represented in the parser and AST | Represent the narrow source marker as a dedicated `CLUSTERED_WORK_DECL` CST/AST node attached to `FnDef`, parsed only from contextual `clustered(work)` item prefixes before `fn\|def`, instead of introducing a generic annotation/decorator system. | This keeps S01 fail-closed and narrow, preserves the existing `@` rejection and non-keyword `clustered` tokenization, and gives the compiler/LSP merge work a stable typed accessor instead of re-parsing token trivia. | Yes | agent |
| D231 | M046/S01/T02 | architecture | Where source-declared clustered work is merged with manifest declarations | Merge and validate source `clustered(work)` declarations inside `mesh-pkg::manifest`, then have both `meshc` and `mesh-lsp` call that shared helper. | This keeps duplicate policy, origin-tagged diagnostics, and execution-metadata planning consistent between compiler and editor analysis instead of duplicating the merge logic in two frontends. | Yes | agent |
| D232 | M046/S01 | requirement | R085 | validated | M046/S01 landed the narrow `clustered(work)` source declaration, merged it into the same clustered execution planner used by `mesh.toml`, and proved both surfaces converge on the declared-handler runtime boundary via `cargo test -p mesh-parser --test parser_tests m046_s01_parser_ -- --nocapture`, `cargo test -p meshc --test e2e_m046_s01 m046_s01_ -- --nocapture`, `cargo test -p mesh-lsp m046_s01_ -- --nocapture`, `cargo test -p meshc --test e2e_m044_s01 m044_s01_ -- --nocapture`, and `cargo test -p meshc --test e2e_m044_s02 m044_s02_ -- --nocapture`. | Yes | agent |
| D233 | M046/S01/T02 | proof | How to keep the M046/S01 source-declared compiler rail truthful while the unrelated single-function LLVM verifier bug remains | Keep the M046/S01 compiler happy-path proof on the known-good M044-shaped work fixture until the unrelated single-function LLVM verifier bug is fixed. | The shipped source-declaration merge path is green, but a smaller source-only fixture that only defines `handle_submit` still reproduces `LLVM module verification failed: Function return type does not match operand type of return inst! ret {} zeroinitializer i64`. Using the broader M044-shaped work fixture keeps the declared-handler registration proof honest without pretending the older codegen issue is solved. | Yes | agent |
| D234 | M046/S02 | architecture | How S02 identifies and inspects runtime-owned startup work without app routes | Use a runtime-owned startup registration list keyed by declared handler runtime name, submit startup work with a deterministic request key derived from that runtime name, and expose the declared runtime name on `meshc cluster continuity` output. | All nodes need to converge on one logical startup run without app glue. A deterministic runtime-owned identity lets continuity dedupe and recover the same work across clustered boots, while CLI exposure keeps route-free inspection discoverable without teaching users an app-owned status route or an opaque internal key. | Yes | agent |
| D235 | M046/S02/T01 | compiler/runtime boundary | How startup-work registrations identify clustered work across build planning and main-wrapper codegen | Register startup work by declared runtime registration name only, and fail codegen if a startup work item does not resolve to an existing declared-handler registration in the LLVM module. | The runtime-owned startup trigger should share the same stable identity surface as declared-handler submission instead of introducing a second executable-symbol contract. Using the runtime registration name keeps manifest-declared and source-declared work aligned, keeps service call/cast handlers off the startup path, and makes missing wrapper plumbing fail at compile time before runtime debugging. | Yes | agent |
| D236 | M046/S02/T02 | runtime | Startup-work runtime identity and keepalive scope for route-free clustered apps | Derive startup request_key/payload_hash from the declared runtime name, require peer stability only after a peer has actually been observed, and spawn a runtime-owned keepalive actor only when clustered startup-work registrations exist. | This gives startup work one stable logical identity across boots, lets intentional single-node clustered boots proceed without a false replica timeout, fails closed once a peer-backed topology has actually emerged but not stabilized, and keeps route-free clustered binaries inspectable without forcing every clustered process to stay alive forever. | Yes | agent |
| D237 | M046/S02 closeout attempt | runtime | M046/S02 startup trigger behavior on standby nodes | Keep route-free startup keepalive on standby nodes, but skip auto-submitting startup work unless the local continuity authority is primary. | The two-node route-free S02 proof was racing because both primary and standby authorities auto-submitted the same deterministic startup request key. The truthful runtime boundary is primary-owned startup execution with standby keepalive/inspection only; after this change, the focused failing rail `cargo test -p meshc --test e2e_m046_s02 m046_s02_cli_tiny_route_free_startup_dedupes_on_two_nodes -- --nocapture` passes and `cargo test -p mesh-rt startup_work_ -- --nocapture` now covers the standby-skip behavior. | Yes | agent |
| D238 | M046/S02 closeout | requirement | R087 | validated | M046/S02 proved that a route-free clustered app can boot, auto-run its clustered work, and be inspected entirely through runtime/tooling surfaces with no app-owned HTTP submit/status routes and no explicit `Continuity.submit_declared_work(...)` call. Evidence: `cargo test -p mesh-rt startup_work_ -- --nocapture`, `cargo test -p meshc --test e2e_m046_s02 m046_s02_cli_ -- --nocapture`, and `cargo test -p meshc --test e2e_m046_s02 m046_s02_ -- --nocapture`. | Yes | agent |
| D239 | M046/S02 closeout | runtime | How simultaneous duplicate node connects are resolved during route-free clustered startup | Resolve duplicate sessions deterministically by local/remote node name so the earlier-sorting node keeps the outgoing session and the later-sorting node keeps the incoming session | The two-node route-free startup rail was failing because both nodes booted together, each kept its own accepted half-connection, and the opposite client halves were dropped. That stranded control traffic on dead transports and made the first runtime-owned remote spawn fail with `mesh node spawn failed ... write_error`. Deterministic direction preference converges both nodes on the same live transport without weakening simultaneous-boot coverage. | Yes | agent |
| D240 | M046/S03 planning | proof-surface | How the new `tiny-cluster/` local proof package should expose work and failover observability | `tiny-cluster/` will be a source-first, route-free package with a plain `[package]` manifest, default trivial `1 + 1` work, an opt-in package-local delay env only for the failover harness, and all startup/failover inspection through `meshc cluster status\|continuity\|diagnostics` plus a direct S03 verifier. | This preserves R088, R092, and R093 while reusing S02’s runtime-owned startup/status contract instead of inventing new routes or app-owned control seams. It also makes the real local proof auditable without spending S05’s public scaffold/docs alignment work early. | Yes | agent |
| D241 | M046/S03/T01 | proof | How tiny-cluster should expose a failover-observable pending window | Keep tiny-cluster source-first and route-free, with a single source `clustered(work)` declaration plus a package-local `TINY_CLUSTER_WORK_DELAY_MS` hook clamped to 5000 ms instead of any manifest `[cluster]` entry or app-owned submit/status route. | S03 needs a real package that later failover rails can hold pending long enough to observe runtime-owned startup, promotion, and recovery truth, but the package must not grow a second declaration surface or invent an app-owned control plane. A bounded local env hook keeps the proof honest and keeps inspection on `meshc cluster status\|continuity\|diagnostics`. | Yes | agent |
| D242 | M046/S03/T03 | runtime | Where the tiny-cluster failover pending-window hook should live | Move the failover-only startup delay from tiny-cluster package code into mesh-rt as a runtime-owned `MESH_STARTUP_WORK_DELAY_MS` dispatch hook keyed to the startup request path. | The user explicitly rejected package-owned delay logic in `tiny-cluster/work.mpl`. A runtime-owned delay keeps the package source-first and visibly trivial (`1 + 1`) while still giving the failover harness a bounded pending window on the real startup record without inventing HTTP or app-owned control seams. | Yes | collaborative |
| D243 | M046/S03/T03 override 2026-03-31 | runtime/proof-surface | How failover-observable pending windows may appear in the tiny clustered proof surfaces after the M046/S03 override | No app/package code or user-facing setup may own delay normalization, sleep helpers, or proof-only timing knobs for clustered failover observability; any pending-window control needed for proof must live behind a Mesh-owned language/runtime seam and stay invisible to example code. | The user explicitly rejected seeing delay-helper code in `tiny-cluster/` and said this behavior should be part of the language. Even if the proof still needs a bounded pending window to observe failover, asking app authors or users to wire that timing in code or config breaks the M046 honesty bar. | Yes — the implementation mechanism may change, but app/user-owned timing seams are out of contract. | human |
| D244 | M046/S03/T04 | runtime/proof | How clustered startup work should expose a failover-observable pending window without app or user timing knobs | Use a bounded language-owned startup dispatch window whenever runtime-owned startup work requires a replica, and surface that window through `startup_dispatch_window` diagnostics instead of any env-driven delay control. | This keeps `tiny-cluster/` source and docs free of timing helpers, removes the remaining `MESH_STARTUP_WORK_DELAY_MS` seam from the failover rail and verifier, and still preserves a truthful mirrored pending window for CLI-only failover observation. | Yes | collaborative |
| D245 | M046/S03 | requirement | R088 | validated | S03 now ships a real repo-owned `tiny-cluster/` package with route-free `Node.start_from_env()` boot, a single source `clustered(work)` declaration, trivial `1 + 1` work, no app-owned timing hooks, and passing proof from `cargo run -q -p meshc -- build tiny-cluster`, `cargo run -q -p meshc -- test tiny-cluster/tests`, `cargo test -p meshc --test e2e_m046_s03 m046_s03_tiny_cluster_package_ -- --nocapture`, `cargo test -p meshc --test e2e_m046_s03 m046_s03_tiny_cluster_startup_ -- --nocapture`, `cargo test -p meshc --test e2e_m046_s03 m046_s03_tiny_cluster_failover_ -- --nocapture`, and `bash scripts/verify-m046-s03.sh`. | Yes | agent |
| D246 | M046/S03 | requirement | R093 | validated | The canonical local clustered proof workload remains visibly trivial (`1 + 1`) while the pending failover observation window moved into a language-owned runtime `startup_dispatch_window` seam; package smoke, e2e startup/failover rails, and `scripts/verify-m046-s03.sh` all pass without any app-owned delay or control logic in `tiny-cluster/`. | Yes | agent |
| D247 | M046/S04 planning | architecture | How S04 rebuilds `cluster-proof/` without preserving the legacy HTTP/Fly proof shape | Rebuild `cluster-proof/` to match the tiny route-free package contract in source (`clustered(work)` + `Node.start_from_env()`), keep only package-specific smoke/docs/deployment files, prove the packaged path through a new M046 route-free CLI/runtime rail, and treat older M044/M045 wrappers as historical aliases instead of preserving routes or fake Fly HTTP probes. | This is the smallest honest way to satisfy R089 while supporting R086, R087, R090, and R093. Keeping `/membership`, `/work`, `CLUSTER_PROOF_WORK_DELAY_MS`, or Fly `http_service` alive just to preserve older verifiers would hide whether Mesh-owned tooling is actually sufficient and would preserve the exact package seams M046 is supposed to delete. | Yes | agent |
| D248 | M046/S04/T02 | packaging | How the `cluster-proof/` proof package should present its Docker/Fly runtime contract | Ship `cluster-proof` as a direct route-free binary container: Docker copies only the built binary into the runtime image, uses it as the entrypoint directly, and `fly.toml` stays process-only without HTTP proxy assumptions. | The package contract is now source-owned `clustered(work)` plus `Node.start_from_env()`. Keeping an entrypoint wrapper, `PORT`, or `http_service` would reintroduce a fake app-owned HTTP story and hide packaging drift that the smoke rail and docker build are supposed to expose. | Yes | agent |
| D249 | M046/S04/T03 | testing | How route-free packaged proof rails should share build/runtime harness logic | Use compiler/meshc/tests/support/m046_route_free.rs as the shared route-free harness, require temp meshc build --output targets outside package directories with pre-created parents, and read retained build-meta.json before launch. | This keeps tiny-cluster and cluster-proof on the same Mesh-owned CLI/runtime proof surfaces, prevents the e2e rails from churning tracked package binaries, and leaves a retained build/log/JSON bundle that future verifiers and wrapper slices can inspect directly. | Yes | agent |
| D250 | M046/S04/T04 | verification | How historical M044/M045 cluster-proof wrapper rails prove the packaged app after the route-free rebuild | Make verify-m044-s05.sh, verify-m045-s04.sh, and verify-m045-s05.sh thin wrappers around scripts/verify-m046-s04.sh with retained phase/bundle artifact checks and focused Rust content-guard tests, instead of replaying deleted HTTP/package/docs steps. | cluster-proof is now a route-free packaged proof, so the old wrapper layers would otherwise keep asserting a deleted /work, delay-hook, Fly HTTP, or docs-scaffold story. Direct delegation preserves the historical aliases while failing closed on the single truthful packaged verifier and its retained artifacts. | Yes | agent |
| D251 |  | requirement | R089 | validated | Slice M046/S04 rebuilt `cluster-proof/` around the tiny route-free contract (`clustered(work)` + `Node.start_from_env()`), removed app-owned cluster/status modules and HTTP/Fly proxy seams, and validated the packaged proof with `cargo run -q -p meshc -- build cluster-proof`, `cargo run -q -p meshc -- test cluster-proof/tests && docker build -f cluster-proof/Dockerfile -t mesh-cluster-proof:m046-s04-local .`, `cargo test -p meshc --test e2e_m046_s03 m046_s03_tiny_cluster_ -- --nocapture && cargo test -p meshc --test e2e_m046_s04 m046_s04_ -- --nocapture && bash scripts/verify-m046-s04.sh`, and `cargo test -p meshc --test e2e_m044_s05 m044_s05_ -- --nocapture && cargo test -p meshc --test e2e_m045_s04 m045_s04_ -- --nocapture && cargo test -p meshc --test e2e_m045_s05 m045_s05_ -- --nocapture`. | Yes | agent |
| D252 | M046/S05 planning | verification/docs | How S05 keeps the scaffold, proof packages, docs, and historical closeout wrapper in behavioral lockstep | Make `scripts/verify-m046-s05.sh` the authoritative equal-surface verifier, keep `scripts/verify-m045-s05.sh` as a thin historical alias to it, and standardize the documented operator flow everywhere as `meshc cluster status`, continuity list, continuity record, then diagnostics. | S05 is a parity slice rather than a new runtime feature slice. One authoritative verifier plus one canonical CLI inspection sequence is the smallest fail-closed way to keep `meshc init --clustered`, `tiny-cluster/`, `cluster-proof/`, and public docs aligned without reviving deleted HTTP/status routes or letting README/doc drift hide contract regressions. | Yes | agent |
| D253 | M046 S05 T01 | scaffold | How clustered scaffolded projects declare and inspect clustered work | Generate package-only mesh.toml, a route-free main.mpl that only bootstraps through Node.start_from_env(), and a work.mpl that declares declared_work_runtime_name() plus one clustered(work) handler; teach CLI status/continuity/diagnostics as the only operator surfaces. | This keeps meshc init --clustered aligned with tiny-cluster/ and cluster-proof/, removes the last app-owned routeful control plane from the scaffold, and makes scaffold/test/doc drift fail closed against one runtime-owned clustered-work contract. | Yes | agent |
| D254 | M046/S05/T02 | testing | How scaffold-era clustered regression rails prove the route-free contract | Use compiler/meshc/tests/support/m046_route_free.rs as the shared scaffold/runtime harness, keep the live two-node CLI proof in e2e_m046_s05, and narrow older scaffold rails to contract/delegation guards instead of reviving routeful HTTP flows. | This keeps one authoritative runtime proof seam for generated scaffolds, preserves retained .tmp/m046-s05 artifacts for diagnosis, and makes historical tests fail closed on source/readme drift without duplicating deleted /health or /work control planes. | Yes | agent |
| D255 | M046/S05/T04 | verification | Authoritative closeout seam for the equal-surface clustered-work slice | Use `scripts/verify-m046-s05.sh` as the sole direct closeout verifier and keep `scripts/verify-m045-s05.sh` only as a thin retained-alias wrapper that delegates to it. | One truthful direct seam plus a copied S03/S04/S05 retained bundle chain keeps docs, historical wrapper rails, and downstream milestone replay aligned while failing closed when any phase or retained artifact drifts. | Yes | agent |
| D256 |  | requirement | R092 | validated | S05 rewrote the scaffold/docs/package runbooks to teach only the runtime-owned route-free clustered story and proved that routeful HTTP proof strings stay out of the canonical surfaces via `npm --prefix website run build`, the targeted routeful-string guard over README/docs, `cargo test -p meshc --test e2e_m046_s05 m046_s05_ -- --nocapture`, and `bash scripts/verify-m046-s05.sh`. | Yes | agent |
| D257 |  | requirement | R092 | validated | S05 rewrote the scaffold/docs/package runbooks to teach only the runtime-owned route-free clustered story and proved that routeful HTTP proof strings stay out of the canonical surfaces via `npm --prefix website run build`, the targeted routeful-string guard over README/docs, `cargo test -p meshc --test e2e_m046_s05 m046_s05_ -- --nocapture`, and `bash scripts/verify-m046-s05.sh`. | Yes | agent |
| D258 |  | requirement | R090 | validated | S05 put `meshc init --clustered`, `tiny-cluster/`, and `cluster-proof/` on one equal-surface route-free contract and verified the lockstep behavior with `cargo test -p mesh-pkg scaffold_clustered_project_writes_public_cluster_contract -- --nocapture`, `cargo test -p meshc --test tooling_e2e test_init_clustered_creates_project -- --nocapture`, `cargo test -p meshc --test e2e_m044_s03 m044_s03_scaffold_ -- --nocapture`, `cargo test -p meshc --test e2e_m045_s01 m045_s01_ -- --nocapture`, `cargo test -p meshc --test e2e_m045_s02 m045_s02_ -- --nocapture`, `cargo test -p meshc --test e2e_m045_s03 m045_s03_ -- --nocapture`, `cargo test -p meshc --test e2e_m046_s05 m046_s05_ -- --nocapture`, and `bash scripts/verify-m046-s05.sh`. | Yes | agent |
| D259 | M046/S06 planning | verification/docs | What should be the authoritative M046 closeout rail after the equal-surface scaffold/package work landed | Make `scripts/verify-m046-s06.sh` the authoritative assembled verifier that wraps `scripts/verify-m046-s05.sh`, reruns the targeted S03/S04 startup and failover truth rails, repoints `scripts/verify-m045-s05.sh` to delegate to S06, and gates milestone validation plus public docs on that final route-free rail. | S06 is composition and closeout work, not new runtime work. Reusing the existing S05 equal-surface verifier while foregrounding the local/package runtime-owned truth rails preserves one truthful operator story, avoids forking another harness, keeps historical aliases thin, and gives milestone validation a single retained proof bundle rooted in Mesh-owned CLI/runtime surfaces. | Yes | agent |
| D260 | M046 S06 T01 | observability | S06 closeout bundle layout | Use `.tmp/m046-s06/verify/latest-proof-bundle.txt` to point at `retained-m046-s06-artifacts`, while retaining the delegated S05 verifier separately in `retained-m046-s05-verify`. | This keeps one authoritative bundle pointer for milestone-closeout inspection without losing the delegated S05 phase/status/log surfaces needed to localize whether a failure came from the assembled S06 layer or the wrapped equal-surface verifier. | Yes | agent |
| D261 | M046/S06 | requirement | R086 | validated | M046/S06 closed the assembled route-free rail and milestone validation now cites a green `scripts/verify-m046-s06.sh` replay plus `.tmp/m046-s06/verify/latest-proof-bundle.txt` as evidence that app code only marks clustered work while runtime/tooling own startup triggering, placement, failover, recovery, and status truth across the scaffold, `tiny-cluster/`, and rebuilt `cluster-proof/`. | Yes | agent |
| D262 | M046/S06 | requirement | R091 | validated | M046/S06 replays the final assembled closeout rail, retains fresh S03 startup/failover and S04 packaged startup bundles under `.tmp/m046-s06/verify/latest-proof-bundle.txt`, and validates M046 with `scripts/verify-m046-s06.sh`, proving `meshc cluster status\|continuity\|diagnostics` are sufficient runtime-owned inspection surfaces for route-free work state and failover truth. | Yes | agent |
| D263 | M046/S06 | verification | Authoritative closeout surface for M046 | Treat `scripts/verify-m046-s06.sh` plus `.tmp/m046-s06/verify/latest-proof-bundle.txt` as the final authoritative M046 closeout surface, with S05 retained only as a delegated equal-surface subrail. | S06's assembled verifier now replays the green S05 rail, reruns targeted `tiny-cluster/` startup/failover and packaged `cluster-proof/` startup truth, publishes one retained S06 bundle root, and grounds milestone validation directly in that evidence chain. Keeping S05 delegated avoids split present-tense proof authority while preserving the lower-level parity rail for diagnosis. | Yes | agent |
| D264 | M047 bootstrap | architecture | Public clustered declaration surface after M046 | Use source-only `@cluster` / `@cluster(N)` as the public clustered function model, retire `clustered(work)` as a public syntax, and drop `.toml` clustering from the new model. | The repo already proved runtime-owned clustered execution strongly enough that the remaining weakness is the programming model. Keeping both the old syntax and manifest declarations would preserve the duplicate-surface confusion the milestone is meant to remove. | Yes — if execution proves the hard cutover is not honestly supportable without a short compatibility bridge. | collaborative |
| D265 | M047 bootstrap | continuity | Meaning of clustered counts | The numeric argument in `@cluster(N)` and clustered route wrappers means replication count, and omitted counts default to `2`. | If the number means different things in different contexts, the new syntax becomes folklore immediately. Replication count with default `2` matches the user's preference and the repo's existing two-node-first clustered proof bar. | Yes — if runtime execution evidence shows the count needs a different public contract. | collaborative |
| D266 | M047 bootstrap | api | Clustered HTTP route syntax | Use wrapper-style route-local clustering with `HTTP.clustered(handler)` and `HTTP.clustered(N, handler)` instead of verb-specific clustered route APIs. | In pipe-chained routers, clustering has to be obvious at the route declaration site. A wrapper form scales across verbs without exploding the HTTP API and keeps clustered routes as sugar over the same general clustered function model. | Yes — if real usage shows the wrapper form is still awkward enough to require broader route ergonomics. | collaborative |
| D267 | M047 bootstrap | launchability | Initial clustered server scaffold shape | The new clustered server scaffold should be a simple SQLite-backed Todo API with actors, rate limiting, several routes, and a complete Dockerfile, optimized for obvious clustering and low boilerplate rather than maximal infrastructure depth. | The user wants a starting point, not a proof app or a mini-platform. A simple Todo API keeps the syntax visible, lets clustered routes be obvious, and still gives the scaffold enough reality to feel worth starting from. | Yes — if execution shows the Todo shape is too simple to demonstrate the new route model clearly. | collaborative |
| D268 | M047/S01 planning | architecture | M047/S01 clustered declaration compatibility boundary | Use `@cluster` / `@cluster(N)` as the S01 proof surface, keep `clustered(work)` parser-compatible until S04, and force both source spellings through one mesh-pkg declaration/export-surface path that carries source provenance and replication counts. | This lets the slice deliver source-first compiler/LSP truth immediately without mixing in repo-wide scaffold/docs churn, and it prevents meshc and mesh-lsp from drifting by making them consume the same validated declaration/export-surface seam. | Yes | collaborative |
| D269 | M047/S01/T01 | parser-ast | How clustered function declarations are represented while `@cluster` and `clustered(work)` coexist | Keep distinct CST node kinds for `CLUSTER_DECORATOR_DECL` and `CLUSTERED_WORK_DECL`, but expose a shared AST-level `ClusteredDecl` wrapper on `FnDef` that reports syntax style, semantic kind, explicit replica count, and declaration span. | Later compiler, mesh-pkg, and LSP work need semantic clustered metadata without reparsing raw tokens, but tests and compatibility work still need to distinguish the new source-first spelling from the legacy compatibility spelling. Separate CST nodes preserve syntax truth; the shared AST wrapper removes downstream token peeking. | Yes | agent |
| D270 | M047/S01/T02 | architecture | Where clustered declaration provenance/count metadata and export-surface construction live for M047 source-first clustered declarations | Store resolved replication-count plus declaration origin/provenance in mesh-pkg metadata/errors, and expose one shared `build_clustered_export_surface(...)` helper from mesh-pkg for public work functions and service-generated handlers. | This keeps meshc and mesh-lsp from re-deriving different clustered answers, preserves source file/range context for later diagnostics work, and gives downstream tasks one stable seam for both source declarations and exported clustered targets. | Yes | agent |
| D271 | M047/S01/T03 | compiler | How clustered replication-count metadata should flow through meshc T03 without changing runtime behavior | Keep `replication_count` and `origin` on `ClusteredExecutionMetadata` / `PreparedBuild`, but keep the codegen-facing declared-handler plan limited to `runtime_registration_name` and `executable_symbol`. | The slice only needs source-first planning truth and source-ranged diagnostics. Keeping the richer metadata on meshc’s prepared build preserves count/origin observability for validation while avoiding any runtime registration or startup behavior change in mesh-codegen. | Yes | agent |
| D272 | M047/S01/T04 | tooling/lsp | How mesh-lsp should anchor source-origin clustered declaration diagnostics for the M047 source-first model | Reuse mesh-pkg provenance, but emit source-origin clustered validation issues only on the currently analyzed relative file and convert the stored declaration byte span directly into the LSP range | S01 proved that shared mesh-pkg provenance is sufficient for real clustered declaration ranges, but without the per-file gate mesh-lsp turns decorated-source failures into phantom project-level diagnostics on unrelated files. Converting the recorded span directly preserves the declaration line without rebuilding a second local range source. | Yes | agent |
| D273 | M047/S02 planning | continuity | M047/S02 runtime seam for source-first clustered replication counts | Use declared-handler registration as the single runtime source of truth for clustered replication counts, derive `required_replica_count = replication_count - 1` from that metadata for startup/direct submit/recovery, and fail closed when requested counts or topology exceed the current one-replica runtime’s honest capability. | S01 already preserves source-first replication counts through mesh-pkg and meshc planning. Carrying that count through declared-handler registration avoids a second startup-only registry, keeps runtime/CLI truth keyed by the same runtime name executors and operator tooling already use, and prevents the runtime from silently clipping `@cluster(N)` down to the legacy single-replica behavior. | Yes | agent |
| D274 | M047/S02/T01 | runtime | Where replication-count metadata lives for ordinary clustered declarations | Store replication_count on declared-handler registrations keyed by runtime name, while leaving startup-work registration as a name-only list | The runtime already resolves ordinary clustered work through the declared-handler registry keyed by runtime name. Carrying replication_count on that existing registry keeps the ABI/source-of-truth singular for submit/startup/recovery lookups and avoids inventing a second startup-only metadata table. Startup registration still only needs ordering/identity. | Yes | agent |
| D275 | M047/S02/T02 | runtime | How M047/S02 maps public clustered replication counts onto the current continuity runtime | Continuity records now store the public replication_count, derive required_replica_count from declared-handler metadata, and durably reject unsupported fanout (>1 required replica) inside the continuity state machine rather than pre-submit validation; startup keeps the existing single-node route-free carveout by relaxing the default two-copy request to zero required replicas only when no peer was observed. | This preserves honest runtime truth for @cluster/@cluster(N), keeps explicit rejection records queryable through operator/CLI surfaces, and avoids silently clipping @cluster(3) down to the current single-replica implementation while still keeping the established single-node startup contract green. | Yes | agent |
| D276 | M047/S02 | requirement | R098 | validated | S02 carried replication_count from source-declared `@cluster` metadata through declared-handler registration, continuity records, runtime/CLI output, and the new `e2e_m047_s02` proof rail. Bare `@cluster` now defaults to `2`, explicit `@cluster(3)` stays visible as `3`, and unsupported higher fanout is rejected durably instead of being silently clipped. | Yes | agent |
| D277 | M047/S03 planning | runtime | How clustered HTTP routes should return `Response` values in M047/S03 | Make the clustered route shim use a synchronous service-style reply transport for `Request -> Response` delivery instead of layering submit/status polling or app-managed continuity loops onto HTTP handlers. | S02 continuity submit/status surfaces are truthful for clustered work state, but they are not a request/response transport. A service-style reply path keeps the HTTP route handler as the clustered boundary, preserves natural downstream handler code, and leaves failure diagnosis in runtime-owned continuity/diagnostic surfaces. | Yes | agent |
| D278 | M047/S03 planning | architecture | How `HTTP.clustered(...)` should integrate with HTTP routing in M047/S03 | Implement `HTTP.clustered(...)` through compiler-generated bare route shims plus synthetic declared-handler registrations, reusing the existing runtime-name/replication-count registry and explicitly leaving generic route-closure ABI widening out of S03. | The repo still proves that generic closure routes fail live while plain handler registration stays stable. Generated bare shims let S03 deliver route-local clustering through the same declared-handler truth surface as ordinary `@cluster` functions without accidentally turning the slice into a broad route-ABI redesign. | Yes | agent |
| D279 | M047/S03 | verification | How to interpret the `m047_s03` Cargo filters during M047/S03 closeout | Treat `cargo test -p mesh-typeck m047_s03 -- --nocapture`, `cargo test -p mesh-codegen m047_s03 -- --nocapture`, and `cargo test -p mesh-rt m047_s03 -- --nocapture` as presence checks only; use the missing `cargo test -p meshc --test e2e_m047_s03 -- --nocapture` target plus absent `HTTP.clustered(...)` symbols as the authoritative blocker signal. | On the current tree the typeck/codegen/runtime filters exit 0 while running zero tests, so they do not prove clustered HTTP route-wrapper implementation. Recording that interpretation keeps future closeout work from misreading empty filter matches as green coverage. | Yes | agent |
| D280 | M047/S04 planning | architecture | M047/S04 clustered cutover boundary | End the compatibility bridge in code and public dogfood surfaces by rejecting `clustered(work)` and `[cluster]`, keep the canonical route-free examples/scaffold on `@cluster pub fn execute_declared_work(...)` so runtime-name continuity stays `Work.execute_declared_work` without a helper, and keep docs/verifiers explicit that `HTTP.clustered(...)` is still unshipped. | D268 explicitly kept legacy compatibility only until S04, R102 requires removing the old public surface instead of teaching both models, S02 already proved runtime-name and replication-count truth do not require `declared_work_runtime_name()`, and S03 closed as a blocker without a real clustered HTTP route wrapper. The honest cutover is therefore source-first ordinary clustered functions plus migrated dogfood/docs/verifier rails, not a docs-only cleanup or an over-claimed HTTP story. | Yes | agent |
| D281 | M047/S04/T04 | testing | How historical route-free e2e rails pin the clustered source contract | Define shared `@cluster` contract strings in `compiler/meshc/tests/support/m046_route_free.rs` and reuse them from the M045/M046 historical rails instead of duplicating helper/marker wording in each test. | The task was a hard cutover from legacy `clustered(work)` and helper text to one source-first `@cluster` contract. Centralizing the stable source declaration, runtime-name guidance, and autostart guidance keeps the historical rails aligned on one contract and makes future wording or scaffold/package drift fail in one place instead of splintering across multiple exact-string tests. | Yes | agent |
| D282 | M047/S04/T06 | verification | Authoritative clustered cutover verifier ownership after M047/S04 | Use scripts/verify-m047-s04.sh as the single authoritative cutover rail and demote scripts/verify-m046-s04.sh, verify-m046-s05.sh, verify-m046-s06.sh, verify-m045-s04.sh, and verify-m045-s05.sh to replayable compatibility aliases that delegate to it. | The slice goal is to end the mixed clustered authoring story. Keeping multiple verifier rails as coequal authorities would preserve the same ambiguity in the proof surface. One M047-owned rail now replays parser/pkg/scaffold/package/docs proofs, retains one coherent .tmp/m047-s04 bundle, and lets historical script names survive without duplicating obsolete logic. | Yes | agent |
| D283 | M047/S04 | requirement | R103 | validated | M047/S04 migrated the repo-owned clustered dogfood surfaces to the source-first model: `meshc init --clustered`, `tiny-cluster/`, `cluster-proof/`, historical route-free rails, README/docs snippets, and the authoritative cutover verifier now all use `@cluster pub fn execute_declared_work(...)` and reject legacy helper/manifest markers. The full slice verification matrix and `bash scripts/verify-m047-s04.sh` passed. | Yes | agent |
| D284 | M047/S04 | requirement | R097 | validated | M047/S04 completed the public syntax hard cutover. `cargo test -p mesh-parser m047_s04 -- --nocapture`, `cargo test -p mesh-pkg m047_s04 -- --nocapture`, `cargo test -p meshc --test e2e_m047_s01 -- --nocapture`, the migrated scaffold/package/docs rails, and `bash scripts/verify-m047-s04.sh` now prove `@cluster` / `@cluster(N)` are the supported public clustered declaration surface while `clustered(work)` is rejected with migration guidance. | Yes | agent |
| D285 | M047/S04 | requirement | R103 | validated | M047/S04 migrated the repo-owned clustered dogfood surfaces to the source-first model: `meshc init --clustered`, `tiny-cluster/`, `cluster-proof/`, historical route-free rails, README/docs snippets, and the authoritative cutover verifier now all use `@cluster pub fn execute_declared_work(...)` and reject legacy helper/manifest markers. The full slice verification matrix and `bash scripts/verify-m047-s04.sh` passed. | Yes | agent |
| D286 | M047/S04 | requirement | R102 | validated | M047/S04 removed the old public clustered surface instead of teaching both models. Parser/pkg/compiler diagnostics now fail closed on legacy `clustered(work)` and `[cluster]`, scaffold/examples/docs/readmes were migrated to `@cluster`, historical wrapper rails were repointed, and the assembled cutover rail `bash scripts/verify-m047-s04.sh` passed with retained artifacts. | Yes | agent |
| D287 | M047/S04 | runtime | How startup automatic recovery should compute required replicas after promotion | For `startup::...` request keys, automatic recovery reuses the startup single-node replica relaxation before re-submitting declared work instead of feeding the raw declared-handler replica requirement back into `submit_declared_work(...)`. | During M047/S04 closeout, the historical `e2e_m046_s03` rail exposed a deterministic post-promotion regression: the promoted standby could reject its own startup recovery with `replica_required_unavailable` even though promotion succeeded. Reusing the existing startup relaxation keeps single-node promoted recovery truthful without widening the public clustered contract. | Yes | agent |
| D288 | M047/S05 planning | architecture | Public clustered function syntax after the user's S05 correction | Treat `@cluster` functions as ordinary user-authored Mesh functions with no public `request_key` / `attempt_id` ceremony and no scaffold-owned `execute_declared_work(...)` special case; runtime continuity metadata must move behind compiler-generated adapters, and both the corrected route-free scaffold and the new Todo starter must dogfood that contract. | The user explicitly rejected the current public clustered syntax and wants source like `@cluster` followed by an ordinary function definition such as `pub fn add() -> Int`. The current tree still hard-codes `(request_key, attempt_id)` into declared-work wrappers and route-free scaffold/example surfaces, so S05 must remove that public ceremony before adding the Todo starter or it will preserve the wrong contract in new scaffolds and docs. | Yes | human |
| D289 | M047/S05/T06 override application | architecture | Canonical public function shape for repo-owned clustered examples and generated clustered scaffolds | Use ordinary `@cluster` function names like `add()` or domain-specific verbs in repo-owned examples, scaffolds, and docs; do not keep `execute_declared_work(...)` as a public example or scaffold convention. Continuity metadata and any stable runtime naming remain compiler/runtime-owned, not user-authored. | The S04 cutover removed `clustered(work)` but froze an interim public shape around `execute_declared_work(...)`, which still reads like proof-app plumbing instead of an ordinary Mesh function. The user explicitly rejected that shape. Leaving it in `tiny-cluster/`, `cluster-proof/`, scaffold output, and docs would preserve the wrong mental model. This decision supersedes the interim S04 dogfood choice in D280, D283, and D285 where those rows treated `execute_declared_work(...)` as the canonical public example shape. | Yes | human |
| D290 | M047/S05 | requirement | R104 | validated | `cargo test -p mesh-pkg m047_s05 -- --nocapture`, `cargo test -p meshc --test tooling_e2e test_init_clustered_todo_ -- --nocapture`, `cargo test -p meshc --test e2e_m047_s05 -- --nocapture`, `bash scripts/verify-m047-s05.sh`, and `npm --prefix website run build` now prove the Todo template generates a SQLite-backed API with real routes, actor-backed rate limiting, restart-persistent state, and a Docker build path. | Yes | agent |
| D291 | M047/S05 | requirement | R105 | validated | The generated scaffold, repo examples, README/docs, and the named S05 rails now teach ordinary `@cluster` functions like `add()` / `sync_todos()` instead of `execute_declared_work(...)`, and the native Todo proof (`cargo test -p meshc --test e2e_m047_s05 -- --nocapture`) shows the starter behaves like a real clustered starting point rather than a proof-app wrapper. | Yes | agent |
| D292 | M047/S05 | launchability | How the S05 Todo scaffold Dockerfile should package the app while the public binary installer lags the current `@cluster` scaffold contract | Package the Todo image from the local `meshc build .` output binary instead of installing Mesh inside the Docker build | Native S05 proof now depends on the current source-first `@cluster` compiler path, while the latest public installer release cannot compile the generated scaffold. Packaging the already-built `./output` binary keeps the Docker path honest, keeps the scaffold self-contained, and avoids pretending a public installer-in-container workflow already supports the unreleased clustered Todo contract. | Yes | agent |
| D293 | M047/S06 planning | verification | How M047/S06 should layer the final clustered closeout proof and docs authority | Keep `scripts/verify-m047-s04.sh` as the authoritative cutover subrail, treat `scripts/verify-m047-s05.sh` as the delegated Todo/native+container runtime subrail, and add `scripts/verify-m047-s06.sh` as the only authoritative assembled closeout rail. Public docs must keep route-free `@cluster` surfaces canonical, present the Todo template as the fuller starter, and explicitly say `HTTP.clustered(...)` remains unshipped. | S04 and S05 already own distinct truthful proof boundaries. Folding them together or replacing them would either reopen the legacy cutover story, hide the lower-level Todo/runtime evidence, or overclaim route-local clustered HTTP support. A layered S06 wrapper plus additive docs updates closes R104/R105/R106 while preserving R099/R102/R103 and keeping R100/R101 explicitly out of scope. | Yes | agent |
| D294 | M047 S06 T02 | docker | How non-Linux hosts should prove the Todo scaffold runtime image truthfully | Keep the generated Todo Dockerfile as a prebuilt-`output` packager, and on non-Linux hosts emit `./output` from a Linux builder stage before building/running the runtime image. | The generated runtime image is supposed to package an already-built app binary, not compile Mesh inside the runtime container. Local proof showed that a host-native macOS `./output` can still make `docker build` succeed while the Linux container would never run it truthfully. Reusing the repo's Linux builder stage to produce `./output` preserves the source contract, keeps the runtime image honest, and makes the failure mode observable via retained builder/output artifacts. | Yes | agent |
| D295 | M047 S06 T03 | observability | Final M047 S06 closeout proof surface ownership | Keep `scripts/verify-m047-s06.sh` as a wrapper that replays `scripts/verify-m047-s05.sh`, copies the delegated verify bundle into `.tmp/m047-s06/verify`, and fails closed on malformed retained handoff instead of sharing or mutating `.tmp/m047-s05/verify` directly. | This preserves the historical S04 cutover rail and S05 Todo/runtime subrail as independent truth surfaces while giving S06 one assembled authority bundle with explicit status, phase, and retained-bundle pointers future agents can inspect without ambiguity. | Yes | agent |
| D296 | M047/S06/T01 | compiler/codegen | How generic Result/Option variant construction should lower scalar payloads for built-package helper rewraps | Box non-pointer payloads before storing them in generic `{i8, ptr}` sum-variant slots so helper-shaped `Ok(...)` / `Some(...)` reconstruction stays ABI-safe. | The built-package SQLite seam failed because helper functions returning generic Result layouts stored raw scalar success payloads like `i64` in the pointer slot, so `meshc build` binaries could `Sqlite.open(...)` but crashed or misused the API on `Sqlite.execute(...)`. Boxing non-pointer payloads at variant construction fixes the root cause without scaffold-specific workarounds, and the focused regression `cargo test -p meshc --test e2e_sqlite_built_package -- --nocapture` now proves CREATE/INSERT/read-back/mismatch handling through retained `.tmp/m047-s06/sqlite-built-package-execute-*` artifacts. | Yes | agent |
| D297 | M047/S06/T01 | verification | How S06 should guard the built-package SQLite execute/query ABI contract | Use a dedicated manifest-backed built-package SQLite regression with retained `.tmp` artifacts instead of relying on broader Todo/native-container proof to catch the seam. | The failing surface was not Todo-specific: a package emitted by `meshc build` could diverge from the in-process `compile_and_run` path even when the higher-level scaffold still looked plausible. A focused rail keeps the failure localized to the AOT lowering/runtime ABI contract, captures the built binary and logs under `.tmp/m047-s06/sqlite-built-package-execute-*`, and prevents future closeout work from masking the seam behind broader scaffold retries or docs-only passes. | Yes | agent |
| D298 | M047/S06 | requirement | R106 | validated | M047/S06 closed the public docs and assembled proof authority on one source-first clustered story. `cargo test -p meshc --test e2e_m047_s06 -- --nocapture`, `bash scripts/verify-m047-s06.sh`, and the passing docs build retained under `.tmp/m047-s06/verify/` now prove README/VitePress pages, migration guidance, and the final closeout verifier consistently teach route-free `@cluster` surfaces, the Todo starter as the fuller layered example, and the explicit non-goal that `HTTP.clustered(...)` is still unshipped. | Yes | agent |
| D299 | M047/S07/T01 | compiler | How the HTTP.clustered route wrapper is represented during typechecking | Keep HTTP.clustered(handler) typed as the underlying handler function while recording wrapper metadata keyed by wrapper call span and validating route-slot consumption in a final post-inference sweep. | This preserves the existing direct and pipe-form HTTP route inference, avoids inventing a fake helper type that would leak through the type system, and still yields focused wrapper-specific diagnostics for bad arguments, conflicting counts, missing imported origins, and non-route-position use. | Yes | agent |
| D300 | M047/S07/T02 | compiler/lowering | How clustered HTTP route wrappers connect MIR lowering to declared-handler registration | Lower `HTTP.clustered(...)` directly to deterministic `__declared_route_<runtime_name>` bare shims and derive route declared-handler plan entries from typecheck wrapper metadata instead of teaching manifest/source clustered-declaration collection about route wrappers. | This lets direct and piped route forms share one lowering path, preserves the real handler runtime name and replication count from typecheck, keeps route shims alive through merge via explicit executable symbols, and preserves the existing startup-work filter by introducing a distinct declared-handler kind rather than overloading `Work`. | Yes | agent |
| D301 | M047 S07 T03 | runtime | How clustered HTTP routes attach runtime metadata and perform request/response execution without widening generic declared-work spawn arguments | Reverse-look up declared-handler registrations by route shim function pointer at router registration time, store the runtime name on the route entry, and execute clustered HTTP routes through a dedicated transient request/response transport that reuses continuity submission/completion truth. | This keeps `HTTP.clustered(...)` on the shared declared-handler registry/runtime-name seam, preserves the route shim itself as the clustered boundary, avoids widening generic remote-spawn arg tags for request/response payloads, and lets unsupported explicit fanout reject durably before any handler work starts. | Yes | agent |
| D302 | M047/S07 | verification | How M047/S07 should prove the clustered HTTP route wrapper truth surface | Use a dedicated `cargo test -p meshc --test e2e_m047_s07 -- --nocapture` two-node temp app that diffs continuity by `request_key` + `declared_handler_runtime_name`, retains the full `.tmp/m047-s07/clustered-http-routes-two-node/` bundle, and keeps `cargo test -p meshc --test e2e_stdlib e2e_m032_route_ -- --nocapture` as the unchanged route-closure guardrail. Leave scaffold/docs adoption to S08. | The new wrapper had to be proven at the compiler/runtime boundary itself, not indirectly through scaffold/docs adoption. Continuity list ordering is not stable enough to use as proof when repeated requests share one runtime name, so the honest evidence path is request-key diffing plus retained cluster/HTTP artifacts. Keeping the existing M032 controls separate preserves the historical guarantee that shipping `HTTP.clustered(...)` does not accidentally widen generic route-closure support. | Yes | agent |
| D303 | M047/S07 | requirement | R101 | validated | M047/S07 proves the route handler itself is the clustered boundary. The lowering rewrites `HTTP.clustered(...)` to deterministic bare route shims that preserve the real handler runtime name, and the runtime/e2e rails (`cargo test -p mesh-rt m047_s07 -- --nocapture` plus `cargo test -p meshc --test e2e_m047_s07 -- --nocapture`) show continuity records and live HTTP requests keyed to `Api.Todos.handle_list_todos` / `Api.Todos.handle_retry_todos`, with unsupported explicit fanout rejecting durably before any downstream helper executes. | Yes | agent |
| D304 | M047/S07 | requirement | R100 | validated | M047/S07 shipped `HTTP.clustered(handler)` / `HTTP.clustered(N, handler)` as a real compiler/runtime-backed route wrapper. `cargo test -p mesh-typeck m047_s07 -- --nocapture`, `cargo test -p mesh-lsp m047_s07 -- --nocapture`, `cargo test -p mesh-codegen m047_s07 -- --nocapture`, `cargo test -p mesh-rt m047_s07 -- --nocapture`, and `cargo test -p meshc --test e2e_m047_s07 -- --nocapture` now prove route-local clustering typechecks, lowers, executes, and reports continuity truth through the shipped wrapper surface. | Yes | agent |
| D305 | M047/S08 planning | architecture | How S08 should dogfood the shipped clustered HTTP wrapper in the public starter and closeout rails | Adopt `HTTP.clustered(1, handler)` on selected Todo scaffold read routes, keep `work.mpl` route-free, and prove the adoption through single-node native/Docker cluster-mode continuity rails while leaving default-count / two-node wrapper semantics under the existing S07 authority. | Research confirmed that plain `HTTP.clustered(handler)` returns `replica_required_unavailable` on a one-node starter, while `HTTP.clustered(1, handler)` keeps the local and Docker starter flows honest and still records truthful continuity metadata keyed to the real route handler runtime name. This preserves the general source-first clustered model, removes stale public authority that says the wrapper is unshipped, and lets the final docs/verifier adopt the shipped wrapper without turning the starter into a mandatory two-node proof app. | Yes | agent |
| D306 | M047/S08/T03 | verification | Authoritative operator seam for Docker single-node clustered-route proofs | Query `meshc cluster status\|continuity` from a helper container that shares the target container's network namespace instead of from the host-published cluster port. | The runtime binds the node listener to the host portion of `MESH_NODE_NAME`; with `@127.0.0.1` inside Docker, host-side queries hit Docker's published port but fail the transient operator handshake with EOF, while a same-netns helper reaches the authoritative listener without changing scaffold/runtime behavior or weakening fail-closed assertions. | Yes | agent |
| D307 | M047/S08/T04 | public-docs | How the public clustered docs and closeout rails should describe shipped HTTP.clustered adoption | Keep the route-free `@cluster` surfaces canonical; only claim explicit-count `HTTP.clustered(1, ...)` adoption on the Todo starter's selected read routes, and point default-count/two-node wrapper behavior at the repo S07 rail (`cargo test -p meshc --test e2e_m047_s07 -- --nocapture`). | The shipped wrapper is real in the Todo starter, but broad `HTTP.clustered(...)` claims would blur the canonical route-free clustered story and overstate what the starter and closeout rails actually prove. Narrowing the public claim preserves truthful authority boundaries while still documenting the recovered wrapper adoption. | Yes | agent |
| D308 | M048 bootstrap | docs | How public docs should treat proof-oriented material during the reset wave | Remove proof-rail and verifier-map material from the primary public docs experience while keeping internal proof rails in the repo. | The user wants evaluator-facing docs instead of a proof-maze. Public docs should stay user-facing, while deeper verifier and milestone proof surfaces remain available internally without being the default learning path. | Yes | human |
| D309 | M048 bootstrap | prioritization | Who the public-facing Mesh reset serves first when goals compete | Optimize the public rewrite for new evaluators first, with scaffold/examples as the first story and deeper reference material second. | The user explicitly chose new evaluators as the priority audience. That sets the ordering for docs, examples, landing, and packages surfaces. | Yes | human |
| D310 | M048 bootstrap | api | Entrypoint model for executable Mesh projects | Keep `main.mpl` as the simple default executable entrypoint and add an optional manifest override for alternate entry paths. | The user wants layout flexibility without turning every project into explicit configuration. Default-plus-override preserves the simple path while removing the hardcoded root-`main.mpl` prison. | Yes | human |
| D311 | M048 bootstrap | tooling | Meaning of update commands for Mesh tooling | `meshc update` and `meshpkg update` mean binary self-update through the existing release/install path. | The user wants explicit update commands for the installed toolchain itself, not just project dependency updates. Reusing the existing release/install path keeps the toolchain lifecycle singular and trustworthy. | Yes | human |
| D312 | M048 bootstrap | architecture | Public example hierarchy after the reset wave | Replace `tiny-cluster` and `cluster-proof` as public teaching surfaces with checked-in generated examples; keep Mesher as the deeper secondary real reference app rather than the primary beginner path. | The user wants scaffold/examples first, Mesher second, and the older proof apps out of the public onboarding story. This removes the current coequal proof-surface sprawl without erasing deeper real-app reference material. | Yes | human |
| D313 | M048 bootstrap | architecture | Truth boundary for the SQLite scaffold's Fly deployment proof | Treat the SQLite Fly proof as truthful single-writer, node-affined storage semantics rather than shared durable clustered SQLite. | The user wants a real two-node Fly proof, but not a dishonest storage story. Fly volumes are machine-local, so the public proof must stay explicit about single-writer or node-affined persistence unless a real replication layer is added later. | Yes | human |
| D314 | M048 bootstrap | architecture | Scope of the load-balancing deep dive milestone | The load-balancing milestone includes both analysis and implementation follow-through if the current Fly-plus-runtime story is insufficient. | The user does not want a documentation-only answer if the current clustered routing story is weak. The milestone should explain current behavior honestly and then carry real implementation work if needed. | Yes | human |
| D315 | M048/S01 planning | tooling | Entrypoint override config shape and shared resolver seam for compiler/test discovery | Use an optional `[package].entrypoint` project-root-relative path in `mesh.toml`, resolve it once against the project root for both build and test flows, and have `meshc test` rewrite execution back to synthetic `main.mpl` while excluding the resolved project entry file from copied sources. | This keeps Mesh on its existing package-only manifest shape, gives build and test one boring shared executable-entry contract instead of duplicated path logic, and preserves the current test-runner model where generated test code replaces the project executable regardless of filename. | Yes | agent |
| D316 | M048/S01/T03 | compiler/codegen | How the compiler should preserve manifest-selected entrypoints through MIR merge when multiple modules lower a `main` function. | Merge MIR modules by processing the designated entry module before deduplicating function symbols so the resolved executable entry wins when multiple source files define `fn main()`. | The new M048/S01 acceptance rail proved discovery and test-runner entry resolution were correct, but builds still executed the legacy root `main.mpl` when both root and override entry files existed because every source-level `main` lowered to the shared `mesh_main` MIR symbol and merge deduped by first-seen order. Prioritizing the designated entry module at merge time fixes the real executable-selection seam without widening the language or manifest contract. | Yes | agent |
| D317 | M048/S01 | compiler/discovery | How non-root executable entry modules are named during discovery | Only root `main.mpl` keeps the special `Main` module name; manifest-selected non-root entry files remain path-derived modules even when marked executable. | S01 needed entrypoint overrides without turning executable selection into a second module-naming contract. Keeping non-root entries path-derived preserves import/module-graph truth while still allowing build and test discovery to mark the resolved file as executable. | Yes | agent |
| D318 | M048/S02 | tooling | How entrypoint-aware project roots should resolve across LSP and editor hosts | Prefer the nearest mesh.toml as the project root, resolve the executable entry through mesh_pkg::manifest::resolve_entrypoint(...), keep non-root entry modules path-derived, and only fall back to legacy root main.mpl / .git / single-file behavior when no manifest root exists. | S01 made executable entrypoints configurable, but override-entry projects still degraded in tooling when roots were anchored on main.mpl or analysis fell back to single-file mode. Sharing manifest-first root detection and entry resolution across mesh-lsp, meshc lsp, Neovim, and VS Code keeps D317 intact and makes failures surface as explicit project diagnostics instead of bogus local import errors. | Yes | agent |
| D319 | M048/S02 | packaging | What source tree meshpkg publish should archive for executable Mesh packages | Make meshpkg publish archive mesh.toml plus every non-hidden, non-.test.mpl Mesh source discovered recursively from the project root under its project-root-relative path, instead of assuming executables live only at root main.mpl or inside src/. | Override-entry projects can place lib/start.mpl and support modules anywhere in the source tree. Reusing Mesh source-discovery rules for tarball membership keeps published packages truthful to build/LSP/test behavior while still excluding hidden and test-only content. | Yes | agent |
| D320 | M048/S03 planning | tooling | How `meshc update` and `meshpkg update` should reuse the installer path without inventing a second updater protocol | Implement a shared `mesh_pkg::toolchain_update` helper that downloads the public platform installer, honors a dedicated `MESH_UPDATE_INSTALLER_URL` staging override plus the existing `MESH_INSTALL_*` installer overrides, refreshes the whole toolchain pair, and makes `meshpkg --json update` fail closed instead of streaming installer prose as JSON. | D311 requires update commands to mean toolchain self-update through the existing release/install path. A shared helper keeps both CLIs on one updater seam, preserves the existing checksum/archive/install contract, gives the acceptance rail a deterministic local installer override, and avoids inventing a misleading partial JSON protocol for streamed installer output. | Yes | agent |
| D321 | M048/S03/T01 | tooling | How mesh self-update launches the canonical installer across platforms | Run the downloaded installer text through `/bin/sh -s -- --yes` on Unix, but on Windows write `install.ps1` plus a bootstrap PowerShell script and return after spawning the bootstrap so the invoking `.exe` can exit before replacement. Forward only `MESH_INSTALL_RELEASE_API_URL`, `MESH_INSTALL_RELEASE_BASE_URL`, `MESH_INSTALL_DOWNLOAD_TIMEOUT_SEC`, and `MESH_INSTALL_STRICT_PROOF` unchanged to the child process, with `MESH_UPDATE_INSTALLER_URL` reserved as the fetch-time override. | This keeps the public installer scripts as the single source of truth, avoids re-implementing release metadata/checksum/install semantics in Rust, and makes the Windows self-overwrite constraint explicit and testable instead of assuming an in-place overwrite can succeed while the CLI binary is still running. | Yes | agent |
| D322 | M048/S03 | verification | How M048/S03 should prove installer-backed toolchain self-update truthfully | Keep `compiler/meshc/tests/e2e_m048_s03.rs` as the retained acceptance rail: host the public `install.sh` plus staged release metadata/assets from a local static server, run `meshc update` from a staged binary outside `~/.mesh/bin`, run `meshpkg update` from an installed fake-home binary, and assert whole-toolchain repair (`meshc`, `meshpkg`, `~/.mesh/version`) while checking credentials by presence/size only. | S03 is only honest if the new commands refresh the same toolchain pair through the same installer/release path users already trust. A local staged server plus fake HOME keeps the proof deterministic, exercises the real installer contract end to end, retains diagnosable `.tmp/m048-s03/...` artifacts, and proves credential preservation without ever reading or printing credential contents. | Yes | agent |
| D323 | M048/S03 | requirement | R113 | validated | M048/S03 shipped explicit `meshc update` and `meshpkg update` commands on the shared installer-backed updater seam, proved the CLI surfaces with `cargo test -p meshc --test tooling_e2e test_update -- --nocapture` and `cargo test -p meshpkg --test update_cli -- --nocapture`, and proved end-to-end staged and installed self-update behavior with `cargo test -p meshc --test e2e_m048_s03 -- --nocapture` plus the focused updater rail `cargo test -p mesh-pkg --test toolchain_update -- --nocapture`. | Yes | agent |
| D324 | M048/S04 planning | verification | How S04 should prove `@cluster` editor parity and init-skill truth without widening older interpolation contracts | Add one dedicated `@cluster` decorator fixture/probe reused by the retained TextMate/Shiki and Neovim syntax rails, and add a dedicated Mesh skill contract test for clustered/runtime guidance instead of widening the interpolation corpus or relying on ad hoc manual review. | The existing interpolation rails already pass and are tightly pinned to the M036 corpus contract. Reusing one small decorator fixture keeps the bare-`cluster` negative case explicit, minimizes blast radius across exact-count corpus tests, and gives both editor surfaces the same syntax oracle. A dedicated skill contract test adds the missing drift tripwire for the auto-loaded Mesh skill bundle so first-contact clustered guidance cannot silently regress again. | Yes | agent |
| D325 | M048/S04/T03 | skill-bundle | How the Mesh init-time skill bundle should teach clustered runtime guidance | Centralize clustered runtime truth in `tools/skill/mesh/skills/clustering/SKILL.md`, keep `tools/skill/mesh/skills/syntax/SKILL.md` and `tools/skill/mesh/skills/http/SKILL.md` as bounded cross-links for decorator and routed-HTTP details, and enforce the contract with `scripts/tests/verify-m048-s04-skill-contract.test.mjs`. | The slice's 10x drift risk is duplicated clustered wording across the auto-loaded skill bundle. A dedicated clustering sub-skill gives one canonical source for `@cluster`, `Node.start_from_env()`, scaffold/init commands, operator CLI commands, and `HTTP.clustered(...)` boundaries, while the retained verifier fails closed if stale pre-reset guidance or missing cross-links reappear. | Yes | agent |
| D326 | M048/S04 | requirement | R114 | validated | S04 closed the remaining editor/skill parity gap with green retained proofs: `bash scripts/verify-m036-s01.sh`, `NEOVIM_BIN="${NEOVIM_BIN:-nvim}" bash scripts/verify-m036-s02.sh syntax`, `node --test scripts/tests/verify-m036-s02-contract.test.mjs`, and `node --test scripts/tests/verify-m048-s04-skill-contract.test.mjs` together prove `@cluster` / `@cluster(N)` highlighting, both interpolation forms, and clustering-aware init-time Mesh skill guidance while keeping bare `cluster` an identifier. | Yes | agent |
| D327 | M048/S05 planning | verification | S05 closeout proof architecture | Use a dedicated Node docs-contract test for the minimal public touchpoints and a shell verifier that composes the retained S01-S04 rails, docs build, and retained artifact bundling instead of introducing a new Rust-only closeout harness. | The missing work in S05 is public-truth drift plus final assembly, not new product behavior. A small Node contract test fails fast on README/tooling/VS Code wording drift, while a named shell verifier can reuse the already validated S01-S04 rails, preserve their diagnosable artifact shapes, and keep the milestone closeout entrypoint lightweight and honest. | Yes | agent |
| D328 | M048/S05 | requirement | R112 | validated | S05's green assembled replay (bash scripts/verify-m048-s05.sh) now replays the S01 entrypoint rail, the S02 manifest-first LSP/editor/package rails, the fast docs-contract test, and the website build together. That gives one retained proof bundle that main.mpl remains the default while override entrypoints like lib/start.mpl behave truthfully across build, test, analyze, publish, and first-contact docs. | Yes | agent |
| D329 | M048/S05 | documentation | Public VS Code README contract for override-entry support | Document same-file definition plus manifest-first hover/diagnostics proof only; omit cross-file definition claims | The real VS Code smoke and mesh-lsp rails prove clean diagnostics, hover, same-file definition, and manifest-first override-entry behavior, but imported cross-module goto_definition remains bounded. Keeping the README scoped to the proved surface prevents evaluator-facing overclaim. | Yes | agent |
| D330 | M048/S05 | verification | How the M048/S05 closeout wrapper replays editor rails | Replay the truthful S02 rails directly with NEOVIM_BIN handling and cargo-before-VS-Code ordering instead of delegating to scripts/verify-m036-s03.sh | The older wrapper still assumes a missing vendor Neovim path and does not own the required target/debug/meshc ordering for VS Code smoke. Replaying the retained rails directly keeps the assembled closeout entrypoint honest and fail-closed. | Yes | agent |
| D331 | M048/S05 | verification | How M048/S05 retains assembled proof artifacts | Copy fixed .tmp/m036-s02 and .tmp/m036-s03 directories directly, snapshot-copy fresh timestamped .tmp/m048-s01/* and .tmp/m048-s03/* buckets, and publish a stable latest-proof-bundle.txt pointer | The assembled verifier needs one diagnosable retained bundle without lying about artifact shape. Direct copies preserve stable editor artifacts, while snapshotting timestamped M048 directories keeps per-run entrypoint/update evidence attributable and inspectable from one stable pointer. | Yes | agent |
| D332 | M049 bootstrap | scope | What the public example and onboarding surface should be after the reset wave | Retire `tiny-cluster`, `cluster-proof`, and `reference-backend` as coequal public onboarding surfaces; move the public first-contact path to scaffold plus generated `/examples`, with Mesher as the deeper real app later in the sequence. | The user explicitly rejected keeping the old proof-app-shaped projects as public teaching surfaces and wants the repo to teach from approachable scaffold/example paths instead of a proof maze. | Yes — if execution proves a specific old surface still needs a temporary transitional role | human |
| D333 | M049 bootstrap | architecture | How the dual-database todo scaffold should divide its public contract | Treat SQLite as the easy local/single-node starter and Postgres as the serious clustered/deployable starter. | This preserves an honest simple starter while giving Mesh one serious shared-durable path. It avoids overclaiming clustered durability for SQLite and keeps the later deployment proof focused on the database that can carry it truthfully. | Yes — if a later replication layer gives SQLite a genuinely shared clustered durability story | collaborative |
| D334 | M049 bootstrap | pattern | How checked-in example apps should relate to `meshc init` output | Commit `/examples` as outputs generated from scaffold templates instead of hand-maintained showcase apps. | Generated examples keep the public example surface mechanically aligned with the scaffold and prevent a second drifting implementation story from appearing beside `meshc init`. | Yes — if a later milestone needs an explicitly non-generated showcase example in addition to the generated examples | collaborative |
| D335 | M050 draft gate | documentation | How public docs should separate distributed primitives from the newer clustered-app story | Keep low-level distributed primitives public, but make clustered apps and source-first `@cluster` examples the main evaluator path. | `Distributed Actors` and clustered apps are not the same abstraction level. Keeping the primitives public preserves the language surface, while leading evaluators through clustered apps first reduces confusion and proof-maze sprawl. | Yes — if later docs work shows a different split is clearer without reintroducing confusion | collaborative |
| D336 | M052/M053 draft gates | architecture | What it means for the packages surface to deploy with the rest of Mesh | Keep the packages site as its own deployed app, but make its verification and deployment part of the normal main CI/release evidence chain. | The packages site already has a separate deployment boundary, and collapsing it into the main site would widen scope unnecessarily. Unifying the evidence chain solves the real trust problem without hiding the deployment boundary. | Yes — if later public-surface work proves a single-site architecture is clearly better | collaborative |
| D337 | M054 draft gate | architecture | What public load-balancing model Mesh should target | Use a platform-agnostic server-side-first load-balancing story; treat Fly as the current proving environment, and defer frontend-aware adapters unless the deep dive proves they are needed. | The frontend should usually point at one public app URL while Mesh runtime and infrastructure handle routing. Starting from server-side truth keeps the public contract simpler and more portable than making every frontend topology-aware by default. | Yes — if the later deep dive shows server-side/runtime routing is not sufficient | collaborative |
| D338 | M049/S01 planning | architecture | How to stage the Postgres todo starter in M049/S01 | Add `meshc init --template todo-api --db postgres` alongside the existing SQLite todo scaffold in S01, and prove it through dedicated M049 generator/CLI/runtime test surfaces instead of reusing the old M047 SQLite runtime helper or rewriting broad onboarding docs in the same slice. | This keeps the slice additive, minimizes blast radius on the still-live SQLite story, and lets the Postgres path establish its own honest migration/build/runtime proof before later slices replace docs, examples, and proof-app onboarding surfaces. | Yes | collaborative |
| D339 | M049/S01/T01 | architecture | todo-api database selection seam for `meshc init` | Use a typed `TodoApiDatabase` selector in `mesh-pkg`, keep `scaffold_todo_api_project` as the SQLite-default wrapper, and make the dedicated Postgres branch fail closed until the Postgres starter content lands. | The CLI now needs explicit `--db` validation without silently falling back to SQLite. A typed dispatcher keeps the legacy SQLite starter stable for existing tests while giving later M049 tasks a real Postgres-specific implementation seam. Returning an explicit error from the Postgres branch is more honest than aliasing `--db postgres` to the SQLite scaffold. | Yes | agent |
| D340 | M049/S01/T02 | architecture | How the Postgres todo-api starter handles schema creation and runtime configuration visibility | Keep the starter migration-first with external `meshc migrate . up` ownership, and keep `DATABASE_URL` out of registry and `/health` surfaces. | The Postgres scaffold needs an honest deployable contract: startup should open a pool and serve HTTP, not create tables. Keeping schema changes in `migrations/` aligns the starter with the proven helper surface (`Migration`, `Repo`, `Query`, `Pg`) and avoids reviving startup DDL. Excluding `DATABASE_URL` from registry and health output preserves useful observability without leaking secrets. | Yes | agent |
| D341 | M049/S01 closeout | requirement | R115 | validated | M049/S01 proved both sides of the Todo starter contract: `cargo test -p meshc --test tooling_e2e test_init_todo_template_db_ -- --nocapture`, `cargo test -p meshc --test tooling_e2e test_init_todo_template_postgres_ -- --nocapture`, `cargo test -p meshc --test tooling_e2e test_init_clustered_todo_ -- --nocapture`, `cargo test -p mesh-pkg m049_s01_postgres_scaffold_ -- --nocapture`, `cargo test -p mesh-pkg m047_s05_scaffold_todo_api_project_ -- --nocapture`, and `set -a && . .tmp/m049-s01/local-postgres/connection.env && set +a && cargo test -p meshc --test e2e_m049_s01 -- --nocapture` now show `meshc init --template todo-api` supports both SQLite and Postgres with current Mesh runtime/ORM/migration patterns. | Yes | collaborative |
| D342 | M049/S01/T03 closeout | architecture | Postgres todo-api starter boot ordering for fail-closed startup | Validate config and connect the PostgreSQL pool before calling `Node.start_from_env()`, then boot the clustered runtime only on the success path. | The live M049/S01 negative rail showed that starting the clustered runtime first leaves keepalive/startup actors alive after `main` logs a missing `DATABASE_URL` error, so the generated process hangs instead of failing closed. Delaying runtime bootstrap until after config and pool readiness preserves the positive bootstrap logs on the happy path while making missing-env and connect-failure paths terminate promptly. | Yes | collaborative |
| D343 | M049/S02 planning | architecture | How to preserve the historical M047 clustered Todo proof while rewriting the public SQLite starter | Move the old clustered Todo contract behind a committed internal fixture copied by tests/verifiers, instead of preserving it through the public `meshc init --template todo-api` SQLite scaffold or adding a hidden legacy starter mode. | S02 has to make the public SQLite starter explicitly local and single-node. Keeping the old clustered Todo shape in the public scaffold would preserve the exact false durability story this slice is meant to remove, while a fixture-backed compatibility seam keeps the historical M047 native/Docker/runtime rails green without inventing another public contract surface. | Yes | collaborative |
| D344 | M049/S02/T01 | testing | How to preserve the historical M047 clustered SQLite Todo proof while the public todo-api starter changes in M049/S02 | Snapshot the old clustered Todo starter under scripts/fixtures/m047-s05-clustered-todo and make the M047 helper/verifier copy that committed fixture instead of invoking meshc init --template todo-api | The historical native/Docker proof needs a stable contract even as the public SQLite starter becomes local-only. A committed fixture preserves the old M047 surface, makes provenance explicit in retained artifacts, and lets the helper fail closed on missing or partial fixture copies instead of silently depending on evolving public scaffold output. | Yes | agent |
| D345 | M049 S02 T03 | testing | How the SQLite todo-api starter should be proven in generated package tests versus live acceptance rails | Keep generated SQLite package tests as compile-smoke/static contract checks, and prove CRUD, restart persistence, rate limiting, malformed ids, and bad-db-path behavior in the live e2e_m049_s02 harness. | Direct generated package-test calls into the SQLite storage helpers still hit Mesh compiler type-shape failures during meshc test, even though the generated starter builds and the live runtime path works correctly. Using the package tests for compile-surface coverage and the dedicated Rust e2e harness for real runtime behavior keeps both proof layers truthful and stable. | Yes | agent |
| D346 | M049/S02/T04 | documentation | How public docs should present todo-api starter modes | Spell the starter modes explicitly with `meshc init --template todo-api --db sqlite` for the honest local starter and `meshc init --template todo-api --db postgres` for the serious shared/deployable starter, keep `meshc init --clustered` as the canonical minimal clustered scaffold, and describe `scripts/verify-m047-s05.sh` as a retained historical clustered Todo rail instead of a public starter proof surface. | The generic `meshc init --template todo-api` wording blurred a local single-node SQLite starter with clustered/operator guidance. Explicit DB flags keep the public contract honest, point clustered/deployable guidance at the Postgres path, and preserve the retained fixture-backed M047 proof rail without overclaiming it as the current public starter. | Yes | agent |
| D347 | M049/S02 closeout | verification | How the retained M047 S05 wrapper should behave after the SQLite starter split | Replay only the M047 cutover rail, the fixture-backed e2e_m047_s05 rail, and the docs build before checking retained provenance and bundle shape | Once M049/S02 moved the historical clustered Todo contract behind a committed fixture, public mesh-pkg and tooling_e2e starter filters no longer measured that retained contract and could fail for unrelated SQLite-starter drift. The truthful wrapper scope is now: delegated M047 cutover replay, fixture-backed historical e2e replay, docs build, and explicit retained-artifact/provenance checks. | Yes | agent |
| D348 | M049/S02/T05 | documentation | How Mesh skills should teach todo-api starter modes after the SQLite/Postgres split | Point clustered guidance at `meshc init --clustered` and `meshc init --template todo-api --db postgres`, keep `meshc init --template todo-api --db sqlite` explicitly local-only, and make the retained M048 skill contract fail closed on generic todo-api or clustered-SQLite wording | The auto-loaded Mesh skills are a first-contact surface. Leaving generic `meshc init --template todo-api` language there would keep teaching the pre-S02 false clustered SQLite story even after the scaffold, README, and website docs were corrected. | Yes | agent |
| D349 | M049/S02 closeout | requirement | R115 | validated | M049/S02 completed the missing SQLite half of the dual-db starter contract on top of the already-green S01 Postgres half. The validating proof now includes `cargo test -p mesh-pkg m049_s02_sqlite_scaffold_ -- --nocapture`, `cargo test -p meshc --test tooling_e2e test_init_todo_template_db_sqlite_ -- --nocapture`, and `cargo test -p meshc --test e2e_m049_s02 -- --nocapture`, which complete the active `meshc init --template todo-api` SQLite/Postgres starter story with current Mesh patterns and live runtime proof. | Yes | agent |
| D350 | M049/S03 planning | workflow | How M049/S03 should refresh and prove checked-in generated examples | Use one repo-owned materializer/check command that generates `/examples/todo-sqlite` and `/examples/todo-postgres` through the public `meshc init` CLI, and make the slice-owned parity rail reuse that same command instead of duplicating generation logic. | A single generation seam keeps committed examples mechanically aligned with scaffold output, preserves the public CLI as the truth source, and gives later slices one fail-closed update/check command plus one retained proof surface instead of ad hoc copy steps and duplicate diff logic. | Yes | collaborative |
| D351 | M049/S03 closeout | verification | How M049/S03 should keep generated `/examples` and parity proof aligned | Use the public materializer check command plus `cargo test -p meshc --test e2e_m049_s03 -- --nocapture` as the authoritative example parity/build rail, with retained `.tmp/m049-s03` manifests and build logs for drift debugging. | S03 introduced checked-in scaffold outputs under `examples/` and a repo-owned materializer that generates them through the public `meshc init` CLI. Reusing that same command in the Rust closeout rail keeps scaffold/example drift detection on one public seam, proves both examples still pass `meshc test` and `meshc build --output`, and leaves S04/S05 one stable retained artifact bundle to inspect instead of ad hoc copy steps or duplicate generation logic. | Yes | agent |
| D352 | M049/S04 planning | repo-structure | Where the retired clustered proof packages should live after public onboarding moves to scaffold plus generated `/examples`. | Move `tiny-cluster` and `cluster-proof` under `scripts/fixtures/clustered/` as stable internal proof fixtures, keep their package/runtime/log identities unchanged, and route both Rust and bash consumers through shared fixture-path helpers instead of repo-root paths. | S04 needs the public first-contact story to stop depending on proof-app-shaped top-level packages, but the retained clustered rails still need durable package-level fixtures. `scripts/fixtures/` already fits frozen reusable proof inputs better than `tests/fixtures/`, and keeping the internal package names/log prefixes stable avoids unnecessary churn across historical assertions, retained artifacts, Docker/Fly config, and runtime diagnostics. | Yes | collaborative |
| D353 | M049/S04/T02 | testing | How retained route-free rails resolve the relocated cluster-proof fixture | Use `compiler/meshc/tests/support/m046_route_free.rs` as the shared owner of cluster-proof fixture root and Dockerfile path constants instead of open-coded repo-root literals. | T02 moved cluster-proof into `scripts/fixtures/clustered/cluster-proof`; centralizing discovery keeps `e2e_m046_s04`, `e2e_m045_s02`, and the Todo Docker builder on one path contract and makes missing fixture files fail closed. | Yes | agent |
| D354 | M049/S04 | requirement | R116 | validated | M049/S04 retired `tiny-cluster` and `cluster-proof` as repo-root/public onboarding surfaces, moved them under `scripts/fixtures/clustered/`, repointed public docs and skills to scaffold plus generated `/examples`, and passed the relevant proof rails: `node --test scripts/tests/verify-m049-s04-onboarding-contract.test.mjs`, `node scripts/tests/verify-m049-s03-materialize-examples.mjs --check`, `bash scripts/verify-m039-s01.sh`, `bash scripts/verify-m045-s02.sh`, `bash scripts/verify-m047-s04.sh`, and `bash scripts/verify-m047-s05.sh`. | Yes | agent |
| D355 | M049/S04/T05 | verification | How shell-side clustered proof fixtures are resolved after retiring the repo-root proof packages | Use `scripts/lib/clustered_fixture_paths.sh` as the single fail-closed shell owner of the relocated `tiny-cluster` and `cluster-proof` fixture roots for historical verifier scripts. | M049/S04 had to retarget the older direct verifier family plus historical wrapper rails after deleting the repo-root proof packages. Centralizing the fixture roots in one sourced helper kept the path move coherent across M039-M047 scripts, made missing fixture files fail closed, and avoided another round of duplicated repo-root literals. | Yes | agent |
| D356 | M049/S05 planning | verification | How M049 S05 exposes and verifies the final scaffold/example onboarding story | Introduce `scripts/verify-m049-s05.sh` as the single assembled scaffold/example truth rail, document it only in `README.md` and `website/docs/docs/tooling/index.md`, and keep it as a wrapper over the existing S01-S04 and M048 proofs rather than a new mega-test target. | S05 needs one named verifier for R116, but the truthful proof surfaces already live in the lower-level scaffold, parity, retirement, retained-clustered, and M048 rails. Reusing those rails avoids duplicated assertions, preserves retained bundle evidence, and keeps the public onboarding split narrow: SQLite remains the honest local starter, Postgres remains the serious shared/deployable path, and historical proof rails stay subordinate retained surfaces instead of re-emerging as a second public story. | Yes | agent |
| D357 | M049/S05/T01 closeout | verification | How `scripts/verify-m049-s05.sh` sources the Postgres replay connection | Resolve `DATABASE_URL` in-wrapper from process env, then repo `.env`, then `.tmp/m049-s01/local-postgres/connection.env`, and record only the source label. | The assembled S05 verifier must not depend on interactive shell inheritance. This fallback order keeps the Postgres replay truthful and fail-closed while avoiding secret leakage in retained artifacts and logs. | Yes | agent |
| D358 | M049/S05/T02 closeout | verification | How the retained M039 node-loss rail interprets post-loss authority health | Keep the one-node membership assertion after standby loss, but allow post-loss authority `replication_health` to be `unavailable` or `degraded` instead of the older `local_only` expectation. | Current route-free startup continuity truth converges membership correctly after standby loss while authority health reflects runtime-owned startup state rather than the older local-only assumption. Updating the expectation fixes the retained rail without relaxing real membership drift. | Yes | agent |
| D359 | M049/S05/T03 closeout | verification | How the retained M047 S05 wrapper avoids host-level concurrency flakes during S05 replay | Run the retained `e2e_m047_s05` replay under `RUST_TEST_THREADS=1` inside `scripts/verify-m047-s05.sh`. | The full `e2e_m047_s05` suite is stable on this tree but can fail with host-level `os error 35` socket races under default test concurrency. Serializing the retained replay preserves the proof contract instead of weakening or skipping the rail. | Yes | agent |
| D360 | M050 planning | docs-sources-of-truth | How the public docs interact with generated example READMEs during M050 | Treat `examples/todo-postgres` and `examples/todo-sqlite` as generator-owned truth anchors and repoint website docs to them unless a deliberate scaffold/parity change is required. | The generated example READMEs already have mechanical parity checks from M049. Unnecessary hand-edits would widen the blast radius into scaffold generation without improving the docs-surface reset. | Yes | agent |
| D361 | M050/S01 planning | docs-verification | How M050 S01 should enforce navigation truth without coupling to runtime/example replays | Add a fast `scripts/verify-m050-s01.sh` docs-graph verifier that proves source graph, retained docs contracts, and built-site footer behavior without requiring Postgres or heavier runtime sample rails; later wrappers can call it as a preflight. | Navigation regressions are source/build-layer drift, not runtime-sample drift. A dedicated fast rail keeps S01 env-free, improves diagnosis, and avoids dragging structural docs work into unrelated database-backed proofs. | Yes | agent |
| D362 | M050/S01 planning | docs-verification | How M050 S01 should enforce navigation truth without coupling to runtime/example replays | Add a fast `scripts/verify-m050-s01.sh` docs-graph verifier that proves source graph, retained docs contracts, and built-site footer behavior without requiring Postgres or the heavier runtime sample rails. | Navigation regressions are source/build-layer drift, not runtime-sample drift. A dedicated fast rail keeps S01 env-free, improves diagnosis, and avoids dragging structural docs work into unrelated database-backed proofs. | Yes | agent |
| D363 | M050/S01 planning | docs-navigation | How M050 should keep proof pages public without leaving them on the default onboarding rail | Keep `Production Backend Proof` and `Distributed Proof` public but demote them structurally: move them into a secondary proof-surface sidebar position, disable their prev/next footers, and make `Getting Started` / `Clustered Example` the only primary onboarding steps. | This preserves discoverability and honest public routes while removing proof pages from first-contact navigation and giving the docs graph a fail-closed structural contract that is separate from later copy rewrites. | Yes | agent |
| D364 | M050/S01/T01 | docs-navigation | How public proof pages stay visible without re-entering the default docs footer chain | Use exact current-page footer matching plus a sidebar-level `includeInFooter: false` opt-out for proof-page links, while keeping `prev: false` and `next: false` in proof-page frontmatter. | The old prefix matcher made `/docs/getting-started/clustered-example/` resolve through `/docs/getting-started/` and self-link. Page frontmatter alone only disables the active page footer, so proof pages also need a sidebar-level footer opt-out to stay public in navigation without becoming neighboring prev/next targets. | Yes | agent |
| D365 | M050/S01/T02 | documentation/verification | What should remain authoritative in the retained M047 docs rails after the M050 onboarding-graph reset | Pin the retained rails to the shared M050 onboarding graph contract plus public path/link markers, and stop using exact paragraph-level wording as the historical truth surface. | The docs already moved to a scaffold/examples-first path. Reusing the shared onboarding-graph contract keeps proof pages public but secondary, while path/link markers preserve example/readme split and historical rail discoverability without forcing later copy slices to resurrect stale proof-maze prose. | Yes | agent |
| D366 | M050/S01/T02 | docs-contract | Scope retained onboarding docs contracts between public entrypoints and proof-rail maps | Keep the root README on public starter links and proof-page discoverability only; keep verifier command rails and historical alias references on Distributed Proof and the retained proof pages. | The scaffold/examples-first onboarding path needs the root README to stay rewriteable without reviving internal proof-rail strings, while the proof pages still need explicit retained rail discoverability and fail-closed contract coverage. | Yes | agent |
| D367 | M050/S01/T03 | verification | M050 onboarding-graph preflight structure | Use direct source-level docs rails plus a single .tmp/m050-s01/verify bundle instead of replaying heavier wrappers or nesting another retained bundle. | The M050 rail only needs to prove the docs graph and built footer behavior. Running the direct onboarding graph test, retained M047 docs-contract Rust tests, production proof-surface verifier, and one VitePress build keeps the preflight fast and env-free. Pointing latest-proof-bundle.txt at .tmp/m050-s01/verify preserves the phase report, logs, and copied built HTML snapshots in one place without extra indirection. | Yes | agent |
| D368 | M050/S02 planning | docs-verification | How S02 enforces first-contact docs truth across README, Getting Started, Clustered Example, Tooling, and the assembled onboarding replay | Add a focused `m050-s02` first-contact docs contract/build verifier and have `scripts/verify-m049-s05.sh` replay it immediately after the S01 graph preflight. | S02 changes user-facing copy and section ordering across multiple public entrypoints. The older retained M047/M049 rails are either too proof-heavy or too broad to be the primary guardrail for this rewrite. A dedicated S02 verifier can pin the starter chooser, current repo links, and built-site output without overloading the S01 structural preflight or forcing the first-contact pages to preserve stale proof-map wording. | Yes | agent |
| D369 | M050/S02/T02 | documentation | Where the retained clustered proof-rail map lives in the public first-contact docs. | Keep retained clustered proof commands on Distributed Proof and related proof-heavy docs; Clustered Example links to that page instead of listing the M047 rails inline. | This keeps the Clustered Example page scaffold-first and easier to evaluate while still preserving proof discoverability on the dedicated proof surfaces that already own the heavier retained M047 contract. | Yes | agent |
| D370 | M050/S02 closeout | verification | How scripts/verify-m049-s05.sh should treat the fallback local Postgres source during retained replay | If DATABASE_URL resolves from .tmp/m049-s01/local-postgres/connection.env and the connection is unreachable, refresh the container-backed source from .tmp/m049-s01/local-postgres/container-meta.txt plus docker port before replaying e2e_m049_s01. | The assembled first-contact/docs replay should not go red because a retained local Postgres container was stopped or republished onto a different host port. The wrapper already owns fallback source resolution, so it can repair that local-only verifier state without leaking connection strings or forcing unrelated docs work to debug stale container metadata. | Yes | agent |
| D371 | M050/S03 planning | documentation | Where the clustered proof-map lives in public secondary docs after M050/S03 | Make `website/docs/docs/distributed-proof/index.md` the single public-secondary clustered proof map, while `website/docs/docs/distributed/index.md` and `website/docs/docs/tooling/index.md` only keep bounded handoffs plus their role-specific guidance. | This keeps proof material public but secondary, makes the low-level distributed-primitives page distinct from the runtime-owned clustered-app/proof path, and stops multiple public surfaces from competing as first-contact or quasi-first-contact clustered routes. It also preserves the honest SQLite-local vs Postgres-shared split by centralizing the clustered verifier ledger in one place instead of scattering it across tooling and primitives guides. | Yes | agent |
| D372 | M050/S03 | requirement | R118 | validated | The docs now separate the evaluator path from low-level distributed primitives: `Clustered Example` remains the primary clustered walkthrough, `Distributed Actors` stays on primitives with bounded handoffs, `Distributed Proof` is the sole public-secondary clustered proof map, and `Production Backend Proof` owns the backend proof handoff. Evidence: `bash scripts/verify-m050-s02.sh`, `node --test scripts/tests/verify-m050-s03-secondary-surfaces.test.mjs`, `bash scripts/verify-m050-s03.sh`, and the retained M049 replay `bash scripts/verify-m049-s05.sh`. | Yes | agent |
| D373 | M050/S03 | requirement | R118 | validated | The docs now separate the evaluator path from low-level distributed primitives: `Clustered Example` remains the primary clustered walkthrough, `Distributed Actors` stays on primitives with bounded handoffs, `Distributed Proof` is the sole public-secondary clustered proof map, and `Production Backend Proof` owns the backend proof handoff. Evidence: `bash scripts/verify-m050-s02.sh`, `node --test scripts/tests/verify-m050-s03-secondary-surfaces.test.mjs`, `bash scripts/verify-m050-s03.sh`, and the retained M049 replay `bash scripts/verify-m049-s05.sh`. | Yes | agent |
| D374 | M050/S03 | verification | How M050/S03 secondary-surface truth is enforced and retained | Use a dedicated `bash scripts/verify-m050-s03.sh` preflight that runs the cross-page source contract, retained M047 docs rails, production-backend proof verifier, one serial VitePress build, and built-HTML assertions, then replay it immediately after `bash scripts/verify-m050-s02.sh` inside `bash scripts/verify-m049-s05.sh` with a retained `retained-m050-s03-verify` bundle. | Secondary-surface drift is a different failure class from first-contact graph drift, backend proof-page drift, and older retained runtime rails. Giving it one bounded verifier keeps failures local, preserves built-site evidence under `.tmp/m050-s03/verify/`, and makes the assembled M049 replay fail before heavier retained wrappers can hide docs-surface regressions. | Yes | agent |
| D375 | M051 planning | testing | Fixture strategy for tooling/editor/LSP/formatter rails after reference-backend | Use a dedicated retained backend-shaped fixture instead of repointing bounded tooling rails at full Mesher. | Mesher is too large and product-coupled for same-file/editor/formatter proofs; a small retained fixture keeps those rails deterministic and honest. | Yes | agent |
| D376 | M051/S01 planning | architecture | Mesher bootstrap and proof surface for M051/S01 | Align Mesher startup to the scaffold-style runtime contract (`DATABASE_URL`/validated app env + `Node.start_from_env()`) and prove it through maintainer-only surfaces (`mesher/README.md`, `compiler/meshc/tests/e2e_m051_s01.rs`, `scripts/verify-m051-s01.sh`) instead of extending public docs or reusing historical M033 proofs as the primary contract. | R119 requires Mesher to become the maintained deeper reference app without turning it back into a public first-contact path. Using the current scaffold/runtime bootstrap seam removes stale app-owned cluster wiring, while a dedicated maintainer runbook and verifier keep the new contract testable without re-promoting Mesher into the evaluator-facing docs that M050 already reset. | Yes | agent |
| D377 | M051/S01/T01 | runtime | Mesher startup bootstrap order | Validate Mesher env config and open the Postgres pool before calling Node.start_from_env(), and use Node.start_from_env() as the only clustered bootstrap path. | This keeps bad app config and DB failures fail-closed before any runtime/listener readiness claim, while delegating clustered-env parsing and bootstrap errors to the runtime-owned contract instead of teaching Mesher-specific peer wiring. | Yes | agent |
| D378 | M051/S01/T02 | observability | Mesher maintainer runtime proof surface for M051/S01 | Use a dedicated `compiler/meshc/tests/e2e_m051_s01.rs` target with `compiler/meshc/tests/support/m051_mesher.rs` and retain redacted runtime artifacts under `.tmp/m051-s01/` instead of reusing the older M033 Mesher helpers. | The older Mesher proof was spread across unrelated M033 storage/runtime slices, hardcoded `127.0.0.1:5432`, and did not retain a dedicated maintainer-facing artifact bundle for bootstrap/auth/readback failures. A dedicated support harness makes the current Mesher startup contract, Postgres dependency, and artifact redaction first-class and easier to debug. | Yes | agent |
| D379 |  | documentation | Where the Mesher maintainer runbook and verifier live during M051/S01 | Use `mesher/README.md` plus `scripts/verify-m051-s01.sh` as the canonical maintainer-facing Mesher run surface, and keep public README/VitePress docs untouched until the later Mesher docs slice. | This gives maintainers one repo-owned migrate/build/run/smoke runbook and one fail-closed replay tied to the real Mesher e2e rail without reintroducing Mesher as a public first-contact surface before S04. | Yes | agent |
| D380 | M051/S01 | testing | Mesher runtime proof build output location for M051/S01 | Build Mesher to an artifact-local binary with `meshc build mesher --output ...` inside the dedicated `.tmp/m051-s01` harness instead of reusing the tracked `mesher/mesher` package output. | The dedicated Mesher maintainer rail needs self-contained retained bundles and should not churn the tracked package binary while it builds, migrates, runs, and archives debug artifacts. Keeping the binary under the artifact root makes the proof rail reproducible and keeps retained evidence aligned with the exact build it launched. | Yes | agent |
| D381 | M051/S02 planning | architecture | Internal home and acceptance scope for the retained backend-only proof after `reference-backend/` retirement work begins | Create a canonical internal retained backend fixture at `scripts/fixtures/backend/reference-backend/`, make `compiler/meshc/tests/e2e_m051_s02.rs` plus `scripts/verify-m051-s02.sh` the maintainer-owned acceptance surface for backend-only proof, keep the deeper multi-instance exact-once scenarios as retained `e2e_reference_backend` coverage outside the S02 acceptance rail, and preserve repo-root `reference-backend/` compatibility surfaces until S03-S05 retarget or remove them. | S02 must preserve the real deploy/migration/health/recovery proof without breaking current public-doc and tooling consumers that still reference repo-root `reference-backend/`. An internal fixture plus slice-owned verifier lets maintainers keep the backend proof alive, keeps Mesher as the deeper maintained app, and avoids expanding this slice into unrelated public-doc or multi-instance concurrency cleanup. | Yes | agent |
| D382 | M051/S02/T02 | testing | Where the retained reference-backend harness should place runtime binaries versus staged deploy bundles | Build the retained backend runtime binary into a stable .tmp/m051-s02 artifact path, but keep staged deploy bundles outside the repo root and publish only bundle pointers/manifests back under .tmp/m051-s02. | The e2e target needs a deterministic retained binary path that never reuses reference-backend/reference-backend, while the staged deploy proof must still demonstrate that a deploy bundle can run from a directory outside the repo root. Splitting these surfaces keeps the retained harness source-only, preserves the deploy-artifact contract, and gives later verifiers a stable pointer location under .tmp/m051-s02. | Yes | agent |
| D383 | M051/S02 closeout | verification | How the retained S02 backend verifier should isolate package-local fixture smoke from later shared-DB rails | Run fixture-smoke as its own phase and kill stale `.tmp/m051-s02/fixture-smoke/build/reference-backend` workers before reusing the DB-backed deploy/recovery phases. | A leftover fixture-smoke worker can claim jobs from the shared database and make `e2e_reference_backend_deploy_artifact_smoke` fail with `staged worker never recorded a processed job in /health` even though the staged binary and deploy-smoke path are healthy. Cleaning those workers before and after the fixture-smoke phase made the assembled S02 verifier truthful again. | Yes | agent |
| D384 | M051/S03 planning | testing | Formatter proof boundary for the retained backend fixture in M051/S03 | Retarget formatter proof to canonical retained backend files and subtrees that already format cleanly, instead of claiming the full `scripts/fixtures/backend/reference-backend/` root is formatter-clean during S03. | S03 is a tooling/editor path migration slice, not a formatter-debt slice. The retained fixture’s `tests/fixture.test.mpl` still trips known formatter issues, so moving the formatter rail to a bounded canonical surface keeps the migrated proof honest and prevents false red or dishonest green acceptance. | Yes | agent |
| D385 | M051/S03 | documentation | Public editor-host proof wording after the retained backend fixture cutover | Keep VS Code and Neovim public docs generic about a backend-shaped proof surface and forbid direct public references to repo-root `reference-backend/` or the internal retained fixture path. | M051/S03 moved the editor-host and tooling rails onto `scripts/fixtures/backend/reference-backend/`, but that fixture is an internal retained proof surface rather than a new onboarding path. The README contract and `scripts/tests/verify-m036-s03-contract.test.mjs` now fail closed on leaked fixture paths or stale repo-root backend wording, preserving the public/examples-first story while keeping the editor-host rails truthful. | Yes | agent |
| D386 | M051/S04 planning | documentation | Public deeper-backend handoff during the M051/S04 docs and scaffold retarget | Keep `/docs/production-backend-proof/` and the compatibility verifier path `reference-backend/scripts/verify-production-proof-surface.sh`, but rewrite their contract so public readers stay on scaffold/examples-first surfaces while Mesher and the retained backend-only proof are maintainer-facing deeper surfaces. | This preserves the M050 public docs graph and compatibility file paths that existing wrappers still consume, while advancing R119 without leaking the internal retained backend fixture or re-promoting repo-root `reference-backend/` as a public next step. | Yes | agent |
| D387 | M051/S04/T02 | docs-contract | What the public-secondary backend proof surfaces hand readers to after the S04 retarget | Keep `/docs/production-backend-proof/` as the compact public-secondary handoff, point deeper maintainer work to `mesher/README.md` plus `bash scripts/verify-m051-s01.sh`, and keep the retained backend-only proof behind `bash scripts/verify-m051-s02.sh` instead of public `reference-backend/README.md` teaching. | This preserves the public scaffold/examples-first route while still naming the real maintainer-owned deeper app and the retained backend-only replay. It removes stale repo-root backend runbook teaching from the public docs graph without dropping the compatibility verifier path that still guards proof-page drift. | Yes | agent |
| D388 | M051/S05 planning | verification | Canonical proof-page verifier path and final acceptance surface after `reference-backend/` deletion | Replace `reference-backend/scripts/verify-production-proof-surface.sh` with the canonical top-level `scripts/verify-production-proof-surface.sh`, and make `bash scripts/verify-m051-s05.sh` the terminal post-deletion acceptance rail that composes S01-S04 proofs. | S05 deletes the repo-root compatibility app, so the public proof-page contract needs a stable verifier path that survives that deletion. A top-level proof-page verifier keeps public docs and historical wrappers truthful, while one slice-owned assembled replay gives the milestone a single post-deletion acceptance surface instead of a fragile chain of ad hoc replays. | Yes | agent |
| D389 | M051/S05/T01 | tooling | Canonical path for the public proof-page verifier before reference-backend deletion | Use scripts/verify-production-proof-surface.sh as the canonical verifier and leave reference-backend/scripts/verify-production-proof-surface.sh as a temporary wrapper until the compatibility tree is deleted. | A stable top-level verifier path lets public docs, historical wrapper rails, and retained proof bundles retarget before the repo-root reference-backend tree is removed, without widening the proof-page contract itself. | Yes | agent |
| D390 | M051/S05 | verification | M051/S05 final retained bundle layout | Copy delegated S01-S04 verify trees into the S05 retained bundle and rewrite each copied `latest-proof-bundle.txt` pointer to the copied child bundle so the final post-deletion evidence stays self-contained. | The post-deletion acceptance rail cannot depend on live child `.tmp` directories or deleted repo-root surfaces. Copying each delegated verify tree plus its pointed proof bundle gives downstream roadmap reassessment and future debugging one stable retained bundle rooted entirely under `.tmp/m051-s05/verify/retained-proof-bundle`. | Yes | agent |
| D391 | M051/S05 | verification | Retained backend recovery visibility contract | Keep `e2e_reference_backend_worker_crash_recovers_job` focused on crash/requeue/processed correctness, and keep `e2e_reference_backend_worker_restart_is_visible_in_health` focused on preserved restart metadata on `/health` after recovery instead of requiring a live degraded-window observation. | During the final M051/S05 serial replay, the retained backend reliably preserved `restart_count`, `last_exit_reason`, and `last_recovery_*` on `/health` after recovery, but the mid-recovery degraded window was not a stable closeout seam. Splitting the proof this way keeps the retained backend rails honest and avoids a flaky requirement that the current post-deletion fixture does not expose reliably. | Yes | agent |
| D392 | M056 planning | product-positioning | How the pitch deck should balance hyperpush product messaging with the broader Mesh story | Make Mesh a first-class part of the deck narrative, not a side note, while still framing hyperpush as the product being pitched. | R120 requires coherent evaluator-facing public positioning. A token-only hyperpush deck would fragment the public story instead of advancing the repo’s strongest differentiator: Mesh-backed fault-tolerant systems behavior. | Yes | agent |
| D393 | M056/S01 planning | architecture | How the `/pitch` deck should own navigation and export state | Implement `/pitch` as a landing-native App Router route with one pitch-local slide data model, hash-addressable browser navigation, and browser-native print/CSS export verified by a landing-local Playwright harness. | S01 needs a real evaluator-facing route that stays visually native to the landing app while keeping slide content, navigation state, and PDF export on one truthful browser surface. A separate PDF generator or duplicated slide sources would make the narrative drift harder to verify and maintain. | Yes | agent |
| D394 | M056/S01/T02 | architecture | How the `/pitch` route should split interactive navigation from route chrome | Keep `/pitch` as a server-rendered route shell that nests a pitch-local client navigation controller inside the deck content, rather than turning the whole route wrapper into a client component. | The route already composes the landing header with a server footer, and Next App Router only allows the server shell to import both sides cleanly. Keeping the ordered slide DOM in the server shell also preserves the print-first/export-first story while the client controller owns hash sync, bounded input handling, and active-slide markers. | Yes | agent |
| D395 | M056/S01 | architecture | How the `/pitch` deck should preserve print/export truth while staying navigable in-browser | Render the deck as one ordered DOM document with explicit `[data-pitch-page]` / `data-pitch-print` markers, then layer hash-synced client navigation and `window.print()` on top instead of using a viewport-locked slideshow or separate PDF generator. | S01 needed one source of truth for route render, navigation, and export. Keeping the slides in ordered DOM sections preserves readable print output, lets the browser-native export path use the same content the live route shows, and gives later polish work stable DOM hooks for hiding chrome and exposing active/export state without duplicating narrative content. | Yes | agent |
| D396 | M056/S02 planning | architecture | How S02 launch polish should extend the `/pitch` route without breaking the S01 proof seam | Keep one ordered `pitchDeck` source and add pitch-local slide-variant renderers that reuse existing landing assets, `WaitlistButton`, and canonical external links while extending the existing route-local Playwright file instead of importing whole homepage sections or adding a second render/export/proof path. | S01 already proved the risky route/navigation/export architecture. S02's real work is narrative and visual finish. Reusing existing landing surfaces inside a pitch-local renderer map keeps R120's Mesh-aware product story coherent, preserves stable slide ids/DOM markers for hash navigation and print export, and avoids duplicated browser harnesses or stitched-on homepage sections that would make the finished deck feel less native. | Yes | agent |
| D397 | M056 S02 T01 | frontend | How the /pitch deck encodes evaluator narrative and slide-specific rendering | Use a validated typed slide-variant model in mesher/landing/lib/pitch/slides.ts and render explicit variant branches in pitch-slide-variants.tsx while preserving the shared route shell markers. | The deck now needs richer slide interiors without losing stable slide ids, ordered DOM sections, or inspectable state markers. A typed variant union plus createPitchDeck validation fails fast on duplicate ids, missing required beats, and empty variant payloads, while the shared shell keeps navigation/export truth stable. | Yes | agent |
| D398 | M053 planning | observability | packages-website role in hosted release evidence | Keep packages-website as a separately deployed app, but make its deploy/public-surface checks hard failures inside the normal hosted release/deploy chain. | R121 is core milestone scope. The honest outcome is one release/deploy evidence chain that fails when packages drift, without collapsing packages into docs architecture or treating it as a side workflow. | Yes | agent |
| D399 | M053/S01 planning | architecture | How M053/S01 will prove serious starter deploy truth | Keep S01 portable-first and starter-owned: the generated Postgres starter should emit its own staged deploy bundle (`deploy` SQL plus stage/apply/smoke scripts), prove that bundle locally against PostgreSQL first, and expose one retained verifier surface that later hosted-chain work can call. Fly may be used later as a proving environment, but it is not the S01 contract. | M053 research explicitly recommends starter-owned staged deploy proof before any hosted or Fly layer. This keeps the serious Postgres starter truthful on its own, preserves SQLite's local-only boundary, and creates the concrete verifier seam S03 can wire into the normal deploy chain alongside packages-site checks. The user’s Fly guidance is retained as an execution option for later Fly-specific validation, not as the core S01 dependency. | Yes | collaborative |
| D400 | M053/S01/T01 | starter-contract | How the Postgres todo starter expresses deployable production-like handoff | Generate a starter-owned staged deploy bundle made of scripts/stage-deploy.sh, scripts/apply-deploy-migrations.sh, scripts/deploy-smoke.sh, and deploy/<package>.up.sql; treat hosted runtimes and Fly-managed databases as downstream concerns rather than part of the starter contract. | The slice goal is a truthful serious starter path that can be generated, tested, and replayed locally without depending on a hosted chain. Making the bundle starter-owned keeps example parity generator-driven, gives later slices one stable deploy surface to consume, and avoids reintroducing Fly-first or SQLite-local wording into the Postgres starter. | Yes | agent |
| D401 | M053/S01 | verification | How staged deploy rails ensure runtime changes reach staged binaries | Force a fresh `cargo build -p mesh-rt` before staged bundle creation whenever the harness shells out to the public `meshc build` path. | The staged deploy script uses the public CLI, which links against `target/debug/libmesh_rt.a`. Without rebuilding that archive first, staged bundle proofs can quietly execute a stale runtime even though the in-process Rust test binary already includes the newer code. The harness must refresh the staticlib before staging or the retained evidence becomes untrustworthy. | Yes | agent |
| D402 | M053/S01 | runtime | When startup work should trigger for clustered HTTP servers | Trigger pending startup work once when `mesh_http_serve` or `mesh_http_serve_tls` starts listening, while keeping the existing main-wrapper trigger for route-free apps behind an idempotent `STARTUP_WORK_TRIGGERED` guard. | Clustered HTTP starters block in `HTTP.serve(...)`, so waiting for `mesh_main` to return leaves startup work registered but never submitted. Moving the trigger wholesale would disturb the route-free proof contract, and spawning the HTTP server inside a scheduler worker starves request handling on the default thread count. An idempotent trigger at HTTP server start fixes long-lived starter truth without breaking route-free apps. | Yes | agent |
| D403 | M053/S02 planning | verification | How M053/S02 proves generated Postgres starter failover truth | Use a host-native two-node staged replay of the generated Postgres starter: real HTTP CRUD and clustered GET prove shared-state behavior, while runtime-owned startup continuity with the existing delay seam proves owner-loss, automatic recovery, stale-primary, and fenced-rejoin truth. | This keeps R122 and the supporting dual-db/generated-starter requirements honest: the proof stays on the generated Postgres starter, avoids turning the starter README or Fly into the product contract, and reuses the existing runtime-owned pending-window/failover semantics instead of adding app-owned sleeps to starter source. | Yes | agent |
| D404 | M053/S02 | requirement | R122 | validated | M053/S01 plus S02 now prove the generated Postgres starter as the truthful clustered deploy/failover path while SQLite remains explicitly local-only. Evidence: `DATABASE_URL=<local docker postgres> cargo test -p meshc --test e2e_m053_s02 m053_s02_staged_postgres_helper_ -- --nocapture`, `DATABASE_URL=<local docker postgres> cargo test -p meshc --test e2e_m053_s02 -- --nocapture`, and `DATABASE_URL=<local docker postgres> bash scripts/verify-m053-s02.sh` all passed; the retained S02 bundle under `.tmp/m053-s02/` records dual-node operator JSON, real HTTP CRUD/read snapshots, owner-loss promotion/recovery diagnostics, and fenced rejoin truth. | Yes | agent |
| D405 | M053/S02 | verification | Promoted generated-starter failover status health in M053/S02 | Accept `replication_health=unavailable` as truthful on the promoted node during owner-loss and fenced-rejoin windows, alongside `local_only` or `healthy` when role/epoch/membership and continuity diagnostics already prove promotion/recovery. | The live S02 failover rail showed the promoted standby correctly reached role=`primary`, `promotion_epoch=1`, owner-loss/automatic-recovery/fenced-rejoin diagnostics, and completed continuity truth while `meshc cluster status` still reported `replication_health=unavailable`. Hardcoding only `local_only`/`healthy` made the proof rail falsely fail against truthful runtime output. | Yes | agent |
| D406 | M053/S03 planning | github-workflows | How M053/S03 will make hosted starter proof and packages drift part of one normal evidence chain | Add a separate reusable hosted starter-failover workflow called from `authoritative-verification.yml` and `release.yml`, then assemble hosted truth in a slice-owned verifier that requires fresh `deploy-services.yml` mainline packages evidence alongside the starter-proof caller runs. | The existing authoritative live-proof workflow is budgeted for the M034 publish proof and should not absorb the longer Postgres starter failover replay. Keeping packages deploy/public-surface checks in `deploy-services.yml` preserves the separate app boundary while the slice-owned hosted verifier turns starter proof, packages proof, and freshness checks into one fail-closed public contract. | Yes | agent |
| D407 | M053 S03 T01 | ci | How to host the serious starter failover proof in the authoritative workflow graph | Add a dedicated reusable workflow with runner-local Postgres for scripts/verify-m053-s02.sh; call it from authoritative-verification as a secret-free whitespace-guarded lane and from release as a tag-gated prerequisite alongside authoritative-live-proof. | The starter failover replay needs its own timeout budget and a local Postgres service, but it should not widen live-proof secrets or fold Fly-specific behavior into the product contract. A separate reusable workflow keeps the proof hosted, reusable, fail-closed, and visible in both mainline and release gating. | Yes | agent |
| D408 | M053/S03/T02 | automation | How hosted verification scripts identify the GitHub repository to inspect | Derive the repo slug from `git remote get-url origin` by default and allow `M053_S03_GH_REPO` as an explicit override. | The repository moved to the organization account (`hyperpush-org/hyperpush-mono`), so hardcoding the old owner/name can send `gh run` queries through redirects and make hosted evidence ambiguous or stale. Resolving from local git metadata keeps verifier targets aligned with the checked-out repo while preserving an escape hatch for fixture tests or unusual remotes. | Yes | collaborative |
| D409 | M053/S04 planning | scope | How M053/S04 handles the landing/docs/packages coherence gap while aligning docs and Fly reference assets | Keep S04 focused on evaluator-facing docs plus retained Fly reference assets and fail-closed docs verifiers; do not rewrite `mesher/landing/` in this slice. | The shipped-contract drift for M053 lives in public docs, the distributed proof map, and the retained Fly reference assets. `mesher/landing/` is still a Hyperpush-branded product surface with its own deploy-health contract, so folding that rewrite into S04 would materially widen scope and risk blocking the R121/R122 docs-contract closeout. S04 should make the starter/packages/Fly story honest now and leave full landing alignment as separate follow-up work for R120. | Yes | agent |
| D410 | M053/S04/T02 | documentation | How Distributed Proof and retained Fly assets should present the M053 clustered contract | Anchor Distributed Proof on the generated Postgres starter's M053 verifier chain (`verify-m053-s01.sh`, `verify-m053-s02.sh`, `verify-m053-s03.sh`), keep SQLite explicitly local-only, and describe Fly/`cluster-proof` as retained read-only reference proof rather than a coequal public starter surface. | The shipped M053 clustered contract now lives on the generated Postgres starter plus the hosted packages/public-surface chain. Leaving Fly or the retained `cluster-proof` fixture framed as equal canonical/public-starter surfaces would contradict the starter-first docs, overstate the retained proof asset, and obscure that SQLite remains outside clustered deploy/failover proof. | Yes | agent |
| D411 | M053/S04/T01 | documentation | How first-contact public docs and guardrails should present the M053 starter contract | Keep the scaffold/examples-first order explicit (clustered scaffold -> local-only SQLite starter -> serious shared/deployable PostgreSQL starter), and treat the retained M047 docs contract as part of the first-contact verification surface. | S04 T01 showed that `scripts/verify-m050-s02.sh` transitively replays the retained M047 docs assertions. Keeping that starter order explicit across README, Getting Started, Clustered Example, and Tooling while aligning the retained rail prevents first-contact wording drift from passing narrow task checks but failing the assembled verifier. | Yes | agent |
| D412 | M053/S04/T03 | verification | How the M053 S04 docs/reference verifier should detect public-doc and retained-reference drift | Use exact-line source checks plus full rendered-text markers in built HTML, and keep the wrapped retained M050/M047 rails aligned instead of weakening them. | T03 showed that short-substring checks can false-positive on valid prose and that the assembled S04 verifier is only honest if the transitive retained docs rails stay truthful. Exact source-line markers, rendered-text assertions, and retained-bundle copies make drift precise without hiding upstream contract failures. | Yes | agent |
| D413 | M053/S05 planning | release-process | How S05 should converge remote main/tag state onto the hosted M053 contract without shipping unrelated local-ahead work | Ship an M053-only rollout commit from `origin/main` that carries the full S01-S04 tree, verify fresh `authoritative-verification.yml` and `deploy-services.yml` runs on that shipped SHA, then reroll the existing binary tag `v0.1.0` as an annotated tag on the same shipped SHA instead of pushing local `HEAD` or widening scope into a version bump. | R121 and R122 only close when the same shipped M053 commit is visible through fresh mainline and release evidence. The live verifier already proves that a workflow-only fix is insufficient: the current green mainline authoritative run predates the starter-failover job and the current green release run points at an older SHA behind a lightweight tag with no peeled `^{}` ref. Building a minimal M053-only rollout commit avoids publishing unrelated M056 `/pitch` work, preserves the generated-starter/public-docs contract from S01-S04, and satisfies the verifier's requirement that the release tag expose truthful peeled-ref freshness on the shipped commit. | Yes | agent |
| D414 | M053/S05/T01 | rollout | How to assemble the M053 hosted rollout candidate from mixed local-ahead history | Build the T02 push target as a synthetic file-scoped commit on top of the latest fetched origin/main, using the proof-critical M053 S01-S04 file set from pre-M056 snapshot 79a030c8 instead of pushing local HEAD or cherry-picking the full ahead stack. | Local HEAD mixed required M053 starter/workflow/docs changes with unrelated M056 /pitch work plus .gsd and incidental landing artifacts, and origin/main advanced during T01. Reapplying the exact M053 ship-set onto the current remote tip preserved new upstream fixes while explicitly excluding unrelated work and avoiding rollback of files added after the earlier base. | Yes | agent |
| D415 | M053/S06 | rollout | How synthetic hosted rollout commits should handle retained regression fixtures | Include newly tracked `scripts/fixtures/...` regression data in the synthetic ship-set alongside the code and verifier files that reference it. | S06 proved that code-only synthetic commits are not enough when hosted lanes execute the full `cargo test -p meshc --test e2e_m053_s02 -- --nocapture` target. SHA `7220ae4d78a7a09a150ae4225c75b6817bf9c1e2` still failed on hosted main because the rollout helper shipped `compiler/mesh-rt/src/dist/node.rs`, `compiler/meshc/tests/e2e_m053_s02.rs`, and `scripts/verify-m053-s02.sh` but omitted the new tracked `scripts/fixtures/m053-s02-hosted-failure-bundle/` directory. The corrective SHA `e5fb36a6fe7e9e56f3a608a608abbaaab6764167` only turned mainline authoritative proof green after the fixture files were included in the synthetic commit. | Yes | agent |
| D416 | M053/S06 | requirement | R121 | validated | M053/S03 through S06 now prove the packages site is part of the normal hosted CI/deploy contract. Evidence: `bash scripts/verify-m034-s02-workflows.sh`, `node --test scripts/tests/verify-m053-s03-contract.test.mjs`, and `bash scripts/verify-m053-s03.sh` all passed; `.tmp/m053-s03/verify/status.txt` is `ok`; `remote-runs.json` shows fresh successful `authoritative-verification.yml`, `deploy-services.yml`, and `release.yml` runs aligned on shipped SHA `e5fb36a6fe7e9e56f3a608a608abbaaab6764167`; and the annotated `v0.1.0` reroll now resolves through the hosted release freshness checks. | Yes | agent |
| D417 | M053/S06 | runtime | startup pending-window override ownership for clustered startup failover proof | Keep `MESH_STARTUP_WORK_DELAY_MS` as a runtime-owned positive-only override inside `startup_dispatch_window_ms(...)`, with the existing 2500ms fallback and zero-delay behavior preserved for non-startup or replica-free requests. | Hosted S06 failures were not a missing mirror transport bug; the staged Postgres starter already exported `MESH_STARTUP_WORK_DELAY_MS`, but `startup_dispatch_window_ms(...)` ignored it and collapsed the promotable startup window back to the 2500ms default on hosted Ubuntu. Restoring the override in `mesh-rt` keeps the timing seam runtime-owned instead of pushing sleeps into starter source or verifier scripts, while focused `startup_work_dispatch_window_` unit coverage proves positive overrides, safe fallback on missing/zero/negative/malformed values, and no widening of non-startup or replica-free requests. | Yes | agent |
| D418 | M054 planning | documentation | How M054 treats Fly and public load-balancing claims | Keep Fly as the current proving environment and ingress evidence, but guard public load-balancing wording with repo-owned docs contract tests that separate proxy ingress from Mesh runtime placement and failover. | The repo already makes strong public claims. The milestone needs explicit guardrails so Fly does not become the product contract and homepage or docs copy cannot drift beyond the real starter/runtime evidence. | Yes | collaborative |
| D419 | M054/S01 planning | architecture | What S01 should change to prove the public load-balancing story before direct request correlation exists | Use a thin one-public-URL ingress harness plus existing `meshc cluster` continuity/diagnostics surfaces in S01; do not add frontend-aware client routing, node-aware browser logic, or new starter-owned placement logic here. | Existing clustered-route and staged starter work already proves that the runtime can preserve ingress, owner, replica, and execution truth across nodes. The current gap is composing that behavior behind one public app URL and retaining auditable evidence. Direct request-to-record correlation is a separate runtime/observability seam and belongs to S02, so S01 should stay at the ingress-proof and bounded-contract layer. | Yes | agent |
| D420 | M054/S01 blocker analysis | architecture | How runtime-owned transient clustered HTTP replies should execute owner-side route handlers | Execute transient clustered HTTP route reply work inside an actor before building the response frame, instead of invoking the route handler directly on the node accept-loop thread. | The standby-first M054/S01 repro showed that owner-side direct invocation crashed inside generated service-call wrappers like `TodoRegistry.get_pool()`, because the transient accept-loop path lacked the actor context that normal HTTP handlers and service calls expect. Moving the transient reply work onto an actor preserves service-call context and removes the owner-side segfault that surfaced as `clustered_http_route_reply_read_failed` on the ingress node. | Yes | agent |
| D421 | M054/S01 | documentation/proof | How the serious Postgres starter's one-public-URL contract stays truthful across scaffold, example, and verifier surfaces | Materialize the committed example README from the scaffold template and make the S01 verifier republish one self-contained retained proof bundle under `.tmp/m054-s01/proof-bundles/` | S01 only stays honest if the generated starter wording, committed example, and retained proof surface all come from the same generator-owned contract and the same copied evidence bundle. Hand-editing the example README or relying on ad-hoc temp paths would let the public contract drift away from the runtime-owned ingress/continuity evidence. | Yes | agent |
| D422 | M054/S02 planning | observability | How M054/S02 should correlate one clustered HTTP response to one continuity record. | Expose the clustered HTTP continuity request key as a runtime-owned `X-Mesh-Continuity-Request-Key` response header, then drive low-level and serious-starter proofs from direct `meshc cluster continuity <node> <request-key> --json` lookups instead of continuity-list diffing. | The runtime already generates the request key before dispatch and the existing CLI already resolves a single continuity record by that key. A response header closes the only missing handoff, keeps the surface platform-agnostic and runtime-owned, preserves the server-side-first/public-one-URL story, and avoids adding starter-owned control routes or frontend-aware routing claims. | Yes | agent |
| D423 | M054/S02/T01 | observability | How clustered HTTP responses should expose request correlation | Inject a runtime-owned `X-Mesh-Continuity-Request-Key` header on clustered HTTP success and runtime-generated rejection responses, while preserving app-supplied headers and using existing `meshc cluster continuity <node> <request_key> --json` for lookup. | The runtime already owns request-key generation before dispatch, so the safest direct-correlation seam is a response header rather than a new body field or starter-owned diff workflow. This keeps routing semantics unchanged, preserves handler responses, and gives operator/debug flows one stable key that maps directly to one continuity record. | Yes | human |
| D424 |  | verification | How the M054/S02 assembled verifier retains delegated and direct-correlation proof state | Copy the entire `.tmp/m054-s01/verify` directory into the S02 proof bundle unchanged, and copy the fresh staged bundle referenced by the new S02 artifact into `retained-staged-bundle/`. | The S02 wrapper needs to delegate S01 without mutating its older diff-based proof surface, while still leaving one self-contained retained bundle for later debugging. Copying the S01 verify directory preserves the delegated phase markers and original bundle pointer verbatim, and copying the staged bundle removes any dependence on an external temp path from `staged-bundle.path.txt`. | Yes | agent |
| D425 | M054/S03 planning | documentation | What public surfaces M054/S03 should close out | Keep S03 on VitePress homepage, distributed-proof docs, OG asset sync, and repo-owned docs/verifier guardrails; defer `mesher/landing` cleanup until that surface has its own verified rail. | S01 and S02 already established the serious starter wording and runtime proof. The remaining gap is drift across VitePress public surfaces and the assembled proof rail. Pulling `mesher/landing` into S03 would add a second toolchain and new verification work without an existing route-specific contract, which is unnecessary for closing R123 truthfully. | Yes | agent |
| D426 | M054/S03 | verification | How public-docs drift is guarded for M054/S03 | Keep exact prose in the fast Node source-contract test; keep the assembled shell wrapper on built-HTML fragments, retained-bundle shape, delegated S02 replay, and redaction checks. | This keeps literal copy drift fail-closed without duplicating long markdown strings across the shell wrapper and Cargo verifier, while still proving the rendered site, retained OG asset, and copied S02 evidence bundle are all aligned. | Yes | agent |
| D427 | M054/S03 | requirement | R123 | validated | M054 now proves the bounded load-balancing story end to end: S01 retained one-public-URL ingress truth for the serious Postgres starter, S02 exposed runtime-owned `X-Mesh-Continuity-Request-Key` direct lookup, and S03 aligned homepage/VitePress/Distributed Proof/OG copy with fail-closed source, Cargo, and assembled verifier rails (`node --test scripts/tests/verify-m054-s03-contract.test.mjs`, `cargo test -p meshc --test e2e_m054_s03 -- --nocapture`, `npm --prefix website run generate:og`, `npm --prefix website run build`, and `DATABASE_URL=<redacted> bash scripts/verify-m054-s03.sh`). | Yes | agent |
| D428 | M055 planning | architecture | Initial M055 repo split scope | Limit M055 to a two-repo split: `mesh-lang` for language-owned surfaces and `hyperpush-mono` for Mesher/product surfaces. Defer separate `mesh-packages` and `mesh-website` repos beyond this milestone. | The real risk is product-versus-language workflow continuity, not maximizing repo count immediately. A two-repo split retires the highest operational coupling first while keeping docs, installers, registry, and packages/public-site surfaces coherent inside `mesh-lang`. | Yes | human |
| D429 | M055 planning | workflow | GSD authority model for the split | Keep `.gsd` authoritative inside each repo, and handle cross-repo work through a lightweight documented sibling-workspace coordination layer rather than one umbrella milestone tree pretending to own both repos. | Existing tests and verifiers already consume repo-local `.gsd`, `.tmp`, and repo-root artifacts. Preserving repo-local authority keeps those contracts truthful, while a small coordination layer is enough for normal day-to-day work across sibling repos without inventing a new orchestration system. | Yes | agent |
| D430 | M055 planning | boundary | Ownership of public starter and ecosystem surfaces during the two-repo split | Keep evaluator-facing generated examples, starter guidance, public docs/install ownership, and the packages deploy contract language-owned in `mesh-lang` for M055. | R116-R122 already pressure the language repo to remain the clear public starting surface. Leaving those surfaces in `mesh-lang` for this milestone keeps one coherent Mesh story while the product repo is extracted and its toolchain contract is made truthful. | Yes | collaborative |
| D431 | M055 planning | architecture | Canonical source for public repo identity | Introduce one canonical repo-identity contract that owns GitHub slugs, public repo URLs, installer/doc roots, and scaffolded runbook link generation or validation across both repos. | Repo slug drift is already a live contract problem across docs, installers, scaffold output, packages UI, and verifier scripts. Centralizing repo identity early prevents the two-repo split from turning link cleanup into a permanent whack-a-mole problem. | Yes | agent |
| D432 | M055/S01 planning | workflow | Durable homes for the M055 split workspace, repo-identity, and installer contracts | Use root `WORKSPACE.md` for the blessed two-repo workspace and repo-local GSD rule, `scripts/lib/repo-identity.json` as the machine-readable repo-identity source, and keep `tools/install/install.{sh,ps1}` as the editable installer sources mirrored into `website/docs/public/`. | S01 needs one maintainer-visible workspace contract and one language-neutral identity source before any extraction begins. A root workspace doc is easy to discover from `README.md` and `CONTRIBUTING.md`, JSON can be validated from JS/Python/shell without inventing a runtime dependency, and keeping installer edits rooted in `tools/install/` preserves one source/copy ownership story while the public installer bytes still live under the docs tree. | Yes | agent |
| D433 | M055 S01 T02 | tooling | How repo identity and installer ownership are centralized for the M055 split contract | Use scripts/lib/repo-identity.json as the canonical repo-identity source, and keep tools/install/install.{sh,ps1} plus website/docs/public/install.{sh,ps1} as plain source/copy scripts validated against that JSON rather than parsing it at runtime. | This gives later slices one durable source of truth for mesh-lang versus hyperpush-mono identity while preserving the simple public installer contract for curl/irm usage. Validation can fail closed on slug or URL drift without adding a repo-local JSON dependency to shipped installer execution. | Yes | agent |
| D434 | M055/S01/T03 | repository-boundary | Ownership of mesher/frontend-exp during the M055 split | Treat mesher/frontend-exp as product-owned mesher repo material, not as a language-owned mesh-lang surface. | The user clarified that frontend-exp will replace the existing frontend later, so split and extraction work should keep it on the mesher/hyperpush side rather than wiring it into language-repo ownership or public language surfaces. | Yes | human |
| D435 | M055/S01 closeout | verification | Authoritative M055/S01 split-boundary acceptance rail | Use a narrow `bash scripts/verify-m055-s01.sh` wrapper that replays the split-contract node test, the repo-identity/local-docs helper, the packages build, the landing build, and the named M046 repo-local `.gsd` cargo seam; do not widen it to unrelated historical rails or the baseline-red whole-app landing `tsc --noEmit` gate. | S01 needed one obvious stop/go command for workspace ownership, repo identity, and repo-local `.gsd` authority. The landing TypeScript gate is still red in unrelated files (`components/blog/editor.tsx`, `components/landing/infrastructure.tsx`, and `components/landing/mesh-dataflow.tsx`), so the truthful consumer-facing seam for this slice is `npm --prefix mesher/landing run build`, while the named M046 cargo test remains the authoritative repo-local `.gsd` regression. | Yes | agent |
| D436 | M055/S02 planning | toolchain | Mesher toolchain and verifier ownership before `hyperpush-mono` extraction | Make Mesher use package-owned `mesher/scripts/*` maintainer surfaces, resolve `meshc` from an enclosing `mesh-lang` source checkout before the blessed sibling `../mesh-lang` workspace and then `PATH`, and keep `scripts/verify-m051-s01.sh` only as a compatibility wrapper over the package-owned verifier. | S02 needs `hyperpush-mono` to look operationally real before extraction without inventing a workspace manager or leaving hidden repo-root coupling in mesh-lang-only scripts. A package-owned script/verifier layer gives the product surface something movable, keeps the blessed sibling-workspace contract explicit, and still lets the existing mesh-lang-side proof rails stay truthful until S04 performs the actual extraction. | Yes | agent |
| D437 | M055/S02/T01 | toolchain | Mesher maintainer scripts toolchain resolution and build staging contract | Resolve meshc in order: enclosing mesh-lang source checkout, blessed sibling ../mesh-lang workspace, then installed PATH meshc; if a higher-priority source tier exists but its target/debug/meshc is missing, fail closed instead of falling through. Package-local build output must stage outside mesher/ via an explicit bundle directory. | The slice needs a product-owned maintainer surface that stays truthful both while Mesher still lives under mesh-lang and after it moves into the blessed sibling workspace. Falling through from a detected source checkout to PATH would hide a broken toolchain contract, and allowing in-package output would preserve the current binary-churn problem the slice is meant to remove. | Yes | agent |
| D438 | M055/S02/T02 | observability | Authoritative Mesher deeper-app verifier ownership | Use `mesher/scripts/verify-maintainer-surface.sh` as the only authoritative Mesher proof replay, and keep `scripts/verify-m051-s01.sh` as a compatibility wrapper that post-validates the delegated verifier markers instead of reimplementing the rail. | The product-owned verifier now exercises the package-local test/build/migrate/smoke contract directly, retains its own phase markers and proof bundle under `.tmp/m051-s01/verify/`, and avoids a second repo-root verifier implementation that would drift from the Mesher-owned surface. | Yes | agent |
| D439 | M055/S02 | documentation | Primary Mesher maintainer contract after M055/S02 | Use `mesher/README.md` plus `bash mesher/scripts/verify-maintainer-surface.sh` as the primary deeper-app maintainer runbook and verifier, and keep `bash scripts/verify-m051-s01.sh` as a mesh-lang compatibility wrapper only. | S02 moved Mesher onto package-owned toolchain/build/migrate/smoke/verifier surfaces that can move into `hyperpush-mono` without hidden repo-root coupling. Making those product-owned surfaces primary keeps the deeper-app story truthful, while the repo-root wrapper remains only to preserve existing mesh-lang compatibility and proof continuity. | Yes | agent |
| D440 | M055/S03 planning | workflow | What the mesh-lang hosted deploy/public proof graph owns in S03 | Restrict the mesh-lang hosted/public proof contract to registry, packages website, docs/public-surface checks, and the slice-owned assembled verifier; remove Hyperpush landing deployment and landing health checks from the language repo workflow/verifier graph. | R120 requires mesh-lang to present one coherent language-owned public story, while the current deploy-services workflow still deploys and verifies product landing surfaces. Splitting the workflow/verifier graph in S03 keeps the packages/public-site contract inside the normal mesh-lang hosted proof while avoiding further monorepo coupling before S04 extracts the product repo. | Yes | collaborative |
| D441 | M055/S03 planning | boundary | How mesh-lang public starter/docs surfaces hand off to the deeper maintained app before S04 extraction | Use a repo-identity-backed product handoff in language-owned scaffold/docs/skill surfaces instead of direct local `mesher/...` source-path teaching; the S02 Mesher contract remains the product-owned implementation seam, but mesh-lang public surfaces stop presenting that local path as language-owned public contract. | S03 must let mesh-lang stand on its own before the actual repo extraction. Direct `mesher/README.md` and `scripts/verify-m051-*` markers in public scaffold/docs/skill surfaces make the language repo’s public contract depend on product-repo source paths living in the same checkout. A repo-boundary handoff preserves the deeper maintained-app story from S02 without baking the current monorepo path into the public contract. | Yes | collaborative |
| D442 | M055/S03/T01 | docs-contract | Canonical source for repo-boundary product handoff metadata across public scaffold/docs/tests | Add a root-level `productHandoff` object to `scripts/lib/repo-identity.json`, keep the language/product repo records unchanged, and have scaffold/docs tests derive public product handoff markers from that contract instead of hardcoding local `mesher/...` or `verify-m051-*` paths. | The S01 language-vs-product repo split already defines repo ownership, so the new handoff metadata should extend that contract rather than fork it. A dedicated `productHandoff` object lets public surfaces point across the repo boundary consistently while leaving older repo-identity consumers intact. | Yes | agent |
| D443 | M055/S03/T02 | docs-contract | How proof-page surfaces should represent the maintained-app handoff after the repo split | Public-secondary proof pages now hand off to the Hyperpush product repo and its Mesher runbook; local `verify-m051*` rails remain documented only as retained mesh-lang compatibility wrappers. | This keeps evaluator-facing docs on a repo-boundary public contract, removes mesh-lang-local product-source-path teaching, and preserves honest retained verifier mapping for maintainers without promoting those wrappers to first-contact surfaces. | Yes | agent |
| D444 | M055/S03/T04 | workflow | How mesh-lang proves deploy-services hosted/public surfaces after the M055 split | Treat deploy-services as the mesh-lang-owned registry + packages/public-surface workflow on main, forbid landing jobs/checks in both source and hosted-evidence verifiers, and keep the shared public-surface helper as the only health-check implementation. | The language repo still owns registry, packages-website, and the public docs/install/package surfaces, but Hyperpush landing is now a product-repo concern. Standardizing deploy-services on the mainline language-owned graph removes the stale landing dependency and keeps source verifiers, hosted evidence, and the assembled slice wrapper on one contract. | Yes | agent |
| D445 | M055/S04 planning | architecture | How M055/S04 should realize the Hyperpush extraction and two-repo evidence contract | Preserve the published `hyperpush-mono/mesher/...` product shape, extract it through an explicit allowlist materializer that excludes local state, and assemble S04 proof by coordinating repo-owned language/product verifiers with explicit repo/ref attribution. | S01-S03 already publish the product handoff as `hyperpush-mono/blob/main/mesher/...`, so flattening the product repo would churn the public contract for no gain. A raw recursive copy is unsafe because the current `mesher/` tree contains nested Git state, dependency caches, build outputs, and local env files. The honest seam is therefore a fail-closed materializer plus one assembled wrapper that shows which repo/ref proved language continuity and which repo/ref proved product continuity. | Yes | agent |
| D446 | M055/S04/T01 | workspace | Canonical extracted Mesher workspace layout and toolchain resolution | Treat `hyperpush-mono/mesher` as the only blessed extracted product package root, resolve `sibling-workspace` against the sibling `mesh-lang` repo from that nested root, and fail closed on flat `<workspace>/mesher` or mixed direct-sibling `../mesh-lang` assumptions. | The public workspace contract already makes Hyperpush the product repo. Letting Mesher scripts or docs keep a flat direct-sibling package model would make the later materializer and two-repo proof rails ambiguous and could silently attribute product proof to the wrong repo layout. | Yes | collaborative |
| D447 | M055/S04/T02 | verification | Which extra root surface the staged hyperpush-mono repo must include so the existing Mesher maintainer verifier stays runnable from the extracted product root | Stage a product-root `scripts/verify-m051-s01.sh` compatibility wrapper alongside the README, landing verifier/workflow, and dependabot templates. | `mesher/scripts/verify-maintainer-surface.sh` still records and copies that wrapper as part of its retained proof bundle. Adding the wrapper to the staged product root keeps the existing verifier truthful and avoids broadening this task into a maintainer-verifier rewrite. | Yes | agent |
| D448 | M055/S04/T03 | coordination | How mesh-lang compatibility and hosted-evidence verifiers resolve sibling product and language repo identity in the two-repo workspace | Use scripts/lib/m055-workspace.sh plus scripts/lib/repo-identity.json as the authoritative sources for sibling hyperpush-mono resolution and default language repo slug resolution. | The in-repo mesher tree and the current origin remote are no longer trustworthy after the two-repo split. A shared helper keeps wrapper and hosted-evidence rails aligned, preserves explicit env overrides for debugging, and makes drift fail closed with visible source markers. | Yes | agent |
| D449 | M055/S04/T04 | evidence | How the two-repo S04 verifier attributes the product repo ref in a staged non-git hyperpush-mono workspace | Record hyperpush-mono continuity as the canonical product repo slug plus `materialized:<manifest fingerprint>`, and record the source mesh-lang git SHA separately in the product repo metadata. | The staged hyperpush-mono repo produced by `scripts/materialize-hyperpush-mono.mjs --check` intentionally excludes `.git`, so there is no truthful product HEAD to record. Using the staged manifest fingerprint makes the product proof reproducible and specific to the exact extracted tree, while the source mesh-lang SHA keeps the lineage to the source checkout explicit. | Yes | agent |
| D450 | M057 planning | planning | Truth source for M057 tracker reconciliation | Use the current local code state in `mesh-lang` and the sibling product repo as the source of truth, then make GitHub issues and project state conform to it. | The repos have already shipped and split in ways the public tracker does not express. Using stale GitHub issue text as truth would preserve drift instead of repairing it. | Yes — if a later automation milestone establishes a stronger canonical tracker source. | collaborative |
| D451 | M057 planning | process | Mutation order for cross-repo tracker reconciliation | Reconcile through an explicit audit ledger first, then fix repo issues, then realign the org project. | The dangerous failure mode is false-green cleanup. A ledger-first flow preserves evidence and prevents the portfolio board from being edited independently of issue truth. | Yes — if execution proves a narrower or broader ordering is required. | collaborative |
| D452 | M057 planning | project-governance | Cross-repo planning surface for M057 | Use org project fields/status as the cross-repo portfolio layer and keep repo milestones optional for local grouping only. | GitHub Projects v2 is the best org-wide portfolio surface, while cross-repo milestone synchronization remains weak. This keeps issue ownership local and board truth cross-repo. | Yes — if later GitHub capabilities make cross-repo milestones the clearer truth surface. | collaborative |
| D453 | M057 planning | boundary | Product repo naming on public tracker surfaces | Normalize stale `hyperpush-mono` tracker wording to the public `hyperpush` repo identity where the external tracker is referring to the product repo, while keeping the local workspace path as an implementation detail. | The code and docs already use a local `hyperpush-mono` workspace path while GitHub resolves the public product repo as `hyperpush`. Tracker wording should follow the public repo identity so ownership reads clearly. | Yes — if the public repo identity changes again later. | collaborative |
| D454 | M057/S01 planning | data-shape | Canonical audit artifact shape for M057/S01 | Persist three layers under the slice: raw live GitHub snapshots, a code-evidence index plus naming/ownership map, and one joined reconciliation ledger keyed by canonical issue URL with project item ids and field snapshots carried through from the raw data. | Later reconciliation slices need deterministic mutation targets without re-querying or re-deriving tracker truth from stale issue prose. Separating raw capture from derived evidence and the final ledger preserves replayability, supports R130/R133, and makes count/evidence mismatches fail closed instead of being hidden inside one opaque report. | Yes | agent |
| D455 | M057/S01/T01 | tracker-reconciliation | Raw GitHub inventory snapshot shape for M057/S01 | Persist four normalized JSON snapshots under the slice directory, keyed by canonical issue URLs, with explicit hyperpush alias->canonical repo capture, project_item_id capture, and schema-seeded tracked project field maps that backfill missing values to null. | Later reconciliation and mutation work needs one deterministic join key across mesh-lang issues, hyperpush issues, and org project items. GitHub canonicalizes hyperpush-mono to hyperpush and ProjectV2 fieldValues are sparse, so storing canonical issue URLs plus schema-driven null backfill avoids alias drift, orphan joins, and false schema-drift errors in downstream tasks. | Yes | agent |
| D456 | M057/S01/T02 | tracker-reconciliation | How the evidence index should encode product repo naming and ownership truth | Persist workspace_path_truth separately from public_repo_truth and normalized_canonical_destination in the naming/ownership map and every evidence row. | The local workspace still uses hyperpush-mono compatibility paths while the public canonical repo and tracker destination are hyperpush. Separating these fields lets downstream ledger and mutation work normalize repo ownership without losing the concrete implementation-path evidence or guessing from stale tracker wording. | Yes | agent |
| D457 | M057/S01/T03 | tracker-reconciliation | How the final M057/S01 ledger should represent missing tracker coverage without breaking the issue/project join contract | Store the canonical ledger as 68 issue-backed rows keyed by canonical_issue_url, and record uncovered shipped surfaces like /pitch in a separate derived_gaps section instead of inventing synthetic issue rows. | The slice contract requires exact 68-row issue coverage and exact 63 joined project rows. A separate derived_gaps section preserves those fail-closed counts while still giving downstream slices concrete create/repoint actions for shipped work that has no tracker row yet. | Yes | agent |
| D458 | M057/S02 planning | tracker-reconciliation | Execution shape for M057/S02 repo issue corrections | Drive all live repo mutations from generated plan/results artifacts, transfer `hyperpush#8` into `mesh-lang` instead of recreating it, and create one retrospective `hyperpush` issue for shipped `/pitch` before closing it as completed. | An explicit manifest-plus-results flow makes the risky repo mutations reviewable and resumable, while transfer preserves `hyperpush#8` history per R132 and a dedicated retrospective `/pitch` issue satisfies R131 without leaving the shipped surface implicit in milestone prose. | Yes | agent |
| D459 | M057/S02/T03 | tracker-reconciliation | How M057/S02 should verify live repo truth after transfer and retrospective issue creation | Verify post-mutation repo state by replaying `gh issue view` against the persisted `final_state` snapshots in `repo-mutation-results.json`, and treat the transferred source issue's absence in the source repo as expected while canonical old→new identity comes from the results artifact. | GitHub issue transfers and retrospective creates change canonical issue identity in ways the source-repo issue number alone cannot express. Replaying live state against the persisted final snapshots makes drift fail at the exact touched issue, while using the results artifact for the old→new mapping preserves the truthful transfer/create history for downstream S03 work. | Yes | agent |
| D460 | M057/S03 planning | tracker-reconciliation | Truth source for M057/S03 org project realignment when S02 live repo state has drifted | Run the retained S02 verifier as an S03 preflight, record any drift in the S03 plan artifact, and derive project mutations from current live repo truth plus the persisted S02 canonical issue mappings instead of replaying stale S01/S02 end states onto the board. | The board must reflect what the repos say now, but S02 already established the canonical transfer/create identities that S03 must preserve. Treating the red S02 verifier as a recorded preflight and then rebasing from current repo truth prevents stale board writes, preserves the canonical identity changes, and keeps the final org project honest about what is done, active, and next. | Yes | agent |
| D461 | M057/S03/T03 | board-reconciliation | How the S03 project planner should treat S01 ledger rows marked leave_untracked | Skip leave_untracked rows as explicit repo-only no-ops, but fail closed if any such row is marked project_backed or already exists on the live project board. | The S01 ledger intentionally keeps a small set of repo issues outside the board snapshot. Treating leave_untracked as an error blocks truthful planning, while silently accepting a live board item for those rows would hide board drift that S03 is supposed to reconcile. | Yes | agent |
| D462 | M057/S03/T04 | runtime | How the M057/S03 project-board applicator handles post-mutation GitHub Projects V2 consistency lag | Retry live board capture and added-row discovery inside the S03 applicator before treating the run as failed, while keeping the shared S01 inventory capture contract strict. | Real apply attempts showed that `gh project item-add` can succeed while the immediate GraphQL refresh briefly reports page-count drift or hides the new row. Retrying inside the slice-specific applicator fixes the live eventual-consistency seam without weakening the canonical S01 inventory verifier that still needs to fail closed on durable snapshot drift. | Yes | agent |
| D463 | M057/S03 T02 | tracker-reconciliation | How the retained S02 verifier should interpret GitHub CLI `stateReason` output on open issues | Normalize blank-string `stateReason` values from `gh issue view --json stateReason` to `null`, while preserving real non-empty values like `reopened`. | GitHub CLI can emit empty strings for open issues, which would create false drift in the retained live-state replay even when issue truth is unchanged. Treating blank strings as null keeps the verifier focused on real state changes while still surfacing genuine reopen metadata. | Yes | agent |
| D464 | M057/S03 T05 | board-reconciliation | How the retained S03 verifier should classify board drift | Delegate the retained S02 verifier first, then only inspect project membership and field coherence if the repo-truth precheck stays green. | If repo issue truth has drifted upstream, board verification should fail as repo-truth drift before maintainers spend time debugging project rows. This keeps failure diagnosis aligned with the slice dependency chain and narrows investigation to the right surface. | Yes | agent |
| D465 | M058 discussion | scope | How M058 should bound frontend/backend scope | Focus the milestone on wiring `mesher/frontend-exp` to existing Mesher backend functionality, allowing only minimal backend fixes for clearly broken seams and excluding new route creation. | The user explicitly wants the milestone centered on connecting already-available backend functionality to the new frontend and does not want new Mesher routes added just to satisfy mock UI surfaces. | Yes — if execution proves a supposedly existing contract is unusable without a narrowly scoped additive seam | human |
| D466 | M058 discussion | architecture | Which frontend framework M058 should target | Migrate `mesher/frontend-exp` from Next.js to TanStack Start during the integration wave. | The user explicitly redirected the architecture from Next.js to the TanStack Router framework and confirmed TanStack Start as the intended target. | Yes — if migration evidence shows TanStack Start blocks honest integration progress for reasons not visible during discussion | human |
| D467 | M058 discussion | pattern | How M058 should structure frontend data access and missing-contract visibility | Keep the dashboard client-heavy, but move backend access into typed TanStack Start route loaders and `createServerFn` wrappers, and publish a checked-in markdown gap ledger for unsupported frontend/backend seams. | This preserves the existing interactive dashboard feel while giving the integration wave one explicit contract seam for live data and one durable artifact for missing endpoints, fields, or mismatched shapes. | Yes — if slice execution shows a thinner or thicker abstraction is required to keep the integration honest | collaborative |
| D468 | M058/S01 planning | frontend-contract | How S01 should handle project selection and unsupported core dashboard controls while wiring live Mesher data | Use a single router-owned `project` slug search param defaulting to `default`, and reduce the core live shell to backend-supported project/status/level/search controls, cards, and chart shapes instead of preserving hardcoded multi-project menus or unsupported mock-era affordances. | Mesher exposes issue and dashboard reads for a project slug or UUID but has no project-list endpoint, and the current hardcoded `hyperpush-*` selectors do not match the seeded local `default` project. Keeping those selectors or unsupported cards/filters would make the live shell look wired while still depending on fake product assumptions. A router-visible `project` param plus an honest reduced overview keeps S01 aligned with the existing backend contract and the milestone boundary against adding new routes. | Yes | agent |
| D469 | M058/S01/T01 | library | TanStack Start version selection for frontend-exp | Pin @tanstack/react-start to 1.167.20 instead of using the latest 1.167.21 release range | npm install against 1.167.21 fails because the published package currently depends on @tanstack/react-start-rsc@0.0.1, which is not available on npm. Pinning 1.167.20 keeps the TanStack Start migration buildable while preserving the TanStack Router 1.168.13 scaffold. | Yes | agent |
| D470 | M058/S01/T02 | architecture | Mesher live dashboard data seam shape for S01 | Map Mesher raw issue statuses and event levels into a reduced live UI model inside src/lib/mesher/normalize.ts while keeping server-function query filters aligned with backend-supported enums. | The backend exposes statuses unresolved/resolved/archived/discarded and levels fatal/error/warning/info/debug, while the mock-era UI expected richer open/ignored/regressed/in-progress and critical/high/medium/low semantics. Centralizing the remap at one normalization seam keeps leaf components honest, preserves backend-only query compatibility for later route search params, and makes malformed or unsupported payloads fail before render. | Yes | agent |
| D471 | M058/S01/T03 | routing | How the dashboard route should map URL-visible filters onto Mesher backend queries | Keep project/status/level/q/panel/issue in a dedicated dashboard search helper, validate them before route use, and only translate the subset that Mesher can express one-to-one into loader deps and backend filters. | The route needs a single inspectable URL state model, but Mesher cannot represent every reduced UI filter value directly (for example ignored and low collapse multiple backend enums). A dedicated helper keeps URL normalization, client-side fallback filtering, and loader invalidation boundaries explicit without reintroducing divergent local header/sidebar state. | Yes | agent |
| D472 | M058/S01/T04 | frontend | How the S01 live dashboard surfaces should consume Mesher data | Render the overview widgets strictly from the reduced LiveDashboardModel and LiveIssue contract, and keep unsupported mock-era fields out of the shared stats/chart/list/detail components. | The S01 route can only truthfully show health, severity breakdown, hourly volume, and reduced issue triage fields from the current Mesher endpoints. Keeping the shared components on that narrow typed seam prevents fake metrics or detail metadata from creeping back in and lets the replay script, route tests, and UI all validate the same backend contract. | Yes | agent |
| D473 | M058/S02 planning | routing | How S02 should represent issue detail, alerts, and settings navigation state in TanStack Start | Keep S02 inside the existing `/` TanStack file route and extend the validated dashboard search model with `view`, `alert`, `event`, and `settingsTab` state instead of introducing new route families for alerts/settings/detail. | S01 already established one router-owned search-state seam for supported dashboard filters and panel state. Extending that seam keeps URL-visible ownership in one place, avoids reintroducing local `activeNav` drift, and stays aligned with the milestone boundary against broad route-tree expansion while still making alerts/settings/detail flows loader-visible. | Yes | agent |
| D474 | M058/S02 planning | frontend-contract | How S02 should handle the mismatch between the current mock UI and the existing Mesher alert/settings contracts | Reduce the alerts and settings UI to backend-supported fields and actions only: fired alerts use `active\|acknowledged\|resolved`, settings expose `retention_days`, `sample_rate`, storage usage, and alert-rule create/toggle/delete, and unsupported mock-era tabs or affordances are removed or demoted. | The current frontend expects severity, channels, team/billing/API-key settings, and other fields the backend does not expose. Preserving those affordances would make the live shell dishonest. A reduced contract keeps S02 inside existing Mesher scope, advances R141 directly, and gives later slices a truthful base instead of a partially fake admin surface. | Yes | agent |
| D475 | M058/S02/T01 | architecture | How S02 Mesher detail and mutation routes are represented in the frontend adapter seam | Extend the shared Mesher URL/fetch adapter to support both project-scoped and non-project-scoped backend paths, and normalize event detail into reduced summary/count fields instead of passing raw JSONB payloads through to the UI. | S02 needs existing Mesher routes under both `/projects/:project/...` and top-level `/issues/:id/...`, `/events/:id`, `/alerts/:id/...`, and `/alert-rules/:id/...`. Keeping them behind one typed fetch/normalization seam preserves endpoint-scoped errors and avoids route-specific ad hoc fetch logic. Reducing event detail to summary fields, counts, and navigation honors the slice redaction constraints while still exposing enough data for honest UI drilldown. | Yes | agent |
| D476 | M058/S02/T02 | ui-state | How the TanStack Start dashboard shell should expose top-level navigation during the S02 live-detail rollout | Restrict the shell sidebar to the router-backed `issues`, `alerts`, and `settings` views and derive the active section entirely from the validated dashboard search model instead of keeping unsupported pages behind local navigation state. | The route contract and validation model only support `issues`, `alerts`, and `settings`, and T02 needs URL-owned navigation plus honest refresh behavior. Leaving additional local-only sections in the same sidebar would preserve a split-brain navigation model where some sections are router-visible and others are not, making stale-selection cleanup and downstream live-data work harder to reason about. | Yes | agent |
| D477 | M058/S02/T03 | observability | How the live alerts route should handle fired-alert read failures | Keep fired-alert read failures inside the alerts view as explicit in-page error state instead of failing the entire dashboard route | The slice contract requires alert read failures, timeouts, and malformed payloads to stay visible without breaking the rest of the dashboard shell. Catching alert-list failures in the route loader and surfacing endpoint-scoped error text in the alerts view preserves navigation, stale-selection cleanup semantics, and later debugging context. | Yes | agent |
| D478 | M058/S02/T04 | frontend-integration | How the live settings surface should handle partial endpoint failures and mutation refresh semantics | Keep project settings, storage, and alert-rule reads fail-soft per endpoint inside the settings view, but revalidate router-owned state only after successful settings and alert-rule mutations. | S02 must keep the reduced settings surface honest when one backend read fails without blanking the entire view, while failed saves should preserve the operator's current form inputs and action context for inspection. Refreshing only after successful mutations keeps the UI sourced from Mesher truth without hiding failed writes behind speculative local state. | Yes | agent |
| D479 | M058/S02/T04 | verification | How the S02 live replay verifier should fail while still cleaning up temporary seeded alert rules | Have the verifier throw structured failures and rely on try/finally cleanup instead of calling process.exit directly inside verification helpers. | The S02 replay verifier may create a temporary low-threshold alert rule to seed a fired alert. Throwing on the first shape or runtime failure keeps the verifier fail-closed, while try/finally still gets a chance to delete the temporary rule so repeated verification runs do not accumulate seeded state in the shared local Mesher database. | Yes | agent |
| D480 | M058/S03 planning | frontend-contract | How S03 should treat team membership while wiring the remaining live admin surface | Ship project-scoped API keys in S03 and treat team membership as a gap-ledger item unless execution finds an already-existing safe org-id and user-discovery seam without adding routes. | The current frontend route model is project-scoped and already fits the existing API-key routes, but the team-membership endpoints require a raw org_id plus user_id inputs that are not present in the live frontend contracts. R139 forbids adding new project→org lookup routes just to make the UI easier, so the honest path is API keys now and explicit ledgering of team membership unless a safe existing seam turns up. | Yes | agent |
| D481 | M058/S03 planning | security | How the S03 frontend should handle Mesher API key values | Mask API-key list data at the adapter/UI seam and confine raw created key values to one-time post-create confirmation state plus redacted verifier handling. | The existing backend list and create responses expose raw key_value strings, but the project rules forbid echoing or logging secrets. Reducing list rows to masked previews and limiting raw create values to the narrowest local confirmation flow keeps the live admin surface useful without leaking secrets into route state, persistent UI, or verification output. | Yes | agent |
| D482 | M058/S03/T02 | ui-architecture | Active dashboard shell should only expose route-backed Mesher surfaces | Removed the fake AI copilot panel from the searchable active-shell panel model and UI, leaving only the issue detail panel as a valid right-side surface. | This keeps router state honest, prevents unsupported product chrome from reappearing through URL state, and aligns shell affordances with live-backed Mesher capabilities. | Yes | agent |
| D483 | M058/S03/T03 | backend-contract | How Mesher should serialize API-key list rows for HTTP responses | Serialize API-key list rows through a typed ApiKey record with Json.encode instead of manual string interpolation | The live S03 replay showed that the manual api_key_to_json path corrupted UUID, label, project_id, and timestamp fields into bogus numeric placeholders even though the database row was correct. Reusing the existing typed Json.encode normalization pattern preserves the real values and keeps the frontend/verifier contract honest without widening backend scope. | Yes | agent |
| D484 | M058/S04 planning | verification | How S04 should treat final proof when replay scripts and the real browser disagree | Use the real browser outcome as the validation gate for R138-R141, fix only minimal broken seams inside existing Mesher routes when possible, and require the final verifier/proof artifact to name any remaining failed-in-proof surface instead of inferring success from earlier contract replays. | Research showed `verify-s02-live-detail-alerts.mjs` passed while the live issue-detail route still failed on malformed timeline data. Final assembly proof must therefore be browser-grounded, with replay rails upgraded to catch the same class of drift without widening backend scope beyond existing route families. | Yes | agent |
| D485 | M058/S04/T02 | verification | How the S04 assembled proof rail should compose prior live verifiers and wording checks | Implement verify-s04-assembled-proof as a fail-closed composed verifier that reruns S02 and S03, statically checks the settings proof copy against deferred-surface wording, then emits one assembled live/empty-but-live/deferred/failed-in-proof summary line. | This preserves the already-hardened live-detail and admin rails, avoids duplicating route-level proof logic, and makes wording drift or malformed runtime payloads fail one named command before browser/UAT closeout. | Yes | agent |
| D486 | M058 | scope | Scope boundary for the dashboard migration | Treat M058 as an equivalence-focused framework migration only; preserve current URLs, visuals, interactions, and mock-data behavior while swapping Next.js for TanStack Start. | The user explicitly wants the app to look identical and work exactly the same, with the framework as the only substantive difference. | Yes — if execution proves strict parity is impossible for a specific surface and the user approves a bounded exception. | human |
| D487 | M058 | convention | Canonical app path and operator contract for the migrated frontend | Rename the product dashboard app from `mesher/frontend-exp` to `mesher/client` as part of the migration, while preserving the same external `dev`, `build`, and `start` command contract. | The user explicitly approved the rename but wants maintainer command ergonomics to stay unchanged. | Yes — if a later product milestone needs a broader repo/app naming reset. | human |
| D488 | M058 | architecture | TanStack migration architecture for the frontend app | Use TanStack Start with file-based TanStack Router routes and keep current mock-data modules authoritative during the migration. | File-based routing keeps the migration close to the current file-router mental model and limiting the change to the framework avoids mixing route/runtime migration with backend integration work. | Yes — after parity is proven and a later milestone moves the app to real Mesher backend data. | collaborative |
| D489 | M058 discussion | scope | What scope bar governs the TanStack Start migration for the dashboard | Treat M058 as a strict framework migration with exact visual and behavioral parity, not a product redesign or backend-expansion milestone. | The user explicitly said the design should be identical at the end and the only intended difference should be the underlying framework. | Yes | collaborative |
| D490 | M058 discussion | architecture | How the app path migration should be executed | Convert the existing `../hyperpush-mono/mesher/frontend-exp` app in place, then rename/move the finished app to `../hyperpush-mono/mesher/client`. | The user rejected a fresh parallel port and approved a mutate-in-place migration that lands at `mesher/client` once parity is proven. | Yes | collaborative |
| D491 | M058 discussion | pattern | How routing should change during the framework migration | Use proper TanStack route modules for the current dashboard sections while preserving the same visible shell, URLs, and interaction model. | The user explicitly rejected a one-route shell and required proper routes without changing the actual UI in any way. | Yes | collaborative |
| D492 | M058 discussion | architecture | What TanStack Start execution model the migration should use | Keep the migration client-first and avoid introducing server functions/loaders unless a specific framework seam truly requires them. | The current app is mock-data-driven and client-state-based, so introducing server behavior during the migration would add risk without helping the parity goal. | Yes | collaborative |
| D493 | M059 discussion | planning | How to handle the pre-existing GSD state collision on milestone ID M058 | Use the next clean milestone ID (M059) for the TanStack Start dashboard migration and treat M058 as a collided/legacy GSD state bucket rather than the active planning target. | `gsd_milestone_status` showed M058 already existed in the DB with completed slices, so reusing it would have mixed the new migration plan with unrelated prior state. The user explicitly chose the next clean milestone instead of repairing the old collision first. | Yes | human |
| D494 | M059 S01 T01 | architecture | How to map the current Next app-router entrypoints into TanStack Start during the in-place migration | Map `app/layout.tsx` to `src/routes/__root.tsx` and map the current dashboard shell from `app/page.tsx` to `src/routes/index.tsx` as a temporary parity adapter | TanStack Start expects document/head/script concerns in the root route, while the current visible shell is the `/` page content. Keeping the shell in the index route preserves visible behavior now and leaves a clean path for S02 to decompose real routes without rewriting the root document again. | Yes | agent |
| D495 | M059/S01/T02 | frontend-migration | How the in-place TanStack Start root should preserve the existing client-heavy dashboard shell and visual font contract during the plumbing swap. | Keep the current dashboard shell mounted through `src/routes/index.tsx` with `ssr: false`, and load Geist fonts via Fontsource stylesheet links from `src/routes/__root.tsx` instead of relying on Next font injection or importing font CSS through `app/globals.css`. | The existing shell was authored as a Next client component and the migration task was explicitly about replacing framework plumbing without widening into route decomposition or UI rewrites. Making the index route client-only preserves the current interactive shell safely under TanStack Start, while root-linked Fontsource CSS restores Geist/Geist Mono on Vite without tripping the Tailwind v4 CSS parser or leaving the app on fallback browser fonts. | Yes | agent |
| D496 | M059/S01 closeout | frontend-migration | How S01 should make the TanStack Start production start command truthful while the current build emits `dist/` output instead of Nitro `.output/`. | Preserve the external `npm run start` contract in `mesher/frontend-exp` with a package-local Node bridge server (`server.mjs`) that serves `dist/client` and forwards all other requests to TanStack Start's built `dist/server/server.js` fetch handler, instead of depending on Nitro's `.output/server/index.mjs` layout during this in-place migration stage. | Closeout verification showed the migrated app's documented `start` command failed because the current TanStack Start/Vite setup built `dist/server/server.js` and `dist/client/` rather than `.output/server/index.mjs`. Adding Nitro would widen the slice into deployment-plumbing changes. A thin package-local bridge preserves the required `dev` / `build` / `start` contract now, keeps the app bootable without Next.js on the runtime path, and leaves a clean later option to adopt Nitro or another deployment target explicitly. | Yes | agent |
| D497 | M059/S02 planning | frontend-migration | How S02 should structure the real TanStack routes for the dashboard shell | Use a pathless `_dashboard` layout route with flat child route files for each current top-level section, keeping Issues on `/` and keeping shared shell state in the layout rather than per-route leaves. | The current dashboard preserves sidebar collapse, shared header dropdown state, AI panel ownership, and Issues filter/detail persistence because one shell stays mounted while sections swap underneath it. A pathless TanStack file-route layout matches that behavior with the least UI drift, preserves the existing entry URL on `/`, and avoids widening the slice into search-param serialization or backend/server-loader work. | Yes | agent |
| D498 | M059/S02/T01 | architecture | How the TanStack runtime should stop depending on the legacy dashboard page monolith during route-parity work | Move the active dashboard runtime into a shared client `components/dashboard/dashboard-shell.tsx` plus `dashboard-route-map.ts`, and have `src/routes/index.tsx` render that shell directly while leaving `app/page.tsx` as a thin wrapper. | This preserves the current mock-data/client-state Issues experience and visible shell while decoupling the TanStack route from the temporary legacy page implementation. It also creates a reusable route-key/title seam and a stable test target for later file-route extraction without introducing loaders, server functions, or backend calls. | Yes | agent |
| D499 | M059/S02/T02 | architecture | How the extracted Issues route leaf should own and expose client state before full file-route decomposition | Keep search, status/severity filters, and selected issue detail in a shell-owned DashboardIssuesState provider, and expose the active Issues state on the extracted page through stable test/inspection attributes. | This preserves Issues behavior across leave-and-return flows when real TanStack file routes replace the temporary shell navigation, prevents route-local state resets, and gives Playwright/browser verification explicit observability for search/filter/detail parity without depending on React internals. | Yes | agent |
| D500 | M059/S02/T03 | routing | How dashboard section routing and unknown-path fallback should work in mesher/frontend-exp | Use a pathless `_dashboard` TanStack layout plus concrete child file routes for each top-level section, and serve unknown paths through a matched root splat route that renders the Issues shell instead of relying on shell-local nav state or a root `notFoundComponent`. | The pathless layout keeps one mounted dashboard shell/provider across route changes, makes deep links first-class, and preserves existing sidebar/header/AI seams. The root splat route satisfies the mock-data fallback requirement without returning a 404 document, which keeps Playwright runtime-signal verification clean on unknown paths. | Yes | agent |
| D501 | M059/S02/T04 | architecture | How the dashboard shell should coordinate TanStack navigation with shell-owned UI state | Sidebar navigation now uses real TanStack route links, while DashboardShell derives the active section from pathname and only runs transient side effects like AI-panel close and settings-header gating on pathname changes. | This removes split-brain top-level nav state, keeps URL/active-nav parity truthful for deep links and history navigation, and preserves shell-local Issues/filter/detail state instead of widening it into router state. | Yes | agent |
| D502 | M059/S02/T05 | verification | Canonical way to run isolated frontend-exp Playwright dev/prod parity projects | Use repo-owned npm scripts that set PLAYWRIGHT_PROJECT (`test:e2e:dev` and `test:e2e:prod`) as the stable isolated contract, while keeping the shared parity spec/config able to serve both environments. | `npm exec playwright ... --project=<name>` on this npm version emits an unknown-config warning and still runs both Playwright projects, so relying on that CLI shape hides whether dev or built production actually failed. The repo-owned scripts give deterministic single-project runs, and the shared spec still proves the same assertions in both environments. | Yes | agent |
| D503 | M059/S03 planning | frontend-migration | How S03 should complete the `frontend-exp` to `client` move without reopening the TanStack route/state migration | Treat S03 as a path-and-contract migration: move the already-migrated TanStack package wholesale to `../hyperpush-mono/mesher/client/`, keep package-local runtime/parity files structurally unchanged unless the rename exposes a real break, and update only the machine-checked external caller surfaces in the same slice. | Research and direct inspection showed the active app package is already path-agnostic internally and the main remaining risk is stale hardcoded `frontend-exp` references in CI, verifiers, docs, and root Playwright config. Keeping S03 narrow preserves the proven route/UI/mock-data behavior from S02 while satisfying the canonical-path and no-Next-runtime requirements with the least risk. | Yes | agent |
| D504 | M059/S03/T02 | verification | How the mesh-lang root Playwright harness should target the moved dashboard package | Mirror the package-local PLAYWRIGHT_PROJECT/npm_config_project selection logic in mesh-lang/playwright.config.ts and point the root harness at ../hyperpush-mono/mesher/client. | This keeps the cross-repo verifier aligned with the canonical mesher/client package-local parity suite and prevents root-level callers from booting both dev and prod servers when only one project was requested. | Yes | agent |
| D505 | M059/S04 planning | verification | How S04 should close the final dashboard migration proof and cleanup scope | Use the existing `../hyperpush-mono/mesher/client/tests/e2e/dashboard-route-parity.spec.ts` as the only browser proof rail for remaining equivalence gaps, and limit operational cleanup to the direct maintainer-facing AGENTS/CONTRIBUTING/SUPPORT/issue-template surfaces plus `./AGENTS.md`, leaving historical planning docs and product mock-data copy untouched. | Research showed the meaningful remaining runtime gaps are narrow and already have stable `data-testid` seams in the canonical package path, so adding a second harness would duplicate truth instead of strengthening it. The remaining path drift is concentrated in human-facing operational guidance, while other `frontend-exp` and Next.js strings still legitimately exist in history files, landing, or mock content. Keeping S04 on one proof rail plus scoped direct-operational cleanup advances R143/R145/R146/R148 with the least risk of reopening settled route/runtime architecture. | Yes | agent |
| D506 | M059/S04/T01 | verification | How S04 should handle runtime regressions surfaced by the final dashboard parity rail | Keep the final proof inside `../hyperpush-mono/mesher/client/tests/e2e/dashboard-route-parity.spec.ts` and fix surfaced runtime console failures in app code rather than weakening the runtime-signal assertions. | The expanded parity rail is the canonical clean-boot/browser-equivalence surface. When it surfaced a Recharts console warning during Issues boot, weakening the console/request tracker would have hidden a real regression. Disabling the chart Area animations in runtime code preserved the existing client-only contract while keeping the clean-runtime proof honest. | Yes | agent |
| D507 | M059/S04/T02 | operability | How S04 should prove the final direct-operational cleanup after the dashboard path migration | Limit cleanup to the selected maintainer-facing docs/templates and prove closure with scoped stale-path/positive-path greps plus the canonical `mesher/client` build, parity, and root-harness rails. | The remaining risk after the path cutover was maintainer drift, not additional runtime architecture work. Targeted grep proof over the selected operational surfaces, combined with the existing build/dev/prod/root-harness checks, closes the migration without reopening historical documents or unrelated product content. | Yes | agent |
| D508 | M060 discussion | scope | What M060 should wire versus what it should defer | Wire every existing Mesher backend route into `mesher/client`, allow only minimal backend seam fixes, and do not add new backend surfaces just to satisfy mock-only screens. | The user wants this milestone centered on connecting the current shell to the existing backend, fixing just enough to work, and not widening into a product-expansion or backend-cleanup wave. | Yes — if execution proves a supposedly existing contract is unusable without one narrowly scoped additive seam. | human |
| D509 | M060 discussion | architecture | How the dashboard should establish real context in M060 | Use the backend's existing seeded/default project-org-API-key reality instead of adding a polished login/session flow. | The user explicitly chose the existing backend reality as the live context seam and did not want the milestone to widen into a new auth UX. | Yes — if execution proves the existing context is not sufficient to support a truthful shell walkthrough. | human |
| D510 | M060 discussion | pattern | How mixed live and still-mocked shell surfaces should behave | Keep the current shell visually intact, leave still-mocked UI in place, and make backend failures visible through minimal truthful feedback including toast-style notifications. | The user explicitly said to change as little UI as possible, not remove mocked UI, and use shadcn/Radix toast-style feedback instead of redesigning the experience. | Yes — if a specific screen proves impossible to keep honest without a small visible affordance adjustment. | human |
| D511 | M060/S01 planning | architecture | How S01 should bridge the seeded Mesher backend into the existing dashboard shell | Use same-origin `/api/v1` proxying plus a typed live-overlay adapter rooted in `DashboardIssuesStateProvider` for M060/S01, merging Mesher read truth into the existing issues shell while preserving unsupported UI fields with stable fallback data. | The backend already exposes the needed seeded default-project reads, but the browser app currently has no proxy/CORS seam and the UI contract is richer than the Mesher API payloads. A proxy + adapter seam preserves the current shell, keeps unsupported surfaces visible, and lets failures surface through the existing toast pattern without widening scope into auth or UI rewrites. | Yes | agent |
| D512 | M060/S01/T01 | architecture | How the Mesher dashboard client resolves its backend target for same-origin /api/v1 reads | Use one shared MESHER_BACKEND_ORIGIN resolver module consumed by vite.config.ts, server.mjs, and playwright.config.ts | The task needed the same-origin proxy target to stay aligned across dev, built-prod, and automated verification. Centralizing origin parsing prevents drift between environments, keeps browser traffic on /api/v1, and lets Playwright either start or reuse the same Mesher backend port the app proxies to. | Yes | agent |
| D513 | M060/S01/T02 | pattern | How selected-issue live read failures should surface in the dashboard shell | Reuse the existing Radix toast + use-toast surface for selected-issue live read failures instead of inventing a new inline notification path. | The slice goal was to keep the current shell materially intact while making backend-backed failures visible. Reusing the mounted toast surface keeps failure feedback truthful, visible, and consistent with the existing UI patterns without redesigning the detail panel. | Yes | agent |
| D514 | M060/S01/T02 | operability | How the deterministic live-issue seed helper should behave when DATABASE_URL is absent in local verifier shells | Make `mesher/scripts/seed-live-issue.sh` reuse an already-running local Mesher backend when one is available, only requiring DATABASE_URL when it must boot its own temporary backend. | Auto-mode and maintainer shells in this split workspace do not reliably inherit DATABASE_URL. Reusing a running backend preserves deterministic seed/readback verification without widening the seam into manual env setup, while still allowing the script to self-boot when full database context is available. | Yes | agent |
| D515 | M060/S02 planning | architecture | Which issue actions S02 should wire live and where the orchestration should live | Keep S02 issue mutations inside `DashboardIssuesStateProvider`, expose only `resolve`, `unresolve`, and `archive` as live maintainer controls, and defer `assign`, `discard`, and `delete` until the shell can represent their backend contracts truthfully. | The existing provider already owns overview bootstrap, selected-issue hydration, cache state, and destructive toasts. Backend mutation success bodies are too thin for optimistic UI, so provider-owned refetch/invalidation is the safest seam. `assign` depends on org-member discovery not present in the seeded default bootstrap, `discard` produces backend status `discarded` that the current shell cannot represent, and `delete` is destructive without an existing affordance. Limiting S02 to resolve/unresolve/archive keeps the maintainer loop truthful without pulling S03 forward. | Yes | agent |
| D516 | M060/S02/T02 | observability | How the Issues summary chrome reports live vs fallback backing | Expose per-card summary source markers (`live`, `derived live`, `fallback`) while keeping the stats bar's overall source mixed when cards come from different seams. | The live Issues overview now mixes backend-backed health/count fields with intentionally fallback-only shell metrics. A single global source label would either overclaim the fallback cards or underclaim the live ones, so per-card markers keep the existing chrome truthful after mutations and refreshes. | Yes | agent |
| D517 | M060/S03 planning | architecture | How S03 should obtain truthful org context for the Team tab | Resolve the seeded default organization by slug through a narrow backend helper/query seam and target Team member calls at `/api/v1/orgs/default/members`, instead of hardcoding the generated org UUID or adding a new project-context route. | The default organization slug is stable in the seed migration while the UUID is runtime-generated. Extending the existing helper/query seam keeps the Team flow inside the current route surface, avoids client hardcoding, and stays aligned with the milestone decision to wire existing backend routes with only minimal seam repairs. | Yes | agent |
| D518 | M060/S03/T02 | ui-state | How the Settings shell should represent mixed live and unsupported surfaces | Use subsection-scoped live state for General, API Keys, and Alert Rules, and remove the page-wide save affordance for unsupported tabs | The backend only supports specific Settings subsections today. A page-global save button implied writes for unsupported controls and created fake-save drift. Subsection ownership with explicit data-state/data-source markers keeps live reads and writes truthful while leaving the rest of the shell visibly present but non-live. | Yes | agent |
| D519 | M060/S03 closeout | verification | How the M060/S03 admin/ops seed helper should establish deterministic proof state | Keep `mesher/scripts/seed-live-admin-ops.sh` database-first: seed/reset/verify the canonical Postgres rows directly and do not depend on reusing whichever Mesher is already listening on the default port. | The seed helper exists to make the admin/ops state deterministic for subsequent dev/prod Playwright runs. Reusing an arbitrary running backend can point at the wrong runtime/DB pair, fight over ports, and make readback fail for environment reasons unrelated to the slice. Direct DB seeding plus DB-level verification keeps the helper stable while the Playwright harness still provides the runtime same-origin proof against a freshly booted backend. | Yes | agent |
| D520 | M060/S04 planning | verification | How S04 should close the milestone's backend-backed shell proof | Add one seeded full-shell Playwright walkthrough sourced from `dashboard-route-map.ts`, reuse the existing Issues/admin-ops seed rails and same-origin diagnostics patterns, and allow only narrow selector/helper/backend seam fixes proven necessary by that walkthrough. | S02 and S03 already proved the individual live seams, but the milestone is not launchable until one runtime exercises the whole dashboard shell together. Using a single assembled walkthrough keeps route coverage centralized, reuses the deterministic seeded local rails, and limits R159 repair work to exact blockers instead of widening into shell redesign or speculative backend cleanup. | Yes | agent |
| D521 | M060/S04/T02 | testing | Default Playwright worker concurrency for Mesher client E2E rails | Run the Mesher client Playwright E2E suite with workers=1 by default | The live Issues, Alerts, Settings, and seeded walkthrough suites all mutate one seeded local Mesher runtime. Parallel workers produced cross-file races, setup timeouts, and false negatives in the canonical combined grep. Serial execution keeps the proof rail truthful while preserving the exact existing verification commands. | Yes | agent |
| D522 | M060/S04 closeout | testing | How Mesher client live suites should own runtime diagnostics and route-parity coverage | Centralize same-origin runtime/request diagnostics and canonical route assertions in `tests/e2e/live-runtime-helpers.ts`, and derive route-parity coverage from `dashboard-route-map.ts` plus currently rendered rows instead of hardcoded fixture ids or per-spec tracking forks. | S04 exposed that copied runtime trackers and stale route-parity fixture assumptions were drifting away from the real shell contract. One shared helper keeps direct-backend rejection, known abort filtering, and route assertions consistent across the walkthrough and route-level suites, while route-map-driven coverage makes the full-shell proof fail when navigation inventory drifts instead of silently testing an outdated list. | Yes | agent |
| D523 | M061 | scope | What M061 should deliver | Make M061 a documentation milestone centered on a client truth inventory plus backend gap map, not a backend implementation wave. | The user wants to know exactly what is still mocked in `@mesher/client` after M060 so later backend work can expand support from a truthful current-state map. | Yes — if later planning decides the gap map should be generated automatically rather than documented manually. | collaborative |
| D524 | M061 | architecture | Where the canonical client truth inventory should live | Keep the canonical mock/live inventory in a maintainer-facing document beside `../hyperpush-mono/mesher/client/`, with milestone artifacts supporting it. | The output is for frontend/backend maintainers and later backend planning; leaving it only in `.gsd/` hides it, and moving it to public docs frames it incorrectly as outward-facing product documentation. | Yes — if the repo later adds a generated inventory surface that supersedes the maintained document. | collaborative |
| D525 | M061 | pattern | How granular the M061 mock/live audit should be | Audit `mesher/client` at route, panel, subsection, and control level wherever a page mixes live and shell-only behavior. | The user explicitly chose a fine-grained audit, and route-level labels would hide the real boundaries on mixed pages like Settings, Issues, and Alerts. | No | human |
| D526 | M061 | pattern | How M061 should map client promises to backend follow-up work | Build the backend gap map from current client-side promises to current backend seams to missing support, and back the classification with repo evidence plus a repeatable drift-proof rail. | The milestone only helps later backend expansion if it follows the current shell as-shipped, not a hypothetical redesign, and if maintainers can rerun a proof surface to keep the map truthful. | Yes — if later work replaces the maintained map with a fully generated inventory. | collaborative |
| D527 | M061/S01 planning | verification | How S01 should keep the new client route inventory truthful without creating a second runtime classification registry | Use `../hyperpush-mono/mesher/client/components/dashboard/dashboard-route-map.ts` as the only structural source of truth for top-level routes, document the human-readable inventory in `../hyperpush-mono/mesher/client/ROUTE-INVENTORY.md`, and guard it with a doc-parity test plus a retained wrapper around the existing seeded Playwright proof rails. | This delivers the maintainer-facing inventory required by M061 while keeping the canonical route list in code, avoiding duplicate runtime registries, and making evidence rerunnable through one structural test plus the already-shipped dev/prod proof suites. | Yes | agent |
| D528 | M061/S02 planning | pattern | How the S02 mixed-surface audit should encode fine-grained route truth | Keep `../hyperpush-mono/mesher/client/ROUTE-INVENTORY.md` as the single canonical surface and expand `## mixed-route breakdown` into structured `Issues`, `Alerts`, and `Settings` tables keyed by stable surface IDs with normalized classifications (`mixed`, `live`, `mock-only`, `shell-only`); describe runtime `fallback` in boundary notes and diagnostics rather than as a canonical inventory classification. | S02 needs machine-checkable panel/subsection/control truth without creating a second runtime registry. Stable table rows let the existing parser/test contract fail closed on drift, while keeping `fallback` as a runtime condition avoids conflating transient live-seam failure with the enduring support boundary the maintainer doc is supposed to capture. | Yes | agent |
| D529 | M061/S02/T02 | tooling | How the route-inventory parser exposes mixed-surface tables without breaking existing top-level callers | Added document-level parse/read helpers that return top-level rows plus mixed-surface sections, while keeping readRouteInventory/parseRouteInventoryMarkdown as top-level-only wrappers | The top-level route map remains the canonical authority from S01, but the mixed-surface contract in S02 needs stable section/surface objects. A layered API lets downstream tests inspect mixed rows without forcing unrelated callers to switch data shapes. | Yes | agent |
| D530 | M061/S02 | verification | How M061/S02 issue-surface Playwright proofs should obtain deterministic seeded issues | Seed the read/action issue rows through same-origin `/api/v1/events` inside the Playwright suites and reopen them through `/api/v1/issues/:id/unresolve`, instead of depending on an external `seed-live-issue.sh` pre-step. | The dev Playwright rail boots its own temporary Mesher backend from `playwright.config.ts`. Depending on a separate shell seeding step can target the wrong runtime or leave the fresh backend without the expected rows, while in-suite same-origin seeding keeps the proof self-contained, deterministic, and aligned with the real browser/backend contract. | Yes | agent |
| D531 | M061/S03 planning | pattern | How M061/S03 should encode the backend gap map without creating a second registry | Keep the backend gap map inside `../hyperpush-mono/mesher/client/ROUTE-INVENTORY.md` as stable route/surface-key tables, and extend `client-route-inventory.mjs` plus `verify-client-route-inventory.test.mjs` to parse and lock those rows while preserving the existing top-level wrappers. | S03 needs a maintainer-facing artifact that backend planners can trust and rerun. Reusing the canonical inventory document and its existing parser/test rail keeps one durable truth surface, makes drift fail closed, and avoids introducing a parallel backend-gap registry that would rot independently. | Yes | agent |
| D532 | M061/S03/T02 | documentation-contract | How the backend gap map should cover wholly mock-backed Mesher dashboard routes | Extend the fail-closed backend-gap contract to dedicated Performance, Solana Programs, Releases, Bounties, and Treasury sections, and keep those routes grouped at route or major-subsection scope with `no-route-family` until `main.mpl` registers a same-origin family. | The parser and tests previously treated backend gaps as mixed-route-only, which would have left the remaining mock-only routes as unverifiable prose. Dedicated sections plus stable subsection keys let maintainers see the missing route families without inventing hidden APIs or exploding every mock CTA into its own row. | Yes | agent |
| D533 | M061/S03/T03 | observability | How the client route inventory parser should validate backend-gap markdown sections | Scan mixed-surface and backend-gap section headings in canonical document order while keeping readRouteInventory()/parseRouteInventoryMarkdown() limited to top-level rows and exposing gap rows only through the document-level helpers. | Ordered section scanning makes section reordering an explicit contract failure with a named heading instead of silent markdown drift, while preserving the existing top-level wrapper API for callers that only need route rows. | Yes | agent |
| D534 | M061/S03/T01 | documentation-contract | How the backend gap map should classify current backend support | Use four stable support statuses: `covered` for fully backed reads/writes already supported, `missing-payload` when the backend family exists but key data remains derived or fallback-backed, `missing-controls` when live reads exist but visible controls outrun available mutations/settings fields, and `no-route-family` when no same-origin backend family exists yet. | Backend maintainers need each client promise mapped to the exact kind of missing support, not a vague partial/live label. A fixed status vocabulary keeps the route inventory actionable for follow-up planning and gives the parser/test contract something precise to fail on when rows drift or overstate backend truth. | Yes | agent |
| D535 | M061/S04 planning | verification | How the canonical maintainer handoff should be packaged and reverified | Close S04 with a product-root wrapper `../hyperpush-mono/scripts/verify-m061-s04.sh` that delegates to the package-owned route-inventory verifier, requires retained status/phase/proof-bundle artifacts, and pairs that with isolated-by-default `seed-live-issue.sh` boot behavior plus a fail-closed structural contract for the handoff markers. | S01–S03 already established the canonical inventory, mixed-surface map, and backend-gap contract. S04’s remaining job is end-to-end packaging: backend maintainers need a root-level closeout command and preserved proof artifacts, while the last reproduced repeatability hazard is stale backend reuse during live-issue seeding. Delegating from a product-root wrapper preserves the package verifier’s route-inventory ownership boundary, retained proof bundles make failures inspectable after reruns, and isolated-by-default seeding removes the main wrong-runtime failure mode without weakening the existing Playwright proof rail. | Yes | agent |
| D536 | M061 S04 T02 | verification | How the M061/S04 closeout rail should be enforced across local replay and CI | Keep `scripts/verify-m061-s04.sh` as a thin product-root wrapper that delegates to `mesher/scripts/verify-client-route-inventory.sh` and fail-closes on delegated artifacts, while CI runs only the structural `node --test mesher/scripts/tests/verify-client-route-inventory.test.mjs` contract. | The delegated package verifier owns the route-inventory proof rail and retained bundle details, so the root wrapper should validate those artifacts instead of duplicating package-local logic. The full wrapper replay is heavier and environment-sensitive, so CI should acknowledge only the structural contract it actually executes rather than claiming to run the local closeout wrapper. | Yes | agent |
| D537 | M061/S04/T03 | observability | How live issue seeding chooses and proves its backend target | Make `seed-live-issue.sh` isolated-by-default with explicit `MESHER_REUSE_RUNNING_BACKEND=true` opt-in, and guard the isolated port picker against high-port overflow. | The previous default reused any responding backend on the chosen port, which made the route-inventory verifier vulnerable to stale-runtime drift. The new default preserves deterministic reruns, keeps reuse available only when explicitly requested, and avoids false failures when the occupied port is already near the upper range. | Yes | agent |
| D538 | M061/S04 closeout recovery | verification | How to respond to combined M061/S04 route-inventory Playwright flake under the shared seeded runtime | Harden shared runtime/client behavior first (toast store lifecycle, heavier per-test timeout where needed, startup timeout for prod build) instead of weakening assertions or removing the combined dev/prod replay | The slice goal is a rerunnable drift-proof rail. The failures reproduced as runtime noise and long-suite timing pressure rather than bad inventory expectations, so the honest fix path is to stabilize the shared proof surfaces and keep the assertion bar intact. | Yes | agent |
