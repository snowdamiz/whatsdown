fn main() do
  let bs = [true, false, true]
  let len = List.length(bs)
  println("${len}")
  let first = List.get(bs, 0)
  let second = List.get(bs, 1)
  println("${first}")
  println("${second}")
end
