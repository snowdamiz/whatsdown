[package]
name = "mesh-common"
version = "0.1.0"
edition = "2021"
description = "Shared types for the Mesh compiler (tokens, spans, errors)"

[dependencies]
rustc-hash = { workspace = true }
serde = { workspace = true }
